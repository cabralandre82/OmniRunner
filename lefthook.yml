pre-commit:
  parallel: true
  commands:
    flutter-analyze:
      root: omni_runner/
      glob: "*.dart"
      run: dart analyze --no-fatal-warnings
    portal-lint:
      root: portal/
      glob: "*.{ts,tsx}"
      run: npx next lint --quiet
