pre-commit:
  parallel: true
  commands:
    flutter-analyze:
      root: omni_runner/
      glob: "*.dart"
      run: flutter analyze --no-pub --no-fatal-infos
    portal-lint:
      root: portal/
      glob: "*.{ts,tsx}"
      run: npx next lint --quiet
