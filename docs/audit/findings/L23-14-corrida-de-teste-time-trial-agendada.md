---
id: L23-14
audit_ref: "23.14"
lens: 23
title: "\"Corrida de teste\" (time trial) agendada"
severity: high
status: fixed
wave: 1
discovered_at: 2026-04-17
fixed_at: 2026-04-21
tags: ["testing", "personas", "coach"]
files:
  - omni_runner/lib/domain/value_objects/time_trial_protocol.dart
  - omni_runner/lib/domain/entities/time_trial_result_entity.dart
  - omni_runner/lib/domain/services/time_trial_threshold_estimator.dart
  - omni_runner/lib/domain/services/time_trial_scheduler.dart
  - omni_runner/test/domain/services/time_trial_threshold_estimator_test.dart
  - omni_runner/test/domain/services/time_trial_scheduler_test.dart
  - tools/audit/check-time-trial.ts
  - docs/runbooks/TIME_TRIAL_RUNBOOK.md
  - package.json
correction_type: code
test_required: true
tests:
  - tools/audit/check-time-trial.ts
linked_issues: []
linked_prs:
  - local:50e4eb4
owner: unassigned
runbook: docs/runbooks/TIME_TRIAL_RUNBOOK.md
effort_points: 3
blocked_by: []
duplicate_of: null
deferred_to_wave: null
note: null
---
# [L23-14] "Corrida de teste" (time trial) agendada
> **Lente:** 23 — Treinador · **Severidade:** 🟠 High · **Onda:** 1 · **Status:** fixed
**Camada:** —
**Personas impactadas:** —
## Achado
— Protocolo de teste (3k, 5k, 30min tempo trial) calcula limiar. Sem agendamento/tracking específico.
## Correção proposta

— Workout type `time_trial` com tratamento especial: resultado atualiza automaticamente `athlete_zones` ([21.5]).

## Referência narrativa
Contexto completo e motivação detalhada em [`docs/audit/parts/`](../parts/) — buscar pelo anchor `[23.14]`.
## Histórico
- `2026-04-17` — Descoberto na auditoria inicial (Lente 23 — Treinador, item 23.14).
- `2026-04-21` — **Fixed (commit `50e4eb4`).** Pipeline puro de domínio em 4 camadas: (a) **VO `TimeTrialProtocol`** com 3 protocolos canônicos (`threeKm` / `fiveKm` / `thirtyMinute`) carregando `kind` snake_case estável + `distanceM?` + `durationS?` + `pacingMultiplier` (1.10 / 1.05 / 1.00 — TT pace é por construção ≥ threshold pace, multiplier < 1 é fisicamente impossível e CI enforça) + `hrMultiplier` (0.92 / 0.95 / 1.00); **`TimeTrialFreshness.maxAgeDays=84`** (~12 semanas) é a janela onde um TT ainda calibra zonas — atleta drifta demais em 12 semanas para TT stale drive Z2 load prescription; `fromKind()` null-safe para resolver persistido; `isDistanceBased`/`isDurationBased` getters. (b) **Entity `TimeTrialResultEntity`** (Equatable) bridge entre execução e calibration pipeline: protocol + `actualDistanceM` + `actualDurationS` + `finishedAt UTC` + `avgHrBpm?` + `sessionId?` + `note?`. Getter `avgPaceSecKm` retorna `null` defensivo se distância ≤ 0 (legacy rows / GPS drop); `isFreshOn()` gate para zone update usa `TimeTrialFreshness.maxAgeDays`. (c) **Service `TimeTrialThresholdEstimator`** puro que converte result em `TimeTrialEstimate{thresholdPaceSecKm, lthrBpm, sourceProtocol, sourceFinishedAt, valid}`. Threshold pace = `avgPace × protocol.pacingMultiplier` (30-min TT: multiplier 1.00 = threshold pace IS avg pace por definição clássica de LT; 5K: 1.05 = TT é ~5% hotter que threshold; 3K: 1.10 = ~10% hotter); LTHR = `avgHr × protocol.hrMultiplier` quando avgHr > 0. Pace floored para integer seconds (atletas leem `:ss`, não ms). **Retorna `TimeTrialEstimate.invalid` em vez de throw** em corrupt input (distância ≤ 0, duração ≤ 0, pace ≤ 0) — coach dashboard com 20+ rows NÃO pode ter uma linha ruim quebrando a tabela inteira; caller trata como "needs re-test". (d) **Service `TimeTrialScheduler`** puro que emite `TimeTrialScheduledWorkout` payload determinístico: sempre `cycleType='test'` (matches `training_plan_weeks.cycle_type` enum já existente com valor `test`), quantiza `scheduledOn` para UTC calendar day (mesma convenção de L23-13 streak / L22-05 groups), título pt-BR locale-apropriado por protocolo, descrição com warmup 15min + cooldown 10min explícitos, `time_trial_kind` field carrega `protocol.kind` para persistência em plan_workouts (follow-up adiciona coluna), rejeita `planId==''` via `ArgumentError`; método `toPlanWorkoutPayload()` retorna `Map<String, Object?>` ready-to-insert (repo follow-up consome). **29 testes Flutter** (estimator 19 casos cobrindo: 30min/5K/3K expected thresholds com valores hand-computed, LTHR absent quando avgHr null ou 0, invalid em distance=0/duration=0, floor rounding edge, isFreshOn same-day/84-day inclusive/85-day stale/future defensive; protocol invariants cardinality=3 + multipliers ≥ 1.0 exhaustive + duration-vs-distance semantic + fromKind round-trip + null/unknown → null + freshness=84; scheduler 10 casos: cycleType=test em todos protocols, UTC quantização, non-UTC normalised, kind em payload, distance vs duration mutual exclusion, title per protocol, description carries warmup+cooldown, coach note conditional, rejects empty planId, payload shape incluindo ISO8601). **CI guard** `check-time-trial.ts` (`npm run audit:time-trial`, 36 checks): protocol file + enum + 3 values + 3 kind strings pinned via regex exato + `every pacingMultiplier ≥ 1.00` (count matches 3 AND all above 1 — regressão que seta multiplier=0.95 falha) + thirtyMinute==1.00 exato + maxAgeDays=84 + fromKind; result entity + avgPaceSecKm + isFreshOn + purity; estimator + class + TimeTrialEstimate + reads pacingMultiplier + reads hrMultiplier + returns invalid on corrupt + purity; scheduler + class + ScheduledWorkout + cycleType='test' + UTC quantização + rejects empty planId + time_trial_kind em payload + purity; runbook + cross-link audit:time-trial + L23-14 + **L21-05 dependency mention** (invariant para garantir que follow-up sequencing não seja esquecido). Smoke-testable: setar `pacingMultiplier: 0.95` em qualquer protocol → `every pacingMultiplier ≥ 1.00` FAIL; remover `cycleType: 'test'` do scheduler → `scheduler pins cycleType = 'test'` FAIL. **Runbook** `TIME_TRIAL_RUNBOOK.md` (~8KB, 8 seções): §1 why (split ship — primitives + follow-up para athlete_zones write pending L21-05); §2 invariants table (10 invariants × 36 checks); §3 **protocol rationale table** com multipliers + why explanation físiológica (30min=1.00 é threshold por definição, 5K é ~5% hotter, 3K é ~10% hotter); §4 how-to (4.1 wire coach UI em 4 steps / 4.2 wire zone updater em 4 steps pending L21-05 + upsert shape com `updated_by='auto_calculated'` / 4.3 add new protocol em 5 steps); §5 **4 operational playbooks** — (5.1) "threshold pace seems too slow" → check `plan_workouts.time_trial_kind` for mis-scheduled; (5.2) estimator invalid → 3 corrupt causes + intencional "needs re-test" UX; (5.3) CI fails → 4 branches; (5.4) stale TT na zone update → `isFreshOn()` gate é upstream do updater, NÃO alimentar estimate stale; §6 detection signals (CI / flutter test / PostHog zero timeTrialCompleted / coach "zones não atualizaram"); §7 rollback (fully additive, 4 files opt-in, zero existing callers); §8 cross-refs L21-05 (pending dep) + L23-06 (shared cycle_type='test' enum) + L23-07/L23-11 (consume TT indirectly via sessions) + L23-13 (RPE+mood obrigatório post-TT é especialmente valioso) + L17-05 (updater failures → AppLogger.warn). **Decisão arquitetural**: (i) **domain primitives shipped agora, updater pending L21-05** — split delivery permite L23-14 fechar como high sem bloquear em L21-05 critical (Wave 0); `TimeTrialEstimate` já está pronto para alimentar `athlete_zones` assim que a tabela existir; (ii) **3 protocolos canônicos, não N** — 3K/5K/30min são consensus coaching; adicionar 10K TT ou 20min TT é content PR via runbook §4.3, não scope creep aqui; (iii) **multipliers ≥ 1.00 enforçados por CI** — multiplier < 1 produziria threshold pace > TT pace (impossível fisicamente); guardar contra typo ou "correção" bem-intencionada; (iv) **`invalid` em vez de throw** — coach dashboard tem 20+ rows, uma linha ruim (GPS drop, clock issue) não pode quebrar o dashboard inteiro; caller trata invalid como "needs re-test"; (v) **cycleType='test' pinado** — matches existing `training_plan_weeks.cycle_type` enum do L23-06 periodization wizard; sharing é intentional; desviar para 'time_trial' cycleType novo fragmentaria enum sem ganho; (vi) **UTC day quantização** para scheduledOn — same rationale que L23-13 streak; athlete timezone é display decision, não storage decision; (vii) **84-day freshness window** = ~12 semanas ≈ half training cycle — athlete drift beyond isso precisa re-test; constante pinada em `TimeTrialFreshness.maxAgeDays`, mudar requer tocar teste + runbook; (viii) **`time_trial_kind` column separada**, não overloaded em `notes` — structured field permite dashboard "next TT" filter + analytics por protocol; `title`/`description` são content que muda, `kind` é stable identifier. **Escopo deliberadamente excluído**: **athlete_zones write** (follow-up L23-14-zones bloqueado em L21-05 — tabela ainda não existe; `TimeTrialEstimate` já está no formato pronto para alimentar UPSERT); **coach UI wizard** (follow-up L23-14-ui: coach seleciona plan + date + protocol, chama scheduler, repo persiste via `scheduleWorkout`); **migration para adicionar `time_trial_kind` column em plan_workouts** (follow-up, aditiva); critical speed estimator multi-distância Jones/Vanhatalo (requer ≥2 TT distintos, out-of-scope MVP — `L23-14-critspeed` follow-up); Edge Function `calculate-zones` que faz percentis das últimas 20 sessões + testes (mencionado em L21-05, é scope daquele finding); analytics `ProductEvent.timeTrialScheduled`/`Completed`/`ZonesUpdated` (follow-up quando UI wiring); integração com `coaching_kpis_daily` para surface "X atletas precisam re-test" (follow-up coach dashboard); VO2max estimator (requires treadmill protocol ou Cooper 12min, out-of-scope — future `L23-14-vo2`); i18n pt-BR → en/es de title/description (reuse `AudioCoachLocale` VO do L22-06 em follow-up L23-14-i18n); notification "você tem um TT agendado para amanhã"; badge "completou primeiro TT" (follow-up L22-09 milestone kind extension). **Verificação**: `flutter analyze` 0 issues em 0.5s nos 4 novos arquivos; `flutter test` 29/29 green (zero platform calls, <200ms); `npm run audit:time-trial` 36/36 checks em ~1s; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerated clean. Cross-refs: **L21-05** (pending critical Wave 0 — creates `athlete_zones` table; L23-14-zones follow-up aguarda), **L23-06** (shared `cycle_type='test'` enum + same pt-BR locale convention em title/description), **L23-07/L23-11** (consume TT results indirectly via sessions aggregation; direct zones consumption pending L23-14-zones), **L23-13** (RPE + mood gate aplica post-TT — especialmente valioso para saber se atleta foi "too hot" no início), **L04-07** (performance signals boundary — TT result stays em plan_workouts + future athlete_zones, NUNCA em coin_ledger.reason financial scope), **L17-05** (futura zone-updater routa failures via AppLogger.warn sem leak de athlete identity). **Batch H finalizado 10/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓, L23-07 ✓, L23-11 ✓, L23-13 ✓, L23-14 ✓.
