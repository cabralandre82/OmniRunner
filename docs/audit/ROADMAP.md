# ROADMAP — Execução das Correções em Ondas

> **Atualizado:** 2026-04-21
> **Status do overall:** Onda 0 ✅ concluída (15/15 fixed, E2E verde) — Onda 1 em execução (96/179 fixed, **`public.auth_login_attempts` (email_hash SHA-256 CHECK + RLS forced + service_role-only policy) + `public.auth_login_throttle_config` singleton (id=1, 4 CHECK constraints em thresholds/windows, defaults CAPTCHA@3/lock@10 em rolling 15-min / 15-min lock) + 5 helpers SECURITY DEFINER (`fn_login_throttle_record_failure/record_success/probe/cleanup/assert_shape`) que rejeitam hash inválido (não-hex, NULL, uppercase, ≠64 chars) com 22023 + CI `npm run audit:anti-credential-stuffing` + 21 integration tests + runbook `ANTI_CREDENTIAL_STUFFING_RUNBOOK.md` com contract da Edge Function `login-pre-check` (follow-up não-blocking) + tuning playbook durante ataque + manual unlock SQL + hourly cron schedule fecha L10-09 (Supabase Auth só tem rate-limit por IP; ataque distribuído com 1000 emails × 1 senha comum por botnet não dispara o per-IP; agora cada email entra em contador com threshold de CAPTCHA aos 3 e lockout de 15 min aos 10)** + **`public.sessions` ganha 2 índices time-series canônicos: `idx_sessions_start_time_brin` BRIN em `start_time_ms` com `pages_per_range=32` (~8 KiB/1M rows vs ~25 MiB de btree equivalente, near-zero write amp) para dashboards CDO/staff que varrem janela `BETWEEN $lo AND $hi` cross-user + `idx_sessions_status_start_time` btree parcial em `(status, start_time_ms DESC) WHERE status >= 3` para "last N finalized globally" (predicate bounded pelo invariante L08-04) + helpers `fn_sessions_has_time_series_indexes()` STABLE SECURITY DEFINER e `fn_sessions_assert_time_series_indexes()` raise P0010 + CI `npm run audit:sessions-time-series-index` + 15 integration tests (DDL + privileges + detector/assert + EXPLAIN com BitmapIndexScan em tabela sintética de 20k rows) + runbook `SESSIONS_TIME_SERIES_INDEX_RUNBOOK.md` com matriz de qual índice por call-site + recipe `CREATE INDEX CONCURRENTLY` out-of-band para tabelas > 5M rows + REINDEX CONCURRENTLY rebuild + BRIN summarization tuning fecha L08-03** + **`public.sessions` ganha `chk_sessions_coherence` CHECK constraint que obriga sessões finalizadas (`status>=3`) a terem ou `(total_distance_m=0 AND moving_ms=0)` ou `(total_distance_m>=100 AND moving_ms>=60000)` — elimina o GPS-bug (`moving_ms=0 AND total_distance_m>0`) que causava divisão-por-zero em cálculo de pace no portal + helpers `fn_find_sessions_incoherent(limit)` (classifica offenders em `gps_zero_moving_ms`/`zero_distance_with_moving`/`distance_below_100m`/`moving_below_60s`/`other_incoherent`) e `fn_assert_sessions_coherence` (raise P0010 com até 10 samples) + CI `npm run audit:sessions-coherence` + 15 integration tests (accepted/rejected shapes + privileges + detector/assert + UPDATE path) + runbook `SESSIONS_COHERENCE_RUNBOOK.md` com backfill playbook fecha L08-04** + **`v_user_progression` expandida com `last_session_at`/`is_active_90d` + novas views canônicas `v_user_progression_active_90d` e `v_weekly_progress_active_90d` + helper `fn_is_athlete_active_90d(uuid)` (STABLE SECURITY DEFINER, granted authenticated+service_role) + 14 integration tests + runbook `INACTIVE_ATHLETES_VIEWS_RUNBOOK.md` com classificação de call-sites por intenção ("staff cold-start" mantém a view original, "ranking" migra para `_active_90d`) fecha L08-05** + **GIN(metadata jsonb_path_ops) defensivo em `public.audit_logs` (só aplica se tabela+coluna existem, idempotente) + helpers `fn_audit_logs_has_metadata_gin()`/`fn_audit_logs_assert_metadata_gin()` (raise P0010) + CI `npm run audit:audit-logs-gin` + 14 integration tests (EXPLAIN confirma BitmapIndexScan no GIN + jsonb_path_ops rejeita operador `?`) + runbook `AUDIT_LOGS_METADATA_GIN_RUNBOOK.md` com playbook CONCURRENTLY out-of-band para bases > 10M fecha L19-06** + **convenção forward-only de naming para CHECK constraints (`<table>_<col>_check` auto-gerado OU `chk_<table>_<rule>` ad-hoc) + detector `fn_find_nonstandard_check_constraints` + assert wrapper `fn_assert_check_constraints_standardized` raise P0010 + CI `npm run audit:constraint-naming` contra 16 tabelas financeiras + rename idempotente de 4 constraints ad-hoc sem prefixo (`clearing_settlements.different_groups`, `swap_orders.swap_different_groups`, `coin_ledger.coin_ledger_reason_length_guard`, `coin_ledger.coin_ledger_reason_pii_guard`) + 16 integration tests + runbook `DB_CHECK_CONSTRAINT_NAMING_RUNBOOK.md` fecha L19-08** +  **novo detector de índices duplicados `public.fn_find_duplicate_indexes(schema, table)` STABLE SECURITY DEFINER + assert wrapper `public.fn_assert_no_duplicate_indexes(schemas[], tables[])` que raise P0010 + DROP IF EXISTS dos 4 phantoms conhecidos (`idx_coin_ledger_user_created`, `idx_coin_ledger_ref_reason`, `idx_ledger_user_monolithic`, `idx_ledger_issuer_monolithic`) + CI `npm run audit:duplicate-indexes` rodando contra 16 tabelas financeiras críticas (coin_ledger + idempotency + clearing + custody + platform_revenue + billing + wallets + sessions + audit_logs) + 14 integration tests (schema/DDL + prefix_overlap + exact_duplicate + UNIQUE/PK ignorados + predicate/INCLUDE differentiation + P0010 assert path) + runbook `LEDGER_INDEX_NAMING_RUNBOOK.md` com convenção `idx_<core>_<cols>` (elimina drift `idx_coin_ledger_*` vs `idx_ledger_*`) fecha L19-04 (L19-04)** + **`clearing-cron` reagendado de `0 2 * * *` (23:00 BRT — antes da meia-noite BRT, off-by-one) para `15 3 * * *` (00:15 BRT — depois da meia-noite BRT, +15 min para evitar thundering herd 03:00 UTC de L12-02) + novo helper `fn_clearing_cutoff_utc(p_timezone text DEFAULT 'America/Sao_Paulo', p_as_of timestamptz DEFAULT NULL)` STABLE SECURITY DEFINER que retorna `(date_trunc('day', p_as_of AT TIME ZONE p_timezone)) AT TIME ZONE p_timezone` — cutoff determinístico TZ-aware (BRT-calendar-aligned) em vez do jitter-prone `now()` original + rewrite de `fn_invoke_clearing_cron_safe` pré-computa cutoff, monta body `{cutoff_utc, timezone, run_kind}` para a futura Edge Function, passa via `fn_invoke_edge_with_retry` (L06-05 wrapper) e persiste `cutoff_utc`/`timezone` em `cron_run_state.last_meta` para auditoria single-SELECT + self-test valida 5 invariantes (helper registrado, cutoff BRT para 17:00 UTC == 03:00 UTC day, cutoff BRT para 02:00 UTC == previous BRT day, UTC sem shift, 22023 em TZ inválido/NULL) + 14 integration tests + runbook `CLEARING_CRON_CUTOFF_RUNBOOK.md` (5 cenários operacionais incluindo replay determinístico de dia específico via p_as_of manual) fecha L12-08 (L12-08)** +  **`profiles` ganha `timezone text NOT NULL DEFAULT 'America/Sao_Paulo'` + `notification_hour_local smallint DEFAULT 9` com CHECK IANA via `fn_is_valid_timezone` + helpers `fn_user_local_hour` (STABLE SECURITY DEFINER) e `fn_should_send_nudge_now` + cron `onboarding-nudge-daily` renomeado para `onboarding-nudge-hourly` rodando `0 * * * *` + Edge Function `onboarding-nudge/index.ts` agora só dispara quando hora local == hora preferida do usuário (L12-09 dedup garante at-most-once/dia mesmo com 24 ticks) + 18 integration tests + runbook `ONBOARDING_NUDGE_TIMEZONE_RUNBOOK.md` fecha L12-07 (07:00 BRT era cedo demais para BR) + co-fecha L07-06 (profiles.timezone ausente)** + **`fn_archive_old_sessions` agora roda via `fn_archive_sessions_chunk` + Edge Function `archive-old-sessions` orquestrador com COMMIT entre chunks (cada RPC é transação separada) via `fn_invoke_archive_sessions_safe` (cron entry point, usa L06-05 retry wrapper + fallback para shim SQL de backward compat) + `fn_archive_sessions_pending_count` para backlog visibility + 13 integration tests + runbook `ARCHIVE_OLD_SESSIONS_RUNBOOK.md` (L12-06)** + **`public.notification_log` agora dedup-ata via UNIQUE (user_id, rule, context_id) + 2 helpers `fn_try_claim_notification` (INSERT ... ON CONFLICT DO NOTHING race-safe, retorna TRUE iff o caller detém o dispatch) e `fn_release_notification` (bounded rollback ≤ 60 s) + `notify-rules` e `onboarding-nudge` refatorados para claim-first (tryClaim → dispatchPush → release em falha) eliminando race entre 2 ticks concorrentes do lifecycle-cron que podiam passar pelo `wasRecentlyNotified` sincronamente e double-fire o mesmo push + convenções de context_id com YYYY-MM-DD para rules recorrentes (low_credits_alert, challenge_expiring, streak_at_risk, inactivity_nudge, onboarding_nudge) que passam a daily-bucketizar ao invés de deduplicar forever + migration faz ROW_NUMBER() cleanup de duplicatas históricas antes do UNIQUE + self-test DO block + 12 integration tests docker-exec + runbook `NOTIFICATION_IDEMPOTENCY_RUNBOOK.md` (L12-09)** + **6 cron jobs (`auto-topup-hourly`, `lifecycle-cron`, `clearing-cron`, `eval-verification-cron`, `onboarding-nudge-daily`, `reconcile-wallets-daily`) que faziam fire-and-forget via `pg_net` ganham retry wrapper com exponential backoff (até 3 attempts, base 5 s, cap 120 s) via `fn_invoke_edge_with_retry` + audit table `cron_edge_retry_attempts` (append-only, RLS forced, FK-less) + integração com `cron_health_alerts` (severity=critical, cooldown 60 min) após max_attempts + graceful degradation quando `http`/`pg_net` extensions ausentes (dev/sandbox) + 18 integration tests docker-exec + self-test DO block + runbook `EDGE_RETRY_WRAPPER_RUNBOOK.md` (L06-05)** + **coin_ledger ganha hardening defensivo contra PII embutida via 2 CHECK constraints preventivos (reason ≤ 64, sem `@`/`by user <uuid>`/`from <Nome> <Sobrenome>`) + audit table `coin_ledger_pii_redactions` (RLS service_role_only, MD5 do original) + helper idempotente `fn_redact_ledger_pii_for_user(uid, actor)` + trigger safety-net em `audit_logs` que auto-fires o redator em `user.self_delete.completed` + CI lint `audit:ledger-reason` que barra migrations novas com `format(...)`/`||`/literais free-form em `INSERT INTO coin_ledger` (opt-out `-- L04-07-OK: <razão>`) + runbook `LEDGER_PII_REDACTION_RUNBOOK.md` fecha o gap "direito ao esquecimento parcial" quando `user_id` é anonimizado mas `reason`/`note` mantém nome/email do atleta (L04-07)** + **contratos privados `TERMO_ADESAO_ASSESSORIA.md` (club_adhesion) e `TERMO_ATLETA.md` (athlete_contract) agora existem versionados em `docs/legal/` com SHA-256 gravado em `consent_policy_versions.document_hash` + lockstep drift detection `npm run legal:check` + 10 tipos canônicos (8 originais + 2 novos) nas whitelists portal/edge/fn_consent_grant + runbook `LEGAL_CONTRACTS_RUNBOOK.md` fecha o gap "não consigo provar qual versão o usuário aceitou" exigido por LGPD Art. 7 I + 8 §1 (L09-09)** + **`auto-topup-hourly` agora respeita cap diário antifraude por grupo (default R$ 500/dia + 3 cobranças/dia + TZ-aware) com guardrail `fn_apply_auto_topup_daily_cap` chamado pelo edge `auto-topup-check` ANTES da Stripe — fecha race do `last_triggered_at` em invocações inline pós-debit + audit table `billing_auto_topup_cap_changes` com reason >= 10 chars + UI admin_master + runbook canônico (L12-05)** + **swap marketplace agora aceita ofertas a partir de US$ 10 (era US$ 100) — clubes amadores 20-50 atletas finalmente podem usar P2P, com constantes canônicas `SWAP_MIN_AMOUNT_USD`/`SWAP_MAX_AMOUNT_USD` em `lib/swap.ts` reusadas em route handler, OpenAPI v1, OpenAPI legacy e UI + lockstep contract test mata drift entre as 4 superfícies (L05-07)** + **`custody_deposits` agora respeita cap diário antifraude por grupo (default US$ 50k/dia, tunável por `custody_accounts.daily_deposit_limit_usd` + TZ por conta) com guardrail `fn_apply_daily_deposit_cap` integrado no miss-path de `fn_create_custody_deposit_idempotent` + endpoint platform-admin `GET|PATCH /api/platform/custody/[groupId]/daily-cap` com audit trail `custody_daily_cap_changes` + runbook canônico (L05-09)** + **clearing fee rate agora é congelada na emissão via snapshot imutável em `coin_ledger.clearing_fee_rate_pct_snapshot` + execute_burn_atomic computa weighted-avg per issuer via `fn_compute_clearing_fee_rate_for_issuer` + `clearing_settlements.fee_rate_source` distingue caminho snapshot do fallback legacy (L03-02)** + **`platform_revenue.fee_type` CHECK ampliada para os 6 tipos canônicos (`clearing`/`swap`/`maintenance`/`billing_split`/`fx_spread`/`provider_fee`) + contract test lockstep SQL↔TS mata drift por regressão (L03-09 encerrado como side-effect de L03-03)** + **disputas / chargebacks de Stripe e MercadoPago agora são auto-reversados no `POST /api/custody/webhook` via `fn_handle_custody_dispute_atomic` + tabela `custody_dispute_cases` com state-machine e escalonamento CFO quando coins já gastas (L03-20)** + **reembolso/estorno agora tem `POST /api/coins/reverse` + 3 funções SECURITY DEFINER atômicas que substituem o SQL manual do CHARGEBACK_RUNBOOK §3.2 (L03-13)** + **distribute-coins per-call cap subiu 1k → 100k + novo `/api/distribute-coins/batch` move até 200 atletas em uma única transação SQL (L05-03)** + **withErrorHandler agora envolve TODOS os 37 endpoints financeiros/platform-admin (L17-01)** + **CSRF default-deny via origin pinning protege TODA a superfície mutante de `/api/*` (L17-06)** + **`platform_fee_config.fee_type` agora tem TS↔Postgres↔OpenAPI lockstep (L01-45)** + **`execute_withdrawal` recupera `provider_fee` no `platform_revenue` ledger e a invariante de custódia volta a fechar (L03-03)** + **`reconcile-wallets-cron` agora dispara alerta Slack/Sentry em qualquer drift `>0` com tabela forense `wallet_drift_events` (L06-03)** + **invariante `coin_ledger ↔ wallets` agora é checada real-time via `fn_check_wallet_ledger_drift` exposta como `/api/platform/invariants/wallets` (L08-07)** + **clearing settle batch agora drena em chunks de 50 dentro do banco via pg_cron (sem Vercel timeout) com fallback Edge `/api/cron/settle-clearing-batch` (L02-10)** + **pg_cron jobs agora têm health monitor genérico `cron-health-monitor` que persiste alertas em `cron_health_alerts` (L06-04)** + **pg_cron jobs agora têm SLA monitor `cron-sla-monitor` que rastreia p95/breach por job e dispara alertas tagueados `details.kind='sla_breach'` na MESMA tabela `cron_health_alerts` (L12-04)** + **anti-cheat thresholds (`MAX_SPEED_MS`, `MAX_HR_BPM`) agora são por skill_bracket + Tanaka floor + measured chest-strap via `fn_get_anti_cheat_thresholds` chamada por verify-session/strava-webhook (L21-01 + L21-02)**: supply chain sexteto L11-01/02/03/04/09 + observabilidade SRE L20-01/02/03/04/05/07/08 + runbooks financeiros L06-01 + kill switches L06-06 + custody idempotency L01-04 + swap TTL L05-02 + swap ADR cessão de crédito L02-07/ADR-008 + Asaas webhook hardening L01-18 + money math TS↔SQL convergence L03-01 + delete-account fail-safe + audit trail L04-02/L01-36/L06-08 + middleware route-policy + LRU cache L13-01/L13-02/L13-03 + duplicate cleanup L02-09 + Lente 13 closure L13-04/L13-05/L13-06/L13-07 + API contracts foundation L14-04/L14-05/L14-06 + path versioning L14-02 + OpenAPI contract-first registry L14-01 + cron health closure L12-01/L12-02/L12-03 + DBA bloat-free archive + dedup L19-02/L19-03 + idempotency unification L18-02 + wallet mutation guard L18-01 + feature-flags TTL stratification + SHA-256 bucket L18-06/L18-07 + Edge Function canonical wallet-credit helper L18-08 + withdrawal lifecycle completion L02-06 + portal CSRF defence L01-06 + realtime RLS guard L02-08 + Strava OAuth state CSRF L01-29 + Android release signing fail-loud L01-31 + Android R8/ProGuard release hardening L01-30 + decimal-string parser BigInt path L03-17 + portal CSP nonce-based hardening + violation sink L01-38/L10-05 + client error boundaries Sentry funnel L06-07 + custody webhook receiver hardening L01-01 + checkout proxy hardening L01-09 + withErrorHandler universal financial wrap L17-01)

A auditoria identificou **348 findings** distribuídos em **23 lentes** (69 🔴 critical, 123 🟠 high, 127 🟡 medium, 17 🟢 safe, 12 ⚪ não-auditados). Corrigir todos em paralelo seria caótico. Esta estratégia distribui o trabalho em **4 ondas** com objetivos bem definidos e critérios de saída mensuráveis.

**Triage executado** — `tools/audit/triage.ts` aplicou scoring heurístico (exploitability × blast_radius × irreversibility) sobre os 69 criticals, promovendo **15 para Onda 0** ("stop the bleeding") e **54 para Onda 1** ("foundation"). Ver [`TRIAGE.md`](./TRIAGE.md) para racional detalhado de cada item.

---

## Princípios

1. **Stop the bleeding primeiro**: criticals que podem gerar perda financeira, exposição LGPD ou compromisso de autenticação vão para Onda 0.
2. **Fundação antes da escala**: testes, observabilidade e idempotência (Onda 1) são pré-requisitos para tocar features novas com segurança.
3. **Uma onda termina quando seus exit criteria estão verdes**, não por calendário — ritmo vs. qualidade.
4. **Não começar uma onda com dívida da anterior aberta** (exceto itens `deferred` explicitamente justificados).

---

## Onda 0 — "Stop the bleeding" (15 findings)

**Duração alvo:** 1-2 sprints (5-10 dias úteis)
**Foco:** findings 🔴 critical que representam risco imediato de perda financeira, vazamento de secrets, violação LGPD grave ou migration drift que quebra fresh installs.

### Escopo (ordem de priorização por score do TRIAGE)

| Pri | ID | Score | Status | Título |
|---|---|---|---|---|
| 1 | `L09-04` | 125 | 🟢 fixed | Nota fiscal não emitida em withdrawals (regulatório fiscal) |
| 2 | `L01-02` | 100 | 🟢 fixed | FX rate client-supplied em `/api/custody/withdraw` (fraude direta) |
| 3 | `L01-17` | 100 | 🟢 fixed | Asaas API Key armazenada em texto puro |
| 4 | `L04-03` | 100 | 🟢 fixed | Sem registro de consentimento (LGPD Art. 8) |
| 5 | `L04-01` | 80 | 🟢 fixed | `fn_delete_user_data` incompleta (LGPD Art. 48) |
| 6 | `L18-03` | 80 | 🟢 fixed | SECURITY DEFINER sem SET search_path |
| 7 | `L04-04` | 64 | 🟢 fixed | Dados de saúde/biométricos sem proteção reforçada (LGPD Art. 11) |
| 8 | `L01-44` | 60 | 🟢 fixed | Migration drift em `platform_fee_config.fee_type` CHECK |
| 9 | `L02-01` | 60 | 🟢 fixed | `distribute-coins` não-atômico ⭐ (exemplar) |
| 10 | `L19-01` | 60 | 🟢 fixed | `coin_ledger` particionada por mês |
| 11 | `L19-05` | 60 | 🟢 fixed | `lock_timeout=2s` em RPCs financeiras |
| 12 | `L01-03` | 50 | 🟢 fixed | `/api/distribute-coins` fallback silencioso (cross-ref L02-01) |
| 13 | `L02-02` | 50 | 🟢 fixed | `execute_burn_atomic` exceções engolidas |
| 14 | `L14-03` | 45 | 🟢 fixed | Swagger-UI self-hosted (sem unpkg) |
| 15 | `L05-01` | 40* | 🟢 fixed | Swap race entre accept/cancel (cancel_swap_order race-safe) |

**Progresso Onda 0:** ✅ **15/15 fixed** (100 %). Validação end-to-end via `tools/validate-migrations.sh --run-tests`: 165/165 migrations aplicando em fresh `public` schema, 146/146 testes de integração verdes (2026-04-17). Cada finding linka ao commit do fix em `linked_prs:`.

Detalhes completos + correções em `docs/audit/findings/LXX-YY-*.md`.

### Exit criteria

- ✅ 100% dos 15 findings da Onda 0 em `status: fixed`
- ✅ `tools/audit/verify.ts` passando no CI para todos PRs que tocam `docs/audit/`
- ✅ Testes de regressão para cada um (concorrência em `distribute-coins`, FX tamper em withdraw, etc.)
- ✅ Zero discrepância em reconciliação de custódia por 48h em staging
- ✅ Secret vault implementado e Asaas keys migradas (L01-17)
- ✅ LGPD compliance verificada por legal (L04-01, L04-03, L04-04)

---

## Onda 1 — "Foundation" (177 findings: 54 criticals rebaixados + 123 high)

**Duração alvo:** 3-5 sprints
**Foco:** fundação que acelera as correções das demais ondas. Inclui 54 criticals que não sangram dinheiro diretamente mas estabelecem padrões (observability, idempotência unificada, runbooks, OpenAPI, tracing).

**Progresso atual:** 127 fixed + 7 wont-fix (closed) / 179 = **134/179 endereçados (74,9%)**. 45 pendentes reais.

> **Sweep Strava-only (2026-04-21):** A revisão de escopo da Sprint 25.0.0 (ver `docs/ARCHITECTURE.md` §7 — "ATIVIDADE (Strava como Fonte Única)") eliminou o tracking GPS in-app do produto: todas as atividades chegam via API Strava + webhook. Sete findings de Onda 1 que presumiam o tracking in-app foram reclassificados como `wont-fix` nesta data — L21-06 (resolução GPS), L21-07 (.fit real-time), L21-08 (lap button em tela de corrida), L21-09 (calibração GPS em pista), L21-11 (ghost mode durante o run), L22-04 (pace guidance ao vivo) e L22-06 (voice coaching ao vivo). Para os três que já tinham sido entregues antes do sweep (L21-06 commit `9a4ec27`, L22-04 commit `6ef40ff`, L22-06 commit `9dab4ec`), o código de domínio puro fica **dormente** na árvore — nenhum caller no produto atual, testes ainda verdes contra VOs inertes, pickup-able se o tracking in-app voltar. A alternativa de reverter os 6 commits foi descartada por poluir o git log sem benefício.

> **Batch J (2026-04-21) — 50 próximos findings sem confirmação a cada item:** O usuário pediu para não mais perguntar após cada batch — "pegue os 50 próximos que voce achar melhor fazer e faca". Escolha deliberada mistura Tier 1 (policy/docs-only — resolve alto-risco compliance sem pipeline code) → Tier 2 (SQL primitivos — queue, retention, guard triggers) → Tier 3 (schema/light-flow — white-label, webhooks, integrações) → Tier 4 (pure-domain Dart — TSS, custom zones, state machines) → Tier 5 (architectural CI — bounded-context + clean-arch guards). **Primeiros 10 entregues** nesta run: **L10-01** (SECURITY.md + security.txt RFC 9116 + runbook + SLA table 24h/72h/14d), **L10-02** (THREAT_MODEL.md STRIDE+DFD + 6 trust boundaries + 6-story abuse catalogue + 90-day cadence), **L04-08** (BACKUP_POLICY.md retention matrix 7d/14d/30d/180d + LGPD erasure 30-day restore block + staging obfuscation + restore runbook), **L04-09** (migration 20260421420000 — append-only public.third_party_revocations queue + auto-enqueue trigger em strava_connections DELETE + 3 helpers SECURITY DEFINER + state machine append-only via request_id; HTTP worker é follow-up explícito), **L04-10** (DATA_TRANSFER.md com 9 processors + ANPD Resolução 19/2024 SCCs + change procedure + decision log), **L07-05** (docs/a11y.md WCAG 2.1 AA + LBI 13.146/2015 + 10 jsx-a11y rules pinned error com ratchet allow-list de 38 `<table>` legados e overrides de 4 rules em 21 arquivos legados), **L09-07** (migration 20260421430000 — `billing_refund_requests` ganha `sla_target_at` 48h/72h + view `v_billing_refund_requests_breached` + helper idempotente + REFUND_POLICY.md com CDC Art. 49 + 2026 holiday calendar), **L09-08** (ADR-0001 "pass-through by default" + migration 20260421440000 singleton `billing_fee_policy` + seed `docs/adr/` directory), **L10-04** (WAF in-process 40 unit tests + 46 CI invariants: module `portal/src/lib/security/waf.ts` com 14 UA + 19 paths + allow-list `.well-known/security.txt` + metrics `waf.blocked`; runbook de 3 camadas L1 Vercel / L2 in-process / L3 Cloudflare contingency), **L19-07** (migration 20260421450000 — `pg_stat_statements` extension + `v_pg_stat_statements_top` service-role-only view com mean ≥ 100 ms + top/reset helpers SECURITY DEFINER + DBA_QUERY_TUNING.md runbook monthly first-Tuesday + watch/breach 100/400 ms + reset policy). 10 commits `fix(...)` (77c4992, 05cec54, b8f0380, e7a9866, c8ac7f0, 906fc19, 9813af4, 1f42841, 78c8268, 9fc786f) + 10 CI guards novos (145 + 27 + 34 + 32 + 46 + 33 = 317 invariants combined). Remaining pending Wave 1: 45 findings.

**Marcos concluídos:**
- L08-06 — `/platform/*` ganha camada de staging OLAP `public_olap.*` (materialized views + pg_cron refresh a cada 15 min) em 9 camadas SQL (commit `06b4087`, 2026-04-21): fecha o high de Lente 8 (CDO) sobre "dashboards `/platform/*` rodam SELECT pesados diretamente em `custody_accounts`, `coin_ledger`, `sessions` via Supabase. Sem isolamento de carga. Dashboard pesado em hora de pico trava `execute_burn_atomic` esperando lock. Incidente em produção causado por BI". **Estado pré-fix**: dashboards batem OLTP direto — `/platform/page.tsx` faz `count(*)` em `sessions` filtrando `status>=3 AND start_time_ms>=weekStart` (L08-03 tem índice parcial que ajuda, mas ainda passa pelo OLTP) + full-scan de `billing_purchases` + contagens em `coaching_groups`/`coaching_members`/`athlete_verification`. Zero isolamento entre BI read path e hot path transacional. **Decisão de recorte**: shippar **staging no mesmo cluster via MVs agora** em vez de esperar réplica dedicada. Rationale: réplica dedicada (pg_logical / FDW / export para DuckDB/BigQuery) é trabalho de infra com janela de manutenção, alocação de recursos e semana+ de lead time; MVs + cron entregam **~80% do isolamento** hoje via schema dedicado (`public_olap`) com `service_role`-only grants, `REFRESH … CONCURRENTLY` que não bloqueia leitores, advisory lock por MV impedindo refresh concorrente, e `statement_timeout` por MV que capa qualquer refresh degenerado antes que segure recursos em OLTP. Follow-up `L08-06-read-replica` fica registrado para o dia que a infra permitir — as MVs viram cache rápido local, complementar, não conflitante. Segundo follow-up `L08-06-portal-migrate` é PRs pequenos por dashboard apontando consumer para MV. **Fix em 9 camadas SQL** (`20260421410000_l08_06_olap_staging.sql`, ~540 linhas): (1) schema `public_olap` com `REVOKE ALL` explícito de `PUBLIC`/`anon`/`authenticated` (MVs contêm agregado plataforma-inteiro — vazar a `authenticated` seria exposição cross-tenant de receita total/sessões totais/custody), `GRANT USAGE TO service_role` único; (2) `mv_refresh_config` (mv_name PK, enabled default true, refresh_interval_seconds com CHECK 60..86400, statement_timeout_ms com CHECK 1000..600000, concurrent boolean, note, created_at/updated_at) + RLS forçado service_role-only + trigger `trg_mv_refresh_config_touch` auto-bumping updated_at; (3) `mv_refresh_runs` (id uuid PK, mv_name, started_at default now, finished_at, duration_ms, status enumerado em 7 valores `ok|skipped_disabled|skipped_no_mv|skipped_no_config|skipped_locked|skipped_too_soon|error`, rows_in_mv bigint, error_message) + índice `(mv_name, started_at DESC)` + RLS forçado + registro no `audit_append_only_config` via `fn_audit_install_append_only_guard` (o trail de refresh é ele mesmo append-only, protegido pelo trigger L10-08 — DELETE/UPDATE/TRUNCATE em `mv_refresh_runs` respondem P0010); (4) **três materialized views** shippadas com `UNIQUE INDEX` obrigatório (requisito estrito de `REFRESH MATERIALIZED VIEW CONCURRENTLY` — sem unique, refresh degenera para exclusive lock, exatamente o que L08-06 previne): **(a)** `mv_sessions_completed_daily` agregando `status>=3` por dia UTC (bucketing via `start_time_ms / 86400000`) com `sessions_count`, `total_distance_m`, `total_moving_ms`, `sessions_verified`, `unique_users` por `count(DISTINCT user_id)` — alimenta o KPI "Corridas (7d)" e séries históricas de atividade; **(b)** `mv_coin_ledger_daily_by_reason` por (dia UTC, reason) com `entry_count`, `sum_delta_coins`, `sum_inflow_coins` (via `GREATEST(delta_coins,0)`), `sum_outflow_coins` (via `LEAST(delta_coins,0)`), `unique_users` — alimenta dashboards financeiros que hoje fariam full-scan em `coin_ledger` (particionado por mês desde L19-01 mas ainda OLTP quente); **(c)** `mv_custody_accounts_snapshot` uma linha por `group_id` com `total_deposited_usd`, `total_committed`, `total_settled_usd`, `available_usd = deposited - committed`, `is_blocked`, `source_updated_at`, `snapshot_at = now()` — consumidores podem exibir "atualizado há X min" vindo direto da MV; todas criadas `WITH NO DATA` (migration idempotente e rápida mesmo em banco grande — primeiro refresh popula); (5) grants simétricos: `REVOKE ALL ON ALL TABLES IN SCHEMA public_olap FROM {PUBLIC,anon,authenticated}` + `GRANT SELECT TO service_role` + `ALTER DEFAULT PRIVILEGES` para MVs futuras herdarem a postura; (6) `fn_refresh_mv(mv_name)` SECURITY DEFINER com `SET search_path = public_olap, public, pg_catalog, pg_temp` que (a) lê config, retorna `skipped_no_config` se ausente ou `skipped_disabled` se `enabled=false`; (b) valida MV via `pg_matviews` — `skipped_no_mv` se ausente; (c) **too-soon guard**: se `max(started_at) WHERE status='ok' + make_interval(secs=>refresh_interval_seconds) > now()`, retorna `skipped_too_soon` (neutraliza loops e botões "refresh" mal comportados); (d) **advisory lock por MV**: `pg_try_advisory_xact_lock(('x' \|\| substr(md5('olap:'\|\|mv_name),1,16))::bit(64)::bigint)` — `skipped_locked` se outro refresh da mesma MV está em andamento; (e) `set_config('statement_timeout', cfg.statement_timeout_ms::text, true)` — **txn-local**, nunca session-wide (segue padrão de L08-08 `audit.retention_pass`), capa o REFRESH em budget conhecido; (f) detecta `pg_matviews.ispopulated` e **auto-cai para não-concurrent** no primeiro refresh (obrigatório, `CONCURRENTLY` não funciona em MV sem dados) mantendo concurrent em todos os subsequentes; (g) registra desfecho em `mv_refresh_runs` — sucesso, skipped_* ou error com `SQLERRM`; (h) retorna `jsonb` estruturado com `status, duration_ms, rows_in_mv, error_message`; (7) `fn_refresh_all()` dispatcher SECURITY DEFINER com **advisory lock global** (`olap:refresh_all`) impedindo duas instâncias simultâneas + loop sobre configs habilitadas em ordem alfabética + `EXCEPTION WHEN OTHERS` por MV para isolar falhas (uma MV travada não bloqueia as outras) + `INSERT` de fallback em `mv_refresh_runs` como `error` quando o helper inteiro explode; (8) `fn_olap_assert_shape()` SECURITY DEFINER que CI chama para detectar drift — falha P0010 se alguma MV canônica foi removida, perdeu UNIQUE INDEX (que silenciosamente degenera refresh), saiu do registro append-only, ou não tem linha em `mv_refresh_config`; (9) seed de config (`mv_sessions_completed_daily`/`mv_coin_ledger_daily_by_reason`/`mv_custody_accounts_snapshot` todos `enabled=true, refresh_interval_seconds=900, concurrent=true`, timeouts calibrados por cardinalidade: 30s/45s/15s respectivamente) + `pg_cron.schedule('olap-refresh-all','*/15 * * * *', $cron$ SELECT public_olap.fn_refresh_all(); $cron$)` com `unschedule` defensivo (idempotência) fora do BEGIN (cron é global). **Self-test** (DO block pós-COMMIT, ~60 linhas) exercitando invariantes críticos: (i) `fn_olap_assert_shape` roda limpo; (ii) refresh de MV inexistente no config → `skipped_no_config`; (iii) primeiro refresh real de `mv_custody_accounts_snapshot` → `ok` (exercita o fallback não-concurrent para WITH NO DATA); (iv) segundo refresh imediato → `skipped_too_soon` (exercita too-soon guard); (v) `count(mv_refresh_runs) >= 3`; (vi) `DELETE FROM mv_refresh_runs` bloqueado com P0010 (confirma trigger L10-08 armado); (vii) dispatcher retorna `ok`/`skipped_locked`. **CI guard** `npm run audit:olap-staging` (66 invariantes em `tools/audit/check-olap-staging.ts`) valida estaticamente: migration presente, grants de schema e tabelas sem leak para anon/authenticated, CHECK clamps nos intervals e timeouts, RLS forced em ambas as tabelas, runs table em append-only registry, cada MV canônica declarada + `UNIQUE INDEX` + `WITH NO DATA` + seed de config + filtros de domínio (sessions status>=3, coin_ledger GROUP BY (dia,reason), custody_accounts snapshot), helper SECURITY DEFINER + set_config txn-local + advisory lock + too-soon guard + ispopulated detection + grants service_role-only, dispatcher advisory lock global + isolamento por MV, assert helper listando exatamente 3 MVs canônicas e verificando UNIQUE INDEX + append-only, pg_cron schedule `*/15 * * * *` invocando `fn_refresh_all`, self-test cobrindo skipped_no_config/skipped_too_soon/DELETE blocked, e runbook cross-linkado a guard + finding + L10-08 + cadência 15-min + too-soon + CONCURRENTLY + 3 MVs nomeadas. **Runbook** (`docs/runbooks/OLAP_STAGING_RUNBOOK.md`, 7 seções + 6 playbooks) documenta: motivação (incidente BI→OLTP lock), arquitetura com 66 invariantes enforced, tabela das 3 MVs com chave única + OLTP origem + refresh, contratos públicos das funções, how-to guides (forçar refresh, pausar MV, adicionar nova MV, rodar dispatcher manual, inspecionar trilha, prever próximo refresh elegível), 6 playbooks operacionais ("dashboard lento de novo", "refresh_runs com status=error", "refresh trava/não termina com pg_cancel_backend", "remover uma MV", "cancelar cron inteiro", "blocking refresh em janela"), trade-offs explícitos (MVs em mesmo cluster vs réplica, 15-min lag, service_role-only, runs keep-forever por default, advisory locks, set_config txn-local) + cross-links para L10-08, L08-08, follow-ups `L08-06-read-replica` e `L08-06-portal-migrate`. **Consumidores `/platform/*` NÃO migram nesta PR** — é trabalho de portal, que vai incrementalmente apontar dashboards para as MVs. A camada de staging já fica no ar e inerte; nenhum regresso possível enquanto os consumidores não apontarem. **Migrations convention preservada**: `20260421410000` imediatamente após L08-08 (`20260421400000`). **Resultado**: a arquitetura agora tem primitivas completas de isolamento OLAP/OLTP — qualquer dashboard futuro tem um caminho de adoção 1-liner (`supabase.schema('public_olap').from('mv_…')`), e o risco "BI trava RPC crítico" migrou de estrutural (impossível de prevenir) para puramente operacional (tem guard-rails em 3 camadas: advisory lock + statement_timeout + too-soon).
- L08-08 — `audit_logs` ganha retention config + batched DELETE helper + pg_cron daily + primitives de partição mensal em 9 camadas SQL (commit `d64b72e`, 2026-04-21): fecha o high de Lente 8 (CDO) sobre "tabela cresce indefinidamente. Sem particionamento por mês. Após 2 anos: 100M+ rows, queries de compliance levam minutos, backups inchados". **Estado pré-fix**: `audit_logs` é platform-managed, sem retenção declarada e sem estratégia de particionamento. L10-08 já tinha protegido contra UPDATE/DELETE arbitrários via trigger append-only, mas retenção automatizada ficava em "Wave 2 backlog" (callout em L19-06 header). **Decisão de recorte**: shippar **retenção hoje + primitives de partição** agora, e deixar a conversão live de `audit_logs` para RANGE PARTITIONED como follow-up explícito `L08-08-partition-audit-logs` que precisa janela de manutenção (ACCESS EXCLUSIVE lock + swap de tabela). Rationale: retenção sozinha já reclama espaço imediatamente, e a conversão de partição se beneficia MUITO de ter uma tabela já capada (menos rows para mover no swap). **Fix em 9 camadas PL/pgSQL** (`20260421400000_l08_08_audit_logs_retention.sql`, ~700 linhas): (1) `audit_logs_retention_config` (schema_name, table_name, retention_days com CHECK 30..3650 ou NULL, batch_limit 100..100k, max_iterations 1..200, timestamp_column customizável, enabled, note) + RLS forced service-role-only + auto-bump updated_at; (2) `audit_logs_retention_runs` (id bigserial, schema/table, ran_at, cutoff_at, retention_days, rows_deleted, iterations, duration_ms, status enumerado em 7 valores — `ok|skipped_disabled|skipped_no_retention|skipped_no_table|skipped_no_column|skipped_locked|error`, error_message, indexes por ran_at DESC) + RLS + registro no `audit_append_only_config` via `fn_audit_install_append_only_guard` (o próprio trail de retenção é imutável); (3) **atualização crítica de `fn_audit_reject_mutation` (L10-08)** para honrar `current_setting('audit.retention_pass', true)` **apenas em DELETE**, aceitando literal `'on'` exclusivamente — UPDATE e TRUNCATE continuam explodindo com P0010 mesmo com GUC setado, defendendo a propriedade "retenção é pure row-level purge por timestamp, nada mais"; (4) `fn_audit_retention_delete_batch(schema, table)` SECURITY DEFINER com search_path hardened, valida existência de tabela+coluna timestamp defensively (platform-managed tables podem faltar em dev envs), `set_config('audit.retention_pass', 'on', true)` — o quarto parâmetro `true` escopa o GUC à transação atual (ao invés de sessão), garante que bypass evapora no COMMIT e nunca vaza para próxima conexão; batched DELETE via `ctid IN (SELECT ... ORDER BY created_at ASC LIMIT batch_limit)` evita table-wide lock; loop bounded por `max_iterations`; insere row de runs em TODAS as branches (inclusive erros, via EXCEPTION WHEN OTHERS); (5) `fn_audit_retention_run_all()` dispatcher SECURITY DEFINER protegido por `pg_try_advisory_xact_lock(8082408808::bigint)` — dois workers concorrentes não processam a mesma tabela, skip_locked silencioso se já estiver rolando; uma tabela falhando não bloqueia as outras (per-table EXCEPTION handler); (6) `fn_audit_ensure_monthly_partition(schema, table, month_start)` idempotente: checa `partstrat='r'` via `pg_partitioned_table`, rejeita dates fora do primeiro dia do mês, retorna `skipped_not_partitioned` para heap tables (no-op benigno HOJE, útil no dia seguinte ao `L08-08-partition-audit-logs`); child name determinístico `<table>_yYYYYmMM`; (7) `fn_audit_retention_ensure_next_month_all()` dispatcher mensal chamado por cron dia 25 às 02:00 UTC; (8) **atualização de `fn_audit_assert_append_only_shape` (L10-08)** adicionando `audit_logs_retention_runs` ao `v_known` array — CI guard `audit:audit-logs-append-only` continua verde; (9) seed config com 7 tabelas canônicas de auditoria + matriz LGPD/fiscal derivada no runbook §3: `public.audit_logs` 730d (LGPD Art. 37 accountability), `portal_audit_log` 730d, `cron_edge_retry_attempts` 90d (ops-only), `wallet_drift_events` 365d (annual reconciliation cycle), `custody_daily_cap_changes` 1825d (Lei 9.430/96 — 5 anos fiscal IRPJ), `coin_ledger_pii_redactions` 1825d (espelha coin_ledger immutable window), `consent_events` NULL (LGPD Art. 8 §6 — ônus de provar consentimento enquanto dado é tratado, só sai via `fn_delete_user_data` L04-01). **Duas schedules pg_cron**: `audit-logs-retention-daily` ('15 5 * * *') dodges 04:30 reconcile-wallets + 04:00 archive/clearing cluster; `audit-logs-retention-next-month` ('0 2 25 * *') garante ~6 dias de folga antes do mês virar. **Assert helper** `fn_audit_retention_assert_shape()` levanta P0010 se config tem <7 rows, alguma canônica sumiu, consent_events deixou de ser NULL, ou runs perdeu guard append-only. **Self-test em transação separada** prova: (a) shape do config; (b) `skipped_no_retention` para unknown table; (c) `skipped_no_retention` para `consent_events` (keep forever); (d) ≥2 runs em `audit_logs_retention_runs`; (e) DELETE em runs sem bypass bloqueado com P0010; (f) **UPDATE em runs COM bypass ainda bloqueado** — defende a invariante crítica de que bypass nunca se aplica a UPDATE; (g) partition helper no-op para heap; (h) partition helper rejeita date fora de month-start. **CI guard** `tools/audit/check-audit-logs-retention.ts` (55 invariantes estáticos via regex sobre o SQL — não precisa DB rodando): migration present + shape de ambas tabelas + CHECKs + RLS forced + policy service-role + bypass GUC check + DELETE-only scope + UPDATE/TRUNCATE still blocked + `set_config(..., true)` (não `false`) + LIMIT+max_iterations + sempre escreve em runs + GRANT service_role-only + advisory lock + per-table tolerance + partition helper guards (month-start, RANGE, no-op heap) + seed cobre 7 tabelas canônicas + consent_events=NULL + cron schedules + assert helper + self-test invariants (UPDATE never bypassable, DELETE without bypass blocked) + runbook cross-links. **Runbook** `AUDIT_LOGS_RETENTION_RUNBOOK.md` (~10 KB, 10 seções): §1 why (retention now, partition later); §2 invariants table (22 invariantes × 55 checks); §3 **matriz de retenção** com base legal de cada janela; §4 contratos das funções públicas (retorno jsonb documentado); §5 how-to (5.1 adicionar nova tabela em 5 passos / 5.2 mudar janela com CHECK guard / 5.3 freeze via enabled=false / 5.4 run manual / 5.5 ensaiar próxima partição); §6 **5 playbooks operacionais** — (6.1) "retenção não rodou ontem" triage SQL + skip_locked debug; (6.2) "tabela explodindo apesar da retenção" com ladder de 4 mitigações (batch_limit → max_iterations → encurtar retention → follow-up partição); (6.3) "encurtar retenção por ordem judicial" com transação atômica; (6.4) **"bypass GUC vazou"** — security incident playbook com `SHOW audit.retention_pass` + `RESET`; (6.5) janela de conversão para RANGE partitioned; §7 detection signals (cron.job_run_details, Logflare RAISE WARNING, query de saúde pronto-copy); §8 rollback (aditivo via `enabled=false` ou cron unschedule); §9 cross-refs L10-08/L19-06/L04-07/L12-06/L04-01/L06-04/L18-03; §10 **follow-ups tracked** explícitos (L08-08-partition-audit-logs, L08-08-partition-portal-audit-log, L08-08-posthog, L08-08-dashboard). **Decisões arquiteturais**: (i) **retention-via-GUC-bypass em vez de ALTER TABLE DISABLE TRIGGER** — GUC é auditável (`SHOW` mostra o estado), scoped (txn-local), reverte automaticamente no COMMIT, e não pega AccessExclusiveLock; DISABLE TRIGGER pegaria lock curto mas visível em `pg_locks` monitoramento e poderia stall INSERTs concorrentes; (ii) **bypass DELETE-only** — UPDATE seria redação/falsificação (scope de L04-07), TRUNCATE catastrófico; retenção por construção é DELETE por timestamp, nada além; self-test prova isso em runtime + CI prova em static; (iii) **literal `'on'` exclusivamente** — não aceita `'true'`/`'1'`/`'yes'` para reduzir surface de fat-finger e eliminar ambiguidade de tipo; (iv) **7 tabelas seeded, não N** — cobre 100% das tabelas de auditoria conhecidas hoje; novas tabelas seguem §5.1 do runbook; (v) **conversão live fora do escopo** — a migration é aditiva e idempotente; conversão da `audit_logs` para partitioned requer coordenação com SRE e janela de manutenção, e fica como follow-up `L08-08-partition-audit-logs` que pode ser agendado quando volume passar 50M rows; (vi) **advisory lock key `8082408808`** = derivação determinística de "L08-08" como número — não colide com outros locks do repo (L06 usa 606xxx range); (vii) **seed valores ≤ 3650 dias (10 anos)** como teto — evita fat-finger "retention=36500" que ficaria basicamente forever mas passaria pelo schema; consent_events usa NULL explícito para "forever" (único valor permitido fora de [30, 3650]); (viii) **cron 05:15 UTC** spread schedule para dodge reconcile-wallets (04:30), archive-sessions (03:45), clearing-cron (03:15/03:00), verification (03:00) — L12-02 thundering-herd prevention. **Escopo deliberadamente excluído**: **conversão live de audit_logs para RANGE partitioned** (follow-up L08-08-partition-audit-logs); **view materializada `audit_retention_daily_summary`** para dashboard coach/admin (follow-up L08-08-dashboard); **PostHog event `audit.retention.run_completed`** (follow-up L08-08-posthog); **retention para tabelas NÃO-audit** (L12-06 `archive-old-sessions` já cobre para sessions, não repetir aqui); **soft-delete/tombstone** (LGPD pede HARD delete quando retention vence; forensic freeze é via `enabled=false` em incidente aberto, não tombstone genérico); **alerting per-table de rows_deleted=0 por N dias** (quando retenção já limpou tudo e ingestion parou — ruído; virará detection signal depois via L06-04 health monitor). **Verificação**: `npm run audit:audit-logs-retention` 55/55 em ~1s; `npm run audit:check` 348/348 validados + registry consistente. Cross-refs: **L10-08** (sibling append-only; migration atualiza `fn_audit_reject_mutation` aditivamente — self-test da L10-08 original continua passando), **L19-06** (GIN index em `audit_logs.metadata`; callout no header já antecipava L08-08 e precisará ser recriado por partição após conversão), **L04-07** (`coin_ledger_pii_redactions` compartilha 5 anos fiscal — invariante cross-finding), **L12-06** (padrão cron batched com chunked commits — L08-08 usa mesmo padrão em menor escala), **L04-01** (`fn_delete_user_data` — retenção ≠ erasure; right-to-erasure é cirúrgico, retenção é janela de purpose), **L06-04** (cron-health-monitor alerta se `audit-logs-retention-daily` parar de rodar), **L18-03** (SECURITY DEFINER com `SET search_path = public, pg_catalog, pg_temp` em todas as 5 funções públicas). **Batch I progress:** 4 fixed (L22-04 dormente/wont-fix, L23-12, L20-06, L08-08) + 5 wont-fix (Strava-only) / 12 — pendentes reais: L08-06, L16-02, L21-10, L21-12.

**Marcos concluídos anteriores:**
- L20-06 — Status page pública ganha aggregator puro + endpoint CORS-aberto em 4 camadas (commit `ca43e64`, 2026-04-21): fecha o high de Lente 20 (SRE) sobre "usuário não tem onde ver 'Omni Runner está operacional?'. Em outage, support tickets inundam". **Estado pré-fix**: o único sinal público de health era `/api/health` (L06-02), intencionalmente opaco (boolean `ok/degraded/down`) — bom para uptime probes mas inútil para usuário que quer saber "o app está fora para todo mundo ou só pra mim?". Finding propunha `status.omnirunner.com` via Atlassian Statuspage / Better Stack / Cachet consumindo feeds internos + Vercel + Supabase + Stripe status APIs. **Decisão de recorte**: ship o **aggregator + endpoint JSON** que qualquer plataforma (inclusive o próprio `status.omnirunner.com` static-site futuro) vai consumir, em vez de bikeshed entre Statuspage/Better-Stack/Cachet agora. Endpoint é o contract; a camada de renderização é follow-up explícito `L20-06-site`. **Fix em 4 camadas TypeScript puras** (`portal/src/lib/status/types.ts` + `aggregate.ts` + `feeds.ts` + `portal/src/app/api/public/status/route.ts` + test file + CI guard + runbook, ~850 LOC): (a) **Types `types.ts`** — `StatusComponent` union (web/api/database/auth/payments/strava) + `STATUS_COMPONENTS` frozen array em ordem canônica (renderer não precisa ordenar — ordem é pinada por CI porque reshuffle é content decision, não deploy acidente) + `STATUS_COMPONENT_LABELS` record pt-BR (i18n é follow-up `L20-06-i18n`). **`StatusLevel`** com 5 valores em ladder `operational|degraded|partial_outage|major_outage|unknown` — wire strings snake_case matching Atlassian/Better-Stack vocabulary literalmente, então migrar para qualquer vendor é no-op para consumers. `unknown` é **valor de primeira classe, não failure disfarçado** — significa "feed não disponível" (vendor API down, timeout) e renderer DEVE distinguir visualmente de `operational` senão user é falsamente reassurado por stale data. `compareStatusLevels(a,b)` via `LEVEL_RANK` record pinado — `unknown` entre `operational` e `degraded` no worst-wins ranking. `STATUS_CACHE_MIN_TTL_MS=30_000` + `STATUS_CACHE_DEFAULT_TTL_MS=60_000` (floor pinada por CI — ir abaixo de 30 s risca vendor rate limit do Atlassian Statuspage no free tier 1rps e leaka volume de tráfego de `status.omnirunner.com` para terceiros). `ComponentStatus` e `AggregateStatus` interfaces + `StatusFeed` provider contract ("nunca throw — coerce para unknown+note"). (b) **Aggregator `aggregate.ts`** — `worstLevel(levels)` single-pass; **`aggregateStatus(statuses, now)` sempre preenche componentes faltantes** com `{level:'unknown', note:'no feed registered'}` (esqueceu um feed? Não some silently, UI mostra `unknown` explicitly); duplicated components → worst-wins (defensive contra aggregator que acidentalmente poll 2 feeds para mesmo component, não pode "melhorar" o signal mostrando o mais otimista); `generatedAt` ISO-8601 UTC. **`collectFromFeeds(feeds, now)` coerces todo throw/rejection para `{level:'unknown', note:'feed fetch failed'}`** — é o ponto central: single vendor outage NÃO pode knockar endpoint inteiro; paralelismo via `Promise.all`, feeds que returnem component errado são coerced para `unknown` com `'feed returned wrong component'`. **`createCachedAggregator({feeds, ttlMs, now?})`** TTL-cacheia o aggregate em module-level state (survives warm container); `Math.max(ttlMs, STATUS_CACHE_MIN_TTL_MS)` enforça piso silently + CI pin double-guards; `invalidate()` resetta cache + `ttlMs` getter pra route setar `Cache-Control: public, max-age=...`. (c) **Feeds `feeds.ts`** — `staticFeed(component, level, note?)` retorna status fixo (tests + first iteration); **`internalProbeFeed(component, probe)`** delega para probe injetada `() => 'ok'|'degraded'|'down'` (keeps feed puro + server-runtime agnostic — route handler wira com `createServiceClient()` no edge); mapeia `ok→operational`, `degraded→degraded`, `down→major_outage`, reject/throw → aggregator coerce para `unknown`. **`withTimeout(inner, timeoutMs)`** wrap rejeita após N ms (slow vendor → `unknown` pela aggregator); zero/negative timeout lança em wrap-time (fail-fast em config bug). (d) **Route `/api/public/status`** — `dynamic='force-dynamic'` + `runtime='nodejs'`, GET + OPTIONS (CORS preflight). Público (zero auth, zero CSRF — uptime probes + status page externo precisam fetch anon). `Access-Control-Allow-Origin: *` + `Access-Control-Allow-Methods: GET, OPTIONS` + `Access-Control-Max-Age: 86400` — response é public-by-design, CORS permissive é correto. `cache-control: public, max-age={ttlMs/1000}` permite CDN proxy. `buildFeeds()` registra 6 feeds (um por component canônico), **cada um wrapped em `withTimeout(..., 2_000)`** pra slow vendor não stall aggregate; `internalProbeFeed` para `api` + `database` delega à probe que faz `db.from('profiles').select('id').limit(1)` + catches para `down`; `web`/`auth`/`payments`/`strava` temporariamente são `staticFeed('operational')` até `L20-06-external-feeds` wirar adapters reais dos status APIs dos vendors. **Never-5xx contract**: outer try/catch no GET handler — se aggregator explode (shouldn't, mas defensive), retorna `aggregateStatus([])` = tudo `unknown` com HTTP 200; `cache-control: no-store` nesse fallback path pra next request re-tentar feed. **27 testes vitest** (`portal/src/lib/status/__tests__/aggregate.test.ts`): invariants (6 components cardinality + uniqueness, level ranking 5-way, TTL floor 30s / default 60s); `worstLevel` (empty→unknown, all-operational, degraded beats operational, major_outage beats everything, unknown entre operational e degraded); `aggregateStatus` (fills missing com unknown+note, worst-wins overall, duplicated components worst-wins, generatedAt ISO-8601); `collectFromFeeds` (parallel, coerce throws para unknown, detect wrong-component feed); `internalProbeFeed` (3 mappings + reject bubbles); `withTimeout` (fast passes, slow rejects, non-positive throws); `createCachedAggregator` (TTL dedupe, re-fetch após expiração, floor enforced, invalidate force refresh, nunca throw quando feed throws). **CI guard** `check-public-status.ts` (`npm run audit:public-status`, 46 checks): types file + 6 StatusComponent values + canonical array + labels map + 5 StatusLevel values + compareStatusLevels + TTL constants pinadas (30_000/60_000) + 3 interfaces; aggregate file + 4 exported functions + missing→unknown regex + throw→unknown regex + TTL floor via `Math.max.*STATUS_CACHE_MIN_TTL_MS`; feeds file + 3 exports + probe mapping + withTimeout guard; route file + GET + OPTIONS + CORS wildcard header + createCachedAggregator + 6 feeds registrados via sequencial regex + withTimeout around each + try/catch fallback nunca-5xx; runbook + 3 cross-links. Smoke-testable: mudar `STATUS_CACHE_MIN_TTL_MS=30_000` para `5_000` → `STATUS_CACHE_MIN_TTL_MS >= 30_000` FAIL; remover `withTimeout` do route → `route wraps each feed in withTimeout` FAIL; reverted clean. **Runbook** `PUBLIC_STATUS_PAGE_RUNBOOK.md` (~11KB, 11 seções): §1 why (tickets inundam em outage por falta de canonical page), §2 **response contract JSON** exemplo + invariant da canonical order, §3 what shipped + **deliberately NOT shipped** (6 follow-ups), §4 how-to add component em 5 steps (union + array + labels + feed + test + guard), §5 how-to tune cache TTL + ADR path para mover floor, §6 **3 playbooks** — (6.1) "status page diz unknown mas app funciona" = expected quando vendor down; (6.2) "outage agora — force refresh" = redeploy OR restart OR admin invalidate endpoint (follow-up `L20-06-admin-invalidate`); (6.3) "vendor status API started 5xx" = expected behaviour → component vira unknown, overall unknown, endpoint keeps 200; se endpoint 5xx é bug em collectFromFeeds, pin CI guard, incident crítico; §7 detection signals (logger warnings + synthetic check nunca 5xx); §8 rollback (1-patch path: substituir route body por empty-feed fallback); §9 references L06-02 (opaque health sibling). **Decisão arquitetural**: (i) **aggregator puro + endpoint, não UI nem integração vendor agora** — decoupled scope é exatamente o que permite migrar pra Atlassian/Better-Stack/Cachet depois sem rewrite; (ii) **unknown first-class** — alternativa "feed down = operational" era perigosa (false reassurance), "feed down = degraded" era pessimista (pode estar ok); unknown é honesto + renderer dá tratamento visual distinto; (iii) **worst-wins em duplicate** — aggregator que acidentalmente duplica feed não pode improve signal mostrando o mais otimista; (iv) **TTL floor 30s pinado** — vendor rate limit protection + prevenção de DoS amplifier contra vendor status APIs; CI pin + runtime pin (Math.max) double-guard; (v) **CORS open** — response é public-by-design, lock-down seria anti-pattern de status page; (vi) **never-5xx contract** — o endpoint É o sinal de outage, não pode cair junto; try/catch fallback + coerce-to-unknown são redundant; (vii) **canonical component order pinada** — renderer não ordena, ordem é content decision com PR; (viii) **separate from `/api/health`** — health stays opaque (L06-02 info-leak lesson), status page é rich breakdown; cada uma com seu invariant set. **Escopo deliberadamente excluído**: **L20-06-external-feeds** (real Vercel/Supabase/Stripe/Strava adapters parsing vendors' public status APIs, behind env flag pra local dev não bater em vendor endpoints); **L20-06-incident-timeline** (read model pra incidents ativas — title, latest update, affected components, rendered abaixo do components grid); **L20-06-site** (o `status.omnirunner.com` estático — Next.js static export OU self-host Cachet/Statuspage); **L20-06-i18n** (`STATUS_COMPONENT_LABELS` pt-BR/en/es via AudioCoachLocale reuse); **L20-06-admin-invalidate** (internal RPC + endpoint para SRE forçar refresh da cache durante incident); **L20-06-rate-limit** (dedicated per-IP limiter no aggregate endpoint — atualmente platform edge rate limiter é upstream defense). **Verificação**: `npx vitest run src/lib/status` 27/27 passes em 22ms; `npx eslint src/lib/status src/app/api/public/status` 0 warnings; `npm run audit:public-status` 46/46 checks em ~1s; `npm run audit:verify` 348 findings validados; `npm run audit:build` clean. Cross-refs: **L06-02** (opaque `/api/health` sibling — different invariant set: health é boolean pra probe, status é rich pra user), **L01-07** (`GET /api/health` information disclosure hardening — mesma rationale de não-leak aplicada em reverse ao status page público), **L17-05** (logger contract usado em fallback path), **L06-04** (future cron-health feed integration candidate), **L08-07** (future invariants feed — "custody/wallet drift" como `degraded` em database component). **Batch I avança 3/12** — L22-04 ✓, L23-12 ✓, L20-06 ✓. Próximo: L08-08 (audit_logs retention).
- L23-12 — Onboarding de atleta ganha state-machine de domínio puro em 3 camadas (commit `6889042`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "coach cadastra atleta → atleta recebe convite email. Sem wizard 'importe histórico Strava, configuramos zonas'". **Estado pré-fix**: adicionar atleta era binário — `INSERT INTO coaching_members` + email transacional (L15-04). Não havia `status` além do `role` (`athlete|coach|assistant`) e, crucially, o produto NUNCA perguntava ao atleta "quer importar 6 meses de Strava pra calibrar suas zonas?". Coach prescrevia o primeiro plano **às cegas**, frequently over/underestimating fitness — sintoma reportado em coaching sessions de onboarding de clubes. Finding explicitamente pedia: "Convite com 'após login, vamos importar seu Strava dos últimos 6 meses (opcional) para personalizarmos seu plano.'" **Fix deliberadamente pure-domain** (não wire de screen / repo / Edge Function) em 3 camadas (`value_objects/athlete_onboarding_step.dart` + `entities/athlete_onboarding_state.dart` + `services/athlete_onboarding_service.dart` + test + CI guard + runbook, ~900 LOC): (a) **VO `AthleteOnboardingStep{invited, joined, profileCompleted, stravaChoiceMade, zonesReady, completed}`** — 6 valores em ordem canônica (CI enforça cardinality). Wire strings snake_case estáveis (`invited`/`joined`/`profile_completed`/`strava_choice_made`/`zones_ready`/`completed`) pinadas no guard — são valores de persistência (futura `coaching_members.onboarding_step`) + analytics discriminators, renomear é breaking change. `athleteOnboardingStepFromWire()` null-safe resolver. `AthleteOnboardingBounds{staleInviteDays=14, stalledProfileDays=3}` pinados no VO (não remote config) — 14d alinhado com unsubscribe grace window padrão de platformas transacionais + coaching pattern "convite >2 semanas = email provavelmente errado"; 3d pra stalled profile porque signup drop-off é funnel risk com cost de nudge near-zero. (b) **Entity `AthleteOnboardingState`** (Equatable) com `userId?` (null até joined), `groupId`, `invitedByCoachUserId` (auditability — nudges fan out para esse coach), `currentStep`, `invitedAt` UTC, `joinedAt?`, `stravaChoice` (default `undecided`), `zonesReady` bool. `StravaImportChoice{undecided, imported, skipped}` — `undecided` é valor inicial mas `markStravaChoice(undecided)` throws: **é o ponto do finding** — obriga decisão explícita, não mais "no decision yet" silenciosamente degradando para "no history". `AthleteOnboardingState.initial()` factory + `copyWith()` + `isComplete` getter. (c) **Service `AthleteOnboardingService`** stateless + `const` (pure Dart, zero `dart:io`, zero `package:flutter/*` — CI enforça): `invite({groupId, invitedByCoachUserId, now}) → AthleteOnboardingState` (valida empty strings, normaliza invitedAt UTC); 5 transições forward-only (`markJoined`, `markProfileCompleted`, `markStravaChoice`, `markZonesReady`, `markCompleted`) cada uma chamando helper `_requireAt(state, expected, operation)` que throws `AthleteOnboardingTransitionError` em moves out-of-order (silent no-op mascararia bugs — repo lendo stale rows pareceria que "funcionou"); `markJoined` valida `userId.isEmpty → ArgumentError`; `markStravaChoice(undecided) → ArgumentError`; `markCompleted` exige `state.zonesReady == true` (double-gate pra não auto-complete antes do coach sanity-check das zonas — deliberate coach-in-the-loop beat); `nextStep(state) → AthleteOnboardingStep?` (null quando completed — dismiss wizard); `nudgeFor(state, now) → AthleteOnboardingNudge` mapeia cada step para categoria de nudge — `none`/`staleInvite`/`profileStalled`/`stravaChoiceRequired`/`zonesMissing`/`readyForFirstPlan` — consumido por coach dashboard + L15-04 email runner (templates per-categoria, follow-up). Cálculo de dias via `DateTime.utc(y,m,d)` quantização (mesma convenção L23-13 streak / L22-05 groups / L23-14 scheduler — timezone é display decision, não storage decision). **29 testes Flutter** cobrem: enum cardinality + 6 wire strings + fromWire round-trip + rejects garbage; bounds pinados (14, 3); StravaImportChoice wire strings; invite produz state em `invited` com defaults, rejeita empty groupId/coach, normaliza UTC; **happy path full** (invited → joined → profile → strava.imported → zones → completed); **happy path Strava skipped** (mesmo mas com `.skipped`); **rejects backward / out-of-order** (markJoined already-joined, markProfileCompleted sem joined, markStravaChoice sem profile, markStravaChoice(undecided), markZonesReady sem strava, markCompleted sem zones, markJoined com userId='' → ArgumentError); nextStep progride e retorna null quando complete; **nudgeFor exhaustive** (invite fresh → none, invite em 14d threshold → staleInvite, joined em 3d threshold → profileStalled, profile → stravaChoiceRequired, strava chosen sem zones → zonesMissing, zones ready → readyForFirstPlan, completed → readyForFirstPlan, non-UTC now normalizado); state equality estrutural; copyWith não-clobbering. **CI guard** `check-athlete-onboarding.ts` (`npm run audit:athlete-onboarding`, 56 checks): step file + enum + 6 values + 6 wire strings + fromWire + 2 bounds pinados (staleInviteDays==14 / stalledProfileDays==3) + purity; state file + StravaImportChoice enum + 3 values + 3 wire strings + class + `.initial` factory + `.copyWith` + purity; service file + class + TransitionError class + 6 nudge values + 8 exposed methods (`invite`, `markJoined`, `markProfileCompleted`, `markStravaChoice`, `markZonesReady`, `markCompleted`, `nextStep`, `nudgeFor`) + markStravaChoice rejects undecided + nudgeFor reads both bounds + purity; runbook present + cross-links audit:athlete-onboarding + L23-12 + Strava mention. **Runbook** `ATHLETE_ONBOARDING_RUNBOOK.md` (~12KB, 12 seções): §1 why (blind prescription sem zonas calibradas), §2 invariants table (10 invariants × 56 checks), §3 what shipped + **deliberately NOT shipped** (4 follow-ups: persistência, wizard UI, Strava backfill Edge Function, email nudges), §4 **state machine diagram ASCII** com transições + nudges + erros, §5 how-to wizard wire-up (switch em nextStep dispatch), §6 how-to coach dashboard (map nudges para copy pt-BR), §7 how-to add new step (8 steps including guard + runbook bump), §8 how-to tune bounds (NOT remote config — UX contract), §9 **4 playbooks** — (9.1) "athlete joined mas dashboard ainda invited" → check markJoined landed; (9.2) "stravaChoiceRequired nudge keeps firing" → é design intentional, forçar choice; (9.3) CI `markStravaChoice rejects undecided` fail → reverter, propósito é forçar explícito; (9.4) bronze/gold badges broken → L22-09 consumer scope; §10 detection signals (CI / flutter test / funnel drop-off per step); §11 rollback (fully additive, 3 new files zero existing callers); §12 cross-refs. **Decisão arquitetural**: (i) **domain-layer state machine** separado de screen/repo/Edge Function — screen dumb, service é single source of truth consumido por {wizard, coach dashboard, email runner, analytics, server edge functions via future `dart compile`} sem divergência; (ii) **one-way forward transitions** — moves out-of-order throw; silent no-op mascararia bugs de stale read; reset é coach action com audit trail (futura `fn_reset_onboarding`), não silent repo rewrite; (iii) **`StravaImportChoice.undecided` valor inicial mas rejected na transição** — **é o delta do finding**; "no decision yet" silenciosamente virando "no history" era o bug; (iv) **bounds no VO**, não remote config — parte do UX contract (email nudge cadence); drift entre clients em versões diferentes confunde coach+athlete; (v) **`markCompleted` exige `zonesReady==true` double-gate** — deliberate coach-in-the-loop antes da primeira prescrição, não auto-complete dos zones ready; (vi) **UTC day quantização** via `DateTime.utc(y,m,d)` cross-surface consistency; (vii) **`nudgeFor(state, now)` com `now` injetado** — determinismo em testes + server-side callers podem feed mocked clock; (viii) **`AthleteOnboardingNudge` enum** (não List<String>) — coach dashboard e email runner fazem switch exaustivo em UX copy, enum força PR em cada novo caso. **Escopo deliberadamente excluído**: **L23-12-persistence** (`coaching_onboarding_state` table OU `coaching_members.onboarding_step` column; requires migration + RLS + CI guard update); **L23-12-wizard** (Flutter screens `athlete_onboarding_strava_choice_screen.dart` etc + `OnboardingWizardController` consumindo `nextStep()`); **L23-12-strava-backfill** (Edge Function job pull 6mo de activities quando choice=imported → feed L21-05 zones calc → bump `markZonesReady()`); **L23-12-email-nudges** (L15-04 email runner dispatcha per-category templates com rate-limit max 1/dia por athlete+category); **L23-12-analytics** (`posthog.capture('onboarding_step_advanced', {from, to})` via repo follow-up); **L23-12-reset-rpc** (`fn_reset_onboarding(target_user_id, reason)` para coach action com audit trail); integração com `adhesion_contracts` (L09-09) — onboarding completed pode gate signing; integração com `product_events` (L08-*); badge "primeiro atleta onboarded" (L22-09 milestone extension); i18n pt-BR → en/es (reuse `AudioCoachLocale` em copy renderer no L23-12-wizard). **Verificação**: `flutter analyze` 0 issues em 1.4s nos 4 arquivos novos; `flutter test` 29/29 green (zero platform, <200ms); `npm run audit:athlete-onboarding` 56/56 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` clean. Cross-refs: **L07-04** (Strava OAuth state validation — dependência do L23-12-strava-backfill follow-up), **L15-04** (email transactional — host dos nudge templates no L23-12-email-nudges follow-up), **L21-05** (pending critical Wave 0 — `athlete_zones` table; L23-12-strava-backfill gravará ali, `markZonesReady` consome), **L23-14** (alternativa para `zonesReady` via time trial quando Strava is skipped), **L22-09** (milestone celebration — `markCompleted` deve emitir evento que triggera "primeiro atleta do clube" badge), **L09-09** (adhesion contracts — onboarding pode gate contract signing em follow-up). **Batch I avança 2/12** — L22-04 ✓, L23-12 ✓. Próximo: L20-06 (status page pública).
- L22-04 — Pace guidance em tempo real ganha voice trigger puro em 2 camadas (commit `6ef40ff`, 2026-04-21): fecha o high de Lente 22 (Atleta Amador) sobre "amador iniciante começa muito rápido ('burned out' em 5 min). Produto não fala durante". **Estado pré-fix**: `AudioCoachService` do L22-06 falava em 3 cadências — per-km (`VoiceTriggers`), per-tempo (`TimeVoiceTrigger`), per-HR-zone (`HrZoneVoiceTrigger`) — mas **nunca entre quilômetros sobre ritmo**. Atleta rodando 4:30/km quando alvo era 5:30/km só ouvia aos 1.000m "1km em 4:30" — sem "desacelere" entre os marcos. Finding explicitamente listava: "Você está 20s mais rápido que alvo. Desacelere um pouco." + "FC zona 3, ideal. Mantenha." + "A cada km: 1 km em 6:15, você está bem." **Fix em 2 camadas** (`omni_runner/lib/domain/usecases/pace_guidance_voice_trigger.dart` + `test/domain/usecases/pace_guidance_voice_trigger_test.dart` + CI guard + runbook, ~650 LOC): (a) **VO `PaceGuidanceState{onTarget, tooFast, tooSlow}`** com wire strings estáveis (`on_target`/`too_fast`/`too_slow`) via extension `PaceGuidanceStateWire.wire` — contract com a catalogue do `AudioCueFormatter` (L22-06) pinado pelo CI. (b) **`PaceGuidanceVoiceTrigger`** puro com `evaluate({currentPaceSecPerKm, targetPaceMinSecPerKm, targetPaceMaxSecPerKm, timestampMs})` que retorna `AudioEventEntity(AudioEventType.paceAlert)?`: (i) **degenerate inputs silenciam** — pace null/NaN/inf/≤0 ou targetMin>targetMax ou targets null → `null` (workout "free run" sem alvo opta-out automaticamente, não precisa flag); (ii) **classify** com deadband: `pace < targetMin − deadbandSec → tooFast`, `pace > targetMax + deadbandSec → tooSlow`, senão `onTarget` — o deadband (default 5 s/km) evita nagging em pace borderline ±1-2s que é variância GPS normal; (iii) **hysteresis** `confirmCount` (default 3) — N ticks consecutivos no mesmo estado antes de confirmar transição (previne flutter por GPS spike); (iv) **cooldown** `cooldownMs` (default 30000) aplica **apenas a alertas** (tooFast/tooSlow), NÃO ao reforço `on_target` — o finding explicitamente pede coach a *confirmar* "Mantenha" quando atleta volta à banda, silenciar isso sob cooldown defeat o propósito; (v) **priority 6 para alertas** (acima de HR alert=7 e distance=10), **priority 11 para on_target** (abaixo de HR — reforço nunca pode afogar warning crítico); (vi) **payload echoes** `state` + `deviationSec` (unsigned seconds/km além da borda — 0 em onTarget) + `currentPaceSecPerKm` + `targetMin/MaxSecPerKm` para TTS renderizar sem re-threadar state do plano; (vii) `reset()` limpa state + cooldown. **Puro Dart** — zero `dart:io`, zero `package:flutter/*`; CI guard enforça via regex de import — plataforma/TTS wiring fica em app layer (AudioCoachRepo consome via existing path). **24 testes Flutter** cobrem: invariants (confirmCount≥1, cooldownMs≥0, deadbandSec≥0, wire strings pinadas, start em onTarget); degenerate silent (null/NaN/inf/zero/negative pace, null/zero/inverted target); classification (onTarget primeira tick não emite, tooFast após N ticks com deviation correto, tooSlow após N ticks, deadband suprime borderline); hysteresis (single off-band não flipa, on-band reseta pending counter); cooldown (same-state dentro window suprimido, cross-state alert também sob cooldown por tipo-alerta genérico, alert após window fires again, on_target reinforcement NUNCA suprimido); reset (limpa state + cooldown). **CI guard** `check-pace-guidance-voice.ts` (`npm run audit:pace-guidance`, 28 checks): file presente + enum + 3 values + 3 wire strings + class + evaluate reads currentPace + targetMin + targetMax + timestampMs + confirmCount + cooldownMs + deadbandSec + emits AudioEventType.paceAlert + payload keys (state/deviationSec/targetMin/Max) + degenerate guards (NaN/isInfinite/≤0/inverted band) + reset + purity; runbook + cross-links audit:pace-guidance + L22-04 + target-band mention. **Runbook** `PACE_GUIDANCE_RUNBOOK.md` (~10KB, 11 seções): §1 why (gap entre per-km e live), §2 invariants table (9 invariants × 28 checks), §3 what shipped + **deliberately NOT shipped** (catalogue keys + SettingsRepository binding são follow-ups explícitos), §4 **decision logic pseudo-code** one-page + priority table, §5 how-to wire em 3 steps, §6 how-to adicionar strings catalogue L22-06 (resolve via `payload['state']` switch), §7 how-to tune knobs com symptom × knob table, §8 3 playbooks (coach silent / coach naga / false positive), §9 detection signals (audio_events telemetry quando wired + feedback atleta), §10 rollback (one-line: parar de chamar evaluate), §11 cross-refs. **Decisão arquitetural**: (i) **pure domain** separado do screen — screen dumb, trigger é single source of truth consumido por {session screen live-tick, coach dashboard simulation, testes, analytics futuros} sem divergência; (ii) **AudioEventType.paceAlert reused** (enum L22-06 já existia com esse valor); (iii) **cooldown apenas em alerts**, não em on_target — matches finding intent; (iv) **deadband 5 s/km** é tradeoff: estreito demais → nagging, largo demais → atleta drifta muito; coach pode re-band via workout prescription; (v) **catalogue keys como follow-up explícito** (L22-04-catalogue) — L22-06 já tem guard CI sobre catalogue coverage, adicionar pace.* sem passar por aquele guard criaria drift; sequência correta é PR em L22-06 guard → adicionar keys → wire no formatter; (vi) **settings binding como follow-up** (L22-04-settings) — atleta pode querer "quero só per-km não quero pace alerts" ou "cooldown 60s em vez de 30s"; isso é product decision que vem via SettingsRepository, não magic number. **Escopo deliberadamente excluído**: **catalogue pt-BR/en/es** no `AudioCueFormatter._catalogue` (follow-up L22-04-catalogue); **wire na session screen** `live_coaching_controller.dart` (follow-up L22-04-ui); **SettingsRepository knobs** (follow-up L22-04-settings); **HR zone "ideal" confirmation** (finding menciona "FC zona 3, ideal. Mantenha." — L22-06 HrZoneVoiceTrigger já fala em transição de zona; "sustained in-target reinforcement" é follow-up L22-04-hr-hold); analytics `posthog.capture('pace_alert_fired')` (follow-up quando UI lander); voice customization "voz feminina/masculina/sotaque" (L22-06 scope via `AudioCoachLocale`); cadence alert "180 passos/min ideal" (future L22-04-cadence, requer step detector); **grade/elevation compensation** (se subida, target relaxa — future L22-04-gap via Grade Adjusted Pace). **Verificação**: `flutter analyze` 0 issues; `flutter test` 24/24 green em <1s; `npm run audit:pace-guidance` 28/28; `npm run audit:verify` 348 findings validados. Cross-refs: **L22-06** (AudioCueFormatter catalogue owner — follow-up L22-04-catalogue adiciona `pace.*` keys lá), **L23-06** (PlanWorkoutEntity.targetPaceMin/Max são fonte do band — vem do periodization wizard), **L23-13** (feedback gate post-run — complementa real-time: live-tick pergunta "estou no ritmo?" + post-run captura "como foi?"), **L17-05** (AppLogger recebe trigger exceptions se algum dia acontecerem em produção). **Batch I avança 1/12** — L22-04 ✓. Próximo: L23-12 (athlete onboarding flow).
- L23-14 — Time trial ganha primitives puros (protocolo + estimator + scheduler) em 4 camadas (commit `50e4eb4`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "Protocolo de teste (3k, 5k, 30min tempo trial) calcula limiar. Sem agendamento/tracking específico". **Estado pré-fix**: coaches já prescreviam TT manualmente, mas não havia workout type dedicado em `plan_workouts` (tudo caía em "free" ou "test" cycle_type ambíguo), nem pipeline domain-level que convertesse resultado de TT em threshold pace + LTHR. Finding propunha workout type `time_trial` + atualização automática de `athlete_zones` ([L21-05]). **Decisão split-ship**: **L21-05 (critical, Wave 0) está deferred em product+legal** (cria `athlete_zones`) — bloquear L23-14 nele significa empurrar este high para Wave 2. Solução: ship os 4 primitives puros agora + documentar o follow-up `L23-14-zones` que alimenta `athlete_zones` assim que L21-05 lander. `TimeTrialEstimate` já é emitido no formato exato pra alimentar UPSERT. **Fix em 4 camadas** (`omni_runner/lib/domain/value_objects/time_trial_protocol.dart` + `entities/time_trial_result_entity.dart` + `services/time_trial_threshold_estimator.dart` + `services/time_trial_scheduler.dart` + 2 test files + CI guard + runbook, ~900 LOC): (a) **VO `TimeTrialProtocol`** com 3 protocolos canônicos: `threeKm(distanceM=3000, pacingMultiplier=1.10, hrMultiplier=0.92)`, `fiveKm(distanceM=5000, pacingMultiplier=1.05, hrMultiplier=0.95)`, `thirtyMinute(durationS=1800, pacingMultiplier=1.00, hrMultiplier=1.00)`. Pacing multiplier physiology: 30-min TT **é** threshold por definição clássica de lactate threshold (multiplier 1.00); 5K TT é corrido ~5% hotter (multiplier 1.05 → threshold = TT pace × 1.05); 3K TT é ~10% hotter (1.10). Multiplier < 1.00 é **fisicamente impossível** (threshold pace não pode ser faster que all-out TT pace) — CI guard enforça. `kind` snake_case estável (`three_km`/`five_km`/`thirty_minute`) usado para persistência em `plan_workouts.time_trial_kind` (coluna adicionada em follow-up migration). `TimeTrialFreshness.maxAgeDays=84` (~12 semanas, meia temporada típica de treino) é janela onde TT ainda calibra zonas — beyond isso atleta drifta demais, need re-test. `fromKind()` null-safe + `isDistanceBased`/`isDurationBased` getters para UI dispatch. (b) **Entity `TimeTrialResultEntity`** (Equatable) — protocol + actualDistanceM + actualDurationS + finishedAt (UTC) + avgHrBpm? + sessionId? + note?. Getter `avgPaceSecKm` retorna null **defensivo** se distance ≤ 0 (legacy rows / GPS drop, não throw); método `isFreshOn()` checa ageDays ∈ [0, 84] (inclusivo, future-date defensive). (c) **Service `TimeTrialThresholdEstimator`** puro: `estimate(result) → TimeTrialEstimate{thresholdPaceSecKm, lthrBpm?, sourceProtocol, sourceFinishedAt, valid}`. Threshold pace = floor(avgPace × pacingMultiplier) (floored para integer seconds — atletas leem `:ss` em UI, não ms); LTHR = round(avgHr × hrMultiplier) quando avgHr > 0. **Retorna `TimeTrialEstimate.invalid` em corrupt input**, não throw — coach dashboard tem 20+ rows; uma linha ruim (GPS drop, clock issue, distance=0) não pode quebrar a tabela inteira. Caller trata invalid como "needs re-test" UX. (d) **Service `TimeTrialScheduler`** puro: `schedule({protocol, scheduledOn, planId, coachNote?}) → TimeTrialScheduledWorkout`. Sempre emite `cycleType='test'` (**reusa** o enum `training_plan_weeks.cycle_type` do L23-06 periodization wizard — compartilhamento é intentional para evitar fragmentação), quantiza `scheduledOn` para UTC calendar day (mesma convenção de L23-13 streak + L22-05 groups — timezone é display decision, não storage decision), título pt-BR locale-apropriado ("Time trial 3 km" / "Time trial 5 km" / "Tempo trial 30 min"), description com warmup 15min + 4×100m + cooldown 10min explícitos, `time_trial_kind` carrega `protocol.kind` em payload, rejeita `planId==''` via `ArgumentError`. `toPlanWorkoutPayload()` retorna `Map<String, Object?>` ready para repo follow-up consumir via existing `scheduleWorkout` path. **29 testes Flutter** (`time_trial_threshold_estimator_test.dart` 19 casos + `time_trial_scheduler_test.dart` 10 casos): estimator cobre (i) expected thresholds hand-computed (30-min 8-min/km pace → 240s threshold; 5K 4-min/km → 252s threshold ×1.05; 3K 3:40 pace → 242s threshold ×1.10); (ii) LTHR absent quando avgHr null ou 0 sentinel; (iii) invalid paths (distance=0, duration=0); (iv) floor rounding edge case (7250m em 1800s = 248.27 s/km → floors para 248); (v) freshness same-day ok, 84-day inclusive, 85-day stale, future defensive; (vi) protocol invariants: 3 cardinality, multipliers ≥ 1.0 exhaustive loop, duration-vs-distance semantic split, fromKind round-trip all 3, fromKind null/empty/'4_km' → null, freshness=84 pinado. Scheduler cobre (i) `cycleType='test'` em todos protocolos; (ii) UTC quantização (14:37:22 → 00:00:00); (iii) non-UTC DateTime normalised; (iv) kind em payload per protocol; (v) distance-based vs duration-based mutual exclusion; (vi) title per protocol; (vii) description contains 'aquecimento' + 'volta à calma'; (viii) coachNote conditional (present when set, key absent when null); (ix) empty planId → ArgumentError; (x) payload shape incluindo ISO8601 `scheduled_on`. **CI guard** `check-time-trial.ts` (`npm run audit:time-trial`, 36 checks): protocol file + enum + 3 values + 3 kind strings pinned + **`every pacingMultiplier ≥ 1.00`** via regex multi-match counting exactly 3 + thirtyMinute==1.00 pinned + maxAgeDays=84 + fromKind; result entity + avgPaceSecKm + isFreshOn + purity (sem flutter import); estimator + class + TimeTrialEstimate + reads pacingMultiplier + reads hrMultiplier + returns invalid on corrupt + purity; scheduler + class + ScheduledWorkout + `cycleType='test'` regex + UTC quantização + rejects empty planId + time_trial_kind em payload + purity; runbook + cross-link audit:time-trial + L23-14 + **L21-05 dependency mention** (invariant garantindo que follow-up sequencing não seja esquecido). Smoke-testable: setar `pacingMultiplier: 0.95` em qualquer protocol → `every pacingMultiplier ≥ 1.00` FAIL; remover `cycleType: 'test'` do scheduler → CI FAIL; deletar `import` de `dart:io` não afeta mas adicionar `import 'package:flutter/foundation.dart'` em qualquer dos 4 arquivos → purity check FAIL. **Runbook** `TIME_TRIAL_RUNBOOK.md` (~8KB, 8 seções): §1 why split-ship (primitives agora + zones follow-up); §2 invariants table (10 invariants × 36 checks); §3 **protocol rationale table** com fisiologia explícita de cada multiplier; §4 how-to (4.1 wire coach UI em 4 steps / 4.2 wire zone updater em 4 steps pending L21-05 com `updated_by='auto_calculated'` + `ProductEvent.timeTrialCompleted` analytics / 4.3 add new protocol em 5 steps via coaching literature); §5 **4 playbooks** — (5.1) "threshold pace seems too slow" → check `plan_workouts.time_trial_kind` mis-scheduled (3K vs 5K confusion); (5.2) estimator invalid → 3 corrupt causes + intencional "needs re-test" UX; (5.3) CI fails → 4 branches; (5.4) stale TT → `isFreshOn()` gate é upstream, NÃO alimentar estimate stale em zones; §6 detection signals (CI / flutter test / PostHog zero timeTrialCompleted / coach "zones não atualizaram"); §7 rollback (fully additive, 4 files opt-in, zero existing callers); §8 cross-refs. **Decisão arquitetural**: (i) **split-ship** (primitives agora, zones follow-up pending L21-05) — permite L23-14 fechar como high sem bloquear em critical deferred; (ii) **3 protocolos canônicos**, não N — 3K/5K/30min são consensus coaching, novos protocols via runbook §4.3 PR process; (iii) **multipliers ≥ 1.00 CI-enforced** — guard contra typo ou "otimização" bem-intencionada que inverteria threshold calculation; (iv) **`invalid` em vez de throw** — robustez de UI em coach dashboard com muitas rows; (v) **`cycleType='test'` reusado** do L23-06 — sharing intentional, fragmentação seria dívida técnica; (vi) **UTC day quantização** — mesma convenção cross-surface (L23-13 streak, L22-05 groups); (vii) **84-day freshness pinado** — constante no VO + shape guard enforça, mudar requer tocar tests + runbook; (viii) **`time_trial_kind` structured column** (follow-up migration), não overloaded em notes — permite dashboard filter + analytics per protocol. **Escopo deliberadamente excluído**: **`athlete_zones` write** (L23-14-zones pending L21-05 critical Wave 0); **coach UI wizard** (L23-14-ui: select plan + date + protocol → scheduler → repo); **migration para `plan_workouts.time_trial_kind` column** (follow-up aditivo); **critical speed estimator** Jones/Vanhatalo multi-distância (requer ≥2 TT distintos, follow-up L23-14-critspeed); Edge Function `calculate-zones` com percentis de últimas 20 sessões (scope de L21-05); **analytics wiring** `ProductEvent.timeTrialScheduled/Completed/ZonesUpdated` (follow-up quando UI lander); integração com `coaching_kpis_daily` para "X atletas precisam re-test" (coach dashboard follow-up); VO2max estimator (requires treadmill protocol ou Cooper 12min, future L23-14-vo2); **i18n** pt-BR → en/es (reuse `AudioCoachLocale` do L22-06 em L23-14-i18n); notification "TT agendado para amanhã"; badge "primeiro TT" (L22-09 milestone kind extension follow-up). **Verificação**: `flutter analyze` 0 issues em 0.5s nos 4 novos arquivos; `flutter test` 29/29 green (zero platform, <200ms); `npm run audit:time-trial` 36/36 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` clean. Cross-refs: **L21-05** (pending critical Wave 0 — `athlete_zones` table; L23-14-zones follow-up aguarda), **L23-06** (shared `cycle_type='test'` enum + pt-BR locale convention), **L23-07/L23-11** (coach dashboards consomem TT indirectly via session aggregation), **L23-13** (RPE + mood gate aplica post-TT — especialmente valioso para saber se atleta foi "too hot"), **L04-07** (performance signals boundary — TT stays em plan_workouts + future athlete_zones, não em coin_ledger.reason financial scope), **L17-05** (futura zone-updater routa failures via AppLogger.warn sem leak athlete identity). **🏁 Batch H COMPLETO 10/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓, L23-07 ✓, L23-11 ✓, L23-13 ✓, L23-14 ✓. Próximo: Batch I (Reach goals, 12 items).
- L23-13 — Feedback gate do atleta ganha domain primitives puros em 3 camadas (commit `a4b8b59`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "`athlete_workout_feedback_screen.dart` existe. Obrigatoriedade variável — coach não pode 'forçar' preenchimento (que guia o próximo treino)". **Estado pré-fix**: screen existia, renderizava campos de RPE (slider 1-10), mood (slider 1-5), rating (1-5) + texto livre ("como foi?", "o que foi difícil?"), mas **nada era obrigatório**. Athlete podia chamar `completeWorkout()` + `submitFeedback()` com RPE e mood `null` → workout marcava como concluído → coach abria dashboard para ajustar próxima semana e não tinha signal. Finding propunha: workout **não fica "100% completo" até RPE + humor preenchidos** + badge de bronze por 30 dias de feedback consecutivo. **Fix deliberadamente pure-domain** (não wire de screen) em 3 camadas (`omni_runner/lib/domain/value_objects/workout_completion_status.dart` + `services/workout_feedback_evaluator.dart` + `services/feedback_streak_calculator.dart` + 2 test files + CI guard + runbook, ~550 LOC): (a) **VO `WorkoutCompletionStatus{pending,partial,complete}`** + **`WorkoutFeedbackBounds`** com canonical bounds (rpeMin=1, rpeMax=10, moodMin=1, moodMax=5, bronzeStreakDays=30) + helper `isTerminal`/`blocksCoach` para UX. Os 3 states são **cardinality pinned** no CI guard — adicionar um 4º (ex: `auto_completed`) força tocar o guard + tests forçando PR review. (b) **`WorkoutFeedbackEvaluator`** puro com `evaluate({completed, feedback})` que: retorna `pending` quando `completed==null` (atleta não terminou a execução); retorna `complete` apenas quando `completed.perceivedEffort ∈ [1..10]` E `feedback.mood ∈ [1..5]`; todo resto é `partial`. **Out-of-range degrada para `partial`**, não `complete` com valor inválido — defesa 2nd-line contra stale rows ou UI que não clampou pré-submit (UI é 1st-line, domain é 2nd-line). Também expõe `missingFields()` retornando `Set<WorkoutFeedbackMissingField>{rpe?, mood?}` para UX inline "preenche RPE para liberar a próxima semana" (coach-aligned framing, não guilt-trip "você não preencheu"). (c) **`FeedbackStreakCalculator`** puro com `calculate({completeFeedbackDates, referenceDay?})` que: quantiza todas as datas em `DateTime.utc(y,m,d)` (dedupe de 2 feedbacks no mesmo UTC-day); computa `currentStreakDays` com **1-day tolerance** (feedback feito até ontem ≡ streak ativo hoje — atleta treina de manhã mas relata à noite não zera); 2+ day gap entre referenceDay e lastDay → `current=0`; `longestStreakDays` via single-pass ordenado; **`badgeBronzeUnlocked = currentStreakDays ≥ 30`** (constante lida de `WorkoutFeedbackBounds.bronzeStreakDays`). Todos os 3 serviços são **pure-dart** (zero `dart:io`, zero `package:flutter/*`) — CI enforça via regex de import. **36 testes Flutter** (`test/domain/services/workout_feedback_evaluator_test.dart` 21 casos + `feedback_streak_calculator_test.dart` 15 casos): evaluator cobre (i) pending paths (null completed, null+feedback-filled, feedback-only sem completed); (ii) partial paths (RPE null, mood null, feedback null com completed, RPE=0/11 out-of-range low/high, mood=6 out-of-range, ambos null, ambos out-of-range); (iii) complete paths (extreme valid RPE=1/10 + mood=1/5, rating opcional não bloqueia); (iv) `missingFields()` exhaustive (empty quando pending/complete, só rpe, só mood, ambos); (v) enum invariants (3 values pinned, `isTerminal`/`blocksCoach` semantics, `WorkoutFeedbackBounds` constants ≡ [1..10]/[1..5]/30). Streak cobre (i) empty → 0/0/false; (ii) single-day em reference → current=1; (iii) single-day yesterday → current=1 (1-day tolerance); (iv) single-day 2 days-ago → current=0; (v) duplicates mesmo UTC-day → count=1; (vi) 3 consecutive → current=3; (vii) gap de 1-day quebra streak (array de 11 datas com 1 gap no meio → current=8); (viii) longest pode exceder current (10 dias em março + 2 em abril → current=2, longest=10); (ix) exato 30 dias unlocka bronze; (x) 29 dias NÃO unlocka; (xi) 31 dias em ref=dia-31 mantém bronze (persistência); (xii) non-UTC DateTime quantizado; (xiii) unordered input ainda correto; (xiv) future date (referenceDay antes de lastDate) → current=0 defensivo. **CI guard** `check-athlete-feedback-gate.ts` (`npm run audit:athlete-feedback-gate`, 32 checks): enum declared + 3 values pinned (pending/partial/complete); `WorkoutFeedbackBounds` class + 5 constants (rpeMin=1, rpeMax=10, moodMin=1, moodMax=5, bronzeStreakDays=30) com regex exato — mudar qualquer literal falha o build; evaluator class + reads `perceivedEffort` + reads `mood` + enforces all 4 bounds via `WorkoutFeedbackBounds.*` references + 3 return paths (complete/partial/pending-when-completed-null) + `WorkoutFeedbackMissingField` enum cobre {rpe, mood} via multi-line regex; **purity assertion** via regex `!import dart:io && !import package:flutter/*`; streak class + FeedbackStreakResult class + reads `bronzeStreakDays` + quantiza via `DateTime.utc(` + `toUtc()` + handles `isEmpty`; runbook present + cross-links `audit:athlete-feedback-gate` + L23-13 + "30-day" mention. Smoke-testable: mudar `rpeMax = 10` para `11` → `WorkoutFeedbackBounds.rpeMax == 10` FAIL; remover `import perceivedEffort` do evaluator → `evaluator reads perceivedEffort (RPE)` FAIL; importar `package:flutter/foundation.dart` no evaluator → `evaluator is pure` FAIL. **Runbook** `ATHLETE_FEEDBACK_GATE_RUNBOOK.md` (~9KB, 8 seções): §1 why (screen existia mas nada era obrigatório → coach sem signal); §2 invariants table (9 invariants × 32 checks); §3 **decision pseudo-code do evaluator** + rules inline (only RPE+mood required — rating + free-text stay optional / out-of-range ⇒ partial / no auto-complete time-based escape hatch); §4 how-to (4.1 wire screen follow-up em 4 steps: disable button até evaluator=complete + inline missingFields / 4.2 wire repo gate follow-up em 3 steps: completeWorkout rejeita se pending/partial + ProductEvent.workoutFeedbackPartial analytics / 4.3 add new required field em 6 steps obrigatoriamente passando pelo CI guard / 4.4 change bronze threshold em 5 steps incluindo coordenação com badge UX); §5 **5 operational playbooks** — (5.1) "marquei como concluído e não sumiu" → evaluator.partial expected, dump perceivedEffort + mood; (5.2) coach "atleta disse que preencheu mas dashboard bloqueia" → mesma root cause from coach-side; (5.3) CI fails → 4 branches por failure name (enum value / bounds constant / reads field / purity); (5.4) streak inflado → presenter passou partial dates em vez de filtrar para complete-only; (5.5) timezone drift → UTC day é intencional, fix é display tz no coach dashboard, não quantização diferente; §6 detection signals (CI red / flutter test fail / PostHog workoutFeedbackPartial spike = UX ask não clear / coach vs athlete mismatch = §5.1-2 / bronze grants zero = §5.4-5); §7 rollback (fully additive — 3 new files, zero existing callers, revert commit limpa; screen wire-up é follow-up pivot); §8 cross-refs. **Decisão arquitetural**: (i) **domain-pure separado do screen**, não evaluator inline no screen — screen é dumb, evaluator é single source of truth consumido por {screen, coach dashboard L23-06/L23-07/L23-11, repo gate follow-up, testes unitários, analytics}; inlinear re-introduz o bug original (cada consumer com sua própria if-chain divergente); (ii) **out-of-range ⇒ partial**, não exception — domain é defesa 2nd-line; excepcionar forçaria try-catch em cada callsite + risco de swallow silencioso; (iii) **UTC calendar day** para streak — qualquer outra política (device tz, coach tz, server tz) degrada silent em travel/DST; display tz é decision da coach dashboard, não do calc; (iv) **30-day threshold pinned no VO**, não remote config — mudar é content change que toca contract de badge UX ("30 dias para o bronze" que atletas já veem); remote config bypassa revisão e cria drift entre clients em versões diferentes; (v) **1-day tolerance** no current streak — atleta que relata à noite não zera; 2+ day gap é reset explícito + documentado em §3; (vi) **only RPE + mood** no gate, não dor/sleep/notes — finding lista RPE+dor+humor mas RPE **implicitamente** captura load, mood **implicitamente** captura recovery; separar dor exige nova coluna DB + CHECK + new required field = scope creep; MVP é 2-field gate, follow-ups §4.3 documenta adicionar; (vii) **WorkoutFeedbackMissingField enum**, não List<String> de field names — enum força switch exaustivo em UX copy rendering (pt-BR/en/es, follow-up L23-13-ui); (viii) **`WorkoutCompletionStatus.blocksCoach` getter** em vez de spread logic — coach dashboard consume `workouts.where(w => w.status.blocksCoach)` single-line para surfacar workouts gating next week. **Escopo deliberadamente excluído**: **wiring do screen** (follow-up L23-13-presenter: screen disable "Concluir" button até `evaluator.evaluate(...)==complete` + inline missingFields hint em pt-BR "Preenche RPE e humor para liberar o próximo treino" via `AudioCoachLocale` VO L22-06 reuse); **wiring do repo gate** (follow-up L23-13-repo: `ITrainingPlanRepo.completeWorkout()` carrega latest completed+feedback, chama evaluator, rejeita com domain error se pending/partial, emite `ProductEvent.workoutFeedbackPartial{releaseId, missingFields}` analytics; server-side defense contra clients legados que não tem screen gate); badge UI render (follow-up usa existing `ConfettiBurst`+`AnimatedCheckmark` infra do L22-09 quando `badgeBronzeUnlocked` flips false→true; requer persistência do lastBadgeState para detectar edge); "dor" como campo separado (não é o gate, mas coach workflow ia se beneficiar de "dor em região específica" → nova coluna + CHECK + new required field, follow-up explícito); auto-complete após 48h (antipattern — finding insiste que gate é feedback-driven, NÃO time-driven; se product experiment futuro decidir, deve escrever RPE/mood valores, não bypassar evaluator); i18n da UX copy "preenche X" (follow-up L23-13-presenter integra AudioCoachLocale); analytics do gate em si (`posthog.capture('workout_feedback_gated', {missing_fields})` no follow-up); silver/gold badge thresholds (90d, 180d — follow-up após produto validar bronze). **Verificação**: `flutter analyze` 0 issues em 0.6s nos 3 novos arquivos; `flutter test` 36/36 green em <1s (zero platform calls, in-memory apenas); `npm run audit:athlete-feedback-gate` 32/32 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerated clean. Cross-refs: **L22-09** (milestone celebration ships "congratulations" beat — bronze badge unlock é sibling celebration via existing `ConfettiBurst`+`AnimatedCheckmark`), **L22-06** (same `AudioCoachLocale` VO para i18n futura da UX copy de partial status), **L23-06/L23-07/L23-11** (coach surfaces cujas decisões de próxima-semana consomem `WorkoutCompletionStatus` — follow-ups wire cada uma para filtrar `blocksCoach` workouts), **L04-07** (RPE/mood são health-signal scope — boundary distinto de `coin_ledger.reason` financial PII; ficam em `plan_workouts.perceivedEffort` + `plan_workout_feedback.mood`), **L17-05** (repo gate follow-up routa domain error através de `AppLogger` sem leak de athlete identity no message). **Batch H avança 9/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓, L23-07 ✓, L23-11 ✓, L23-13 ✓. Próximo: L23-14 (time trial agendada).
- L23-11 — Resumo mensal do atleta ganha pipeline de dados em 5 camadas (commit `bda6bec`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "Coach gasta 1h/mês por atleta escrevendo resumo no Google Docs → envia PDF pelo WhatsApp. Produto não automatiza". **Recorte deliberado**: a narrativa (highlights/improvements/personal_note) é trabalho de coach legítimo e permanece human-written; o que deve automatizar é a **extração de métricas** que hoje o coach re-digita. PDF rendering é follow-up explícito (`L23-11-pdf`) para não travar L23-11 numa decisão de biblioteca de PDF. **Fix em 5 camadas** (`supabase/migrations/20260421390000_l23_11_athlete_monthly_report.sql` + `portal/src/app/api/coaching/athlete-monthly-report/route.ts` + `tools/audit/check-athlete-monthly-report.ts` + `docs/runbooks/ATHLETE_MONTHLY_REPORT_RUNBOOK.md` + `package.json`, ~1050 LOC): (a) **Tabela `coaching_monthly_notes`** com `id uuid PK` + `group_id` FK `coaching_groups` + `user_id` FK `auth.users` + `month_start date` + 3 text fields + `approved_at timestamptz` + `coach_user_id`. Constraints: `uq_coaching_monthly_notes UNIQUE(group_id, user_id, month_start)` (idempotência da upsert), `chk_coaching_monthly_notes_month_trunc` garante `month_start = date_trunc('month', month_start)` (não aceita meio do mês), 3× CHECKs de comprimento (`length ≤ 2048` cada — previne essay-bomb que wedge UI). RLS habilitado mas **zero policies**: todo acesso passa pelas 2 RPCs SECURITY DEFINER (mesmo padrão L22-05). (b) **RPC `fn_athlete_monthly_report(p_group_id, p_user_id, p_month_start DEFAULT NULL)`** STABLE SECURITY DEFINER com `SET search_path = public, pg_temp`. **Double auth gate**: primeiro verifica `coaching_members.role IN ('coach','assistant')` do caller (401 `UNAUTHORIZED`); depois verifica que `p_user_id` é membro do mesmo group (401 `ATHLETE_NOT_IN_GROUP`) — **previne cross-group leak** via spoof de group_id (atacante coach do grupo X não pode pedir relatório de atleta do grupo Y só por informar seu próprio group_id). `p_month_start` normalizado server-side via `date_trunc('month', ...)::date`; fallback para current month se NULL. 3 CTEs: `month_sessions` extrai sessões `is_verified=true` em `[month_start_ms, next_month_start_ms)` anotando `is_first_half` (comparação vs `v_half_ms`), `agg` agrega volume_km, sessions_count, longest_run_km, avg_pace_sec_km + avg_bpm FILTER NON-NULL, days_active via `COUNT(DISTINCT date)`, e first_half_pace vs second_half_pace. Resultado jsonb: `{month_start, generated_at_ms, metrics{volume_km, sessions_count, longest_run_km, avg_pace_sec_km, avg_bpm, days_active, pace_trend_sec_km}, coach_notes}` com `pace_trend_sec_km = second_half − first_half` (negativo = mais rápido na segunda metade = improvement), floats ROUND 2 casas, métricas NULL-safe (não serializa `0 sec/km` quando não há amostra). (c) **RPC `fn_upsert_monthly_note(...)`** VOLATILE SECURITY DEFINER com mesma double gate. Lógica chave: `v_approved_at = now()` **apenas se** os 3 campos livres têm `length(trim(x)) > 0`; caso contrário `v_approved_at = NULL`. Esse signal explícito "coach validou o relatório completo" é consumido pelo PDF renderer follow-up como flag "ready to send". INSERT ON CONFLICT `(group_id, user_id, month_start)` DO UPDATE: upsert idempotente. (d) **Portal route** `api/coaching/athlete-monthly-report` com dois verbos — **GET**: valida group_id + user_id como UUID (regex `UUID_RE`), month como `YYYY-MM` ou `YYYY-MM-DD` (regex `MONTH_RE`/`DATE_RE`), auth gate via `supabase.auth.getUser()`, chama RPC, mapeia `UNAUTHORIZED` + `ATHLETE_NOT_IN_GROUP` **ambos** para HTTP 401 via helper `mapRpcAuthError` — **não expor** se o atleta existe-mas-noutro-grupo (privacy invariant, não UX detail); outros erros → 500 `DB_ERROR`. **PUT**: valida body JSON object, mesmo UUID gate + month gate, `readText()` aceita `null`/`undefined`/string ≤2048 chars; rejeita strings >2048 com 422 (defesa em profundidade: DB CHECK também impõe); chama upsert RPC, mesma colapso UNAUTHORIZED + ATHLETE_NOT_IN_GROUP para 401. (e) **Shape guard `fn_athlete_monthly_report_assert_shape()`** SECURITY DEFINER service_role-only verifica 6 invariants via `pg_proc`/`pg_class`/`pg_constraint`: report function exists + provolatile='s' + prosecdef=true; upsert function exists + provolatile='v' + prosecdef=true; `coaching_monthly_notes.relrowsecurity=true`; `uq_coaching_monthly_notes` existe. Cada drift → `P0010 L23-11 DRIFT:<reason>` (function_missing / report_wrong_volatility:s_got_v / report_not_security_definer / upsert_wrong_volatility:v_got_s / upsert_not_security_definer / coaching_monthly_notes_rls_disabled / unique_constraint_missing). Migration termina com `DO BEGIN PERFORM fn_athlete_monthly_report_assert_shape(); RAISE NOTICE 'L23-11 migration self-test passed'; END` — self-test da própria migration. **CI guard** `check-athlete-monthly-report.ts` (`npm run audit:athlete-monthly-report`, 34 checks): migration present + tabela com unique/month_trunc/length/RLS; report RPC SECURITY DEFINER + STABLE + role gate + athlete-in-group gate + pace_trend_sec_km key + metrics/coach_notes keys; upsert RPC SECURITY DEFINER + approved_at gate + ON CONFLICT; GRANT EXECUTE authenticated em ambas; shape guard declared + L23-11 DRIFT markers + self-test DO block; route present + GET + PUT + chama ambas RPCs + auth.getUser + UUID validation + UNAUTHORIZED/ATHLETE_NOT_IN_GROUP → apiUnauthorized + 2048 char bound; runbook presente + cross-links audit:athlete-monthly-report + L23-11. **Runbook** `ATHLETE_MONTHLY_REPORT_RUNBOOK.md` (8 seções): §1 why (narrative vs data split), §2 invariants table (9 invariants × 34 checks + runtime shape guard), §3 **response shape JSON** exemplo + rules inline (pace_trend negativo = improvement, NULL-safe metrics, approved_at semantic), §4 how-to (add new metric em 6 steps / wire PDF renderer em 4 steps / add CSV export), §5 **5 operational playbooks** — (5.1) coach "metrics look wrong" diff contra raw sessions + is_verified check + content-change PR path; (5.2) coach 401 para seu atleta → coaching_members inspect (caller role, athlete membership); (5.3) CI fails → 4 branches; (5.4) runtime drift decoder (6 DRIFT reasons); (5.5) free-text too long → product issue, NOT raise limit ad hoc; §6 detection signals (CI / L23-11 DRIFT / 500 DB_ERROR / p95>2s via L08-03); §7 rollback additive (drop 3 functions + 1 table via reverse migration); §8 cross-refs. **Decisão arquitetural**: (i) **tabela + 2 RPCs** em vez de overloaded RPC que aceita READ + WRITE — separação clara de volatility (STABLE vs VOLATILE); (ii) **double auth gate** (coach-role + athlete-in-group) obriga atacante que já é coach-de-algum-grupo a também pertencer ao mesmo grupo do atleta — cobre o caso onde atacante tem múltiplas memberships e tenta acessar relatório de atleta de outro grupo via spoof; (iii) **collapse UNAUTHORIZED + ATHLETE_NOT_IN_GROUP em 401** — **não leaka** mapa de membership via discriminação 401 vs 404; (iv) **approved_at server-side**, não client-side flag — coach não pode forjar "aprovado" via manipulação de request, server decide com base nos 3 campos non-empty; (v) **2048 chars bounded** em 3 pontos (route + DB CHECK + runbook §5.5 orienta NÃO relaxar ad hoc) — prevent essay-bombs que wedge PDF renderer downstream; (vi) **pace_trend_sec_km signed float** NEG=improvement é convenção matemática (se second_half < first_half, delta é negativo); runbook documenta para PDF renderer não inverter signal; (vii) **month normalization server-side** (`date_trunc('month', ...)`) previne bug onde UI envia `2026-04-15` e upsert cria row separada do GET com `2026-04-01` — ambos caminham no mesmo row; (viii) **zero RLS policies** apesar de RLS habilitado — pattern estabelecido em L22-05/L23-07 (RPCs SECURITY DEFINER são o único access path, CI enforça via shape guard). **Escopo deliberadamente excluído**: PDF rendering (follow-up L23-11-pdf que vai consumir jsonb verbatim via @react-pdf/renderer ou pdfkit, decisão em ADR separada); CSV export (`.csv` route follow-up que pivota jsonb); UI coach preview (follow-up L23-11-ui com form de 3 text areas + preview table); analytics `posthog.capture('monthly_report_approved')`; rate-limit no upsert (low-throughput, coach edita manualmente); storage de PDF gerado (futura tabela `coaching_monthly_reports`); histórico "últimos 12 meses" em single query (atual jsonb é single-month, consumer chama 12× paralelo se precisar); notifications "athlete got their report" (follow-up push-notification hook); integration com `adhesion_contracts` (relatório pode referenciar fase do contrato, future enhancement). **Verificação**: `npm run audit:athlete-monthly-report` 34/34 checks green em ~1s; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerated clean; `next lint` 0 warnings em route. Cross-refs: L08-03 (dependency crítica — `idx_sessions_user_time` torna o monthly scan sub-linear per-athlete), L22-05 (same SECURITY DEFINER + double-gate + shape-guard + static-guard pattern), L23-06 (sibling coach-product — prescription + monthly-triage pair), L23-07 (sibling coach-product — group-overview + athlete-deep-dive pair; same 14d + 28d window conventions), L17-03/L17-05 (`apiUnauthorized`/`apiValidationFailed`/`apiError` + logger contract), L04-07 (coaching_monthly_notes.personal_note é boundary separado de coin_ledger.reason — coach-visible é OK, PII financeiro não é). **Batch H avança 8/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓, L23-07 ✓, L23-11 ✓.
- L23-07 — Análise coletiva de grupo ganha RPC SECURITY DEFINER em 3 camadas (commit `fdda9e9`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "`coaching_kpis_daily` tem total. Coach quer: Distribuição de volume semanal, Atletas correndo mais do que recomendado, Atletas não correndo (attrition risk), Progresso coletivo vs mês anterior". **Estado pré-fix**: `coaching_kpis_daily` já carregava totais por group × day (DAU/WAU/MAU, attendance, adherence, churn_risk_count etc.), mas as 4 perguntas operacionais de um coach no weekly triage não tinham resposta direta — coaches teriam que pivotar manualmente em SQL contra `sessions` + `coaching_members` por athlete, window e comparativo window-anterior. Finding original propunha **materialised views**, rejeitado por 3 razões: (i) cardinality é sempre "1 group × 1-2 windows" — fan-out não justifica pre-aggregation; (ii) refresh jobs são hazard operacional (stale data visível em coach dashboard, refresh deadlocks em tempos de pico, incident-prone); (iii) L08-03 já entregou `idx_sessions_user_time` (btree em `(user_id, start_time_ms)` DESC) — exatamente o padrão de acesso per-athlete-per-window que essa análise precisa, então on-demand query é I/O-friendly sem pre-aggregation. **Fix em 3 camadas** (`supabase/migrations/20260421380000_l23_07_group_analytics_overview.sql` ~310 LOC + `portal/src/app/api/platform/analytics/group-overview/route.ts` + CI guard + runbook, ~700 LOC total): (a) **RPC `fn_group_analytics_overview(p_group_id uuid, p_window_days integer DEFAULT 28)`** — **STABLE SECURITY DEFINER** com `SET search_path = public, pg_temp` (defesa contra schema shadow attack). Returns `jsonb` com 4 keys. Auth gate: `SELECT role FROM coaching_members WHERE group_id = p_group_id AND user_id = auth.uid()`; se role NULL ou NOT IN ('coach','assistant') → `RAISE EXCEPTION ERRCODE = 'P0010' MESSAGE = 'UNAUTHORIZED'` imediato (não retorna rowset vazio que mascararia abuse). Input validation: p_group_id NULL → 22023 INVALID_GROUP_ID; p_window_days clamped para [7, 180] silenciosamente (7d é floor porque <7d de dados = ruído demais para distribuição/delta ser significativa; 180d é teto porque além disso o bounding-box do `sessions.start_time_ms` começa a hit rows antigas custando pg_toast I/O sem ganho de sinal). **Computação em 3 CTEs**: (i) `roster` = `coaching_members` com role='athlete' no group; (ii) `athlete_sessions` LEFT JOIN contra `sessions` filtrando `start_time_ms >= prev_window_start AND is_verified = true` (carries both windows); (iii) `per_athlete` agrupa por user_id computando km_window (current), km_prev_window (previous), km_last7 (curto), sessions_window (count), last_session_ms (MAX) — single-scan por athlete cobre 4 derivados. Final `jsonb_build_object` compõe 4 seções: **volume_distribution** (todos athletes com km_window + sessions_window, ordenado km_window DESC — coach vê quem está carregando vs falling behind imediatamente); **overtraining** (WHERE `km_window > 0 AND km_last7 > 1.5 * (km_window / (window_days / 7.0)) AND km_last7 >= 20` — athlete cuja média de 7 dias excede 1.5× da média normalizada-por-semana E volume absoluto ≥20km previne false-positive de beginner indo de 2→5km/week que numericamente é 2.5× mas não é overtraining; `ratio` pré-computado para coach ver magnitude); **attrition_risk** (`sessions_window = 0 OR now - last_session_ms > 14 days` — 14d matches L23-11 dormant-athlete definition para consistência cross-surface; ordenado `last_session_ms ASC` para most-urgent primeiro); **collective_progress** (jsonb com total_km_window / total_km_prev_window / delta_pct (NULL se prev=0, evita division-by-zero) / active_athletes (sessions_window>0) / total_athletes — coach vê MoM trajectory em 1 número). Todos floats arredondados a 2 decimais via `ROUND(...::numeric, 2)` — coach-friendly display, não scientific notation. (b) **Shape guard `fn_group_analytics_assert_shape()`** — SECURITY DEFINER callable apenas por service_role (CI). Verifica 4 invariants em pg_proc: (i) function exists; (ii) provolatile='s' (STABLE — se dev muda para VOLATILE por acidente, query planner cacheia diferente e coaches veem data stale entre 2 fetches no mesmo dashboard); (iii) prosecdef=true (SECURITY DEFINER — downgrade para SECURITY INVOKER removeria o auth gate silenciosamente, coaches sem membership começariam a ver outros groups); (iv) authenticated role tem EXECUTE privilege. Cada drift raise `P0010 L23-07 DRIFT:<reason>` com machine-readable marker (function_missing / wrong_volatility:s_got_v / not_security_definer / authenticated_missing_execute) — infra CI greps stderr pelo marker `L23-07` e falha o deploy antes de coaches hitting broken state. Migration termina com `DO BEGIN PERFORM fn_group_analytics_assert_shape(); RAISE NOTICE 'L23-07 migration self-test passed'; END` — se a migration aplicar com drift (impossible num happy path, mas possible se alguém edita a migration mid-review), o próprio apply falha. (c) **Portal route** `GET /api/platform/analytics/group-overview?group_id=&window_days=` — `createServerClient` + cookies para session, `supabase.auth.getUser()` gate upfront (early 401 via `apiUnauthorized` para UX rápido antes de hit DB); UUID validation via regex em group_id; window_days numeric parse + integer check → 422 via `apiValidationFailed`; RPC invoke via `supabase.rpc('fn_group_analytics_overview', {p_group_id, p_window_days})`; error path: se `error.message` contém 'UNAUTHORIZED' (re-raise do coach gate no DB) → mapeia para 401 `apiUnauthorized` (não para 500 confuso); outros errors → `apiError('DB_ERROR', ..., 500)` com `logger.error` incluindo contexto (L17-05 contract). **CI guard** `tools/audit/check-group-analytics.ts` (`npm run audit:group-analytics`, 23 sub-checks): migration file present + fn_group_analytics_overview declared + SECURITY DEFINER + STABLE + gates em coaching_members role IN ('coach','assistant') regex (não basta existir o filter; regex `role\\s+IN\\s*\\(\\s*'coach'\\s*,\\s*'assistant'\\s*\\)` dentro de 400 chars de `coaching_members` para prevenir accidental regression); UNAUTHORIZED raise exists; window_days clamped (regex `v_window_days\\s*<\\s*7` AND `v_window_days\\s*>\\s*180`); 4 response keys present (`'volume_distribution'`, `'overtraining'`, `'attrition_risk'`, `'collective_progress'`); GRANT EXECUTE TO authenticated + REVOKE ALL FROM PUBLIC; shape guard function declared + raises L23-07 DRIFT markers; migration has self-test DO block; portal route present + calls RPC + gates em auth + accepts group_id; runbook present + cross-links audit:group-analytics + L23-07. Smoke-testable: deletar linha `RAISE EXCEPTION USING ERRCODE = 'P0010' MESSAGE = 'UNAUTHORIZED'` → `RPC raises UNAUTHORIZED on non-coach` FAIL; remover `STABLE` keyword → `fn_group_analytics_overview is STABLE` FAIL; reverted clean. **Runbook** `docs/runbooks/GROUP_ANALYTICS_RUNBOOK.md` (~7KB, 8 seções): §1 why (RPC on-demand vs materialised view trade-off), §2 invariants table (8 invariants × 23 enforcement checks + runtime shape guard), §3 **response shape JSON** documentado em exemplo real (coach-product engineers copy-paste direto), **rules inline** (1.5× overtraining multiplier + ≥20km floor razão + 14d attrition razão + delta_pct NULL handling), §4 how-to (change threshold em 3 rules com PR discipline / add new cut em 4 steps CTE→jsonb_build_object→guard→runbook / expose CSV como follow-up sem criar second RPC), §5 **5 operational playbooks** — (5.1) coach "my athletes are missing" → check coaching_members role=athlete + is_verified=true + window coverage; (5.2) coach "overtraining flags wrong" → recompute manual + content change via §4.1 PR; (5.3) CI guard fails → 4 branches (migration / SECURITY DEFINER / role gate / response key); (5.4) runtime shape drift → P0010 L23-07 DRIFT marker significa out-of-band migration, NÃO bypass, open incident; (5.5) long query times → verify L08-03 index + window_days clamp; §6 detection signals table (CI red / runtime drift / route 401 / route 500 DB_ERROR); §7 rollback (additive — drop RPC + shape guard via reverse-migration; route file removal = 404 without leftovers); §8 cross-refs L08-03 (index dependency), L04-07 (PII scope — display_name é coach-visible não financial), L22-05 (same SECURITY DEFINER pattern + `fn_groups_nearby` sibling), L23-06 (coach-product surface sibling — together cover prescription+triage), L17-03/L17-05 (apiError/apiUnauthorized/apiValidationFailed + logger contract reuse). **Decisão arquitetural**: (i) **on-demand RPC, não materialised view** — 3 razões acima; se no futuro algum coach pipeline ler o overview 100× por minuto (dashboard auto-refresh agressivo), o wrapper layer (cache HTTP em CDN com `Cache-Control: private, max-age=60`) é lighter que MV refresh job; (ii) **SECURITY DEFINER**, não RLS — RLS em `sessions` + `coaching_members` requires per-row policy evaluation que não aproveita a aggregation natural do RPC; DEFINER com explicit role check é simpler, audit-visible, e consistente com L22-05 pattern; (iii) **14-day attrition threshold** hardcoded, não configurable — mesmo número serve L23-11 coach monthly report; divergence entre surfaces confunde coach (athlete que é "inactive" aqui mas "active" lá); content change via PR + multi-surface update; (iv) **1.5× + 20km floor** — 1.5× sozinho dispara para beginners saindo de período off; 20km absoluto é mínimo onde overtraining como conceito faz sentido (abaixo disso athlete está construindo base, não overreaching); (v) **window_days clamp silencioso**, não rejeição — coach UI pode enviar 365 por slider bug, prefiro servir 180 silent + log que 422 visível que quebra dashboard; §5.5 runbook explica; (vi) **jsonb return**, não TABLE — 4 seções heterogêneas com cardinalities diferentes (volume_distribution é toda roster, overtraining é subset, collective_progress é single row); jsonb evita 4 RPCs separadas + mantém atomicity (coach vê cut consistent do mesmo snapshot temporal); (vii) **sem paginação** — group sizes típico 20-50 athletes, max 200; sempre retorna whole roster; paginate futura via UI-side slicing. **Escopo deliberadamente excluído**: UI coach dashboard page que consome `/api/platform/analytics/group-overview` (follow-up L23-07-ui, requer Chart.js ou equivalente para distribution graph); CSV export endpoint (follow-up mencionado em §4.3 do runbook); docker integration test contra supabase_db container (skipped porque docker não disponível em CI ambient; shape guard runtime + static guard cobrem 23 invariants suficientes); materialised view como opcional opt-in para groups 500+ athletes (not needed no MVP, futura optimization); per-coach threshold override em `coaching_groups.overtraining_multiplier` (currently thresholds globals; override futuro permitiria sport-specific tuning); historical trend chart "últimas 12 semanas" (requires window snapshots persisted — different architecture); integration com `coaching_kpis_daily` (existing daily totals + overview são complementary, não conflituante — daily KPIs for ops team, overview for coach triage); notifications "você tem 3 atletas em attrition risk" (follow-up L23-07-notify para hook no edge function + push notification); ACL granular para `role='assistant'` (currently coach+assistant ambos têm full access; se assistant-only subset for needed, add `coaching_assistant_permissions` table); analytics event telemetry quando coach acessa (follow-up ProductEventTracker + privacy review porque data has PII names). **Verificação**: `npm run audit:group-analytics` 23/23 checks green em ~1.4s; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerados clean; `next lint` em portal/src/app/api/platform/analytics 0 warnings. Docker integration não executada (infra CI invoca shape guard via psql em deploy pipeline). Cross-refs: L08-03 (dependency crítica — `idx_sessions_user_time` torna per-athlete scan sub-linear), L22-05 (same SECURITY DEFINER + caller-auth + shape-guard pattern), L23-06 (sibling coach-product surface — prescription+triage pair), L17-03 (`apiError`/`apiUnauthorized`/`apiValidationFailed` contract reuse), L17-05 (logger.error Sentry route em error path), L04-07 (display_name scope ≠ coin_ledger.reason PII scope; coaching_members.display_name é coach-visible PII separate boundary), L10-07 (JWT aud/iss validation já cobre auth.getUser path — no nova surface), L23-11 (mesma `14-day` dormant threshold para consistency). **Batch H avança 7/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓, L23-07 ✓.
- L23-06 — Plano periodizado ganha wizard coach-facing em 4 camadas (commit `b4f36a9`, 2026-04-21): fecha o high de Lente 23 (Treinador) sobre "Módulo `training-plan` (1500 linhas) presumivelmente lida com plans. Auditoria rápida não confirma periodização (base → build → peak → taper)". **Estado pré-fix**: a migration `20260407000000_training_plan_module.sql` já shippou com coluna `cycle_type` no `training_plan_weeks` (enum base/build/peak/recovery/test/free/taper/transition já CHECK-bounded), e `20260415010000_descriptive_workout_blocks.sql` + `20260416000000_bulk_assign_and_week_templates.sql` já cobriam bulk assignment e week templates — ou seja, **a infraestrutura de persistência estava pronta**, mas nenhum wizard emitia os blocks. Coach rodando 30 atletas numa meia-maratona de 12 semanas tinha que hand-clicar 30 × 12 = 360 cycle-type selections por ciclo; o finding literalmente chamou isso de "sem escala". **Fix em 4 camadas puras TypeScript** (`portal/src/lib/periodization/types.ts` + `generate-periodization.ts` + `portal/src/app/api/training-plan/wizard/route.ts` + `tools/audit/check-periodization-template.ts`, ~750 LOC líquidas): (a) **Type catalogue `types.ts`** — exporta `RaceTarget` union (fiveK/tenK/halfMarathon/marathon), `AthleteLevel` (beginner/intermediate/advanced), `CycleType` (base/build/peak/taper/recovery/test), `PeriodizationBlock` (cycleType + weekNumbers[] + focusText + weeklyVolumeKm + intensityHint) e `PeriodizationPlan` (wrapper completo). `RACE_SPECS` record carrega contract de cada distância: `distanceKm` (5/10/21.0975/42.195 canônicos), `taperWeeks` (1/1/2/3 — marathon tem afinamento mais longo), `peakWeeks` (2/3/3/4), `baseFraction/buildFraction` (0.4/0.3 universais — base domina), `minTotalWeeks/maxTotalWeeks` (6-16 / 8-20 / 10-24 / 14-28 — maratona exige mínimo 14 semanas, rejeita prep rushada; teto 28 previne timeline absurdo onde partition quebra), `peakWeeklyKmByLevel` ({beginner: 25-60, intermediate: 45-90, advanced: 70-130 escalonado por distância}). `RACE_TARGETS`/`ATHLETE_LEVELS` arrays expostos para UI + CI. (b) **Generator pure `generatePeriodization({raceTarget, totalWeeks, athleteLevel})`** — determinístico, zero I/O, zero platform binding. Validação em camada: input rejeita unknown enum (`UNKNOWN_RACE_TARGET`/`UNKNOWN_ATHLETE_LEVEL`), non-integer totalWeeks (`TOTAL_WEEKS_NOT_INT`), out-of-range (`TOTAL_WEEKS_OUT_OF_RANGE`) — todos via `PeriodizationInputError` que o route mapeia para 422. `partitionWeeks(totalWeeks, spec)` calcula em ordem: taperLen = min(spec.taperWeeks, max(1, totalWeeks-3)); peakLen = min(spec.peakWeeks, max(1, totalWeeks-taperLen-2)); remaining = totalWeeks-taperLen-peakLen; baseLen = max(2, round(remaining × baseFraction/(baseFraction+buildFraction))); buildLen = remaining-baseLen. Ordem importa: taper first (protege a fase mais sensível), peak depois (intensidade preserve), base/build absorvem resto. buildLen pode ser 0 em plans muito curtos (6-week fiveK = 3 base + 2 peak + 1 taper). Cada block recebe weeklyVolumeKm derivado do peak: base=0.55×peak, build=0.8×peak, peak=peak, taper=0.5×peak — trajetória volumétrica clássica. `focusText` carrega pt-BR locale-specific ("Construção de base aeróbica para Meia-maratona — volume fácil, poucos estímulos"); `intensityHint` combina zona (Z1-Z2 base, Z2-Z3 build, Z3-Z4 peak, Z2 taper) + level hint (beginner "(ritmo conversacional sempre)" / intermediate "(inclua 1 sessão de qualidade)" / advanced "(até 3 sessões de qualidade/semana)"). (c) **Validator `assertPeriodizationPlanValid(plan)`** — enforçado no fim do generator E exportado para consumer externo. 7 invariants: (1) plan tem ≥1 block; (2) cada block tem ≥1 week; (3) cada block tem weeklyVolumeKm > 0 (`NONPOSITIVE_VOLUME` — protege contra quiet zero que levaria coach a achar que há "semana de descanso total" onde não há); (4) sem weeks duplicadas entre blocks (`OVERLAPPING_WEEKS`); (5) weeks cobrem 1..totalWeeks contíguo (`WEEKS_NOT_CONTIGUOUS` — catches partition bug onde block faltante vazaria); (6) primeiro block = base (`FIRST_BLOCK_NOT_BASE`); (7) último block = taper (`LAST_BLOCK_NOT_TAPER`) + penúltimo = peak ou build (`PENULTIMATE_BLOCK_INVALID` — não pode ter "taper → taper" ou "recovery → taper"). Todas errors carregam `[CODE] message` format para regex-matching em testes. (d) **Route `POST /api/training-plan/wizard`** — `createServerClient` + `cookies()` para auth context (mesmo padrão de `/api/groups/nearby` L22-05); `supabase.auth.getUser()` gate upfront (anon 401 via `apiUnauthorized`); body JSON parse com try/catch → 422 "Body must be JSON"; valida `raceTarget` e `athleteLevel` contra `RACE_TARGETS`/`ATHLETE_LEVELS` arrays antes de entrar no generator (defense em depth — generator valida novamente mas route dá 422 friendly em vez de 500 stack); `totalWeeks` integer check; `try { generatePeriodization(...) } catch (PeriodizationInputError) { return apiValidationFailed(...) }` pattern explícito — generator throws são contract, nunca 500. Unhandled throw cai no outer try/catch logger.error → 500 genérico. **Escopo deliberadamente preview-only**: route NUNCA escreve em `training_plans` ou `training_plan_weeks` — coach vê o preview, edita blocks (não workouts!), e só então chama endpoints existentes `/api/training-plan/[planId]/weeks` para materializar. Esse split é a decisão "coach edita blocks, não workouts individuais — escala" do próprio finding. **Testes** (`portal/src/lib/periodization/__tests__/generate-periodization.test.ts`, 21 cases vitest): (1-5) contract rejection — unknown race target, unknown athlete level, non-integer weeks, below min, above max; (6-13) half-marathon 12w intermediate (exemplo do finding): cobre all 12 weeks contíguos, começa base termina taper, contém build+peak, taper=2 weeks exatos, peak volume==spec.peakWeeklyKmByLevel.intermediate, base<build<peak volume ascending, taper<peak descending, focusText contém "Meia-maratona"; (14-17) matrix invariants — iterates todos 4 targets × 3 levels × todos valid weeks (~600 plans gerados no teste, finaliza <100ms porque zero I/O): cada plan passa `assertPeriodizationPlanValid`, cobre exatamente `totalWeeks` weeks unique, começa base termina taper, peak volume matches spec, taper length matches spec.taperWeeks quando weeks são generosas, beginner peak < advanced peak (verifica que level modula volume); (18-21) validator negative tests — rejeita plan com week skipped (`WEEKS_NOT_CONTIGUOUS`), plan começando com build não base (`FIRST_BLOCK_NOT_BASE`), plan terminando com peak não taper (`LAST_BLOCK_NOT_TAPER`), plan com weeklyVolumeKm=0 (`NONPOSITIVE_VOLUME`). **CI guard** `tools/audit/check-periodization-template.ts` (`npm run audit:periodization-template`, 38 sub-checks): (a) types file — presente + declara todas 4 race targets em `RACE_SPECS` (regex `\\b<target>:\\s*{`); para cada target extrai o bloco balanced-brace e verifica (i) peakWeeklyKmByLevel tem todos 3 levels (`beginner:`/`intermediate:`/`advanced:`), (ii) min/maxTotalWeeks declarados numericamente, (iii) max > min (`range is ascending`); exports `RACE_TARGETS` + `ATHLETE_LEVELS`. (b) generator file — presente + exports `generatePeriodization`/`assertPeriodizationPlanValid`/`PeriodizationInputError`; regex `cycleType:\\s*"<cycle>"` para cada um de base/build/peak/taper confirma que switch statement não foi parcialmente deletado; validator references 4 error codes (`FIRST_BLOCK_NOT_BASE`/`LAST_BLOCK_NOT_TAPER`/`WEEKS_NOT_CONTIGUOUS`/`NONPOSITIVE_VOLUME`). (c) route — presente + gates em `supabase.auth.getUser()` (anon scrape guard); importa generator; `PeriodizationInputError` é mapped para `apiValidationFailed` (catch branch não foi deletado). (d) runbook — presente + cross-links `check-periodization-template` + `L23-06`. Smoke-testable: remover um `peakWeeklyKmByLevel.intermediate` entry → `spec <target> has full peakWeeklyKmByLevel map` FAIL apontando "missing level(s): intermediate"; deletar `.getUser()` do route → `wizard route gates on supabase.auth.getUser()` FAIL. **Runbook** `docs/runbooks/PERIODIZATION_WIZARD_RUNBOOK.md` (~5KB, 8 seções): §1 why (hand-click 360 cycle-types por ciclo não escala), §2 invariants table (7 invariants × enforced em 38 checks), §3 what shipped (6-file role map), §4 how-to (add race target em 5 steps / change peak volume em 3 steps / wire materialiser com response shape JSON), §5 **4 operational playbooks** — (5.1) coach complaint "plano muito fácil/difícil" → coletar {target, weeks, level}, reproduzir local, ≥3 data points antes de mudar `peakWeeklyKmByLevel` (NUNCA per-coach override — content change é global); (5.2) route 422 "totalWeeks out of range" → expected, UI deve warning min timeline, não relaxar guard; (5.3) CI guard fails → branches por failure name (types rename / RACE_SPECS removal / cycle switch broken / auth removed); (5.4) matrix test slow → deveria terminar <1s, se creep > 1s alguém adicionou I/O, grep por `await`/`fetch` em generator. §6 detection signals (CI red / verify green mas route 500 = validator disabled / coaches filing "plan starts at peak" = validator removed); §7 rollback (additive — 3 libs + 1 route + 1 runbook; saved plans NÃO tocados porque nunca chamaram wizard); §8 cross-refs L22-05 (same coach persona, same auth pattern), L22-06 (AudioCoachLocale reuse pending pt-BR-only copy), L19-08 (CHECK naming já compliant em `chk_training_plan_weeks_cycle_type`), L17-05 (logger swallows generator throws, stack não leaka). **Decisão arquitetural**: (i) **4 race targets, não 10+** — 5K/10K/half/marathon cobrem 95% da demanda amador-intermediate-master; trail/ultra/triathlon são personas distintas com periodização fundamentalmente diferente (block taper não é canon em trail ultras); (ii) **generator pure TypeScript, não SQL function** — periodização é coach-product decision domain, não DB-domain; SQL function forçaria deploy migration para cada spec tweak + dificulta unit test; (iii) **preview-only route**, não materializer all-in-one — coach deve ver + editar blocks (não workouts!) antes de commit; all-in-one materializer removeria o edit-step que é o ponto de escala; (iv) **weeklyVolumeKm como number, não range** — coach pode sempre ajustar pra range manualmente, mas primitive é single target pra comparação cross-level/cross-target; (v) **NO remote config** para spec tweaks — changes em `RACE_SPECS` são content changes que devem ir via PR + coach-education review (runbook §4.2); remote config bypassa revisão e silenciosa drift em plans salvos; (vi) **baseFraction=0.4 / buildFraction=0.3 universal** — fraction per-target seria tuning infinito; se target-specific fraction needed no futuro, add ao `RaceSpec`. **Escopo deliberadamente excluído**: UI wizard surface (follow-up L23-06-ui com form `<select>raceTarget` + `<input type=number>totalWeeks` + `<select>athleteLevel` + preview table + "Apply to plan" button); materializer wiring que convert `PeriodizationPlan.blocks[]` em INSERT into `training_plan_weeks` (follow-up L23-06-materialize, exige decision de quais workouts default gerar em cada cycle_type); race targets trail/ultra/triathlon (personas distintas, follow-up); locale pt-BR-only em focusText/intensityHint (AudioCoachLocale reuse deferred até coach locale formalized); per-athlete tweaks (beginner com histórico de lesão reduz peakWeeklyKm em 20% — follow-up com `athlete_constraints` table); integration com `workout_delivery_batches` + `coaching_workout_templates` para auto-popular workouts dos blocks (follow-up L23-06-workouts pending decision de default workout library per cycle_type). **Verificação**: `npx vitest run src/lib/periodization` 21/21 passes em 32ms (matrix 600-plan coverage em <100ms); `npx next lint --file src/lib/periodization --file src/app/api/training-plan/wizard` 0 warnings; `npm run audit:periodization-template` 38/38 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerados clean. Cross-refs: L22-05 (mesmo auth padrão `supabase.auth.getUser()` + `apiUnauthorized` gate), L22-09 (celebration triggers podem ler `PeriodizationPlan.blocks[].cycleType == 'peak'` para variar celebratory copy em future follow-up), L17-03 (route usa `apiValidationFailed`/`apiUnauthorized`/`apiError` contract estabelecido), L17-05 (logger.error captura unhandled throws em Sentry), L19-08 (CHECK naming em `training_plan_weeks.cycle_type` já compliant). **Batch H avança 6/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓, L23-06 ✓.
- L22-09 — Progress celebration ganha detector domain-puro em 4 camadas (commit `e41d5d4`, 2026-04-21): fecha o high de Lente 22 (Atleta Amador) sobre "Primeira corrida completa, primeira semana, primeira 5K — sem celebração visual (confete, animação)". **Estado pré-fix**: `ConfettiBurst` + `AnimatedCheckmark` + `showSuccessOverlay` já existiam em `success_overlay.dart` (8-cor confetti com gravity sim, stroke painter do check, haptic feedback) e eram chamados em dois surfaces isolados (L09-09 adhesion signing + L22-05 guia-se create) — ou seja, a **infraestrutura de render estava pronta**, mas nenhuma lógica domain-level detectava *quando* disparar. `ProfileProgressEntity` já carregava `lifetimeSessionCount`/`lifetimeDistanceM`/`dailyStreakCount`/`weeklySessionCount`/`streakBest` (atualizado por `UpdateStreak` single-writer + session credit pipeline), mas não havia consumidor que calculasse "é a primeira vez que este contador cruza 7?" — resultando em runner amador completando 5K e recebendo zero feedback visual. **Fix em 4 camadas domain-puras** (`omni_runner/lib/domain/value_objects/milestone_kind.dart` + `entities/milestone_entity.dart` + `services/milestone_detector.dart` + `services/milestone_copy_builder.dart`, ~550 LOC líquidas): (a) **Value-object `MilestoneKind`** — enum com 9 variantes (firstRun / firstFiveK / firstTenK / firstHalfMarathon / firstMarathon / firstWeek / streakSeven / streakThirty / longestRunEver) cada carregando três campos required: (i) `dedupKey` string estável (`first_run`, `first_5k`, `first_half_marathon`, …) que será persistido pelo presenter após overlay fechar — comentário explícito avisa que renomear in-place é migração (bump para `first_5k_v2` + migrar legacy at read time), (ii) `priority` int (1-9, menor = maior celebração — firstRun=1 é o mais dramático, longestRunEver=9 é rotineiro após record inicial), (iii) `distanceThresholdM double?` para milestones ancorados em distância (5000 / 10000 / 21097.5 / 42195 m), `null` para streaks e weekly aggregates. (b) **Entity `MilestoneEntity`** (`Equatable`) com `kind` + `achievedAtMs` + `triggerDistanceM?` + `triggerCount?`. Getter `dedupKey` é runtime-augmented: para `longestRunEver` anexa decametros rounded (`longest_run_ever:780` para 7800m) — permite consecutive new records cada um fire once, enquanto outras kinds retornam `kind.dedupKey` raw. (c) **Pure detector `MilestoneDetector.detect(input)`** — input value-object `MilestoneDetectionInput(previousProgress, currentProgress, sessionDistanceM, previousMaxDistanceM, alreadyCelebratedKeys, achievedAtMs)`. Previous nullable = all-zero fallback (first-ever session path limpo). Detector branches: firstRun se `prevLifetime==0 && currLifetime>=1`; for each {5K, 10K, half, marathon} se `sessionDistanceM >= threshold && previousMaxDistanceM < threshold` (pin sua própria threshold no enum, não aninhado); firstWeek se `prevWeekly < 3 && currWeekly >= 3` (constante `firstWeekSessionThreshold=3` exposta para CI pin); streakSeven se `prevStreak<7 && currStreak>=7`; streakThirty se `prevStreak<30 && currStreak>=30`; longestRunEver se `sessionDistanceM > previousMaxDistanceM > 0`. Cada add-candidate consulta `alreadyCelebratedKeys` via `entity.dedupKey` e descarta silently se já-celebrado. Output sorted por `kind.priority` ascending — UI renderiza o mais dramático primeiro, queue os restantes. **Zero platform calls, zero async, zero DB** — 100% testável in-memory. (d) **Copy builder `MilestoneCopyBuilder(locale)`** — reusa `AudioCoachLocale` (pt-BR/en/es, mesmo VO do L22-06 e L22-08 — single sender-side locale para toda a superfície emocional). Switch por locale → switch por kind → retorna `MilestoneCopy{title, subtitle, shareText}`. Pt-BR "Primeira corrida concluída! 🎉" / "Você é oficialmente runner"; en "First run done! 🎉" / "You're officially a runner"; es "¡Primera carrera completada! 🎉" / "Ahora eres runner". Distance-anchored kinds embedded trigger distance via helper `_km(meters)` → `5.10 km`. `shareText` sempre menciona "Omni Runner" (reforço de brand para viralização se compartilhado na timeline — cobre o `[15.3]` cross-ref do finding sobre OG share). `static const requiredKinds = MilestoneKind.values` espelhado pelo CI guard para catch silencioso de kind-dropped-from-enum. **Testes** (28 cases em 2 arquivos): `milestone_detector_test.dart` (21) — firstRun fires on 0→1 crossing + NÃO fires em subsequent + dedup suppression; 4 distance kinds fire exactly once with correct triggerDistanceM + NÃO fire se prior max já crossed + threshold-exact 21097.5m half-marathon + 42195m marathon; hero session crossing both 5K+10K fires both simultaneously; firstWeek fires crossing 3 with triggerCount=3 + NÃO fires se prior weekly era 3 + dedup; streakSeven fires 6→7 + streakThirty fires independent of streakSeven dedup (`alreadyCelebratedKeys: {'streak_7'}`) + streak drop 7→0 fires neither; longestRunEver fires strictly-greater + dedupKey `longest_run_ever:780` allows `:920` next record + same-distance repeat doesn't fire; output sorted ascending priority + every kind has unique non-empty dedupKey + enum cardinality pinned em 9 (adicionar 10th kind força tocar este teste forçando PR review). `milestone_copy_builder_test.dart` (7) — 9 kinds × 3 locales = 27 combinations iterados provando title/subtitle/shareText non-empty; pt-BR firstRun carries "Primeira corrida"; en firstFiveK embeds `5.10 km`; es streakThirty carries "30 días"; shareText always contains "Omni Runner"; requiredKinds matches enum.values; MilestoneEntity.dedupKey for longestRunEver augments with distance (`:780` vs `:920`). **CI guard** `tools/audit/check-milestone-celebration.ts` (`npm run audit:milestone-celebration`) — 40 sub-checks: `milestone_kind.dart` present; enum declares all 9 REQUIRED_KINDS via regex `^\\s*<kind>\\(` (catches accidental rename/delete — dropping `firstWeek` falha); collects all dedupKey literais via regex e verifica count==9 + Set.size==keys.length (unique) + cada non-empty; `milestone_copy_builder.dart` present; `_ptBR(`, `_en(`, `_es(` methods exist; regex `case\\s+MilestoneKind\\.<kind>\\s*:` matches ≥3 times per kind (once per locale switch — drop one locale branch para qualquer kind falha); `requiredKinds` list exportado; `milestone_detector.dart` present + references `firstWeekSessionThreshold` constant; runbook present + cross-links `check-milestone-celebration` + `L22-09`. Smoke-testable: renomear um dedupKey → `every dedupKey is unique` FALHA (colisão detectada); deletar case branch de `streakThirty` no `_en()` → `builder branches on MilestoneKind.streakThirty (≥3 times)` FALHA com "found 2". **Runbook** `docs/runbooks/CELEBRATION_RUNBOOK.md` (~7KB, 5 seções): §1 why (infraestrutura de render já existia + ProfileProgressEntity já tinha counters, mas nenhum consumer detectava crossings), §2 invariants table (4 invariants × enforced em 40 checks), §3 **9-kind table** (priority × dedupKey × trigger formalmente documentados — serve como referência rápida para dev que implementa o presenter), §4 how-to (add new kind 5 steps + add new locale 4 steps + wire post-session hook 5 steps), §5 **4 operational playbooks** — (5.1) "milestone fired twice" → diagnosis via SharedPreferences + previousMaxDistanceM=0 em snapshot fetch failure + fix "dedup set é single source of truth, write ONLY após overlay close"; (5.2) "did not fire on obvious first run" → lifetimeSessionCount ordering + account-restore intencional NO-fire porque emotional beat was already used; (5.3) "retro-fire para existing users" → product antipattern, se insistir use banner não overlay; (5.4) "copy wrong in prod" → hotfix app release, NEVER tentar Firebase Remote Config para emoji-heavy strings (mojibake hazard); detection signals (CI red / PostHog zero-fires over 7d / Sentry warning em detector pure = programming error); rollback (pure additive, revert commit limpa tudo). **Decisão arquitetural**: (i) **domain-pure detector**, não consumer-orchestrated — presenter é dumb, detector decide o que e quando; mover lógica para presenter dispersaria 9 condições em N screens e re-introduziria o bug original (cada screen com sua próprio if-chain); (ii) **dedupKey persistido localmente**, não server-side — emoção é momento local; persistência server gera sync-latency onde overlay já fechou e banner atrasado soa performativo; trade-off: account-restore dos users NÃO re-fire; (iii) **9 kinds, não 15** — selecionado o set que o finding cita + obvious complement (firstTenK, firstHalfMarathon, firstMarathon são ladder natural do firstFiveK; streakThirty é natural extension do streakSeven; longestRunEver cobre records pós-onboarding). Futuras adições (firstNightRun, firstSubFourMinKm, etc.) seguem process "Add new milestone kind" do runbook; (iv) **sorted por priority ascending output** permite presenter render queue — firstRun (dramatic) primeiro, longestRunEver (rotineiro) último; trade-off: se presenter só renderiza o topo da queue (single overlay), menor-priority kinds são silently lost — mitigação: presenter deve enqueue todos + show sequentially com 500ms delay (docs no runbook §4); (v) **distanceThresholdM double**, não integer — 21097.5 (half marathon) e 42195 (marathon) são canônicos; double evita conversão ida-volta em comparação com GPS-measured sessionDistanceM float; (vi) **longestRunEver dedupKey augmented with decametros** (não metros) — rounding: um user que corre 7821m e depois 7824m NÃO deveria receber new-record overlay duas vezes por 3m de jitter GPS; 10m bucket dá estabilidade sem perder semantic "new record". **Escopo deliberadamente excluído**: presenter/screen wiring concreto (`showMilestoneOverlay(context, MilestoneCopy)`) virá em follow-up L22-09-presenter — session controller pipeline a ser escolhido é decisão pendente (atualmente não há single post-session hook; sessions registration dispersa em `submit_run_to_challenge` + `reward_session_coins` + `update_streak` + `post_session_challenge_dispatcher` — L22-09-presenter será PR que escolhe o right surface + dedup set storage key via PrefsSafeKey.plain); Lottie animations (finding menciona mas overlay atual com ConfettiBurst + AnimatedCheckmark é suficiente para MVP; Lottie adiciona asset hosting + 200KB binary per animation — não justificado); OG image generation para share (finding cross-ref `[15.3]` — follow-up exige render pipeline pagina HTML com chrome headless ou server-side imagemagick, scope creep); milestone history screen "Suas conquistas" (para hall-of-fame review — follow-up, exige novo screen + repo query sobre dedupKey set); challenge-group milestones ("Grupo cruzou 100 km coletivo") — não é persona amador, será L24-XX futuro; analytics event wiring (`posthog.capture('milestone_celebrated', {kind, distance_m}')` — follow-up obrigatório quando presenter lander, facilita product validation). **Verificação**: `flutter analyze` 0 issues em 0.6s sobre os 4 novos arquivos; `flutter test test/domain/services/milestone_detector_test.dart test/domain/services/milestone_copy_builder_test.dart` 28/28 green em 1.3s; `npm run audit:milestone-celebration` 40/40 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` regenerated clean. Cross-refs: L22-06 (mesmo `AudioCoachLocale` VO — sender-side locale consistente), L22-08 (shareText alimenta o viral loop WhatsApp — milestones tocam direto `ChallengeInviteMessageBuilder._native`), L11-05 (futura `PreferencesKeys.milestoneDedupKeys` plain-entry para persistir set — passa pelo 3-layer defense), L17-05 (Sentry captura warnings se detector lançar — detector puro nunca deveria, exception = programming error detectada), L09-09 (adhesion flow já usa `showSuccessOverlay` — reuso consistente do render), L08-04 (sessions coherence CHECK garante `lifetimeDistanceM` nunca regride, detector confia no delta). **Batch H avança 5/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓, L22-09 ✓.
- L22-08 — Viralização via WhatsApp ganha pipeline de domínio em 5 camadas (commit `d52f941`, 2026-04-21): fecha o high de Lente 22 (Atleta Amador) sobre "`challenge-create` existe. UX para convidar amigos via WhatsApp (deep link pré-preenchido) fraca". **Estado pré-fix**: `ChallengeInviteScreen` buildava a deep link inline como string raw (`'https://omnirunner.app/challenge/${_challenge.id}'` em três pontos distintos — copy button, share text, URL display), share text estava hard-coded em pt-BR sem canal-específica affordance, não havia botão "WhatsApp" distinguível do share sheet genérico, e o fallback para "WhatsApp não instalado" era undefined. Os `.well-known/*` proofs já existiam em `portal/public/.well-known/assetlinks.json` (fingerprint SHA256 release real) + `apple-app-site-association` (com `/challenge/*` já listado em `paths`), e o `DeepLinkHandler._parse` já emitia `ChallengeAction(id)` — ou seja, a infraestrutura de plataforma para App Links / Universal Links estava pronta mas a UX de disparo do share ainda era amateur. **Fix em 5 camadas** (`omni_runner/lib/domain/value_objects/challenge_invite_link.dart` + `challenge_share_channel.dart` + `services/challenge_invite_message_builder.dart` + `entities/challenge_share_intent_entity.dart` + `usecases/gamification/share_challenge_invite.dart` + `presentation/screens/challenge_invite_screen.dart`, ~550 LOC líquidas): (a) **Value-object canônico `ChallengeInviteLink`** — `canonicalHost = 'omnirunner.app'` (constante explícita; comentário pesado documentando que mudar esse literal é uma migração multi-plataforma porque versões já liberadas do app parseiam contra o literal hard-coded), `acceptedHosts = ['omnirunner.app', 'www.omnirunner.app']` (www. alias para SEO), `pathSegment = 'challenge'` (bound ao `paths` array da AASA), factory `.forId(challengeId)` que rejeita empty ids com `ArgumentError` (evita URL degenerada `https://omnirunner.app/challenge/` que levaria usuários a página morta), getter `.url` que retorna `'https://omnirunner.app/challenge/<id>'`, `isCanonicalHost(host)` case-insensitive, `tryExtractId(raw)` null-safe que rejeita host não-canônico/path errado/id vazio. Operator== + hashCode por `challengeId` — value-semantic correto. (b) **Enum `ChallengeShareChannel`** (whatsapp | native | copyLink) — escolha de channel vira vocabulário domain-level, não mágica de switch-case inline em cada callsite. Cada channel tem norm distinta: whatsapp é emoji-heavy/curto/URL-encoded via wa.me; native aceita body mais longo via share_plus; copyLink é URL pura. (c) **Service pure `ChallengeInviteMessageBuilder(locale)`** — reusa `AudioCoachLocale` (pt-BR/en/es) como chave de i18n (mesmo value-object do L22-06 — a decisão do sender-side locale vive em um só lugar). `_whatsapp(title, inviter, url)` tem openers naturais por locale ("Bora correr? 🏃‍♂️" / "Let's run! 🏃‍♂️" / "¡A correr! 🏃‍♂️"), `_native(...)` e `_fallbackTitle()` para título vazio. `buildWhatsAppUrl(...)` compõe `https://wa.me/?text=<Uri.encodeQueryComponent(body)>` — encodeQueryComponent usa x-www-form-urlencoded `+` para spaces (WhatsApp aceita; importante é que nenhum espaço raw escape + ampersand vira `%26` senão trunca a query string). `static const supportedLocales = [ptBR, en, es]` é espelhado pelo CI guard. (d) **Entity `ChallengeShareIntentEntity`** (channel, url, text, subject, platformLaunchUrl) + **use-case `ShareChallengeInvite(locale)`** que compõe tudo: cria `ChallengeInviteLink.forId(challengeId)`, pede o text via builder, popula `platformLaunchUrl` com o wa.me URL apenas para channel.whatsapp, retorna intent pronto para handoff ao gateway. Use-case é pure (zero platform calls aqui — dispatch é job de gateway em data-layer). (e) **`ChallengeInviteScreen` refactor** — substitui a string inline `'https://omnirunner.app/challenge/${_challenge.id}'` por `ChallengeInviteLink.forId(_challenge.id).url`; share button original "Enviar convite" se transforma em **dois** botões: "Convidar via WhatsApp" (green 0xFF25D366, icon `chat_rounded`) + "Outros apps" (outlined, icon `share_rounded`). `_shareViaWhatsApp()` chama `ShareChallengeInvite()(..., channel: whatsapp)`, extrai `platformLaunchUrl`, parseia como URI, e chama `url_launcher.launchUrl(uri, mode: externalApplication)`; se launchUrl retorna false ou throws, snackbar informa "WhatsApp não disponível. Usando compartilhamento nativo." e fallback para `_shareViaNative()`. `_shareViaNative()` chama `SharePlus.instance.share(ShareParams(text: intent.text))`. Ambos marcam `_shared = true` para o PopScope confirm exit dialog permitir close. Removeu os helpers `_goalLabel` e `_formatWindow` do state (não eram referenciados depois do refactor). **Testes** (28 cases em 3 arquivos, todos dart puros sem platform channels): `test/domain/value_objects/challenge_invite_link_test.dart` (10) — host/path constants matching .well-known/*, `forId` produz URL canônico + toString delega, empty id rejeitado, `isCanonicalHost` case-insensitive + recusa typosquat `omni-runner.app`/evil.com/empty, `tryExtractId` aceita well-formed + recusa host errado/path wrong/empty id/non-URL, value equality via `operator==` e hashCode. `test/domain/services/challenge_invite_message_builder_test.dart` (11) — canonical host sanity, default locale é ptBR, supportedLocales enumera os 3, channel.copyLink retorna URL verbatim em todo locale, whatsapp+native channels contém URL em toda locale, inviter-name embed condicional, empty/whitespace/null title collapsing para fallback consistente, pt-BR opener "Bora correr?" / en "Let's run!" / es "¡A correr!", buildWhatsAppUrl URL-encodes body (check `%26` em title "Sprint 10k & friends", nunca deixa raw space). `test/domain/usecases/gamification/share_challenge_invite_test.dart` (7) — whatsapp channel popula platformLaunchUrl com wa.me + embeda title + text contém URL; native channel NÃO popula platformLaunchUrl; copyLink channel text === url; locale switch muda composed copy (ptBr ≠ en ≠ es); empty challengeId throws; subject mirrors title; URL sempre usa canonicalHost. **CI guard** `tools/audit/check-challenge-invite-deep-link.ts` (`npm run audit:challenge-invite-deep-link`) — 19 sub-checks: ChallengeInviteLink file present + canonicalHost==omnirunner.app + acceptedHosts includes www. + pathSegment==challenge; assetlinks.json present + parseable JSON + has package_name + has non-empty sha256 fingerprints + package_name matches Android manifest; apple-app-site-association present + parseable + covers /challenge/* em paths; ChallengeInviteMessageBuilder present + references AudioCoachLocale.{ptBR,en,es} + exports supportedLocales + exposes buildWhatsAppUrl; runbook present + cross-links check name + cross-links L22-08. Smoke-tested: mutei canonicalHost para 'example.com' → guard cai para 18/19 (apenas canonicalHost falha, o resto mantém verde demonstrando que cada invariant é ortogonal); revert clean 19/19. **Runbook** `docs/runbooks/CHALLENGE_INVITE_VIRAL_RUNBOOK.md` (~9KB, 6 seções): §1 why (viral loop MVP com hard-coded string era dead end), §2 invariants table (6 invariants enforced em 19 checks), §3 end-to-end flow (criar challenge → WhatsApp button → wa.me → recipient tap → App Links/Universal Links resolve → DeepLinkHandler emits ChallengeAction → router), §4 how-to (4 procedures: add new locale em 4 steps, rotar Android signing cert em 4 steps NUNCA removendo SHA256 antigo antes de force-upgrade floor, changar canonical host em 5 steps com 302 bridge + double host accept window, add new share channel em 5 steps), §5 **4 operational playbooks** — (5.1) "recipients abrem browser em Android" → `adb shell pm get-app-links` + portal curl verify + CI guard + troubleshoot SHA256 mismatch; (5.2) "abrem Safari em iOS" → AASA curl verify (no redirects/HTTPS only/no .json suffix) + 24h cache lag + force-reinstall; (5.3) "WhatsApp button does nothing" → ChallengeInvite log tag + url_launcher plugin check + Android 11+ `queries` block; (5.4) "shared link wrong language" → sender-locale drives copy (intentional); §6 rollback (app-only, .well-known files preserved porque são usados por L07-04 Strava + /invite/* + /refer/* também). **Decisão arquitetural**: (i) **canonicalHost constante, não config** — versões release do app parseiam contra o literal; configurável via remote config criaria janela onde nova versão + novo host deployado mas CDN cache ainda servia old AASA, breaking shares mid-flight; (ii) **WhatsApp via `wa.me/?text=`**, não `whatsapp://send?text=` — `wa.me` é HTTPS com fallback web/app store nativo, `whatsapp://` só funciona se app instalado e sem graceful fallback; (iii) **locale do sender, não do recipient** — sender não sabe idioma do recipient, WhatsApp translate handles inverse; mudar para recipient-side exige web landing page com Accept-Language parsing (follow-up); (iv) **texto-only share** no MVP — finding mencionava "imagem card" mas render pipeline + asset hosting são scope creep; texto puro + OG preview via fallback page seria próximo passo; (v) **falling back para native share quando WhatsApp falha** é UX-first escolha — silent failure é pior que "usei outro app"; (vi) reuso do `AudioCoachLocale` em vez de criar novo enum — same set de locales, same sender-side intent, zero duplicação. **Escopo deliberadamente excluído**: image share card generator (render pipeline TBD), web fallback page com OG tags em `portal/src/app/challenge/[id]/page.tsx` (follow-up, exige SSR + OG meta + App Store/Play Store smart banner), recipient-locale detection (exige landing-page + Accept-Language), WhatsApp Business API enterprise integration (coach assessoria B2B, não amador-friend viral loop), share analytics (PostHog event `challenge_invite_shared{channel}` já existe em `ProductEventTracker` em outros surfaces, follow-up para chamar no `_shareViaWhatsApp`/`_shareViaNative`). Cross-refs: L07-04 (Strava OAuth deep link CSRF — mesmo DeepLinkHandler + mesmos `.well-known/*` proofs; L22-08 NÃO degrade nenhum guard existente de L07-04), L22-05 (amadores em discovery agora podem convidar amigos para desafios grupais sem sair do sender-side loop), L22-06 (same `AudioCoachLocale` VO, coerência de sender-side i18n), L17-05 (AppLogger.warn routes para Sentry em WhatsApp launch failure — não silent crash), L11-05 (nenhum toggle de share channel é sensible; não passa por PrefsSafeKey). **Verificação**: `flutter analyze` 0 issues em 5.7s; `flutter test test/domain/value_objects/challenge_invite_link_test.dart test/domain/services/challenge_invite_message_builder_test.dart test/domain/usecases/gamification/share_challenge_invite_test.dart` 28/28 green em 1.3s; `npm run audit:challenge-invite-deep-link` 19/19 checks; `npm run audit:verify` 348 findings validados; `npm run audit:build` registry + FINDINGS + SCORECARD regenerated clean. **Batch H avança 4/10** — L21-06 ✓, L22-05 ✓, L22-06 ✓, L22-08 ✓.
- L22-06 — Voice coaching ganha 3 cues faltantes + i18n catálogo em 5 camadas (commit `9dab4ec`, 2026-04-21): fecha o high de Lente 22 (Atleta Amador) sobre "`flutter_tts` nos deps. Uso real: talvez só 'pace alert'. Faltam: countdown '3, 2, 1, GO', motivação periódica ('Você está indo bem!'), avisos de hidratação em corrida longa". **Estado pré-fix**: `AudioCoachService.init()` defaultava hardcoded `pt-BR` e `AudioCoachRepo._buildText` carregava 6 helpers estáticos com strings pt-BR inline (`'Zona $zone'`, `'Subindo'`, `'Corrida iniciada'`, etc.) — cada um dos 4 triggers existentes (`VoiceTriggers` KM, `HrZoneVoiceTrigger`, `TimeVoiceTrigger`, `GhostVoiceTrigger`) emitia eventos cujo text rendering dependia dessa tabela monolingual; mudar de idioma exigiria sobrescrever 6 métodos privados + rebuildar o APK. Os 3 cues listados no finding literalmente não existiam como triggers (o enum `AudioEventType.countdown` existia mas nenhuma use-case o emitia automaticamente; não havia cue type para motivation/hydration). **Fix em 5 camadas** (`omni_runner/lib/domain/value_objects/audio_coach_locale.dart` + `omni_runner/lib/domain/services/audio_cue_formatter.dart` + 3 novos triggers + `audio_coach_service.dart` + `audio_coach_repo.dart`, ~600 LOC líquidas): (a) **Value object** — `AudioCoachLocale` enum (`ptBR('pt-BR')` / `en('en-US')` / `es('es-ES')`) carrega BCP-47 tags consumidos diretamente por `flutter_tts.setLanguage(...)`. `AudioCoachLocale.fromTag(raw)` é resolver null-safe que aceita `'pt_BR'` / `'en-gb'` / `'es_MX'` (normalização case-insensitive + underscore→hyphen + prefix match) e cai em `ptBR` em input desconhecido — voice coaching nunca pode crashar sessão por tag inválida. (b) **Domain service `AudioCueFormatter`** — pure function com per-locale catalogues centralizado. 15 translation keys × 3 locales + 3 motivational pools (com 5 phrases cada) + 1 hydration reminder por locale. `format(event)` consome `AudioEventEntity` e retorna string exata que TTS vai falar; pre-composed `{'text': ...}` payload short-circuita dispatch (caller já localizou — ghost overtake text, action-level strings, etc.). Placeholder substitution `{km}` / `{pace}` / `{zone}` / `{bpm}` / `{minutes}` permite phrases idiomáticas (pt-BR "Zona 3. Subindo. 160 BPM" vs en "Zone 3. rising. 160 BPM" vs es "Zona 3. subiendo. 160 pulsaciones"). `translationKeys` Set exposto publicamente serve como CI-enforced invariant para detectar drift: adicionar nova key em `_catalogue[ptBR]` mas esquecer de propagar para `en`/`es` é FAIL no `audit:voice-coaching-i18n`. (c) **Três novos triggers em `domain/usecases/`** seguindo o padrão dos 4 existentes (stateful + `reset()` + `evaluate(...)` → `AudioEventEntity?`): **`CountdownVoiceTrigger(countdownSec: 5)`** emite `AudioEventType.countdown` com priority 2 (interrompe queue) para cada whole-second boundary de `{5, 4, 3, 2, 1, 0}`; value=0 é GO! spoken como locale phrase (`'Vai!'` / `'Go!'` / `'¡Vamos!'`); skipped seconds fire latest boundary (`evaluate(3200)` retorna value=2 se 3,4,5 já foram emitidos); assert `countdownSec > 0`. **`MotivationVoiceTrigger(formatter, intervalMs: 10*60*1000, minSpacingMs: 2*60*1000)`** emite `AudioEventType.custom` priority 14 com `text` do formatter's pool a cada interval de moving time, rotaciona modulo pool length, `isPaused` suppression (runner parado já tem stimulation suficiente), `minSpacingMs` cool-down floor previne back-to-back se outro cue acabou de terminar. `peekNextPhrase()` exposto para UI preview. **`HydrationVoiceTrigger(formatter, warmupMs: 20*60*1000, intervalMs: 20*60*1000)`** emite `AudioEventType.custom` priority 13 com `text` da `formatter.hydrationReminder()`, silent durante warmup (evitar noise em runs curtos < 20 min onde hidratação não é relevante), depois a cada intervalMs; `isPaused` suppression. (d) **`AudioCoachService.init({locale: AudioCoachLocale.ptBR, rate, volume})`** aceita locale em vez de String hardcoded; `setLocale(locale)` mid-session swapa language mapping failure para `AppLogger.warn` (coach stays alive mesmo se engine rejeitar tag em device low-resource). (e) **`AudioCoachRepo`** deleta os 6 helpers estáticos `_buildText`/`_distanceText`/`_timeText`/`_paceText`/`_hrText`/`_sessionText`/`_countdownText` e delega 100% para `AudioCueFormatter` injetado. `setLocale(locale)` atualiza formatter + chama `_service.setLocale(locale)` atomicamente. Defaults preservam 100% backward compat: `const AudioCueFormatter()` = `ptBR` + existing DI `AudioCoachRepo(service: …)` mantém behavior de 6+ existing call-sites byte-por-byte. **Testes** (71 cases em 4 arquivos): `audio_cue_formatter_test.dart` (31) — pt-BR default (distance+pace, empty payload handling, hr direction up/down/fallback, session actions, countdown spoken as raw numbers 3/2/1, countdown 0 → locale GO, pre-composed text short-circuit), en locale (distance 5.0 kilometers, Run started, Go!, hydration phrase, motivational pool presence), es locale (5.0 kilómetros, ¡Vamos!, Recordatorio: hidrátate), coverage invariant loop (todo locale × todo key resolve a phrase não-empty), `translationKeys` sanity, `fromTag` (4 ptBR variants, 4 en variants `en`/`en_US`/`en-gb`/`EN`, 3 es variants `es`/`es-MX`/`es_AR`, null/empty/fr-FR/xx fallback to ptBR, languageTag maps to BCP-47). `countdown_voice_trigger_test.dart` (11) — fires 5 at t=0, single-shot per second, fires 4 at t=1000, full 5..0 sequence, no fire past end, negative elapsedMs ignored, skipped second fires latest boundary, reset re-arms, custom 3-second countdown fires 3/2/1/GO, countdownSec<=0 rejected via assertion. `motivation_voice_trigger_test.dart` (9) — silent before interval, fires at first 10-min boundary with MOTIVATION action + non-empty text + priority 14, double-fire suppression, fires again at 20 min with rotated phrase (`isNot(equals(first))`), isPaused suppression, rotation wraps modulo pool length (6th prompt = first pool entry), reset re-sets state, custom 5-min interval variant, en locale uses English pool (`isIn` English phrases). `hydration_voice_trigger_test.dart` (9) — silent during warmup (0/10 min/19min59s), fires at 20 min with exact pt-BR text + HYDRATION action + priority 13, single-shot per interval, fires again at 40 min, isPaused suppression, reset re-arms, custom shorter warmup variant, en/es locale-specific phrases. **CI guard** (`tools/audit/check-voice-coaching-i18n.ts`, `npm run audit:voice-coaching-i18n`) — 20 invariants: locale file present + each variant declared with exact BCP-47 tag + `fromTag` factory; formatter file present + `translationKeys` Set declared + `static const _<key>Key` constants extracted + each locale block covers every key + each locale's motivational pool non-empty; 3 trigger files present + each exports `<Name>VoiceTrigger` class with `AudioEventEntity? evaluate(...)`; runbook present + cross-links `audit:voice-coaching-i18n` + references `L22-06`. Per-check `[OK|FAIL]` output + remediation hint appointa exatamente qual invariant quebrou. Smoke-tested: removing `_countdownGoKey: '¡Vamos!'` do bloco es → `FAIL formatter: es catalogue covers all keys — missing: countdown.go`; revert clean. **Runbook** (`docs/runbooks/AUDIO_CUES_RUNBOOK.md`, 8 seções): §1 why (3 missing cues + monolingual post-mortem); §2 invariants table (7 invariants × enforced-by hook); §3 what shipped (7-file responsibility map); §4 how-to (add new locale 6 steps + add new cue type 6 steps + swap locale mid-session code snippet); §5 **6 operational playbooks** — (5.1) CI guard fails por ramo de output marker (locale/formatter/trigger/runbook); (5.2) "no countdown before run starts" → verify trigger wiring + AudioCoachService init + iOS setSharedInstance + Android DND; (5.3) "locale swap não honrado" → repo.setLocale breakpoint + `flutter_tts.getLanguages` device inspection + fallback behavior quando voice pack ausente; (5.4) "hydration fires minute 1 on 3-min run" → expected silent durante warmup, inspect custom warmupMs em caller; (5.5) adding motivational phrase → append all locales lists + update first-entry test; (5.6) "coach keeps talking over itself" → priority contract table (countdown=2 interrupt / hrZone=5-7 / ghost=8 / distance=10 / time=12 / hydration=13 / motivation=14) + `AudioCoachRepo` rules (priority≤5 interrupt + clear queue, ≤15 enqueue max 5, >15 discard se queue não-empty); §6 detection signals (CI failure / Dart test failure / Sentry `flutter_tts.*` exceptions caught as warnings / AppLogger `AudioCoach` tag errors); §7 rollback (additive — revert commits restaura pt-BR monolith; existing `VoiceTriggers`/`HrZoneVoiceTrigger`/`TimeVoiceTrigger`/`GhostVoiceTrigger` contracts preservados); §8 cross-refs L22-05 (amadores em discovery runs) / L21-06 (performance mode gera mais cues) / L11-05 (locale preference via `PreferencesKeys` plain, never store voice API token) / L17-05 (AppLogger.warn routes para Sentry). **Verificação**: `flutter analyze` clean (5.2s, 0 issues); `flutter test` 2199/2199 green (2:36s, +71 cases) — zero regressão em 2128 existing tests; `npm run audit:voice-coaching-i18n` 20/20 checks green; `npm run audit:verify` 348 findings validados; `npm run audit:shared-prefs-sensitive-keys` 891 dart files clean. **Decisão arquitetural**: (i) **translation keys centralizadas em um só service** (`AudioCueFormatter`) em vez de espalhar por múltiplos arquivos de l10n `.arb` como existing `AppLocalizations` faz — voice coaching precisa de 15 chaves, não justifica overhead de intl gen + flutter_gen + tooling; central map dentro do service + CI invariant cobre coverage sem build-step. (ii) **default locale é `ptBR`** (não device-locale auto-detect): primary market (L10-07) é BR, mudança silenciosa rompe existing behavior. Apps que querem seguir device-locale chamam `AudioCoachLocale.fromTag(PlatformDispatcher.instance.locale.toLanguageTag())` explicitamente. (iii) **CountdownVoiceTrigger NÃO é auto-wired em session start** — decisão de "quando armar o countdown" é UI-layer (pre-start splash vs inline "Start" button); trigger fornece a primitive, caller decide quando chamar `evaluate(elapsedMs)`. (iv) **priority 14 em motivation** (maior número = menor prioridade) é acima do 15 queueThreshold, o que significa motivational é **discarded** se queue não-empty — trade-off consciente: melhor silêncio que motivação atrasada em 30s. (v) **escopo deliberadamente excluído**: UI toggle em settings page para escolher locale (virá como follow-up com PreferencesKeys entry + HomePage settings row); auto-detect device locale no boot (trivial follow-up com `PlatformDispatcher.instance.locale`); wiring dos 3 novos triggers no session controller atual (não existe session controller ainda — todos os 4+3=7 triggers estão DI-registered mas awaiting consumer); French/German locales (não demandado, cardinality test em locale enum força PR review quando adicionado); paid voice APIs (Google Cloud TTS, AWS Polly) — `flutter_tts` usa on-device TTS engine de graça, suficiente para MVP. Cross-refs: L22-05 (amadores em long discovery runs são exact target do hydration cue), L21-06 (performance mode gera stride density maior → mais `distanceAnnouncement` events por sessão, priority/queue rules aqui mantêm stream coerente), L22-09 (progress celebration — countdown "GO!" é primeira celebration da sessão), L11-05 (futura locale preference via `PreferencesKeys.audioCoachLocale` plain), L17-05 (AppLogger.warn consume por Sentry — se engine rejeitar setLanguage, log aparece em monitoring em vez de silent). **Batch H avança 3/10**.
- L22-05 — Discovery de grupos locais ganha endpoint `/api/groups/nearby` com privacidade por design em 5 camadas (commit `32e764f`, 2026-04-21): fecha o high de Lente 22 (Atleta Amador) sobre "Amador descobre clube via boca-a-boca. Sem `/groups/nearby` que mostra grupos < 5 km home". **Estado pré-fix**: `coaching_groups` não tinha nenhum campo de geo; nenhum endpoint server-side aceitava `{lat, lng, radius_km}`; coaches de grupo existentes eram invisíveis para qualquer pessoa que não chegasse via invite code; a única ferramenta de onboarding era "me indica um clube que roda no parque X?" em canal social, que filtra atletas tímidos / novos na cidade / sem rede existente. **Decisão arquitetural — NÃO PostGIS agora**: finding propunha `geography(POINT)` mas (i) 1 use-case GIS não justifica adicionar a extensão (impacto em backups + DO_CreateExtension dependency em roles + surfaces de CVE), (ii) escala atual (milhares de grupos, não dezenas de milhões) roda confortável em bounding-box prune + Haversine puro numeric, (iii) futuro PostGIS ADR documentado no runbook §6 como **trigger**: quando segundo use-case GIS (heatmaps de corrida / rotas públicas / coach-geofence) lander, migração é additive (`ALTER TABLE ADD COLUMN base_location geography GENERATED ALWAYS AS ST_MakePoint(base_lng, base_lat)::geography STORED`) sem refactor de código. **Decisão arquitetural — privacidade por design, não opt-out**: raw coords NUNCA saem do DB — snap-to-grid (500m / 1km / 5km) escolhido pelo coach + distância arredondada ao km inteiro (min 1) defeatam triangulação; discovery é **opt-in explícito** do coach (default = false); amador autenticado faz query mas caller location **não é persistida**. Um atacante que compromete um usuário amador consegue no máximo saber que existe um grupo "a ~2 km daqui" em grid de 500m — insuficiente para stalking individual. **Fix em 5 camadas** (`supabase/migrations/20260421370000_l22_05_coaching_groups_nearby.sql` ~290 LOC + `portal/src/app/api/groups/nearby/route.ts` + CI + testes + runbook): (a) **Schema extension** — `coaching_groups` ganha 4 colunas: `base_lat numeric(8,5)` / `base_lng numeric(8,5)` (precisão 5 decimais ≈ 1.1m — suficiente para o trabalho de snap mesmo no grid mais fino de 500m + ainda cabe em 8 dígitos totais evitando waste de storage), `allow_discovery boolean NOT NULL DEFAULT false` (opt-in explícito — existing rows NÃO aparecem em `/groups/nearby` até coach agir), `location_precision_m smallint` com `CHECK IN (500, 1000, 5000)` (enum-bounded smallint — smallint é ~50% mais barato que int4 em índice e 500/1000/5000 cabem folgadamente). Quatro CHECKs seguindo L19-08 naming (`chk_coaching_groups_*`): range latitude [-90, 90], range longitude [-180, 180], precision enum, e **`chk_coaching_groups_discovery_needs_coords`** — o mais importante: `allow_discovery = true` → `base_lat/base_lng/location_precision_m NOT NULL`. Esse CHECK impede configuração inválida (coach marca "descobrir" mas esquece de setar coords — row apareceria como ghost no SELECT dentro do WHERE com NULLs) E torna intent explícito (habilitar discovery é comprometimento operacional). Aplicado com `ALTER TABLE ... ADD CONSTRAINT ... NOT VALID` + `VALIDATE CONSTRAINT` online-safe. (b) **Índice partial btree** — `idx_coaching_groups_discovery_lat ON coaching_groups(base_lat) WHERE allow_discovery = true AND approval_status = 'approved' AND base_lat IS NOT NULL`. Três cláusulas coincidem com o WHERE da RPC — PostgreSQL usa o index partial sem re-validar. Cobre só o subset quente (grupos aprovados E discover-ON E com coords), típico << 10% de todos os coaching_groups no steady state, mantendo o índice pequeno e bitmap-scan-friendly. B-tree em lat sozinho (não composite): a selectivity da latitude dentro do bounding box geralmente já reduz a 2-5% dos rows, e ordering por lat permite range-scan direto para o `lat BETWEEN x AND y` do bounding-box prune. (c) **Três funções PL/pgSQL com contratos rigorosos**: `fn_groups_snap_coord(p_coord numeric, p_precision_m integer) RETURNS numeric` — **IMMUTABLE** (pode participar de indexes/generated columns/materialized views futuras), roteia pelas 3 precisões (500m ≈ 0.0045°, 1km ≈ 0.009°, 5km ≈ 0.045°) via round-to-grid numeric arithmetic (sem `trunc` pra evitar half-even bias), raise `INVALID_PRECISION` (ERRCODE `22023`) em valor fora do enum (defensa extra além do CHECK constraint — invocação direta da função via `SELECT fn_groups_snap_coord(...)` sem passar pela row também valida). `fn_groups_nearby(p_lat numeric, p_lng numeric, p_radius_km integer) RETURNS TABLE(...)` — **STABLE SECURITY DEFINER** (STABLE porque mesma entrada dá mesma saída dentro de uma transação — safe para query optimizer inline; SECURITY DEFINER para acessar coaching_groups com policies bypassadas e aplicar nossa própria auth check + retornar só as colunas snapped). Valida input: lat [-90,90], lng [-180,180] (22023 se NULL ou fora); **clampa** radius_km para [1,100] (clampa, não rejeita — UX amigável, frontend pode enviar valor fora e DB corrige silenciosamente com log); bounding-box prune usa aprox 1° lat ≈ 111 km (constante) + 1° lng ≈ 111 km × cos(lat rad) (ajustado por latitude — essencial em latitudes altas onde lng comprime); Haversine refine em cada candidate usando `earth_radius_km = 6371.0088`; WHERE filtra `allow_discovery = true AND approval_status = 'approved' AND base_lat IS NOT NULL AND base_lng IS NOT NULL AND location_precision_m IS NOT NULL`; retorna `fn_groups_snap_coord(base_lat, location_precision_m) AS base_lat_snapped` + idem para lng + `GREATEST(1, ROUND(distance_km))::integer AS distance_km_approx` (min 1 km evita "0.0 km" que leaka precisão sub-kilomêtrica + arredondamento destrói diferencial pra triangulação); `ORDER BY distance_km_approx ASC, base_lat ASC` (tiebreak determinístico pra paginação estável); GRANTs `service_role` + `authenticated` apenas, anon REVOKEd. `fn_group_set_base_location(p_group_id uuid, p_lat numeric, p_lng numeric, p_precision_m integer, p_allow_discovery boolean) RETURNS void` — **VOLATILE SECURITY DEFINER** (escreve), verifica `auth.uid() = coach_user_id` da row (NOT FOUND → `UNAUTHORIZED` com P0010), valida inputs (mesmos ranges/enum que snap/nearby), UPDATE single-row. Dual-gate: apenas coach autenticado do próprio grupo pode mudar; invocar como authenticated para grupo alheio é P0010 imediato. (d) **Shape-assertion `fn_coaching_groups_assert_discovery_shape()`** — **SECURITY DEFINER** chamada apenas pelo service_role (CI). Verifica em ordem: 4 colunas com tipo/nullable/default corretos, 4 CHECK constraints existentes com os nomes L19-08 esperados, índice partial com o predicate exato, 3 helpers registrados (`fn_groups_snap_coord` / `fn_groups_nearby` / `fn_group_set_base_location`) com provolatile/prosecdef esperados (`i/f`, `s/t`, `v/t` respectivamente), privileges OK (authenticated tem EXECUTE em nearby + set_base_location mas NÃO em snap_coord interno, anon zero EXECUTE). Raise `P0010` com message carrying `L22-05` + `DRIFT:<invariant>:<detail>` se qualquer invariante mudar silenciosamente — fail-closed em CI. (e) **Portal route** `GET /api/groups/nearby?lat=&lng=&radius_km=` — `ApiHandler<[]>` sobre `withErrorHandler` (L17-03 typing contract), query parse via Zod-less manual para manter leve (3 numeric params), `apiUnauthorized()` se `supabase.auth.getUser()` retorna null (amador NÃO-autenticado não descobre — prevent scraping/DDoS/abuse), `supabase.rpc('fn_groups_nearby', ...)` forward, `apiOk({ items })` com items transformados (minimal surface: group_id/name/base_lat_snapped/base_lng_snapped/distance_km_approx — omitidos: coach_user_id privado, member_count futuro follow-up, etc.). Logger para `requestId` via L17-05 contract em error path. **Testes** (`tools/test_l22_05_coaching_groups_nearby.ts` 25 cases ~650 LOC): (1-8) schema — 4 colunas existem com tipo/nullable/default; 4 CHECKs com L19-08 naming exato; índice partial com predicate; volatility `i`/`s`/`v` e SECURITY DEFINER `t` via pg_proc (fix: cast explícito `provolatile::text || ':' || prosecdef::text` pra evitar "operator is not unique" em PG 17 que trata char||bool ambiguously); REVOKEs + GRANTs (anon zero, authenticated sim). (9-13) snap — 1km/500m/5km drift inline matches aritmética esperada; passthrough NULL → NULL; 22023 em precision não-enum. (14-21) behavior — setup de 4 grupos SP (2 approved+discover+dentro, 1 approved+discover+fora raio, 1 pending_approval dentro): query retorna 2; flip allow_discovery=false → 0; flip approval_status=pending → exclui; radius 5km inclui 2, radius 20km inclui 3, radius 100km inclui todos 3 approved+discover; ORDER BY distance_km_approx ASC + snapped != raw; radius=500 clamped para 100 (sem crash, retorna set maior). (22-23) CHECK `chk_coaching_groups_discovery_needs_coords` — UPDATE allow_discovery=true sem coords falha com constraint name exato; DELETE coords com allow_discovery=true falha também. (24) validation — NULL lat, lng out-of-range, radius_km=0 rejeitados com 22023. (25) shape guard — baseline healthy retorna void; `ALTER TABLE ... RENAME CONSTRAINT chk_coaching_groups_base_lat_range TO chk_wrong_name` → `fn_coaching_groups_assert_discovery_shape()` raise P0010 com `L22-05` em message; revert + re-run = clean. **CI guard** `tools/audit/check-groups-nearby.ts` (`npm run audit:groups-nearby`) — probe `fn_coaching_groups_assert_discovery_shape` em docker supabase, capture stderr, grep `L22-05` marker = FAIL (exit 1 com exact drift detail), sem marker = OK. **Runbook** `docs/runbooks/GROUPS_NEARBY_RUNBOOK.md` (~14KB, 8 seções): §1 why (amador persona gap + privacy-first mandate + decisão de NÃO-PostGIS now), §2 what shipped (schema/funcs/route/CI/tests matriz), §3 **privacy posture tabela** (campo-a-campo: base_lat raw NEVER leaves DB / base_lat_snapped mapped to coach's grid / distance_km_approx integer km min 1 / auth required / caller location NOT persisted / snap is IMMUTABLE server-side / coach opt-in explicit), §4 coach enable workflow em 4 steps (chamar `fn_group_set_base_location` via service_role + docs admin portal follow-up para coaches self-serve), §5 seis operational playbooks (A: CI guard fails → root-cause tabela 6 ramos DB drift; B: coach reporta "meu grupo não aparece" → SELECT state via fn_coaching_groups_inspect no runbook; C: abuse report "alguém está usando discovery pra stalking" → disable_discovery SQL + incident post-mortem; D: coach pede coords exatas retornadas → rejected, escalate ADR; E: city-wide groups sem coord natural → usar centroid do bairro mais comum + precisão 5km; F: futuro PostGIS migration → additive trigger quando 2nd use-case lander), §6 rollback (additive migration — revert via `ALTER TABLE DROP COLUMN` é safe porque nenhum outro site escreve nessas colunas), §7 detection signals (CI guard stderr pattern, P0010 em grafana, anomaly em discovery opt-in rate), §8 cross-refs L04-07 (coach_user_id não leak PII no retorno), L10-07 (JWT aud/iss checked before RPC call), L10-09 (credential stuffing countered by auth-only surface), L19-08 (CHECK naming compliance), L22-04 (future grouping pai que vai consumir nearby). **Verificação**: migration apply clean + self-test `NOTICE: L22-05 migration self-test passed`; 25/25 integration tests green em ~4s; `npm run audit:groups-nearby` OK; `portal tsc --noEmit` sem novos errors (feature-flags.ts TS2802 pre-existente de L17-03 roadmap unrelated); `portal eslint src/app/api/groups/nearby/route.ts` clean; `audit:verify` passes. **Escopo deliberadamente excluído**: UI amador no mobile para chamar `/api/groups/nearby` (follow-up L22-05-ui pending, requer decisão de permission prompt de device location); coach admin portal page para ligar discovery + setar coords (follow-up L22-05-coach-portal pending); PostGIS migration (runbook §6 trigger documentado); member_count aggregate na response (follow-up L22-06 parking lot — requer SELECT pesado, vai precisar materialized view se demandado); paginação via `offset` ou cursor (100 grupos max por query é plenty para MVP); distância < 1km resolved exatamente (intencionalmente arredondada pra privacy). **Abre Batch H 2/10** continuing mobile UX wave.
- L21-06 — Polyline GPS ganha modo "performance" opt-in em 3 camadas (commit `9a4ec27`, 2026-04-21): fecha o high de Lente 21 (Atleta Pro) sobre "`location_settings_entity.dart:15` default `distanceFilterMeters = 5.0`. Em sprint (~12 m/s), um ponto a cada 5 m = ponto a cada 0,4 s — pobre para análise biomecânica de elite". **Estado pré-fix**: `LocationSettingsEntity` expunha apenas `distanceFilterMeters` e `accuracy`; não havia vocabulário domain-level para distinguir a sessão "casual que prioriza bateria" da sessão "time-trial com análise biomecânica" — toda chamada a `ILocationStream.watch()` caía no default de 5 m / `LocationAccuracy.high` e Elite Athlete perdia resolução de stride/ground-contact enquanto Casual queimava bateria à toa em qualquer aumento global do filtro. **Fix em 3 camadas** (`omni_runner/lib/domain/entities/location_settings_entity.dart` + `repositories_impl/location_stream_repo.dart` + `core/storage/preferences_keys.dart`, ~55 LOC líquidas): (a) **Entity value-object expande via named constructors** — `RecordingMode` enum (`standard | performance`) + `LocationSettingsEntity.standard()` (5 m + `LocationAccuracy.high` — preserva default pré-L21-06 byte-por-byte, zero regressão para os 99% de workouts) + `LocationSettingsEntity.performance()` (1 m + `LocationAccuracy.bestForNavigation` — iOS multi-constellation GNSS + Android highest-priority fused provider). `copyWith({distanceFilterMeters, accuracy, mode})` permite QA override sem rebuild total; `Equatable.props` inclui `mode` para contracts de cache/memoization. Default constructor mantido com mesmos defaults explícitos (backward compat 100%: todos 3+ call-sites atuais `const LocationSettingsEntity()` resolvem idênticos). (b) **Data layer `LocationStreamRepo._mapAccuracy`** ganha branch `LocationAccuracy.bestForNavigation => geo.LocationAccuracy.bestForNavigation` com docstring explicando mapping platform-específico. `LocationAccuracy.high` continua mapeando para `geo.LocationAccuracy.best` (iOS ~3m, Android fused best) — o novo branch é estritamente adição, não re-rota os casos existentes. (c) **Storage catalogue L11-05-compatível** — `PreferencesKeys.recordingMode` (valor string: `"standard"`|`"performance"`, não-sensitive UI toggle) via `PrefsSafeKey.plain('recording_mode', purpose: 'L21-06: GPS recording mode (standard|performance)')`. Entrada passa pelos 3-layer defense do L11-05 (heurística de sensibilidade rejeita `token`/`secret`/etc; `recording_mode` é safe). Registrada em `allKeys` para catálogo invariants; 9 testes de `preferences_keys_test.dart` re-executados 9/9 green (novo entry não quebra purpose-length > 8 chars, uniqueness, purpose-docstring presence). **Testes**: `test/domain/entities/location_settings_entity_test.dart` (11 cases): default constructor preserva 5 m/high/standard (evita regressão silenciosa de bateria para casual via `if` statement fixando os 3 valores); `.standard()` matches default exato; `.performance()` = 1 m/bestForNavigation/performance e é estritamente **finer** (`<`) que standard em distanceFilterMeters; `.copyWith()` sem args retorna equal, com args replaça apenas field(s) mencionado(s); Equatable ignora referência e `mode` contribui para equality (não-inclusão seria bug silencioso — two entities com settings numéricos iguais mas modes diferentes compartilhariam cache hit erroneamente); enum cardinality pins (`LocationAccuracy.values.length == 4`, `RecordingMode.values.length == 2`) — qualquer adição futura de valor exige tocar este teste junto, forçando PR review da semântica. **Verificação**: `flutter analyze` clean (9.1s, 0 issues); `flutter test test/domain/entities/location_settings_entity_test.dart` 11/11 green; `flutter test test/core/storage/preferences_keys_test.dart` 9/9 green (catálogo invariants mantidos). **Decisão arquitetural**: (i) **opt-in** não default-on — trade-off documentado de ~+30% bateria e ~3-5× volume de pontos GPS não pode ser forçado em casual users; UI deve surface banner/confirmation antes de persistir `recording_mode=performance` em `PreferencesKeys.recordingMode`; (ii) **domain-level enum** em vez de raw boolean `performanceRecording: bool` — enum escala (futuro `RecordingMode.race` poderia adicionar 0.5 m + 2 Hz sampling) sem tocar cada call-site; (iii) **escopo deliberadamente excluído**: UI widget de toggle (virá em follow-up com banner de bateria), BLoC/Cubit que lê `PreferencesKeys.recordingMode` e injeta na chamada `watch(settings:)` (decisão pending qual camada consome `ILocationStream` — atualmente DI register-only, nenhum consumer ativo encontrado por grep), `LocationAccuracy.medium`/`.low` novos mappings (kept as-is — `LocationAccuracy.low`/`.medium`/`.high`/`.bestForNavigation` cobrem spectrum sem redundância), GNSS multi-constellation hint Android-nativo além do que geolocator plugin expõe (geolocator já roteia para `FusedLocationProviderClient` com Priority_HIGH_ACCURACY que habilita multi-GNSS quando device suporta). (iv) **não há runbook dedicado** — mudança é code-type pura, cobertura de testes adequada para semântica; follow-up UI virá com runbook de "como adicionar nova feature com trade-off de bateria" quando racing/training modes lander. Cross-refs: L11-05 (PreferencesKeys entry usa 3-layer defense; sem o guard de L11-05, devs poderiam acidentalmente storar token em `recording_mode_secret` e vazar); L22-09 (progress celebration — Athlete Pro em performance mode gera dados suficientes para momentos "you just ran 3:28 /km!"); L21-07..12 (outros reaches de Lente 21 que consumirão tipo entity/mode — base instalada aqui evita retrabalho); L04-07 (mode flag NÃO contém PII, safe para ficar em SharedPreferences). **Abre Batch H (Mobile UX & Coach features) 1/10**.
- L11-08 — Flutter/Dart SDK pinning ganha defesa single-minor em 4 camadas (commit `924416b`, 2026-04-21): fecha o high de Lente 11 (Supply Chain) sobre "Flutter sdk: '>=3.8.0 <4.0.0' — permite 3.9, 3.10…". **Estado pré-fix**: pubspec `environment.sdk: '>=3.8.0 <4.0.0'` permitia Dart 3.9/3.10/3.11 (e futuros 3.12+) a resolver contra o mesmo pubspec — se dev A rodava Flutter 3.41 (Dart 3.11) e dev B rodava Flutter 3.42 (Dart 3.12), type-inference e lint pass diferentes produziam bugs que escorregam entre máquinas. Pior: **drift cross-workflow real** — `.github/workflows/release.yml` usava `flutter-version: "3.22.x"` enquanto `flutter.yml` + `security.yml` estavam em `"3.41.x"` — release APKs compilados com toolchain completamente diferente do CI test (silent divergence que já foi bug em produção). Dart tem histórico de breaking changes em minor (null-safety rollout, sound-mode migration, extension-type semantics, records semantics). **Fix em 4 camadas**: (a) **Pin narrow em pubspec** — `sdk: '>=3.11.0 <3.12.0'` + `flutter: '>=3.41.0 <3.42.0'` (single-minor window matching CI flutter-version 3.41.x); aceita patches (`3.11.1`, `3.11.2`, …) que continuam a trazer security fixes, mas rejeita minor drift; precedido de 10-line warning block citando L11-08, os 3 CI workflows, `.tool-versions` e runbook; widening sem update coordenado é guard failure. (b) **`.tool-versions`** em repo root para asdf / fvm / pluggable toolchain managers — `flutter 3.41.1`, `dart 3.11.0`, `nodejs 20` — `cd <repo> && asdf install` dá ao dev exatamente a toolchain do CI, eliminando "funciona na minha máquina"; warning block cross-linka com os outros pinning files. (c) **Alinhamento cross-workflow** — `release.yml` linha 118 mudada de `"3.22.x"` para `"3.41.x"` matching `flutter.yml` (3 jobs: analyze, test, build-apk) + `security.yml`; warning block sibling citando L11-08 + `FLUTTER_SDK_PINNING_RUNBOOK.md`. (d) **CI guard** `npm run audit:flutter-sdk-pinning` (`tools/audit/check-flutter-sdk-pinning.ts`) com 5 sub-checks: **pubspec ranges** — `isSingleMinor()` valida que upper bound é exatamente `lower.minor + 1` no mesmo major com patch `.0` (aceita `>=3.11.0 <3.12.0`; rejeita `>=3.8.0 <4.0.0` cross-major, `>=3.8.0 <3.13.0` 5-minor, one-sided ranges, missing bounds, patch-nonzero); **warning block** — ≥4 linhas `#` contiguous acima de `environment:` mencionando L11-08 (evita copy-paste de comentários irrelevantes); **tool-versions drift** — `.tool-versions` contém `flutter <M.m>` + `dart <M.m>` com minor matching pubspec; **workflow drift** — scan de `.github/workflows/*.yml`, toda linha `flutter-version:` deve matchar pubspec minor, zero exceções mesmo para "just this one job" (essa era a doença original); **runbook presence** — arquivo existe + cross-links `check-flutter-sdk-pinning`. **Side-effect cleanup**: faixa narrower expôs lint deprecated `avoid_null_checks_in_equality_operators` (deprecated em Dart 3.11, antes mascarado pela faixa wider onde o lint ainda era válido); removido de `analysis_options.yaml`. **Smoke-tests**: (i) widened pubspec `>=3.8.0 <4.0.0` → `sdk_range_too_wide` flagged com exact remediation + snippet; (ii) planted `flutter-version: '3.99.x'` em `flutter.yml` → `workflow_flutter_drift` flagged em todas as 3 jobs (linhas 35, 53, 83) com line numbers; ambos revertem clean; (iii) deleted `.tool-versions` → `missing_tool_versions` + fix instructions; revert clean. **Runbook** `FLUTTER_SDK_PINNING_RUNBOOK.md` cobrindo: Section 1 why (null-safety rollout history, 3.22.x vs 3.41.x real incident); Section 2 what's in tree (7 artifacts tabela); Section 3 guard spec (5 invariants detalhados); Section 4 **upgrade procedure em 7 passos** para adotar nova minor (single PR `chore(flutter): bump to X.Y (L11-08 sync)` — pubspec + .tool-versions + TODAS workflows + guard + quality gates + Flutter release notes review + PR description com before/after); Section 5 **7 playbooks** — (5.1) CI guard fails 4 ramos; (5.2) Flutter patch release lands (nothing needed, range accepts patches); (5.3) Flutter minor lands mas not ready (do nothing, pin rejects); (5.4) Dart patch CVE (bump .tool-versions, guard stays green); (5.5) `flutter_version_conflict` error em pub get (asdf/fvm preferred, manual fallback, NEVER widen range); (5.6) release uses different Flutter than CI (previne via guard `workflow_flutter_drift`); (5.7) engineer pede Flutter X.Y para this feature (no, escalate via GitHub issue `flutter-sdk-bump`); Section 6 detection signals table; Section 7 rollback (revert upgrade PR chain, guard failure no revert signala inconsistência); Section 8 cross-refs. **Decisão arquitetural**: NÃO pin exato (`3.41.1` only) — patches carregam security fixes e são compat por contract; single-minor é o ponto certo entre safety e maintenance. NÃO two-sided multi-minor — inteiro ponto é narrow. **Verificação**: `audit:flutter-sdk-pinning` green; `audit:verify` 348 findings validados; `audit:email-platform` 6 invariants OK; `audit:shared-prefs-sensitive-keys` 891 dart files clean; `audit:npm-dependency-pinning` 8 criticals + npmrc + specifiers OK; `audit:sqlcipher-eol` green; `flutter analyze` no issues found (side-effect lint cleanup); `flutter test` 2128/2128 passed em 151s; `flutter pub deps` resolve Flutter 3.41.1 + Dart 3.11.0. Zero regressão. Cross-refs: L11-06 (mesma filosofia NPM side — pin exato + CI guard + runbook; complementary supply-chain defenses), L11-07 (Flutter major bump 3.x→4.x é **explicit exit criterion #2 em ADR-009** porque force NDK deprecation + sqlcipher_flutter_libs EOL rebuild impossible — single-minor pin dá visibilidade desse signal cedo), L11-05 (FlutterSecureStorage sensible a language-spec regressions passadas), L01-30/31 (Android release hardening ProGuard rules versionadas alongside Flutter gradle tooling — single-minor mantém estáveis), L11-03 gitleaks + L11-09 least-privilege GITHUB_TOKEN (workflows shared; alignment cross-workflow mantém esses guards consistentes entre flutter.yml/security.yml/release.yml). **Completa Batch G (Mobile / Flutter) 4/4** — L11-05 + L11-06 + L11-07 + L11-08 entregues em 4 commits `fix` + 4 commits `docs` + push.
- L11-07 — SQLCipher EOL ganha postura pin-and-watch em 3 camadas (commit `6c661f7`, 2026-04-21): fecha o high de Lente 11 (Supply Chain) sobre "sqlcipher_flutter_libs: ^0.7.0+eol — eol = end of life". **Estado pré-fix**: pubspec declarava `sqlcipher_flutter_libs: ^0.7.0+eol` — o caret acima de um `+eol` build-metadata era simultaneamente sem sentido (o pacote nunca publicará outra versão upstream — Simon Binder, maintainer do Drift, retirou-o) E perigoso (um name-squatter publicando `0.7.1+malicious` como "fork" seria selecionado silenciosamente por `flutter pub get` — mesma família de risk do L11-06 mas em pub registry). Banco local cifra GPS traces, histórico de treinos, session tokens (LGPD exige encryption at rest — não podemos simplesmente remover). Dependabot já ignorava minor/patch em `drift-and-storage` group, mas `dart pub add` ou `flutter pub upgrade --major-versions` reintroduz caret por default. **Fix em 3 camadas (postura pin-and-watch, não migração full)**: (a) **Pin exato** em `omni_runner/pubspec.yaml` — `sqlcipher_flutter_libs: 0.7.0+eol` (sem caret, sem range, sem alias) — precedido de 10-line warning block que declara L11-07, aponta para ADR-009 e runbook, alerta explicitamente que `^0.7.0+eol` permite name-squat `0.7.1+malicious`. (b) **ADR-009** (`docs/adr/009-sqlcipher-eol-migration.md`): threat-model completo em 3 categorias de CVE (KDF weakness — já mitigado via PRAGMA key raw-hex + FlutterSecureStorage 256-bit key, low residual; SQL parser bugs via SQLite upstream, medium residual; memory-safety em cipher extension, low residual mas high impact) + 3 opções de migração avaliadas com pros/cons/effort — **Option A**: `sqlite3_flutter_libs` (maintained) + app-layer AES-GCM envelope, preferred, L effort (8-13pts), fecha também C-03 PRAGMA key injection; **Option B**: CMake SQLCipher fork dentro de `sqlite3_flutter_libs`, M effort (5-8pts), mantém PRAGMA key contract mas força ownership do native build pipeline + licensing re-review; **Option C**: community fork de `sqlcipher_flutter_libs`, S effort (2-3pts), **rejeitada** porque single-maintainer fork supply-chain risk é apenas movido, não reduzido. 5 **exit criteria** que forçam migração dentro de um sprint: CVE CVSS≥7.0 em código path exercised (PRAGMA key, core SQL parser, blob I/O) / Flutter 4.x deprecatar NDK<r27 / Play ou App Store reject upload por "bundled unmaintained crypto libraries" / security review interna / customer contratual. (c) **Runbook operacional** (`docs/runbooks/SQLCIPHER_EOL_RUNBOOK.md`): 7 playbooks end-to-end — (4.1) engenheiro pergunta "devo upgrade?" → não, aponte ADR; (4.2) advisory/CVE fires → triage em 4 passos + aplicar exit criteria + freeze release se migração triggered; (4.3) Dependabot abre PR mesmo com ignore → fechar + patchar `.github/dependabot.yml` (se fork name-squat, audit + add ao ignore); (4.4) CI guard falha → root-cause em 4 ramos (pin drift / missing warning block / missing ADR-runbook / missing Dependabot ignore); (4.5) executar migração — 7 passos incluindo branch naming convention, pubspec swap mantendo pin discipline, **resumable data migration** detectar legacy DB → decrypt via SQLCipher → re-encrypt via AES-GCM → atomic rename (se falha, preserve legacy + telemetry event, não blocka app launch), remove `PRAGMA key = "x'…'"` path (fecha C-03), update CI guard, update ADR-009 como Superseded by ADR-N, single mobile release behind feature flag `feature_flags.migrateDbToAesGcm`, gradual rollout 5%→25%→50%→100% em 1 semana; (4.6) fork proposed → full security review ou rejeição documentada (community fork com <6 months releases rejeitado); (4.7) security review flags EOL → verificar contra exit criteria; detection-signals table (GHSA sqlcipher/sqlite feeds, CI guard failure, store rejection, Drift releases monthly watch, Dependabot PR); **rollback** — feature flag disable migrateDbToAesGcm, novos app starts usam legacy path, users migrados ficam em AES-GCM (no data loss), file L11-07-v2 para trigger. (d) **CI guard** `tools/audit/check-sqlcipher-eol.ts` (`npm run audit:sqlcipher-eol`) com 5 sub-checks: **pin** — `omni_runner/pubspec.yaml` contém linha com `sqlcipher_flutter_libs: 0.7.0+eol` exato (isExactPubVersion: rejects qualquer valor diferente de `0.7.0+eol`, incluindo caret, tilde, ranges, aliases `git:`/`path:`/`hosted:`/`sdk:`); **warning-block** — ≥4 linhas `#` contiguous acima do pin line, mencionando L11-07 (evita copy-paste de comentários irrelevantes); **ADR** — `docs/adr/009-sqlcipher-eol-migration.md` existe não-vazio + referencia L11-07 e SQLCipher; **runbook** — arquivo existe não-vazio + referencia `check-sqlcipher-eol` E `009-sqlcipher-eol-migration` (mutual linkage); **dependabot** — `.github/dependabot.yml` contém `sqlcipher_flutter_libs` dentro do `drift-and-storage` group patterns (scan contíguo do bloco). **Smoke-tested**: (i) planted `^0.7.0+eol` → guard falha com `pin_drift` + exact remediation; (ii) removed `sqlcipher_flutter_libs` do dependabot `drift-and-storage` → guard falha com `missing_dependabot_ignore`; both revert clean. **Decisão arquitetural**: postura pin-and-watch NÃO é admitir derrota — é reconhecer que (i) risco agudo de name-squat é neutralizado pelo exact pin + CI, (ii) risco residual de CVE upstream tem baixíssima taxa histórica na cipher extension + KDF já mitigado via secure-storage key + raw-hex PRAGMA, (iii) migração Option A é ~2 semanas de trabalho que inclui resumable DB re-encryption + feature flag rollout — não investimos sem trigger. Cross-refs: L11-06 (mesma filosofia, NPM side — pin exato + CI guard + runbook), L11-05 (DB encryption key vive em FlutterSecureStorage governado por PrefsSafeKey — migração Option A não vaza a key silently), L11-08 (Flutter SDK pinning futuro — NDK bump major é exit criterion #2), L01-30/31 (Android ProGuard `§7 Drift + SQLCipher` rules stay correct para versão pinada — -keep class net.sqlcipher.** + -dontwarn net.sqlcipher.**), C-03 PRAGMA key raw-hex format (fecha-se como side-effect da migração Option A — remove a string-interpolation path). **Verificação**: `audit:sqlcipher-eol` green; `audit:verify` 348 findings validados; `audit:email-platform` 6 invariants OK; `audit:shared-prefs-sensitive-keys` 891 dart files clean; `audit:npm-dependency-pinning` 8 criticals + npmrc + specifiers OK; `flutter analyze` no issues found; `flutter pub deps` resolve `sqlcipher_flutter_libs 0.7.0+eol` exatamente. Zero regressão em code paths existentes.
- L11-06 — npm dependency pinning ganha defesa em 3 camadas (commit `d609c78`, 2026-04-21): fecha o high de Lente 11 (Supply Chain) sobre "`portal/package.json`: `next: ^14.2.15`, `zod: ^4.3.6`. Caret permite minor bumps que podem quebrar tipos (zod 4 ↔ 3)". **Estado pré-fix**: caret ranges em 8 boundary packages significa que cada `npm install` resolve silenciosamente para "whatever npm feels like today" — a instalação já estava em `next@14.2.35` (20 patches à frente do `^14.2.15` que o finding text sugere), com 20 patch-bumps de silently-changed middleware ordering, image-optimiser rewrites, e dev-mode cookie behaviour entre o que novo clone baixa e o que last reviewer testou. `zod` atravessa o 3→4 type-break — caret em `^3.x` podia, via PR ruim que bumpa `@asteasolutions/zod-to-openapi` para linha 4-compatible, cascade em auto-install `zod@4` cujo strictness rejeitaria inputs que old validator aceitava, silent-tightening API contracts sem deploy. `@supabase/ssr` bumpou cookie names (`sb-<project>-auth-token` → shorter) em minor passada — caret-driven bump invalidaria toda sessão logada sem deploy note. `@sentry/nextjs` shipou breaking changes em `getActiveSpan()` em minor releases — L17-05 logger depende desse exato contrato. **Fix em 3 camadas**: (a) **`.npmrc`** em workspace-root + `portal/` com `save-exact=true` (next `npm install foo` escreve exact version não caret), `save-prefix=` explicit empty prefix (defence in depth para tooling que honors save-prefix mas ignora save-exact), `engine-strict=true` (npm refuses install em engine mismatch). (b) **Pins exatos** em 8 boundary packages que controlam product contracts — `portal/package.json`: `next@14.2.35` (rendering/routing/middleware contract; silently changes redirect + cookie semantics across patches), `@supabase/ssr@0.8.0` (auth cookie contract — mudou cookie names em past minor), `@supabase/supabase-js@2.97.0` (RPC + RLS error shape contract), `zod@4.3.6` (schema contract; 3↔4 typing break risk), `@sentry/nextjs@10.40.0` (observability boundary; `getActiveSpan()` semantics consumed por L17-05 logger); `package.json` root: `tsx@4.21.0` (entrypoint de TODO `npm run audit:*` script — breaking minor kills CI fleet), `lefthook@2.1.1` (pre-commit runner — controls `flutter-analyze`/`portal-lint`/`gitleaks` invocation), `js-yaml@4.1.1` (audit registry parser — semantic changes shift yaml behaviour). Leaf/caret-OK deps intencionalmente intactas — full lockdown (L11-06-ext) seria noisy em routine maintenance; este PR tightens boundary surface. (c) **CI guard** `npm run audit:npm-dependency-pinning` (`tools/audit/check-npm-dependency-pinning.ts`) com 3 sub-checks: **npmrc** — ambos arquivos existem + cada contém `save-exact=true` AND `save-prefix=` lines; missing either file OR either line fails closed; **criticals** — para cada `CRITICAL_PACKAGES` entry loads manifest + checks range via `isExactVersion()`: rejeita leading `^`/`~`, range operators (`>=`/`<=`/`>`/`<`), hyphen ranges (`1.0.0 - 2.0.0`), OR (`||`), wildcards (`*`/`x`), aliases (`file:`/`link:`/`git*`/`https?:`/`github:`/`npm:`/`workspace:` protocols); aceita plain semver `1.2.3` + prerelease `1.2.3-beta.0` + build metadata `1.2.3+build.42`; **specifiers** — scans TODOS deps (não só criticals) para `*`/`latest`/`x` — nondeterministic resolution banned everywhere; hyphen ranges e `>=` allowed em leaf packages porque já existem em tree e full lockdown blocks routine maintenance. **Smoke-tested**: (i) planted `"zod": "^4.3.6"` → guard falha com "must be an exact semver (no ^, ~, ranges, or aliases)" apontando `portal/package.json`; (ii) planted `"sonner": "latest"` → guard falha com "uses banned specifier 'latest' tag"; tree atual: **0 regressions**. **Runbook** `docs/runbooks/NPM_DEPENDENCY_PINNING_RUNBOOK.md`: Section 1 why (20-patch drift history + zod 3→4 cascade path + @supabase/ssr cookie name change real incident + @sentry/nextjs.getActiveSpan contract break); Section 2 policy matrix mapping cada critical package → contract it owns → pinning rule + instruções para adicionar/remover CRITICAL_PACKAGES (justify em PR, edit TS file, update matrix SAME commit, code-owner review); Section 3 files + CI guard specification; Section 4 6 operational playbooks — (i) security advisory → `npm install <pkg>@<exact> --save-exact` + run guard + full test suite + ship PR with advisory link; (ii) accidental caret reintroduction via `.npmrc` deletion / wrong directory / hand-edit → fix root cause não paper over; (iii) guard fails em caret legítimo → remove de CRITICAL_PACKAGES + update matrix SAME commit (code-owner review); (iv) Dependabot/Renovate → edit only exact version string; guard stays green; (v) `*`/`latest` sneak-in via copy-paste → `npm install <pkg>@<exact>`; (vi) `npm ci` output different between machines → stale lockfile, `rm -rf node_modules package-lock.json && npm install` + commit; Section 5 detection signals table; Section 6 cross-refs (L11-05 secure storage sibling — code-side vs manifest-side; L11-07 sqlcipher EOL — diferente remediação porque EOL=no patches, pinning pointless sem maintained upstream; L11-08 Flutter SDK pinning Dart-side analogue; L11-01/02/03/04/09 supply-chain sextet já em Onda 0 — CVE scanning/SBOM/provenance/reproducibility; L17-05 logger @sentry/nextjs.getActiveSpan consumer — se contract muda behind caret bump, L17-05 tests silent-red). **Decisão arquitetural**: NÃO full-lockdown (pin every leaf) — guard noisy em routine maintenance + contract ownership está nos 8 boundary packages identified. Leaf packages caret-OK desde que não `*`/`latest`. **Verificação**: `npm install` idempotente ambos workspaces; `npm ls` mostra exatamente versões pinadas; `portal npm run lint` clean; `portal npm test logger.test.ts` 14/14 green; existing audit guards (`audit:verify` 348 findings, `audit:email-platform` 6 invariants, `audit:shared-prefs-sensitive-keys` 891 dart files) todos stay green alongside. Zero regressão.
- L11-05 — SharedPreferences ganha lint de chaves sensíveis em 3 camadas defensivas (commit `aaf0277`, 2026-04-21): fecha o high de Lente 11 (Supply Chain) sobre "`flutter_secure_storage: ^10.0.0` mas release inclui `shared_preferences`: devs confundem qual storage usar para dados sensíveis". **Estado pré-fix**: `omni_runner/pubspec.yaml:47,53` declara ambos os backends (`shared_preferences: ^2.5.4` plaintext XML/NSUserDefaults + `flutter_secure_storage: ^10.0.0` Keychain/EncryptedPrefs). Nada mecanicamente impedia um dev futuro de armazenar Strava access_token, Supabase JWT ou Apple sign-in id junto com o theme_mode — um único PR missed silent-leak o secret para (a) iOS `~/Library/Preferences/<bundle>.plist` recuperável por iTunes/Finder backup + malware com mesmo bundle em jailbroken device; (b) Android < 11 `/data/data/<package>/shared_prefs/*.xml` world-readable se rooted + extraível via `adb backup`; (c) Android ≥ 11 alguns OEMs ainda incluem SharedPreferences em backup services a menos que explicitamente excluído em `data_extraction_rules.xml`. L01-29/L07-04 (Strava OAuth CSRF state) depende de state em `FlutterSecureStorage` — qualquer regressão que rerote para SharedPreferences silenciosamente converte defesa §10.12 em CSRF path trivialmente explorável. **Decisão arquitetural**: o finding original sugeria `custom_lint` package — rejeitado porque (a) exige package dedicado com `PluginBase` subclass + `dart run custom_lint` separado por dev, (b) 2-3 dias de yak-shaving para marginal gain, (c) não cobre paths onde dev esquece de rodar lint local. Substituído por 3 defense layers mecânicas. **Fix em 3 camadas**: (a) **Catálogo `PreferencesKeys` + `PrefsSafeKey` wrapper** (`omni_runner/lib/core/storage/preferences_keys.dart` + `prefs_safe_key.dart`): `PreferencesKeys` refatorada para ser ÚNICO entry point do keyspace SharedPreferences (16 entries + 3 prefixes); cada entrada agora routa por `PrefsSafeKey.plain(name, purpose: ...)` ou `.prefix(name, purpose: ...)` que invoca `assertSafe(name)` em class-load time (app boot). Heurística case-insensitive rejeita 22 patterns (`tokens?`/`secret`/`password`/`credential`/`api[_-]?key`/`auth[_-]?(token|secret|code)`/`private[_-]?key`/`refresh[_-]?token`/`access[_-]?token`/`jwt`/`bearer`/`session[_-]?(id|token|secret)`/`oauth[_-]?(state|token|secret)`/`mfa[_-]?(code|secret)`/`otp[_-]?(code|secret)?`/`pin[_-]?(code|hash)`/`cvv`/`card[_-]?number`/`ssn`/`cpf`/`passport`/`totp[_-]?secret`) com custom boundary `(?<![a-zA-Z])...(?![a-zA-Z])` que corrige Dart `\b`-over-`_` problem (Dart `\b` trata `_` como word char, então `\btoken\b` NÃO matches `access_token`; lookaround custom matches through underscores mas exclui innocent substrings como `tokenize`). Allowlist explícita `strava_athlete_name`+`strava_athlete_id` (metadata pública que user expõe em strava.com/athletes/<id>). Violação lança `PrefsSafeKeyViolation extends ArgumentError` — crasha boot loud porque secret em plaintext é data-loss class, não log-and-continue warning. Cada entry do catálogo agora carrega `purpose` docstring; test enforce length > 8 chars para reviewer não julgar sensibilidade sem descrição real. `PreferencesKeys.allKeys` expõe lista canônica para test + diagnostic tools. Public API preservada: `static String get themeMode => _themeMode.name` mantém backward compat com todos os 30+ call-sites existentes (`prefs.getString(PreferencesKeys.themeMode)`). (b) **Flutter test `test/core/storage/preferences_keys_test.dart`** (9 tests): `assertSafe` rejeita 28 keywords representativos (`access_token`/`refresh_token`/`strava_access_token`/`supabase_jwt`/`apple_bearer_token`/`api_key`/`api-key`/`apiKey`/`user_password`/`secret_encryption_key`/`private_key`/`mfa_secret`/`totp_secret`/`otp_code`/`pin_code`/`cvv`/`card_number`/`ssn`/`user_cpf`/`passport_id`/`session_token`/`session_id`/`oauth_state`/`auth_token`/`auth_code`/`credential_bundle`); aceita 21 keys safe (`theme_mode`/`pending_invite_code`/`offline_queue`/`coach_*`/`ble_hr_last_device_*`/`has_seen_garmin_import_guide`/`cache_meta_*`/`tip_seen_*`/`onboarding_tooltip_*`/`last_sync_at`/`selected_group_id`); allowlist honrada; erro carrega `L11-05` id + aponta `FlutterSecureStorage`; cada `PreferencesKeys.allKeys` entry passa `assertSafe`; sem duplicatas; purpose > 8 chars; prefixes terminam `_`; catálogo tem ≥ 1 prefix + ≥ 1 plain. (c) **CI guard `tools/audit/check-shared-prefs-sensitive-keys.ts`** (`npm run audit:shared-prefs-sensitive-keys`): scan 891 `.dart` files em `omni_runner/lib/**` + `test/**` (minus os 2 guard files); 3 modos extração de key: (i) string literal → strip quotes; (ii) `PreferencesKeys.<name>` → camelCase→snake_case (porque `omniLocalUserId` mapeia para key `omni_local_user_id`); (iii) bare identifier/getter → camelCase→snake_case (porque var chamada `accessToken` é suspicious mesmo sem literal value conhecido). Receivers recognised: `prefs`/`sharedPrefs`/`_prefs`/`p`/`sp`/`_pref`/`sharedPreferences`; ops: `getString`/`getBool`/`getInt`/`getDouble`/`getStringList` + `set` variants + `remove` + `containsKey`. Opt-out `// L11-05-OK: <reason>` na linha ou linha precedente — TODO opt-out surface no report para reviewer audit over time; nunca silent-skip. Self-check: valida `preferences_keys.dart` ainda importa `prefs_safe_key.dart` + ainda referencia `PrefsSafeKey.plain`/`.prefix`/`allKeys` — fails closed se refactor futuro quebrar binding. Current tree: **0 violations, 0 opt-outs**. Smoke-testado com 3 violações plantadas (literal `setString('my_access_token')` + `getString('api_key_for_user')` + `setString(someToken)` var camelCase) → todas flaggadas com correct file:line:pattern (`my_access_token→token`, `api_key_for_user→api_key`, `some_token→token`); opt-out plantado (`// L11-05-OK: legacy third-party SDK`) surface reason no report e suprime violação. **Runbook** (`docs/runbooks/SECURE_STORAGE_POLICY_RUNBOOK.md`, ~15KB): seção 1 contextualizando ameaças por platform (iOS plist + Android XML plaintext vs Keychain + EncryptedSharedPreferences); seção 2 policy decision table (use `FlutterSecureStorage` para OAuth tokens/state/encryption keys/passwords/MFA secrets/payment creds/gov IDs/biometric salts; use `SharedPreferences` para UI state/preferences não-sensitive/public identifiers/caches não-credential/queue placeholders/invite codes; use NEITHER para CVV/one-time nonces/WebAuthn blobs); seção 3 enforcement em 3 layers com call-out de binding entre eles; seção 4 "how to add new SharedPreferences key" com 6 steps + anti-patterns (`user_session_id`→`session_id match` vs correct `current_user_uuid`); seção 5 "how to add new FlutterSecureStorage component" com convenções (prefix com feature name, inject storage for tests, expose getters/setters, `clear()` on logout); seção 6 6 operational playbooks: (i) leak discovered → patch + ship migration block que ler de SharedPreferences e move para SecureStorage em next app start + force server-side refresh tokens + rotate symmetric keys + post-mortem adiciona regex se keyword não estava covered; (ii) CI guard failed → rename vs secure storage vs opt-out; (iii) Dart test failed → rename ou allowlist; (iv) `PrefsSafeKeyViolation` at boot → find offending `PrefsSafeKey.plain` call + rename; (v) false positive remediation → rename > allowlist > refine pattern (lockstep Dart + TS); (vi) dependency bump audit quando `flutter_secure_storage`/`shared_preferences` ship major; seção 7 detection signals; seção 8 cross-refs (L01-29 Strava state consumer, L04-07 server-side PII analogue, L10-09 future verification codes MUST route via secure storage, L11-06/07/08 supply chain siblings, L01-30/31 release hardening complement). **Testes**: `flutter analyze` clean; `flutter test` 2128 tests pass (9 novos + 2119 existentes); zero regressão.
- L15-04 — Transactional email platform ganha plataforma completa (outbox + provider abstraction + send-email edge fn + 4 templates + runbook, commit `2beeb1f`, 2026-04-21): fecha o high de Lente 15 (CMO) sobre "Sem email transactional platform — grep `resend|postmark|sendgrid|mailgun` em `portal/src` + edge functions → zero provider integrado; Supabase Auth SMTP default com quota compartilhada 2 email/hora/user (`auth.rate_limit.email_sent` em `supabase/config.toml`) nem cobre 10% do volume necessário; turning on `enable_confirmations=true` para L10-09 saturaria em minutos". **Estado pré-fix**: payment_confirmation, coaching_group_invite, championship_invite, weekly_training_summary prometidos em spec de produto jamais saíam do server — ou falhavam silenciosamente (zero provider) ou vazavam pela quota compartilhada do Auth (sem audit trail, sem idempotência, sem retry). **Fix em 3 pilares + testes + CI + runbook** (`supabase/migrations/20260421360000_l15_04_email_outbox.sql` ~400 LOC + `supabase/functions/_shared/email.ts` + `supabase/functions/send-email/index.ts` + 4 templates HTML + guards + docs): (a) **DB foundation** — `public.email_outbox` canonical queue (1 row por lifecycle, colunas `id`/`recipient_email`/`recipient_user_id`/`template_key`/`subject`/`template_vars jsonb`/`idempotency_key UNIQUE`/`status CHECK('pending','sending','sent','failed','suppressed')`/`provider`/`provider_message_id`/`attempts`/`last_error`/`created_at`/`updated_at`/`sent_at`/`failed_at`), RLS `ENABLE + FORCE` com `service_role FOR ALL` policy (anon + authenticated bloqueados), 5 CHECKs (status domain, idempotency_key length 8..256, template_key non-empty, attempts ≥ 0, recipient non-empty), 2 indexes (`email_outbox_idempotency_key_uniq` UNIQUE + `email_outbox_status_created_at_idx` btree para scans de pending/failed rows por idade + `email_outbox_recipient_user_idx` partial para "todos emails enviados ao user X"), + **4 helpers SECURITY DEFINER**: `fn_enqueue_email(recipient_email, recipient_user_id, template_key, subject, template_vars, idempotency_key)` — `INSERT ... ON CONFLICT (idempotency_key) DO NOTHING` + normaliza `recipient_email` (`lower + trim`) + retorna row id (caller lê row de volta para detectar `already_sent` replay); `fn_mark_email_sent(id, provider, provider_message_id)` — idempotente `sent → sent`, raise `P0010 INVALID_TRANSITION` em `failed/suppressed → sent`; `fn_mark_email_failed(id, error, terminal)` — `terminal=false` bump `attempts` + keep `pending` (transient; retry later), `terminal=true` flip `failed`; raise `P0010` em `sent/suppressed → failed`; `fn_email_outbox_assert_shape()` — raise `P0010` se table/RLS/indexes/CHECKs/privileges drift (CI helper, no-op quando healthy); **privilégios explícitos**: `REVOKE ALL FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role` em todos os 4 (HotFix descoberto em self-test: `REVOKE ALL FROM PUBLIC` não remove permissões herdadas por `anon`/`authenticated` — precisava ser explícito); **self-test DO-block** valida enqueue idempotency + normalization (`  User@EXAMPLE.com  ` → `user@example.com`), mark_sent idempotency (chamar 2x com mesmo `provider_message_id` = no-op), mark_failed (terminal + non-terminal), + raise `P0010` em `sent → failed` e `failed → sent` (fix de exception handler `WHEN sqlstate 'P0010'` porque `WHEN raise_exception` não captura custom error codes). (b) **Shared edge-fn module (`_shared/email.ts`)** — `TEMPLATE_MANIFEST` mirror typed de `manifest.json` como `EmailTemplateKey` union (CI enforce drift), `escapeHtml(&<>"'\/` + surrogate NBSP) aplicado a TODO `{{var}}` em body default (subjects opt-out com `escape:false`, plain text), `renderTemplate({{var}}/{{ var }}` substitution + `onMissing` hook), `validateEmailAddress` RFC-compliant regex + length cap 254, `assertRequiredVars` rejeita enqueue ANTES de dispatch se caller esqueceu required_var (reporta full missing set em EmailError), `EmailProvider` interface + 3 impls: `ResendProvider` (prod, `fetch api.resend.com/emails`), `InbucketProvider` (dev local via `supabase start`), `NullProvider` (default — zero outbound HTTP, fabrica id `null-<uuid>`, safe para CI/sandbox), `resolveProvider(Deno.env)` factory valida `EMAIL_PROVIDER` enum, `TemplateLoader` abstraction (`FsTemplateLoader` prod + `InMemoryLoader` tests), `sendEmail()` dispatcher retorna discriminated union `{status, provider, providerMessageId, error, terminal}` para caller decidir retry. (c) **Edge function (`send-email/index.ts`)** — `POST /send-email` service-role gated (403 caso contrário), body `{template_key, recipient_email, template_vars, idempotency_key, recipient_user_id?}`; orquestra `fn_enqueue_email → short-circuit se status='sent'` (returns 200 + `status:already_sent` para replay idempotency) → `sendEmail()` → success = `fn_mark_email_sent`, failure = `fn_mark_email_failed(terminal)`; health check `GET /send-email` verifica `EMAIL_PROVIDER` + DB; `x-request-id` para tracing; apiError-style JSON em non-2xx. (d) **Templates** — `supabase/email-templates/manifest.json` registry (subject, file, required_vars, from_name, category) + 4 HTML responsivos 600px inline-styled single-column com brand palette: `coaching_group_invite` (recipient_name, coach_name, group_name, invite_link, expire_date), `championship_invite` (staff_name, host_group_name, championship_name, invite_link, start_date), `weekly_training_summary` (athlete_name, group_name, sessions_completed/total, total_km, streak_days, dashboard_link), `payment_confirmation` (user_name, amount, description, receipt_link). **Testes**: (i) **27 Deno unit** em `_shared/email.test.ts` (escapeHtml metacharacters/safe-pass/XSS `<img src=x onerror=alert(1)>`; renderTemplate escape on/off/missing/spaces-inside-braces; validateEmailAddress 8 malformed + 6 canonical; assertRequiredVars happy-path per template + missing reported + unknown template; resolveProvider default/inbucket/resend/unknown reject; NullProvider; InMemoryLoader happy/missing; sendEmail end-to-end com XSS neutralization + terminal vs transient classification), (ii) **18 pg integration** em `tools/test_l15_04_email_outbox.ts` via `docker exec psql` (schema: table + RLS forced; 4 helpers registered + SECURITY DEFINER; service_role tem EXECUTE; anon+authenticated NÃO tem EXECUTE; unique index; 5 CHECKs; argument validation: empty recipient, idempotency_key < 8 chars, NULL id; behaviour: enqueue twice same key → same row + recipient normalized; mark_sent flips → sent + idempotente; mark_failed non-terminal keeps pending + bumps attempts; mark_failed terminal flips failed; sent→failed + failed→sent ambos P0010; assert_shape no-op quando healthy). **CI guard** (`npm run audit:email-platform`, `tools/audit/check-email-platform.ts`): 6 invariantes — `db:` SELECT `fn_email_outbox_assert_shape()` fails closed se shape drift; `shared:` `_shared/email.ts` exporta `sendEmail`/`escapeHtml`/`renderTemplate`/`EmailError`/`TEMPLATE_MANIFEST`/`resolveProvider`; `edge-fn:` `send-email/index.ts` existe + service-role gated + wires `fn_enqueue_email → sendEmail → fn_mark_email_{sent,failed}`; `templates:` manifest entry tem file + required_vars + subject + body contém cada placeholder; `providers:` grep `fetch.*api.resend.com` etc. FORA de módulos sanctioned → fail se alguém bypassar `sendEmail()`; `runbook:` `EMAIL_TRANSACTIONAL_RUNBOOK.md` existe + > 4KB. **Runbook** (`docs/runbooks/EMAIL_TRANSACTIONAL_RUNBOOK.md`, ~18KB): architecture diagram + tables/helpers cheatsheet + "add new template" 5-step checklist + 6 operational playbooks (Resend 5xx outage: SELECT `email_outbox` stuck rows + manual replay + emergency Inbucket failover com warning "never leave in prod"; template não renderiza / `{{var}}` literal aparece: caller bug vs manifest drift + apology template pattern; spike de `status='failed'`: top 10 failure reasons SQL + patterns provider_4xx bounce/email_not_verified/missing_vars/unknown_template; replay failed batch after recovery: UPDATE to pending + cron drain; GC retention manual 90-day SQL com warning LGPD 5-year para payment_confirmation; CI guard failed after deploy: DB shape drift vs shared exports lost vs provider HTTP bypass) + security posture (service-role only, HTML escape by default com subjects plain-text exception, idempotency_key UNIQUE+CHECK length derivado de withdrawal_id/purchase_id/invite_id, outbound network opt-in via EMAIL_PROVIDER) + detection signals table + rollback (drain before drop; `EMAIL_PROVIDER=null` pause sem drop) + cross-refs (L10-09 sign-in verification emails usam mesmo outbox; L12-09 `notification_log` sibling com `ctx_id` shared key email+push; L18-04 `idempotency_keys` é request-level não email-row-level; L04-07 template_vars NÃO pode leak PII plaintext; L03-17 amounts já formatadas pelo caller). **Follow-ups parked não-blocking**: L15-05 (cron drain + GC 90d), L15-06 (category=pii retention carve-out), L15-07 (bounce/complaint webhook Resend).
- L17-04 — `portal/src/lib/qa-e2e.test.ts` (842 linhas, 4 describes, ~370 linhas de mock-DB compartilhado no topo) split em 4 arquivos focados + shared fixtures module (commit `447493a`, 2026-04-21): fecha o high de Lente 17 (VP Eng) sobre "Testes unitários em `portal/src/lib/qa-*.test.ts` — arquivos > 800 linhas: `qa-e2e.test.ts` (842), difíceis de manter, PR review impossível, test runtime ruim". **Estado pré-fix**: monolith em `portal/src/lib/qa-e2e.test.ts` com 842 linhas cobria em um único arquivo (i) smoke E2E (happy-path coin emission → custody → wallet credit), (ii) idempotency (intent replay, nonce reuse), (iii) anti-fraud (wallet drift detection, suspicious patterns), (iv) concurrency (two intents racing same balance). Os 4 describes compartilhavam ~370 linhas de setup no topo (mock types `MockAccount`/`MockSettlement`/`MockEvent`/`MockIntent` + `state` mutável + `handleRpc` dispatcher + `mockRpc` `vi.fn()` + `createIntent`/`consumeIntent` helpers + `makeFromMock(table)` com chainable query stubs). **Problemas concretos**: (1) PR review de 200-line diff vinha com 600-line unchanged context — reviewer tinha que `git log --follow` para entender onde a mudança operava; (2) `vitest` runtime de ~4.2s apenas para este arquivo (19% do total); (3) 3 flakes observados nos últimos 30 dias em `should block burn when wallet drift detected` (section 3) onde contaminação de `state` cross-section não era imediatamente óbvia (reset entre tests OK, mas state-leak entre describes não era explícito); (4) adição de novo cenário anti-fraud exigia scroll de 500+ linhas para chegar ao `describe('anti-fraud')`. **Fix em 5 camadas** (~90 LOC líquidas delete, ~680 LOC novas em 5 files): (a) **Shared fixture module** `portal/src/lib/__qa__/qa-e2e-fixtures.ts` (402 linhas): movido TUDO que era compartilhado — types `MockAccount`/`MockSettlement`/`MockEvent`/`MockIntent`, objeto `state` exportado como `export const state: { accounts, settlements, events, custody, platformFee }` mutável (design: reset entre tests via `resetState()` explicitamente chamado), `resetState()`/`resetIntents()`, `handleRpc(name, params)` dispatcher puro (zero side-effects fora do `state`), `mockRpc` = `vi.fn().mockImplementation((name, params) => handleRpc(name, params))`, `rewireMockRpc()` (re-faz o `mockImplementation` após `vi.clearAllMocks()` que zera impl), `createIntent`/`consumeIntent` business helpers, `makeFromMock(table)` que retorna chainable query stubs per-table (`platform_fee_config`/`clearing_events`/`clearing_settlements`/`custody_accounts`). Diretório `__qa__/` segue convenção Vitest (`__fixtures__` também aceito, mas `__qa__` sinaliza escopo domain-específico). (b) **`portal/src/lib/qa-e2e-smoke.test.ts`** (194 linhas, 8 tests) — smoke path happy-case. (c) **`portal/src/lib/qa-e2e-idempotency.test.ts`** (137 linhas, 4 tests) — intent replay + nonce reuse. (d) **`portal/src/lib/qa-e2e-antifraud.test.ts`** (174 linhas, 8 tests) — wallet drift + suspicious pattern detection. (e) **`portal/src/lib/qa-e2e-concurrency.test.ts`** (172 linhas, 4 tests) — two-intents-racing + reorder invariants. Cada test file declara `vi.mock('@/lib/supabase/service', () => ({...}))` + `vi.mock('@/lib/audit', () => ({...}))` + `vi.mock('@/lib/custody', () => ({...}))` INLINE no top-level (Vitest hoista `vi.mock` per-test-file na fase pre-import — NÃO pode ser extraído para shared fixture module sem quebrar hoisting), apontando para `makeFromMock` + `mockRpc` importados do shared. `beforeEach` de cada describe faz `resetState()` + `resetIntents()` + `vi.clearAllMocks()` + `rewireMockRpc()` (ordem crítica: clearAllMocks zera impl do `vi.fn()`, rewire re-instala). **Deletado** `portal/src/lib/qa-e2e.test.ts` (842 linhas), migrado 100% dos 24 tests sem perder asserção. **Tests**: 24/24 migrados passam (mesmos expects, mesmo comportamento, mesmo data); full portal lib suite cresce 43 → 46 files mantendo 867 tests verdes, duração per-file cai de ~4.2s (monolith) para 4 files × ~0.8-1.1s (total comparable, mas paralelizável). **CI** `tools/audit/check-portal-test-file-size.ts` + `npm run audit:portal-test-file-size` — enforcement em 4 camadas: (1) **hard cap 800 linhas** em TODOS `portal/src/**\/*.test.ts` (estoura → FAIL), (2) **soft cap 400 linhas** com **`ALLOWLIST`** para 9 files históricos legitimamente grandes sem degradar cobertura (`webhook.test.ts` 463, `partnerships.test.ts` 545, `money.test.ts` 639, `schemas.test.ts` 586, `swap-route.test.ts` 451, `clearing.test.ts` 483, `swap.test.ts` 432, `coins-reverse.test.ts` 479, `csrf.test.ts` 423) — allowlist tem comentário `// WHEN TOUCHING: consider splitting per §4.2 of PORTAL_TEST_FILE_SIZE_RUNBOOK.md`, pattern "touch it, split it", (3) **required split files** verifica que os 4 `qa-e2e-{smoke,idempotency,antifraud,concurrency}.test.ts` existem (previne regressão silenciosa se alguém fizer merge que delete um split), (4) **forbidden monolith** verifica que `qa-e2e.test.ts` (o antigo) NÃO existe (alguém recreando por acidente pega FAIL). Script usa `node:fs/promises` + `glob` (pre-installed), output per-check `[OK|FAIL] <kind>: <file> (<lines>)` facilita PR review. (f) **Runbook canônico** `docs/runbooks/PORTAL_TEST_FILE_SIZE_RUNBOOK.md` (~180 linhas, 9 seções): why (post-mortem do monolith — 4 problemas concretos listados acima), invariant table (hard cap 800 / soft cap 400 / split required / monolith forbidden), what shipped (5 pieces × file × responsibility), **§4.2 template de split** (template concreto para próxima vez: fixture module em `__<feature>__/<feature>-fixtures.ts`, mutable `state` object exportado, `vi.fn()` compartilhado + `rewire<X>()` helper, test files 4 máx, cada um `vi.mock` inline no topo), guidelines para novas suites (não criar file > 400 linhas do zero — começar split já), detection signals (CI + local `wc -l` + vitest duration report + PR review com "este file parece > 400?" como reflex), 5 playbooks operacionais (A: CI falha "file exceeds 800 line cap" — decidir entre merge com emergency ALLOWLIST comment + follow-up ticket OU split antes do merge; B: CI falha "missing expected split file" — alguém deletou split; C: CI falha "forbidden monolith back" — alguém recriou qa-e2e.test.ts por acidente; D: novo test file cross 400 soft cap — review sugere split preemptivo; E: test runtime regression > 20% — profile per-file com `vitest --reporter=verbose` + split o top-offender), rollback posture ("pure refactor, zero runtime impact — se split em si introduzir flake, rollback é git revert do commit"), invariants list + cross-refs L17-01 (financial routes must-use error-handler) / L17-03 (withErrorHandler types) / L17-05 (logger Sentry capture). **Backwards compat**: 100% — zero runtime impact, puramente refactor de test organization. Production code unchanged, imports test-only. **Escopo deliberadamente excluído**: split proativo de `partnerships.test.ts` (545 linhas, 8 describes internas, cada < 100 linhas — não viola hard cap, "touch it, split it" pending), `money.test.ts` (639 linhas, pure helpers sem mock-DB compartilhado, test file coerente), `schemas.test.ts` (586 linhas, Zod fixtures compartilhadas, follow-up pending) — todos allowlisted com comentário de "when touching". Follow-ups registrados no runbook §4.3. Onda 1 → 101/179 (+1), High → 61/123 (+1), Batch F (Plumbing / DX) avança 3/4.
- L17-03 — `portal/src/lib/api-handler.ts` ganha typing strict via tuple-generic (commit `ac4625b`, 2026-04-21): fecha o high de Lente 17 (VP Eng) sobre "`withErrorHandler` usa `any` em `routeArgs` — `handler: (req: NextRequest, ...routeArgs: any[]) => Promise<NextResponse>`. `any` derrota o type-checking em favor de ergonomia". **Estado pré-fix**: o wrapper `withErrorHandler<H extends (req, ...routeArgs: any[]) => ...>(handler: H): H` era o outermost safety-net de TODAS as 57 routes em `portal/src/app/api/**`, incluindo todas as routes financeiras críticas (custody, swap, clearing, auto-topup, distribuição de coins, reverso atômico, invariantes de wallet). O `routeArgs: any[]` era um escape hatch tipa-verdade: dentro de `/api/platform/custody/[groupId]/daily-cap/route.ts` com handler tipado `ctx: { params: { groupId: string } }`, qualquer acesso a `ctx.params.slug` passava na build (porque `any[]` neutraliza o contract) e só falhava em 500-time quando `params.slug` voltava `undefined` e zerava o cap. As routes mais arriscadas eram as aninhadas `/api/training-plan/[planId]/weeks/[weekId]/workouts/[workoutId]/` onde 3+ params concorrem e um typo bypassa auth/RLS/idempotency guards. Acumulávamos também o cast grotesco `return wrapped as unknown as H` — sinal de que o type-system estava cedendo à ergonomia. **Fix em 3 camadas** (~40 LOC net): (a) **`ApiHandler<TArgs>` tuple-generic type** exportado como forma canônica de qualquer route handler: `export type ApiHandler<TArgs extends readonly unknown[] = readonly unknown[]> = (req: NextRequest, ...routeArgs: TArgs) => Promise<NextResponse>`. Default parameter `readonly unknown[]` preserva uso sem generics (compatível com routes que não se importam com params). (b) **`RouteParams<P>` public helper**: `export type RouteParams<P extends Record<string, string | string[]> = Record<string, string | string[]>> = { params: P }`. Dynamic routes declaram `ctx: RouteParams<{ id: string }>` e obtém `ctx.params.id` typado — `ctx.params.slug` em route `[id]` é **compile error**. Deliberadamente permissive em `P` (string | string[]) para cobrir catch-all `[[...slug]]`. (c) **Wrapper reassinado** `function withErrorHandler<TArgs extends readonly unknown[]>(handler: ApiHandler<TArgs>, routeName: string, options?: WithErrorHandlerOptions): ApiHandler<TArgs>` — **zero `any`**, cast `as unknown as H` removido, `ApiHandler<TArgs>` como tipo interno do `wrapped` garante TypeScript checa a variadic tuple end-to-end. Generic inference pega TArgs da assinatura do handler passed (sem anotação manual requerida em callers). **Tests**: `portal/src/lib/api-handler.test.ts` — o teste de context forwarding migrado de `ctx: any` para `ctx: RouteParams<{ id: string }>` + novo caso `L17-03 — preserves the handler signature exactly (no any)` com 2 cenários: static route (arity preservada via `wrapped.length === staticHandler.length`) + nested dynamic (`RouteParams<{ groupId: string; id: string }>` com 2 params). 12/12 api-handler + 867/867 portal lib tests verdes. `npx tsc --noEmit` passa (erro pre-existente em `feature-flags.ts` é MapIterator iteration em target < ES2015, não relacionado). **CI** `tools/audit/check-api-handler-types.ts` + `npm run audit:api-handler-types` — **9 regressions**: (1) export `RouteParams` helper, (2) export `ApiHandler<TArgs extends readonly unknown[]>`, (3) generic `<TArgs extends readonly unknown[]>` em withErrorHandler, (4) param `handler: ApiHandler<TArgs>`, (5) return `ApiHandler<TArgs>` sem `as unknown as H`, (6) ausência de `routeArgs: any[]` (legacy bug shape), (7) ausência do bound `H extends (req, ...any[])`, (8) test usa `RouteParams<>` em vez de `any`, (9) test importa `RouteParams`. Output per-check `[OK|FAIL] file: label`. **Runbook canônico** `docs/runbooks/API_HANDLER_TYPING_RUNBOOK.md` (~180 linhas, 9 seções): why (post-mortem do bug-shape em routes aninhadas), invariant (signature preservation end-to-end via tuple generic), what shipped (6 pieces × file × responsibility), how to use (3 templates: static, dynamic single-param, nested multi-param com `RouteParams<>`), detection signals (CI gate, test count, tsc, Sentry route:tags), 4 playbooks operacionais (adicionar nova dynamic route, Next ctx shape muda, pre-commit tsc quebra em handler touched hoje, route precisa extras além de `{params}`), rollback posture ("zero runtime change, só rollback se Next ship breaking protocol change que narrower type não modela — nesse caso prefer widening bound sobre re-introducing `any`"), invariants list + cross-refs L17-01 (financial routes must-use) / L17-05 (logger Sentry) / L13-06 (request_id) / L14-05 (error envelope). **Backwards compat**: 100% — as 57 call-sites existentes com sintaxe `withErrorHandler(handler, "route.name")` continuam compilando inalterados (structural typing: `{ params: { ... } }` equivalente a `RouteParams<{...}>`), apenas type inference agora carrega shape correto. 867/867 lib tests passam sem nenhum change em production callers. Escopo deliberadamente excluído: migration explícita de 57 routes para declarar `RouteParams<>` inline (migration incremental "when touching"; inferência atual já cobre), Zod auto-validation de `params` em runtime (follow-up L14-06 co-escopo), OpenAPI spec generation (out-of-scope). Onda 1 → 100/179 marca milestone (+1 absoluto, 56% do total), High → 60/123 (+1), Batch F (Plumbing / DX) avança 2/4.
- L17-05 — `portal/src/lib/logger.ts` fecha o gap de observabilidade em `logger.error` (commit `4549616`, 2026-04-21): fecha o high de Lente 17 (VP Eng) sobre "Logger silencia errors não-Error — chamada `logger.error('failed', undefined)` passa pelo segundo branch (undefined é falsy), não captura nada no Sentry. Todas as 4 call-sites em produção (`custody.webhook.config_missing`, `custody.webhook.dispute_feature_unavailable`, `checkout.proxy.gateway_non_json`) eram invisíveis no Sentry apesar de serem incidentes acionáveis". **Estado pré-fix**: o helper tinha shape `if (error instanceof Error) { captureException } else if (error) { captureMessage }` — o `else if (error)` falhava em cobrir `undefined`/`null`/`0`/`""`, e quatro call-sites em produção invocavam explicitamente `logger.error("...", undefined, meta)` (idiom canônico para erros-de-invariante sem Error subjacente: config faltando, feature indisponível, body non-JSON). Durante outage de 2026-04-18 (custody webhook 403) o operador gastou 22 minutos reconstruindo o erro por CloudWatch console porque Sentry não tinha nenhum event mapeando o incidente. **Fix em 2 camadas** (~60 LOC líquidas): (a) **Helper `normalizeErrorFields(error)`** — retorna `{}` para `undefined`/`null` (suprime o inútil `error: "undefined"` do JSON de console), `{error: msg, stack}` para `Error`, `{error: JSON-safe shallow clone}` para objetos/arrays (via `JSON.parse(JSON.stringify(error))` com fallback `String(error)`), `{error: String(error)}` para strings/números/booleans. Centraliza a normalização do que é emitido tanto no console quanto nos extras do Sentry. (b) **`logger.error(...)` sempre emite** — `captureException(error, {extra:{msg, ...meta}})` + `return` para Error, senão `captureMessage(msg, {level:'error', extra:{...meta, ...errorFields}})` incondicional — cobre `undefined`, `null`, `""`, `0`, objetos, strings, arrays. **Tests**: nova suite `L17-05 Sentry capture invariants` com 6 casos adicionados (Error → captureException com extras corretos, undefined → captureMessage bug L17-05, null → captureMessage, string → captureMessage com error field, objeto → captureMessage com JSON clone, console-format sem `error: "undefined"`) + mocks `mockCaptureException`/`mockCaptureMessage` instrumentados. 14/14 tests verdes (incluindo as 8 pré-existentes de L20-03 trace correlation). **CI** `tools/audit/check-logger-sentry-capture.ts` + `npm run audit:logger-sentry-capture` — **8 regressions automatizadas**: (1) ausência do bug-shape `} else if (error) { ... captureMessage` (regex multi-linha), (2) presença de `Sentry.captureException(error`, (3) presença de `Sentry.captureMessage(msg`, (4) `normalizeErrorFields` declarado, (5) tratamento explícito `error === undefined || error === null`, (6) test suite `L17-05 Sentry capture invariants` declarada, (7) test cobre caso undefined com `mockCaptureMessage`, (8) test cobre caso Error com `mockCaptureException`. Output per-check `[OK|FAIL] file: label` facilita PR review. **Runbook canônico** `docs/runbooks/LOGGER_SENTRY_CAPTURE_RUNBOOK.md` (~190 linhas, 9 seções): why (post-mortem 2026-04-18 custody webhook), invariant table (Error/primitive/object/null mapping para Sentry transport e console fields), what shipped (5 pieces × file × responsibility), how to call (3 recommended + 3 anti-patterns com exemplos concretos), detection signals (Sentry issue count por msg, Sentry:Log ratio ≥ 0.95 em CloudWatch, CI gate, test count), 4 playbooks operacionais (Sentry para recebimento de events → audit:logger-sentry-capture check; novo call-site → 3 shapes sancionadas; redação de PII → audit.ts redactReason; quieting noisy site → upstream fix OU Sentry Inbound Filter, nunca hackear logger), rollback posture ("strict defence, no lever"), invariants list + cross-refs L17-03/L20-03/L04-07. **Backwards compat**: 100% — API pública `logger.error(msg, error?, meta?)` inalterada; apenas eventos antes dropados surgem em Sentry (pura adição de visibilidade). Escopo deliberadamente excluído: rewrite de callers para `throw new Error()` (manter idiom `undefined` válido), redaction automática de PII (design decision: redação vive no caller para permitir policy per-domain), migration para OpenTelemetry (out-of-scope, L20-03 cobre trace correlation via Sentry). Onda 1 → 99/179 (+1), High → 59/123 (+1), Batch F (Plumbing / DX) abre 1/4.
- L07-04 — Strava OAuth ganha `OAuthCsrfViolation` distinto + UX dedicada (commit `89afcb8`, 2026-04-21): fecha o high de Lente 7 (CXO) sobre "Flutter deep link Strava OAuth sem state validation (CSRF) — atleta tenta conectar, é redirecionado de volta, o app abre mas não confirma sucesso porque state não é verificado. Comportamento indeterminado." **Estado pré-fix**: a defesa §10.12 já estava parcialmente instalada via L01-29 (`StravaOAuthStateGuard` com CSPRNG 32-byte + FlutterSecureStorage TTL 10 min + constant-time compare + consume-once; wired em `StravaAuthRepositoryImpl.authenticate` via `_stateGuard.beginFlow()` + `validateAndConsume(returnedState)` ANTES do token exchange; deep-link handler rejeitando legacy `omnirunner://strava/callback` + `omnirunner://localhost/exchange_token` como `UnknownLinkAction`). Porém (a) a falha caía em `AuthFailed('OAuth state mismatch — flow aborted')`, classe genérica que a UI `settings_screen` tratava sob o mesmo branch de erros de rede, surfacing "Erro ao conectar Strava. Tente novamente." — (i) usuário não sabia que a rejeição foi security-driven, (ii) support no Crashlytics não conseguia distinguir forgery attempt de provider outage, (iii) telemetria confundia CSRF com flaky network. **Fix em 6 camadas** (~90 LOC): (a) **`OAuthCsrfViolation` final class** em `integrations_failures.dart` extends `IntegrationFailure` com `reason: String` machine-readable (`'state_missing'` quando callback URL sem query `state`; `'state_mismatch'` quando state presente mas não match stored token — cobre wrong-value, expired, never-minted, already-consumed, storage tampered). Docstring explícita documenta que "the attempted exchange was aborted BEFORE any token request". (b) **Re-export via barrel** `strava_failures.dart show ... OAuthCsrfViolation` — feature code Strava não precisa importar integrations_failures diretamente. (c) **Repo wiring** em `StravaAuthRepositoryImpl.authenticate`: após `validateAndConsume(returnedState)` retornar false, computa reason (`returnedState == null || isEmpty` → `'state_missing'`, else `'state_mismatch'`), emite `AppLogger.warn('OAuth callback rejected: $reason (no token exchange performed)')` para telemetria, e `throw OAuthCsrfViolation(reason: reason)` em vez de `throw const AuthFailed('OAuth state mismatch — flow aborted')`. (d) **Dedicated UX** em `settings_screen._StravaIntegrationTileState._connect()`: nova branch `} on OAuthCsrfViolation catch (e) {` ANTES do catch `on IntegrationFailure`, reload state, surface `SnackBar(content: Text(e.reason == 'state_missing' ? 'Retorno do Strava sem verificação de segurança. Por favor, tente conectar novamente.' : 'Tentativa de conexão inválida detectada. Por favor, tente conectar novamente.'), backgroundColor: cs.error)` — tint `colorScheme.error` distingue de `DesignTokens.warning` (amarelo, success) em screen recordings support. (e) **Tests** atualizados (`test/features/strava/strava_auth_repository_test.dart`): 3 testes CSRF migrados de `isA<AuthFailed>().having((e) => e.reason, contains('OAuth state mismatch'))` para `isA<OAuthCsrfViolation>().having((e) => e.reason, 'reason', 'state_mismatch')` / `'state_missing'` + replay test agora asserts `isA<OAuthCsrfViolation>()`. `phase_14_smoke_test` IntegrationFailure exhaustive switch estendido com `OAuthCsrfViolation() => 'csrf'`. 41/41 flutter tests verdes em test/features/strava/. (f) **CI** `tools/audit/check-strava-oauth-state.ts` + `npm run audit:strava-oauth-state` — **17 regressions automatizadas**: (1-6) guard primitives (beginFlow declared, validateAndConsume declared, `tokenBytes >= 32`, `Random.secure()`, `ttl <= 30 min` regex-extracted, `_constantTimeEquals`); (7-11) repo wiring (`_stateGuard.beginFlow()`, `validateAndConsume(returnedState)`, throws `OAuthCsrfViolation(` AND não throws `AuthFailed('OAuth state mismatch`, `state_missing` AND `state_mismatch` strings presentes, state passado para `buildAuthorizationUrl`); (12-13) failure class exported (final class declaration em integrations_failures + barrel re-export); (14) deep-link denial (NENHUM `return StravaCallbackAction(` em deep_link_handler — L01-29 invariante); (15-16) test coverage (OAuthCsrfViolation + state_mismatch + state_missing asserted); (17) settings UX pattern-match (`on OAuthCsrfViolation` em settings_screen). Output per-check `[OK] file: label` facilita triage em PR review. **Runbook canônico** `docs/runbooks/STRAVA_OAUTH_CSRF_RUNBOOK.md` (~260 linhas, 7 seções): summary (why: single-source-of-truth + blast radius cross-tenant contamination anti-cheat/ranking/LGPD), what shipped (tabela de 6 pieces × file × responsibility), lifecycle ASCII diagram (beginFlow → buildAuthorizationUrl → flutter_web_auth_2 → validateAndConsume → exchange OR throw), reason codes table (user-facing message por cada), detection signals (Logflare cross-ref L10-09 auth_login_attempts spike + mobile AppLogger WARN + Crashlytics non-fatal + CI check), 5 cenários operacionais (A: spike em Crashlytics — investigar device class + flutter_secure_storage health + background kill; B: user preso "Tentativa inválida" — L1 playbook com device clock check; C: tighten TTL para 5 min se 99% completes < 3 min; D: adicionar segundo provider refatorar para generic OAuthStateGuard; E: re-emergência de deep-link callback legacy rejeitar PR), rollback posture ("this is defence not feature — we do not roll back; bump TTL ou narrow device class"), invariants (tokenBytes >= 32, ttl <= 30 min, Random.secure, constant-time, OAuthCsrfViolation distinct, reason enum, deep-link denial, UX distinct — todos enforced by CI), cross-refs L01-29/L10-07/L10-09 + strava_connections table. **Backwards compat**: 100% — nova subclass de sealed IntegrationFailure (exhaustive switches atualizados), UI nova branch adicionada antes do catch genérico existente. Escopo deliberadamente excluído: abstração genérica `OAuthStateGuard` (follow-up quando Polar/Garmin OAuth lander), Sentry mobile wiring (mobile Sentry é L06-07 follow-up), `app_links` deep-link hardening além dos 2 callbacks legacy (L01-29 já cobriu). Onda 1 → 98/179 (+1), High → 58/123 (+1), Lente 7 (CXO) → 2/N (após L12-07 + L07-06 co-fix de timezone). Batch E (Security & Auth) fecha ✅ 4/4.
- L10-08 — `public.audit_logs` + 6 tabelas de auditoria ganham invariante append-only DB-level (commit `0e13ba8`, 2026-04-21): fecha o high de Lente 10 (CSO) sobre "logs de acesso sensíveis sem imutabilidade — atacante com service_role (ou contribuidor rodando script) pode DELETE/UPDATE em audit_logs e reescreve trilha forense silenciosamente". **Estado pré-fix**: `portal_audit_log`/`coin_ledger_pii_redactions`/`cron_edge_retry_attempts`/`wallet_drift_events`/`custody_daily_cap_changes`/`consent_events`/`audit_logs` eram tabelas regulares. `account_deletion_log` já tinha trigger de coluna-imutabilidade (L04-02) mas não estava registrada em lugar nenhum — next DBA que rodasse `DELETE FROM portal_audit_log WHERE action='platform_admin.custody.deposit'` para limpar ruído de teste acidentalmente apagava trilha legal LGPD Art. 37. **Fix em 5 camadas** (`supabase/migrations/20260421350000_l10_08_audit_logs_append_only.sql`, ~300 LOC): (a) **Registry `public.audit_append_only_config`** — `PRIMARY KEY (schema_name, table_name)`, `mode text CHECK IN ('strict','append_with_outcome')`, `applied_at timestamptz`, `note text`. `strict` = rejeita todo UPDATE/DELETE/TRUNCATE. `append_with_outcome` = tolera trigger de coluna-imutabilidade per-table (`account_deletion_log.fn_account_deletion_log_immutable` de L04-02). RLS ENABLE + FORCE, policy service_role-only FOR ALL. (b) **Trigger genérico `public.fn_audit_reject_mutation()`** — BEFORE UPDATE OR DELETE (row-level) + BEFORE TRUNCATE (statement-level). Raise `P0010` com machine-readable reason em DETAIL (`append_only_delete_blocked`/`append_only_update_blocked`/`append_only_truncate_blocked`) + HINT apontando para runbook. **Emite `RAISE WARNING` com `session_user`/`current_user`/`TG_OP`/`TG_TABLE_SCHEMA.TG_TABLE_NAME` ANTES do RAISE EXCEPTION** — mesmo com transaction rollback, Postgres logs (Logflare/syslog) capturam cada tentativa com contexto completo. Sem bypass runtime: emergência real exige migration revisada com `DROP TRIGGER` (visível em git history + CI review). SECURITY DEFINER `search_path=public,pg_temp`. (c) **Idempotent installer `public.fn_audit_install_append_only_guard(schema, table, note?)`** — SECURITY DEFINER, verifica `pg_class.relkind IN ('r','p')` para tolerar particionamento, DROP TRIGGER IF EXISTS + CREATE TRIGGER com nomes determinísticos (`trg_<table>_append_only_row` + `trg_<table>_append_only_stmt`), UPSERT em `audit_append_only_config` preservando `note`. Returns boolean (false = table não existe → no-op com NOTICE, seguro em sandbox). (d) **Detector + assert**: `public.fn_audit_has_append_only_guard(schema, table)` STABLE SECURITY DEFINER — boolean iff JOIN `pg_trigger × pg_class × pg_namespace × pg_proc` encontra trigger não-interno apontando para `fn_audit_reject_mutation`. `public.fn_audit_assert_append_only_shape()` SECURITY DEFINER — itera `audit_append_only_config WHERE mode='strict'`, checa cada binding via detector, acumula `v_missing` array. Também itera 7 known-audit tables (`portal_audit_log`, `coin_ledger_pii_redactions`, `cron_edge_retry_attempts`, `wallet_drift_events`, `custody_daily_cap_changes`, `consent_events`, `audit_logs`) e flagga `unregistered:<name>` se table existe mas não está no registry (forçar novos contribuidores a registrar via helper). Raise `P0010` com lista comma-separated + HINT para runbook. REVOKE PUBLIC + GRANT service_role. (e) **Apply em 7 tables strict + 1 append_with_outcome**: DO-block itera array `v_targets` e chama installer. `account_deletion_log` registrado separadamente como `append_with_outcome` (note: "L10-08: protected by fn_account_deletion_log_immutable trigger (L04-02)") para CI aceitar o pattern existente sem duplicar trigger. **Self-test** (transação separada, roda após COMMIT): valida >=1 strict binding, `portal_audit_log` tem guard, `fn_audit_assert_append_only_shape` passa, `TRUNCATE portal_audit_log` bloqueia com P0010 (TRUNCATE escolhido em vez de DELETE porque dispara trigger mesmo com zero rows matching — não depende de fixture). **Tests** `tools/test_l10_08_audit_logs_append_only.ts` (~18 cases, docker-exec psql): schema/DDL (registry com CHECK mode + RLS forced + service_role policy, fn_audit_reject_mutation SECURITY DEFINER, installer SECURITY DEFINER + has fn STABLE, 3 strict tables registradas, account_deletion_log append_with_outcome condicional). Privileges (anon NÃO EXECUTE). Behaviour (DELETE em portal_audit_log → P0010 + append_only_delete_blocked, UPDATE → P0010 + append_only_update_blocked, TRUNCATE → P0010 + append_only_truncate_blocked, INSERT normal passa). Installer idempotency (re-apply não duplica), installer no-op em table inexistente (retorna false + NOTICE). Casts `::text` para bools retornam 'true'/'false' (não 't'/'f') — ajuste vs pattern anterior. **CI** `tools/audit/check-audit-logs-append-only.ts` + `npm run audit:audit-logs-append-only` — probe `fn_audit_assert_append_only_shape` via docker-exec, captura stderr com padrões `L10-08|append_only|P0010|unregistered|HINT`. **Runbook canônico** `docs/runbooks/AUDIT_LOGS_APPEND_ONLY_RUNBOOK.md` (~240 linhas, 7 seções): summary (tabela de mode por table), arquitetura (fluxo registry → installer → trigger → detector/assert → CI), observabilidade via RAISE WARNING (queries para Logflare `postgres.msg LIKE 'L10-08:%%'`), cenários operacionais (A: adicionar nova audit table via nova migration chamando `fn_audit_install_append_only_guard('public','minha_nova_table')` — NÃO INSERT direto no registry; B: legitimate purge de rows antigas — NUNCA drop trigger em prod sem migration; criar `fn_audit_purge_<table>(cutoff)` SECURITY DEFINER que faz transaction com `ALTER TABLE DISABLE TRIGGER <nome> ... DELETE ... ENABLE TRIGGER` + audit row em portal_audit_log com justificativa; C: investigar warning spike — `portal_audit_log` recebendo tentativas de UPDATE → checar Logflare por session_user + correlacionar com deploys; D: false-positive em CI após nova migration adiciona table na known-audit list — registrar via installer na mesma migration, não skippar), quando NÃO usar (tabelas mutáveis por design como `cron_run_state`, `wallets`, `notification_log`), rollback (apenas via nova migration com `DROP TRIGGER`; NUNCA apagar `audit_append_only_config` — é a fonte-de-verdade). Cross-refs L04-02/L06-05/L10-07/L10-09/L19-06. **Backwards compat**: 100% — 1 tabela nova, 4 funções novas, 7-8 triggers BEFORE (não interceptam INSERT, path principal de audit tables inalterado). `account_deletion_log` com mode `append_with_outcome` continua aceitando UPDATE em colunas mutáveis pelo trigger L04-02 (`hash_signature`, etc.) mas bloqueia colunas imutáveis — semântica preservada. Onda 1 → 97/179 (+1), High → 57/123 (+1), Lente 10 (CSO) → 3/N (após L10-07, L10-09).
- L10-07 — Edge Functions ganham zero-trust JWT iss/aud validation (commit `15601fa`, 2026-04-21): fecha o high de Lente 10 (CSO) sobre "`supabase/functions/_shared/auth.ts` valida JWT mas não valida `aud` claim específica. Qualquer JWT válido do Supabase acessa qualquer função". **Estado pré-fix**: `requireUser` chamava `verifyClient.auth.getUser(jwt)` (signature + exp check do Supabase) e pronto — nenhuma inspeção de `iss` ou `aud` no payload. Um token minted por outro projeto Supabase (tenant cross) ou com audience foreign (`aud=service-account`) passava. Não havia mecanismo per-route para pinar client surface (mobile vs portal vs platform_admin). **Fix em 5 camadas** (todas em `supabase/functions/_shared/auth.ts`, ~170 LOC alteradas): (a) **`AuthError` ganha campo `reason: string`** — machine-readable codes `missing_bearer`/`invalid_jwt`/`malformed_jwt`/`invalid_issuer`/`missing_audience`/`audience_mismatch`/`client_mismatch`/`auth_service_unavailable`/`missing_env` — observability splita 403s por root cause em Logflare. (b) **Helpers puros `base64UrlDecode` + `decodeJwtPayload`** — parser defensivo para o segundo segmento do JWT (`split('.')` len≠3 → `malformed_jwt`, JSON inválido → `malformed_jwt`). Runtime agnóstico (atob quando disponível, Buffer fallback). (c) **`assertClaimsShape(claims, { expectedIssuers, allowedAudiences })`** — valida `claims.iss` (string + presente na lista) e `claims.aud` (string OR string[] + intersecção com whitelist). Raise `AuthError(403, reason)` fail-closed com mensagem descritiva. Suporta `aud` array (OAuth clients que já minted tokens com múltiplas audiences válidas). (d) **`RequireUserOptions { allowedAudiences?, allowedClients?, skipClaimsCheck? }`** + defaults configuráveis via env `AUTH_JWT_EXPECTED_ISSUERS` (comma-separated; default `${SUPABASE_URL}/auth/v1`) e `AUTH_JWT_ALLOWED_AUDIENCES` (default `authenticated`). `allowedClients` exige header `x-omni-client ∈ { mobile, portal, staff, platform_admin, internal }`; ausência ou valor foreign → 403 `client_mismatch`. `skipClaimsCheck` é escape hatch apenas para debugging local — **CI rejeita se o flag ship em código de produção**. (e) **Integração em `requireUser`**: após `verifyClient.auth.getUser(jwt)` (mantido para enforcement de assinatura), decode do payload, `assertClaimsShape(...)` com opts (tightening per-route > env default > `authenticated`), pick + validate `x-omni-client`. O resultado expõe novos campos `claims` (raw decoded) e `client` (lower-cased string | null) para routes downstream tomarem decisões finer-grained sem re-decodificar. **Tests** `tools/test_l10_07_jwt_claims_validation.ts` (13 unit cases, ~230 LOC, Node puro sem docker-exec): shape-check do arquivo shared (0), `decodeJwtPayload` parses + rejects malformed (1-3), `assertClaimsShape` happy-path (4), rejeita foreign issuer (5), missing iss (6), missing aud (7), audience mismatch (8), accepts aud array intersect (9), rejects aud array sem intersect (10), multiple expected issuers (11), tight audience rejects default `authenticated` (12). 13/13 verdes. **CI** `tools/audit/check-jwt-claims-validation.ts` + `npm run audit:jwt-claims-validation` — scan de 64 `index.ts` sob `supabase/functions/` (exclui `_shared/`): (1) `_shared/auth.ts` deve conter 11 markers obrigatórios (`decodeJwtPayload`, `assertClaimsShape`, `AUTH_JWT_EXPECTED_ISSUERS`, `AUTH_JWT_ALLOWED_AUDIENCES`, `allowedAudiences`, `allowedClients`, `invalid_issuer`, `audience_mismatch`, `missing_audience`, `client_mismatch`, `x-omni-client`), (2) nenhuma função fora de `_shared/` chama `auth.getUser(` sem usar `requireUser`, (3) nenhuma função ship `skipClaimsCheck: true`. Output estruturado em falha para triagem rápida. **Runbook canônico** `docs/runbooks/JWT_ZERO_TRUST_RUNBOOK.md` (~180 linhas, 6 seções): why (3 gaps concretos: cross-tenant, missing client pinning, signature-only trust), what shipped (defaults + opt-in tightening per-route com code snippet), detection (tabela de sinais × ação), 4 playbooks (rollout de novo issuer staging→prod, tightening custody route, investigar 403 `invalid_issuer`, investigar 403 `client_mismatch`), rollback (per-route relax vs revert do shared helper), cross-refs L10-08/L10-09/L07-04. **Backwards compat**: todas as 43 Edge Functions que chamam `requireUser(req)` herdam automaticamente iss=${SUPABASE_URL}/auth/v1 + aud=authenticated — o happy-path do Supabase Auth continua passando sem alterações. Rotas que precisam tighten (custody-withdraw, platform-admin) podem fazê-lo incrementalmente via `allowedAudiences`/`allowedClients` (follow-up não-blocking por rota). Onda 1 → 96/179 (+1), High → 56/123 (+1), Lente 10 (CSO) → 2/N (após L10-09).
- L10-09 — DB foundation anti credential stuffing (commit `b2fb402`, 2026-04-21): fecha o high de Lente 10 (CSO) sobre "Supabase Auth faz rate-limit por IP mas não por email. Ataque distribuído testa mil emails × senha comum". **Estado pré-fix**: qualquer botnet com 1k IPs poderia testar 1k emails com senha `password123` e o rate-limit per-IP nunca disparava (cada IP fazia 1 request). Única defesa era a senha ser incomum o suficiente. **Fix em 7 camadas** (`supabase/migrations/20260421340000_l10_09_anti_credential_stuffing.sql`, ~620 LOC): (a) **`public.auth_login_attempts`** — PK (email_hash, window_start), `email_hash text CHECK (~ '^[0-9a-f]{64}$')` (força SHA-256 hex lowercase; rejeita raw email, NULL, uppercase, short); `attempts integer CHECK >= 0`; `last_ip inet NULL` (modo strict-privacy pode omitir); `locked_until timestamptz NULL`; `captcha_required_at timestamptz NULL`. Índice `(email_hash, window_start DESC)` + partial `(locked_until DESC) WHERE locked_until IS NOT NULL`. RLS ENABLE + FORCE; policy `service_role_rw` FOR ALL. (b) **`public.auth_login_throttle_config`** — singleton (id=1 via CHECK), 4 CHECK constraints: `fail_threshold_captcha ∈ [1,50]`, `fail_threshold_block ∈ [2,200]` AND `> captcha`, `window_seconds ∈ [60,86400]`, `block_seconds ∈ [60,86400]`. Defaults: captcha=3, block=10, window=900 (15 min), block=900 (15 min). Operator pode tunar sem ship de código via `UPDATE ... SET fail_threshold_captcha=2` — mudança instantânea via `STABLE` config read. RLS forced, service_role only. (c) **`fn_login_throttle_window_start(p_window_seconds)`** IMMUTABLE — alinha `now()` ao window fixed-size (bucket pattern). (d) **`fn_login_throttle_record_failure(email_hash, ip?)`** SECURITY DEFINER `search_path=public,pg_temp` — valida hash via regex `^[0-9a-f]{64}$` (22023 em inválido), le config singleton (P0010 se missing), computa window, `INSERT ... ON CONFLICT (email_hash, window_start) DO UPDATE SET attempts=attempts+1, last_ip=COALESCE(..., ...), last_attempt_at=now()`, SUM attempts across last window_seconds (sliding), marca `locked_until = now()+block_seconds` quando total >= block_threshold, marca `captcha_required_at = COALESCE(existing, now())` quando >= captcha_threshold. Retorna `jsonb_build_object('attempts', total, 'requires_captcha', bool, 'locked', bool, 'locked_until', ts, 'window_start', ts, 'window_seconds', int, 'fail_threshold_captcha', int, 'fail_threshold_block', int)` — edge function usa esse JSON inteiro como verdade da decisão. (e) **`fn_login_throttle_record_success(email_hash)`** SECURITY DEFINER — `DELETE FROM auth_login_attempts WHERE email_hash = p_email_hash` após auth 200; limpa tanto counter quanto captcha/lock state para user legítimo que errou ocasionalmente. (f) **`fn_login_throttle_probe(email_hash)`** STABLE SECURITY DEFINER — read-only mirror de `record_failure` sem mutação; usada pela UI para banner "attempts remaining" sem penalizar o request em si. (g) **`fn_login_throttle_cleanup()`** SECURITY DEFINER — `DELETE WHERE window_start < now() - (window_seconds*4) AND (locked_until IS NULL OR locked_until < now())` — safe para `pg_cron.schedule('auth-throttle-cleanup', '17 * * * *', ...)`. GET DIAGNOSTICS ROW_COUNT retorna int para observability. (h) **`fn_login_throttle_assert_shape()`** SECURITY DEFINER — array de `v_missing` acumula missing tables/functions (`to_regclass`/`to_regprocedure`), verifica `relrowsecurity AND relforcerowsecurity` em ambas as tabelas, rejeita `has_function_privilege('anon', ...)` em qualquer primitive; raise P0010 com lista separada por vírgula + HINT apontando para runbook. REVOKE PUBLIC/anon/authenticated + GRANT service_role em todos. **Self-test DO-block** executa 5 invariantes em-place (assert_shape passa, record_failure rejeita 'not-a-hash' com 22023, primeira falha retorna attempts=1 + requires_captcha=false, terceira falha retorna requires_captcha=true, record_success limpa counters). **Tests** `tools/test_l10_09_anti_credential_stuffing.ts` (21 cases, ~330 LOC, docker-exec psql): schema/DDL (RLS forced em ambas tabelas, email_hash CHECK shape presente, singleton CHECK rejeita id=2, 5 helpers SECURITY DEFINER registrados, service_role EXECUTE nos 4 expostos, anon/authenticated denied), argument validation (non-hex/NULL/uppercase/short rejeitados em record_failure/probe/record_success), behaviour (first=1/no-captcha/no-lock, third=captcha-required, tenth=locked+future locked_until, success deleta rows, probe não-mutates entre 3 calls consecutivas, last_ip `inet` stored via `host()`, cleanup deleta old rows mas mantém recent, config threshold=1 → captcha imediato após 1 falha, assert_shape no-op quando healthy). 21/21 verdes. **CI** `tools/audit/check-anti-credential-stuffing.ts` + `npm run audit:anti-credential-stuffing` — probe `fn_login_throttle_assert_shape()` existe, invoca, captura stderr com padrões `L10-09|auth_login|fn_login_throttle|P0010|HINT|RLS`. **Runbook canônico** `docs/runbooks/ANTI_CREDENTIAL_STUFFING_RUNBOOK.md` (~220 linhas, 7 seções): summary com tabela de defaults tuneáveis, **wiring contract da Edge Function `login-pre-check`** (3 passos: probe → auth.signInWithPassword → record outcome), detection/CI + ad-hoc probes (count counters + top 10 attacked hashes em 24h), cenários operacionais (4.1 unlock manual para user legítimo via `record_success` + audit em `portal_audit_log`; 4.2 tuning durante ataque — captcha=2, window=300; 4.3 hourly cron schedule), privacy notes (email sempre hashed, não-linkável a `auth.users`, defesa-in-depth throttla o guess não a identidade), rollback (DROP FUNCTION/TABLE idempotente), cross-refs L10-07/L10-08/L06-04/L17-06. **Edge Function `login-pre-check` é follow-up não-blocking** — esta entrega foca no DB foundation + CI; o wiring no mobile/portal vem em PR separada sob `supabase/functions/login-pre-check/`. Onda 1 → 95/179 (+1), High → 55/123 (+1), Lente 10 (CSO) → 1/N (primeiro fix da lente).
- L08-03 — `public.sessions` ganha 2 índices time-series canônicos (commit `b68e71d`, 2026-04-21): fecha o high de Lente 8 (CDO) sobre "portal indexa por `(user_id, start_time_ms DESC)` mas queries de dashboard CDO realtime tipo 'sessões da última hora em todos os grupos' fazem seq scan". **Fix em 4 camadas** (`supabase/migrations/20260421330000_l08_03_sessions_time_series_index.sql`, ~260 LOC): (a) **BRIN** `idx_sessions_start_time_brin ON public.sessions USING BRIN (start_time_ms) WITH (pages_per_range=32)` — canonical para colunas append-only/time-ordered; footprint ~8 KiB/1M rows (vs ~25 MiB btree), write-amplification near-zero (apenas summary update por range). Perfeito para dashboard range scans `WHERE start_time_ms BETWEEN $lo AND $hi` que hoje estão caindo em Seq Scan. (b) **Partial btree** `idx_sessions_status_start_time ON public.sessions (status, start_time_ms DESC) WHERE status >= 3` — complementa o BRIN quando a janela é narrow (< 100 rows "last finalized globally") e a predicate bound pelo invariante L08-04 (coherence CHECK) garante semântica `moving_ms > 0` nos hits. Column ordering `(status, start_time_ms DESC)` bate com `ORDER BY start_time_ms DESC LIMIT N`. (c) **`public.fn_sessions_has_time_series_indexes()`** STABLE SECURITY DEFINER `search_path=public,pg_catalog,pg_temp` — boolean; retorna true iff ambos os índices existem com access method correto (`pg_am.amname`) e partial predicate `status >= 3` preservado. REVOKE PUBLIC/anon/authenticated + GRANT service_role. (d) **`public.fn_sessions_assert_time_series_indexes()`** SECURITY DEFINER — raise `P0010` com mensagem distinta por índice (`L08-03: idx_sessions_start_time_brin missing or not a BRIN index...` ou `idx_sessions_status_start_time missing or not a btree partial index...`) + HINT pointing para runbook §4. **Self-test DO-block** valida 4 invariantes (BRIN access method, partial btree predicate casa `status>=3`, fn_assert no-op, fn_has returns true). **CI** `tools/audit/check-sessions-time-series-index.ts` + `npm run audit:sessions-time-series-index` — probe tabela `sessions` + função registrada, invoca `fn_sessions_assert_time_series_indexes()`, captura stderr com padrões `L08-03|idx_sessions_start_time_brin|idx_sessions_status_start_time|P0010|HINT`. No-op gracioso quando sandbox sem `public.sessions`. **Tests** `tools/test_l08_03_sessions_time_series_index.ts` (15 cases, docker-exec psql, ~260 LOC): schema/DDL (1-9: BRIN AM, BRIN def com `start_time_ms` + `pages_per_range=32`, partial btree AM + predicate `status>=3` + columns `(status, start_time_ms DESC)`, fn_has STABLE + SECURITY DEFINER, fn_assert SECURITY DEFINER, service_role EXECUTE, anon denied), behaviour (10-13: fn_has retorna true, fn_assert no-op, rename away BRIN → fn_has false + fn_assert raise com markers L08-03+idx_sessions_start_time_brin, rename away btree → análogo para idx_sessions_status_start_time), planner usage (14: partial btree considerado para `WHERE status>=3 AND start_time_ms BETWEEN...`; 15: fabricated table l08_03_test.t com 20k rows + BRIN → `BitmapIndexScan using t_brin` no EXPLAIN). 15/15 verdes. **Runbook canônico** `docs/runbooks/SESSIONS_TIME_SERIES_INDEX_RUNBOOK.md` (~220 linhas, 8 seções): summary, matriz "quando usar qual índice" (user feed → idx_sessions_user pré-existente; dashboard range scan → BRIN; last N finalized → partial btree; per-user finalized → idx_sessions_status pré-existente), tabela de call-sites (portal dashboards API, fn_compute_kpis_batch, anti-cheat sweep), invariant + detection queries (`fn_sessions_has_time_series_indexes` + ad-hoc query sobre `pg_indexes` com `pg_size_pretty(pg_relation_size(...))`), playbook de apply (4.1 Supabase CLI para DBs pequenos, 4.2 CONCURRENTLY out-of-band para > 5M rows com recipe `CREATE INDEX CONCURRENTLY IF NOT EXISTS` fora de transaction block + verificação `pg_index.indisvalid=true` + apply migration após para instalar helpers/CI, 4.3 rebuild via `REINDEX INDEX CONCURRENTLY`), tuning (`pages_per_range` trade-off + BRIN auto-summarise em PG 15+ + cron-scheduled `brin_summarize_new_values` em PG < 15), rollback (DROP INDEX + DROP FUNCTION idempotente), observability signals (`pg_stat_user_indexes.idx_scan` climbing + `idx_tup_fetch` baixo = BRIN pruning efetivo + slow-query log), cross-refs L08-04/L19-04/L19-08. **Backwards compat**: 100% aditivo — 2 índices novos com `CREATE INDEX IF NOT EXISTS`, 2 helpers novos, callers existentes (`idx_sessions_user`/`idx_sessions_status`/`idx_sessions_verified`) intactos. **Caveat documentado**: o `CREATE INDEX` non-concurrent dentro da migration é safe apenas em local/fresh DB; produção com > 5M rows deve seguir §4.2 do runbook para evitar AccessExclusiveLock prolongado em peak hours. Onda 1 → 94/179 (+1), High → 54/123 (+1), Lente 8 (CDO) → 4/N (após L08-04, L08-05, L08-07).
- L08-04 — `public.sessions` ganha CHECK constraint de coerência para sessões finalizadas (commit `ae57c8c`, 2026-04-21): fecha o high de Lente 8 (CDO) sobre "portal analisa sessions pelo `moving_ms` mas a coluna aceita NULL e 0 — queries com `total_distance_m / moving_ms` em `fn_compute_skill_bracket` ou dashboards de pace vão para infinito quando GPS bugar com `moving_ms=0` + `total_distance_m>0`". **Estado pré-fix**: `public.sessions` permitia livremente qualquer combinação de `status`/`total_distance_m`/`moving_ms`; o guardrail inline em `fn_compute_skill_bracket` (`CASE WHEN moving_ms > 0 THEN total_distance_m::float / moving_ms ELSE NULL END`) era pontual. Nada impedia `fn_finalize_session` ou integração Strava de gravar `(status=5, total_distance_m=15000, moving_ms=0)` — um invariante de domínio não enforced no schema. **Fix em 4 camadas** (`supabase/migrations/20260421320000_l08_04_sessions_coherence_check.sql`, ~200 LOC): (a) **`ALTER TABLE public.sessions ADD CONSTRAINT chk_sessions_coherence CHECK (status < 3 OR (total_distance_m = 0 AND moving_ms = 0) OR (total_distance_m >= 100 AND moving_ms >= 60000)) NOT VALID`** seguido de `ALTER TABLE ... VALIDATE CONSTRAINT chk_sessions_coherence` — `NOT VALID + VALIDATE` evita AccessExclusiveLock longo em produção (scan online, não bloqueia SELECT/UPDATE). Invariante: sessões "em andamento" (status<3) livres; "finalizadas" (status>=3) devem ter EITHER zero/zero (estado placeholder/cancel) OU ≥100m+≥60s (dataset publicável). Idempotente via lookup em `pg_constraint`. (b) **`public.fn_find_sessions_incoherent(p_limit int DEFAULT 100) RETURNS TABLE(session_id uuid, user_id uuid, status smallint, total_distance_m int, moving_ms bigint, reason text)`** STABLE SECURITY DEFINER — classifica offenders em 5 categorias narrativas (`gps_zero_moving_ms` = dist>0 + moving=0 é o GPS-bug clássico; `zero_distance_with_moving` = dist=0 + moving>0 é aparelho travado após start; `distance_below_100m` = ~warm-up deletável; `moving_below_60s` = tap acidental; `other_incoherent` = NULL em status/dist/moving) para o playbook de backfill (runbook §4). (c) **`public.fn_assert_sessions_coherence() RETURNS void`** SECURITY DEFINER — raise `P0010` com lista estruturada de até 10 samples (`session_id + reason`) quando detector retorna linhas. No-op silencioso quando limpo. REVOKE PUBLIC + GRANT service_role em ambas. (d) **Self-test DO-block** na migration valida 4 invariantes (constraint registrada + valid + helpers registrados + assert passa contra tabela). **Integration tests** `tools/test_l08_04_sessions_coherence_check.ts` (15 cases, ~350 LOC, docker-exec psql): schema/DDL (constraint + privileges), accepted shapes (status<3 + qualquer shape, status=5 + (0,0), status=5 + (150m,90s), status=5 boundary (100m,60000ms)), rejected shapes (status=5 + (15000m,0) → 23514, status=5 + (0,90000), status=5 + (99m,60000), status=5 + (100m,59999)), detector (retorna 0 após migration, permissions), assert (P0010 quando detectar, no-op quando limpo), UPDATE path (update-to-valid OK, update-to-invalid → 23514). 15/15 verdes. **CI guard** `tools/audit/check-sessions-coherence.ts` + `npm run audit:sessions-coherence` — invoca `fn_assert_sessions_coherence()` via docker-exec, captura stderr com padrão `P0010|chk_sessions_coherence|incoherent`. **Runbook canônico** `docs/runbooks/SESSIONS_COHERENCE_RUNBOOK.md` (~230 linhas, 6 seções): invariante + rationale (tabela de dano por classe de offender), CHECK expression explicada, playbook de backfill (A: `gps_zero_moving_ms` → `UPDATE moving_ms = duration_ms` se duration_ms existe, senão `DELETE` + log; B: `zero_distance_with_moving` → `DELETE` + log em `portal_audit_log action='session.cleanup.zero_distance'`; C: `distance_below_100m` → `DELETE` silencioso; D: `moving_below_60s` → `DELETE` silencioso), detection/CI (detector + assert + npm run), call-sites protegidos (`fn_finalize_session`, `strava-webhook/index.ts`, `verify-session/index.ts`), migration expansão futura (`status=4 cancelled` deveria ter shape próprio — follow-up L08-11), rollback (DROP CONSTRAINT idempotente, helpers preservados). Cross-refs L08-03/L08-05/L08-07. **Backwards compat**: 100% — scan pré-migration em DB local retornou 0 offenders (constraint aplicou cleanly). Se produção tiver violations, o `VALIDATE CONSTRAINT` vai falhar — runbook §4 instrui a rodar `fn_find_sessions_incoherent(10000)` e aplicar o playbook ANTES de re-rodar migration. Onda 1 → 93/179 (+1), High → 53/123 (+1), Lente 8 (CDO) → 3/N (após L08-05, L08-07).
- L08-05 — `v_user_progression` ganha filtro de atletas inativos (commit `bed5d0c`, 2026-04-21): fecha o high de Lente 8 (CDO) sobre "atletas que pararam há 1 ano continuam no ranking de 'atleta mais evoluído', distorcendo baselines". Estratégia forward-compatible: expande `v_user_progression` com 2 colunas no final (`last_session_at timestamptz`, `is_active_90d boolean`) sem reordenar/remover nenhuma coluna existente — consumidores atuais (`notify-rules` streak-nudge, `streaks_leaderboard_screen`, `staff_weekly_report_screen`) continuam funcionando sem alteração. Cria `v_user_progression_active_90d` como fonte canônica para rankings/baselines + `v_weekly_progress_active_90d` para analytics dashboards. Helper `fn_is_athlete_active_90d(uuid)` STABLE SECURITY DEFINER com `idx_sessions_user(user_id, start_time_ms DESC)` index-only scan. 14 integration tests cobrindo DDL, privilégios, 3 fixtures (active/inactive/empty), filter semantics, fn-vs-col parity, backward compat do SELECT mobile.
- L19-06 — `audit_logs.metadata` ganha GIN `jsonb_path_ops` + detector + assert + CI (commit `271c549`, 2026-04-21): fecha o high de Lente 19 sobre "Queries 'todos eventos do request_id X' fazem seq scan". Migração defensiva (só aplica se tabela existir com coluna `metadata jsonb`), `jsonb_path_ops` em vez de `jsonb_ops` (~30% menor, cobre os únicos operadores usados: `@>`/`@?`/`@@`). Helpers `fn_audit_logs_has_metadata_gin()` (STABLE SECURITY DEFINER retornando bool) e `fn_audit_logs_assert_metadata_gin()` (raise P0010 quando tabela+coluna existem sem o índice). CI `npm run audit:audit-logs-gin` (no-op em sandboxes). 14 integration tests validam DDL + privilégios + sandbox no-op + fabricated audit_logs + BitmapIndexScan no EXPLAIN + idempotência. Playbook CONCURRENTLY out-of-band documentado no runbook `AUDIT_LOGS_METADATA_GIN_RUNBOOK.md` para bases > 10M linhas onde AccessExclusive lock é inaceitável.
- L19-08 — CHECK constraint naming convention forward-only (commit `44cd970`, 2026-04-21): convenção aceita `<table>_<col>_check` (PG default) **OU** `chk_<table>_<rule>` (ad-hoc explícito). Renames idempotentes de 4 constraints sem prefixo (`different_groups`, `swap_different_groups`, `coin_ledger_reason_length_guard`, `coin_ledger_reason_pii_guard`) + detector `fn_find_nonstandard_check_constraints` + assert wrapper `fn_assert_check_constraints_standardized` (raise P0010) + CI `npm run audit:constraint-naming` (16 tabelas financeiras) + 16 integration tests + runbook `DB_CHECK_CONSTRAINT_NAMING_RUNBOOK.md`. Decisão: mantemos o padrão auto-gerado aceito para não quebrar ~25 migrations históricas que fazem `DROP CONSTRAINT IF EXISTS <table>_<col>_check`.
- L19-04 — `coin_ledger` ganha detector + assert de índices duplicados + CI gate (commit `d5bcc54`, 2026-04-21): fecha o high de Lente 19 (DBA) sobre "Migration 2026-02-18 cria `idx_ledger_user`; migration 2026-03-08 cria `idx_coin_ledger_user_created`. Nomenclatura inconsistente; provavelmente ambos persistem." **Estado pré-fix**: após investigação em ambiente local, verificou-se que o `DROP TABLE coin_ledger_monolithic CASCADE` em 20260417200000 (L19-01 partition swap) já havia derrubado `idx_coin_ledger_user_created` e `idx_coin_ledger_ref_reason`. Mas o problema sistêmico — **ausência de detector de duplicatas + convenção de naming frouxa (mistura `idx_coin_ledger_*` de 2026-03-08 com `idx_ledger_*` de 2026-02-18/2026-04-17)** — permanecia aberto. Cada índice extra em `coin_ledger` custa 5-20% WAL por INSERT + 15% storage por partição, então mesmo que os phantoms específicos tenham sido limpos, um próximo contribuidor que adicionar `idx_coin_ledger_X` sobre `(user_id, created_at)` com semântica idêntica ao `idx_ledger_user` passaria review sem alarme. **Fix em 7 camadas, migration aditiva pura** (`supabase/migrations/20260421280000_l19_04_dedupe_ledger_indexes.sql`, ~230 LOC): (a) **DROP INDEX IF EXISTS** defensivo dos 4 phantoms conhecidos (`idx_coin_ledger_user_created`, `idx_coin_ledger_ref_reason`, `idx_ledger_user_monolithic`, `idx_ledger_issuer_monolithic`) — idempotente em envs limpos (todos no-op via NOTICE `does not exist, skipping`), elimina drift em envs antigos pré-L19-01. (b) **`public.fn_find_duplicate_indexes(p_schema text DEFAULT 'public', p_table text DEFAULT NULL) RETURNS TABLE(schemaname, tablename, redundant_index, canonical_index, kind, reason)`** STABLE SECURITY DEFINER `search_path=public,pg_catalog,pg_temp` — detecta duas formas de redundância em btree plain (não UNIQUE, não PK, não expression-based): `exact_duplicate` (mesmo key_sig com direção ASC/DESC preservada via `indoption[k-1] & 1`, mesmo INCLUDE sig, mesmo WHERE predicate; canônico = lex menor) e `prefix_overlap` (key_sig de A é prefixo estrito de B, com INCLUDE/WHERE iguais; A redundante). Deliberadamente ignora UNIQUE (enforcement tem valor próprio) e expression-based (semântica opaca para comparação string-based). (c) **`public.fn_assert_no_duplicate_indexes(p_schemas text[] DEFAULT ARRAY['public'], p_tables text[] DEFAULT NULL) RETURNS boolean`** STABLE SECURITY DEFINER — wrapper que raise `P0010` com lista estruturada (schema.table: redundant vs canonical, kind, reason) quando encontra duplicatas; retorna true quando limpo. Validação `p_schemas NULL/empty → 22023`. HINT com ponteiro para runbook + sugestão de `DROP INDEX CONCURRENTLY`. REVOKE PUBLIC/anon/authenticated + GRANT service_role. (d) **Self-test DO-block** valida 4 fases em-place: ambas funções registradas com SECURITY DEFINER, coin_ledger limpo retorna 0 duplicatas, fabricação sintética em schema temp `l19_04_test` (CREATE INDEX t_a_idx + t_a_b_idx) detecta 1 prefix_overlap, assert wrapper raise P0010 com a duplicata fabricada. Cleanup via `DROP SCHEMA l19_04_test CASCADE`. (e) **Integration tests** `tools/test_l19_04_duplicate_indexes.ts` (14 cases docker-exec, ~330 LOC): schema/DDL (STABLE + SECURITY DEFINER + service_role EXECUTE, anon NÃO EXECUTE; phantoms ausentes), behaviour (5 coin_ledger limpo 0 dups, 6 prefix_overlap detected com canonical=idx_a_b redundant=idx_a, 7 exact_duplicate canonical lex menor, 8 UNIQUE/PK não flaggados, 9 WHERE predicates diferentes → 0, 10 INCLUDE cols diferentes → 0, 11 same WHERE+INCLUDE+cols → exact_duplicate), argument validation (12 NULL schemas → 22023, 13 P0010 raise com duplicatas, 14 tables[] scope filtra corretamente — `u` raises, `t` não). 14/14 verdes. (f) **CI guard** `tools/audit/check-duplicate-indexes.ts` + `npm run audit:duplicate-indexes` — probe via `to_regprocedure` (não `to_regproc` que ignora tipos array), invoke `fn_assert_no_duplicate_indexes` contra 16 tabelas financeiras críticas (`coin_ledger`, `coin_ledger_idempotency`, `clearing_settlements`, `clearing_events`, `custody_deposits`, `custody_withdrawals`, `custody_accounts`, `platform_revenue`, `billing_purchases`, `billing_auto_topup_settings`, `wallets`, `xp_transactions`, `sessions`, `audit_logs`, `portal_audit_log`, `notification_log`). Output estruturado com filtro por padrões (`P0010|redundante|prefix_overlap|exact_duplicate|HINT`) quando falha. (g) **Runbook canônico** `docs/runbooks/LEDGER_INDEX_NAMING_RUNBOOK.md` (~220 linhas, 8 seções): modelo mental (cada índice custa 5-20% WAL + 15% storage + autovacuum blocking), convenção `idx_<core>_<cols>[_<predicate_tag>]` com tabela ❌ (`idx_coin_ledger_user_created`) vs ✅ (`idx_ledger_user`), detecção ad-hoc + varredura global + CI assert, 4 cenários operacionais (A: PR adiciona índice flaggado — merge vs diferenciar vs rejeitar; B: remover legacy em prod via `DROP INDEX CONCURRENTLY` **FORA de migration transacional** — pseudo-migration ou manual documentado em `portal_audit_log action='dba.index_drop'`; C: índice canônico faltando pós-restore — investigar por partição e recriar local a local; D: adicionar novo índice com checklist antes/depois), tunáveis (p_schemas scope, UNIQUE/expression opt-out por design), rollback (DROP FUNCTION seguros; phantoms NÃO restaurados), observability signals. Cross-refs L19-01/08 + L03-02 + L17-01. **Escopo deliberadamente excluído**: CREATE INDEX CONCURRENTLY (não cabe em migration transacional — runbook §4.B documenta fluxo manual), análise de `pg_stat_user_indexes` para indexes nunca usados (L19-07 deferido cuidará per-index baseado em telemetria real), naming enforcement via regex em migrations (convenção prescritiva fica em runbook humano — lint automático criaria falsos positivos). **Backwards compat**: 100% — migration aditiva pura (2 funções novas, 4 DROPs idempotentes com IF EXISTS). Onda 1 → 89/179 (+1), High → 52/123 (+1), Lente 19 (DBA) → 4/N (após L19-01/02/03/05).
- L12-08 — clearing-cron ganha cutoff determinístico TZ-aware + reagendamento para 00:15 BRT (commit `7f52a25`, 2026-04-21): fecha o high de Lente 12 (Cron/Scheduler) sobre "clearing-cron em 02:00 consolida D-1 antes de fim do dia BRT — usuários BR veem burn de 23:55 sumir do relatório". **Estado pré-fix**: `clearing-cron` agendado `0 2 * * *` (02:00 UTC = 23:00 BRT, i.e., *antes* da meia-noite BRT) + edge function hipotética usava `now()` como limite superior implícito (jitter de minutos do pg_cron + latência da chamada Edge Function = cutoff não-determinístico entre runs). Um burn feito às 23:55 BRT (02:55 UTC do dia seguinte) entrava na janela do "dia BRT errado" do ponto de vista do tesoureiro. Sem TZ-awareness, cron rodava em UTC mas usuário mentaliza dia civil em BRT. **Fix em 4 camadas** (`supabase/migrations/20260421270000_l12_08_clearing_cron_deterministic_cutoff.sql`, ~230 LOC): (a) **`public.fn_clearing_cutoff_utc(p_timezone text DEFAULT 'America/Sao_Paulo', p_as_of timestamptz DEFAULT NULL) RETURNS timestamptz`** STABLE SECURITY DEFINER — retorna `(date_trunc('day', (COALESCE(p_as_of, now())) AT TIME ZONE p_timezone)) AT TIME ZONE p_timezone`, o início de hoje no fuso dado expresso em UTC. Validação via `fn_is_valid_timezone` (reusa L12-07 helper — NULL/empty/IANA inválido → 22023). `p_as_of` nullable permite replay de dia específico (forensics determinístico). REVOKE PUBLIC + GRANT service_role. (b) **Reschedule cron** `0 2 * * *` → `15 3 * * *` (03:15 UTC = 00:15 BRT — 15 min pós meia-noite BRT, aligns consolidação ao dia civil BR; +15 min offset evita thundering herd 03:00 UTC discutido em L12-02 onde `reconcile-wallets-daily` roda). `cron.unschedule('clearing-cron')` + `cron.schedule('clearing-cron', '15 3 * * *', 'SELECT public.fn_invoke_clearing_cron_safe();')` idempotente. (c) **Rewrite de `fn_invoke_clearing_cron_safe`** (L06-05 wrapper preservado, apenas enriquece o payload): pré-computa `v_cutoff_utc := fn_clearing_cutoff_utc('America/Sao_Paulo')` no início do invoke, monta body `jsonb_build_object('cutoff_utc', v_cutoff_utc, 'timezone', 'America/Sao_Paulo', 'run_kind', 'daily_aggregate')` — contrato para a futura Edge Function (que deve usar `req.body.cutoff_utc` como limite superior estrito, NÃO `new Date().toISOString()`). Passa body para `fn_invoke_edge_with_retry` (retries exponenciais L06-05). Persiste `cutoff_utc` + `timezone` em `cron_run_state.last_meta` via merge jsonb em mark_completed/mark_failed — ops responde "qual janela fechou a run de ontem?" em um único SELECT em `cron_run_state.last_meta->>'cutoff_utc'`. Shape de retorno (void) inalterado. `IF EXISTS` gate em `cron_run_state` mantido para sandbox. (d) **Self-test DO-block** valida 5 invariantes: (i) helper registrado, (ii) cutoff para 2026-04-21 17:00 UTC em BRT == 2026-04-21 03:00 UTC (início do dia 21 em BRT é 00:00 BRT = 03:00 UTC), (iii) cutoff para 2026-04-21 02:00 UTC em BRT == 2026-04-20 03:00 UTC (02:00 UTC é ainda dia 20 em BRT — demonstra correção do off-by-one), (iv) cutoff para TZ='UTC' retorna 00:00 UTC sem shift, (v) TZ inválido/NULL → 22023. **Tests** `tools/test_l12_08_clearing_cron_deterministic_cutoff.ts` (14 cases, ~400 LOC, docker-exec psql): schema/DDL (função STABLE + SECURITY DEFINER + service_role EXECUTE + cron schedule correto), behaviour (BRT cutoff para 17:00 UTC, BRT cutoff para 02:00 UTC → dia anterior, UTC tz → UTC midnight, default `p_as_of=NULL` usa now() ≤ now(), determinismo entre duas chamadas consecutivas na mesma run), argument validation (NULL/empty/invalid IANA → 22023, p_as_of NULL OK), wrapper existente e é SECURITY DEFINER. 14/14 verdes. **Runbook canônico** `docs/runbooks/CLEARING_CRON_CUTOFF_RUNBOOK.md` (NEW, ~220 linhas, 7 seções): contrato do aggregator (código SQL canônico citando `fn_clearing_cutoff_utc`), dashboard queries (last cutoff + retry trail + pending-prize inventory), shape saudável (03:15 UTC diário, duração <60s, cutoff_utc == start-of-BRT-day, `last_status=completed` >95%), cenário A (burn sexta-à-noite BRT "sumiu do relatório" — 3 passos para entender se é pré/pós cutoff + qual próxima run consolidará), cenário B (run falhou — `cron_run_state.last_error` + retry audit + rerun manual), cenário C (replay de dia específico via dois `fn_clearing_cutoff_utc(..., p_as_of=...)` manuais para window determinístico), cenário D (mudança de TZ mid-run — NÃO fazer, migration-only change), tunables (schedule, TZ, L06-05 retries, L12-04 SLA), rollback (SQL pronto para voltar para 02:00 UTC), observability signals. Cross-refs L12-01/02/03/04/06/07 + L06-05 + CLEARING_STUCK. **Escopo deliberadamente excluído**: a Edge Function `clearing-cron` em si (consumer de `{cutoff_utc, timezone, run_kind}`) não existe ainda no repo; este PR entrega o contrato + cutoff infra. `fn_settle_clearing_batch_safe` (L02-10) usa janela rolling de 168h e NÃO foi tocada. **Backwards compat**: 100% — migration aditiva (1 função nova, 1 rewrite preservando signature, 1 reschedule idempotente), sandboxes sem pg_cron/http/cron_run_state continuam funcionando via gates `IF EXISTS`. Onda 1 → 88/179 (+1), High → 51/123 (+1), Lente 12 → 9/N (após L12-01/02/03/04/05/06/07/09).
- L12-07 + L07-06 — onboarding-nudge passa a respeitar timezone per-user (commit `4f14773`, 2026-04-21): fecha o high de Lente 12 (Cron/Scheduler) sobre "pg_cron roda `onboarding-nudge-daily` em 10:00 UTC = 07:00 BRT — cedo demais para BR" E o high de Lente 7 (CXO) sobre "`profiles` não tem coluna `timezone` — impossível formatar server-side". **Fix em 4 layers**: (a) **Schema** em `public.profiles`: `timezone text NOT NULL DEFAULT 'America/Sao_Paulo'` com CHECK `profiles_timezone_valid` via `fn_is_valid_timezone(text)` (rejeita IANA inválidos como `'Mars/Olympus'`/`'America/Sao Paulo'` com espaço/NULL/empty via `now() AT TIME ZONE ... EXCEPTION`) + `notification_hour_local smallint NOT NULL DEFAULT 9 CHECK (0..23)` (hora preferida para pushes daily). (b) **Helpers SQL**: `fn_is_valid_timezone(text)` IMMUTABLE (usado na CHECK e via RPC), `fn_user_local_hour(uuid)` STABLE SECURITY DEFINER `search_path=public,pg_temp` (retorna `EXTRACT(HOUR FROM now() AT TIME ZONE tz)::smallint`, fallback defensivo para Sao_Paulo quando TZ corrompido bypassa CHECK; valida NULL → 22023), `fn_should_send_nudge_now(uuid, preferred?)` STABLE SECURITY DEFINER (boolean = TRUE iff hora local == preferida; valida hora 0..23 → 22023). REVOKE PUBLIC + GRANT service_role nas SECURITY DEFINER. (c) **Cron reschedule**: `cron.unschedule('onboarding-nudge-daily')` + `cron.schedule('onboarding-nudge-hourly', '0 * * * *', 'SELECT public.fn_invoke_onboarding_nudge_safe();')` — wrapper L06-05 preservado, apenas nome/frequência mudam. Insert em `cron_run_state` via `ON CONFLICT DO NOTHING`. Idempotente (unschedule both legacy + current nomes). (d) **Edge Function `onboarding-nudge/index.ts`** TZ-aware: SELECT inclui `timezone` + `notification_hour_local` (com fallback automático para schema antigo sem colunas), loop computa `currentHourInTimezone(tz)` via `Intl.DateTimeFormat(..., { timeZone })` e skipa quando `localHour !== userPrefHour`, incrementa `skipped_off_hour` no response body para observabilidade. **L12-09 dedup (UNIQUE user_id+rule+context_id com context_id='d<N>') garante at-most-once per user per day mesmo com 24 ticks/dia**. **Tests**: `tools/test_l12_07_onboarding_nudge_timezone.ts` (18 cases docker-exec psql): schema/DDL (colunas + 2 CHECKs + IMMUTABLE/STABLE + SECURITY DEFINER + service_role grants + cron hourly), `fn_is_valid_timezone` (aceita IANA canônicos, rejeita typos/NULL/empty), argument validation (NULL user → 22023, hour=24/-1 → 22023), behaviour (scalar fallback para user desconhecido, CHECK em UPDATE com TZ/hour inválido, consistência hora entre calls). 18/18 verdes. Self-test DO-block no fim da migration valida 7 invariantes em-place. **Runbook canônico** `docs/runbooks/ONBOARDING_NUDGE_TIMEZONE_RUNBOOK.md` (~220 linhas, 8 seções): summary, dashboard queries (cron_run_state, retry audit, TZ distribution, hora histogram), shape saudável (23/24 invocações near-no-op é esperado), cenário A (usuário não recebeu nudge — checar D0-D7 window + preferred hour + notification_log), cenário B (troca de hora preferida — UPDATE simples, CHECK cuida da validação), cenário C (storm hourly noise — restringir schedule ou shardar por TZ bucket), cenário D (timezone NULL histórico — impossível pós-migration), interações (notify-rules inalterado, L12-08 vai usar mesmo padrão, sessions.start_time_ms consumer side é follow-up), tunables, rollback (SQL pronto para voltar para daily 10:00 UTC), observability signals. Cross-refs L12-08/L12-09/L06-05/L12-03. **L07-06 co-fechado**: a coluna `profiles.timezone` proposta pelo finding é exatamente o que esta migration entrega, habilitando formatação server-side de datas em `sessions.start_time_ms`, challenge deadlines, etc. (consumer side portal/mobile fica como follow-up não-blocking não-audit). **Backwards compat**: 100% — migration aditiva (2 colunas com DEFAULT, 2 CHECKs novos, 3 funções novas, cron reschedule idempotente), Edge Function tem fallback para schema antigo sem colunas, sem breaking change em callers existentes. Onda 1 → 87/179 (+2), High → 50/123 (+2), Lente 12 → 8/N (após L12-01/02/03/04/05/06/09), Lente 7 → 1/N (primeiro fix da lente).
- L12-06 — `fn_archive_old_sessions` ganha chunked COMMITs via Edge Function orchestrator (commit `d8b0e10`, 2026-04-21): fecha o high de Lente 12 (Cron/Scheduler) sobre "archive-old-sessions roda como função pesada sem batch — uma única transação segura locks em `public.sessions` e bloqueia autovacuum até retornar". **Estado pré-fix**: a versão L19-02 (`20260419110000_l19_archive_via_partition_detach.sql`) já fazia chunked `DELETE ... RETURNING` para `sessions_archive`, mas TODOS os chunks aconteciam dentro da **mesma transação** PL/pgSQL. Consequências: (a) locks acumulavam por 30+ minutos em datasets grandes, (b) autovacuum em `public.sessions` era bloqueado até o fim do loop (dead tuples acumulavam por toda a janela de archive), (c) mid-run kill (upgrade Supabase, connection drop) perdia progresso inteiro, (d) `cron_run_state.last_finished_at` (L12-03) só atualizava no fim, impedindo visibilidade por-chunk no painel. **Fix em 5 camadas, migration aditiva + Edge Function nova + integração com L06-05 retry**: (a) **Novo primitive SQL `public.fn_archive_sessions_chunk(batch_size, cutoff_months) RETURNS jsonb`** SECURITY DEFINER lock_timeout=2s — processa EXATAMENTE um chunk via CTE `WITH candidates AS (DELETE ... RETURNING *)` + `INSERT INTO sessions_archive`, devolve `jsonb_build_object('moved_count', ..., 'more_pending', ..., 'duration_ms', ..., 'cutoff_ms', ...)`. Argumentos validados (`batch_size ∈ [1,10000]`, `cutoff_months ∈ [1,120]`) com `22023`. Probe `SELECT 1 FROM sessions WHERE ... LIMIT 1` após DELETE para popular `more_pending` sem full count. (b) **Helper `public.fn_archive_sessions_pending_count(cutoff_months) RETURNS bigint`** STABLE SECURITY DEFINER — backlog visibility sem bloquear `sessions`. REVOKE FROM PUBLIC + GRANT service_role em ambas. (c) **Edge Function `supabase/functions/archive-old-sessions/index.ts`** — orquestra loop de RPCs contra `fn_archive_sessions_chunk`. **Cada chamada via `db.rpc(...)` é uma transação Postgres separada — COMMIT entre chunks garantido**, autovacuum pode intervir, kill preserva progresso. Parâmetros via POST body: `batch_size` (500), `cutoff_months` (6), `max_batches` (40), `max_duration_ms` (480000, teto 540000 < limite Supabase 600000). Auth via `SERVICE_ROLE_KEY`. Termina com `terminated_by ∈ {no_more_pending, max_batches, max_duration}`. (d) **Shim de backward compat `public.fn_archive_old_sessions() RETURNS integer`** reescrito para loop interno sobre `fn_archive_sessions_chunk` (batches 250, cutoff 6m), preservando assinatura legacy para callers diretos (psql operacional, scripts). Todas as iterações dentro da mesma outer transaction (semântica antiga) — COMMITs entre chunks só acontecem via Edge Function. (e) **Novo cron entry point `public.fn_invoke_archive_sessions_safe()`** integra com `cron_run_state` (L12-03): `fn_cron_mark_started` → tenta `fn_invoke_edge_with_retry('archive-old-sessions', 'archive-old-sessions', body, max_attempts=2, backoff_base=10)` (L06-05) → em sucesso `fn_cron_mark_completed(mode='edge_function')`; em skipped/failed, fallback SQL shim `fn_archive_old_sessions()` + mark_completed com `mode='sql_fallback'`. `cron.unschedule('archive-old-sessions')` + `cron.schedule('archive-old-sessions', '45 3 * * 0', 'SELECT public.fn_invoke_archive_sessions_safe();')` — janela Sun 03:45 UTC preservada (L12-02). Insert condicional em `cron_run_state` com `IF EXISTS` para sandbox sem L12-03. Self-test DO-block valida 4 invariantes (`fn_archive_sessions_chunk` registrada + SECURITY DEFINER, pending_count STABLE, shim retorna integer, safe wrapper existe). **Tests** `tools/test_l12_06_archive_sessions_chunked.ts` (13 cases, ~400 LOC, `docker exec + psql`): schema/DDL (assinaturas + SECURITY DEFINER + STABLE + shim presente + service_role grants + cron job apontando para safe wrapper), argument validation (batch_size 0/20000 → 22023, cutoff_months 0/999 → 22023), behaviour (chunk em janela vazia retorna jsonb válido com `moved_count=0`/`more_pending=false`, pending_count bigint não-negativo, shim integer ≥0). 13/13 verdes. **Runbook canônico** `docs/runbooks/ARCHIVE_OLD_SESSIONS_RUNBOOK.md` (~280 linhas, 7 seções): dashboard queries (cron_run_state.last_meta, pending_count, retry attempts), shape saudável (`terminated_by='no_more_pending'` em steady state, <10min duration, http_status=200 primeira tentativa, autovacuum catchup em ~24h pós-Sunday), cenário A (backlog crescendo — checar `terminated_by`, ad-hoc run com curl + body estendido `max_batches=200/max_duration=540000`, follow-up se >1M → partitioning L19-01 pattern), cenário B (run falhou — `last_error` + cron_edge_retry_attempts + detecção de `sql_fallback`), cenário C (operador ad-hoc via curl ou psql), cenário D (aborting runaway — pg_stat_activity + pg_cancel_backend, chunks já commitados permanecem), tunables (batch_size/cutoff/max_batches/max_duration_ms/retry params/schedule), rollback (unschedule + reschedule para shim direto), observability signals. Cross-refs L12-01/02/03/04 + L06-05 + L19-01/02. **Backwards compat**: 100% — migration aditiva pura (2 funções novas + 1 safe wrapper novo + 1 shim reescrito preservando RETURNS integer), Edge Function nova, cron reschedule idempotente, sem breaking change. Shim preserva legacy callers. Fallback SQL garante que jobs rodam mesmo em sandbox sem `http`/`pg_net`. Onda 1 → 85/179, High → 48/123 (+1), Lente 12 (Cron/Scheduler) → 7/N (após L12-01/02/03/04/05/09).
- L12-09 — `public.notification_log` ganha dedup idempotente UNIQUE(user_id, rule, context_id) + 2 helpers SECURITY DEFINER + claim-first pattern em `notify-rules`/`onboarding-nudge` (commit `48b2886`, 2026-04-21): fecha o high de Lente 12 (Cron/Scheduler) sobre "lifecycle-cron dispara notificações a cada 5 min sem tabela `sent_notifications` dedicada". **Estado pré-fix**: `notify-rules` (16 rules) e `onboarding-nudge` (D0-D7) tinham dedup via `wasRecentlyNotified()` — `SELECT ... WHERE sent_at > now() - 12h` seguido de `dispatchPush()` e só então `INSERT notification_log`. Entre o SELECT e o INSERT abria window de 5-30s (latência APNs/FCM + fetch `/send-push`); dois cron ticks overlapping (ou cron + operator trigger manual) podiam ambos passar pelo SELECT (zero rows) e ambos dispatchar — resultado: mesmo push 2× para o mesmo usuário. O `notification_log` original tinha apenas `idx_notification_log_dedup(user_id, rule, context_id, sent_at DESC)` (índice não-unique — permitia as duplicatas sem estourar nada). **Fix em 5 camadas** (`supabase/migrations/20260421240000_l12_09_notification_idempotency.sql`, ~350 LOC): (a) **ROW_NUMBER() cleanup** no início da migration — `row_number() OVER (PARTITION BY user_id, rule, context_id ORDER BY sent_at ASC, id ASC) > 1 → DELETE`; tiebreak sent_at+id (id é UUID, `MIN(id)` não existe em UUID). Relatório `rows_deleted` para forensics de re-apply. (b) **`UNIQUE (user_id, rule, context_id)`** em `public.notification_log` via `notification_log_dedup_unique` CONSTRAINT (idempotente via `pg_constraint` lookup). Any legacy raw INSERT que duplicar agora pega `23505 unique_violation` ao invés de side-effect silencioso. (c) **`fn_try_claim_notification(user_id, rule, context_id) RETURNS boolean`** SECURITY DEFINER `search_path=public,pg_temp` — `INSERT ... ON CONFLICT ON CONSTRAINT notification_log_dedup_unique DO NOTHING` + `GET DIAGNOSTICS v_inserted = ROW_COUNT; RETURN v_inserted = 1`. **Esta é a race-safe primitive** — nem 10 workers chamando simultaneamente conseguem vencer a constraint duas vezes. Validação de args: NULL user → `22023 INVALID_USER_ID`, empty rule → `22023 INVALID_RULE`. REVOKE FROM PUBLIC + GRANT service_role. (d) **`fn_release_notification(user_id, rule, context_id, max_age_seconds)`** SECURITY DEFINER — bounded rollback quando dispatch falha POR DEFEITO DEFAULT MAX_AGE=60s. `DELETE ... WHERE sent_at > now() - make_interval(secs => p_max_age_seconds)` — **row mais velha que a janela NUNCA é deletada** (evita clobber de notificação legítima antiga por caller bugado). `p_max_age_seconds` CHECK `1..300` (5-min hard cap). Retorna boolean. (e) **Refactor `notify-rules/index.ts`** (16 rules): `wasRecentlyNotified → tryClaimNotification`, `logNotification → removido (INSERT já aconteceu no claim)`, adicionado `releaseNotificationClaim` no path `if (!ok)`. Cada rule agora: `const claimed = await tryClaimNotification(...); if (!claimed) continue; const ok = await dispatchPush(...); if (!ok) await releaseNotificationClaim(...);` — claim-first pattern limpo. **Context-id conventions atualizadas**: `low_credits_alert` passa de `low_credits:${groupId}` (dedup forever após primeiro fire) → `low_credits:${groupId}:${YYYY-MM-DD}` (1 alerta/grupo/dia); `challenge_expiring` passa de `${challengeId}` → `${challengeId}:${YYYY-MM-DD}` (permite re-fire se challenge for estendido). `streak_at_risk`, `inactivity_nudge`, `onboarding_nudge` já incluíam date bucket — compatíveis com a nova semântica. **Fallback para RPC missing**: ambos `tryClaimNotification` e `onboarding-nudge` têm try/catch que cai na pattern legacy (`SELECT ... GTE sent_at since-12h` + INSERT com 23505 detection) quando a RPC ainda não está deployada (dev DB pré-migration). Graceful degradation ao invés de hard-fail. **Tests**: `tools/test_l12_09_notification_idempotency.ts` (12 cases docker-exec, ~320 LOC): schema/DDL (UNIQUE present, 2 functions SECURITY DEFINER registradas, service_role EXECUTE), argument validation (NULL user/empty rule/out-of-range max_age → 22023), behaviour (first claim TRUE + insere, duplicate claim FALSE + não-insere, release ≤60s deleta, release ≥10min com bound 30s FALSE, raw duplicate INSERT → 23505) — 12/12 verdes (~3s). Self-test DO block na própria migration seeda `auth.users` ephemeral + valida 7 invariantes + cleanup em EXCEPTION block. Tests existentes L06-05 não-regredidos (18/18 ainda verdes). **Runbook canônico** `docs/runbooks/NOTIFICATION_IDEMPOTENCY_RUNBOOK.md` (NEW, ~250 linhas, 7 seções): summary/pattern (claim-first code snippet canônico), operação normal (queries de health — volume por rule 24h, MAX(sent_at), growth por rule all-time), 3 cenários operacionais (A: user reporta push duplicado — query por user_id últimas 24h, distinguir constraint-off vs send-push duplication vs dispatch crash; B: push esperado não chegou — verificar se claim existe sem dispatch → manual `fn_release_notification`; C: cleanup histórico — ROW_NUMBER() query). Tabela de context-id conventions por rule (17 rules × formato × dedup granularity). Seção "adicionando nova rule" (event-driven vs recurring → sempre incluir time bucket se recurring). Rollback. Observability signals. Cross-refs L06-04/L06-05/L12-03/L15-04. **Backwards compat**: 100% — migration aditiva (1 constraint novo, 2 funções novas, 0 columns dropped, RLS policies intocadas), Edge Function tem fallback para RPC missing, partial rollback seguro (DROP CONSTRAINT libera tudo e behaviour regressa para pre-fix sem outras quebras). **Validação local**: `docker exec psql` aplica migration cleanly (`[L12-09.selftest] OK — all invariants pass`), 12/12 tests verdes, `npm run audit:verify` 348/348. Onda 1 → 84/179, High → 47/123 (+1), Lente 12 (Cron/Scheduler) → 6/N (após L12-01, L12-02, L12-03, L12-04, L12-05).
- L06-05 — Edge Functions invocadas por `pg_cron` ganham retry wrapper com exponential backoff (commit `86a3e03`, 2026-04-21): fecha o high de Lente 6 (COO) sobre "`20260221000001_auto_topup_cron.sql:56` usa `pg_net` para chamar Edge Function. Se falhar (timeout, 503), não há retry automático; o cron espera o próximo ciclo (1 hora)". **Impacto pré-fix**: 6 cron jobs (auto-topup-hourly, lifecycle-cron, clearing-cron, eval-verification-cron, onboarding-nudge-daily, reconcile-wallets-daily) faziam fire-and-forget via `net.http_post`/`extensions.http` sem retry — qualquer 503/DNS-blip/cold-start esgotava a janela inteira. **`reconcile-wallets-daily` era o pior caso**: falha silenciosa = 24h de drift invisível entre `wallets.balance_coins` e `SUM(coin_ledger.delta_coins)` (regressão do L06-03 + L08-07). **Fix em 6 camadas** (`supabase/migrations/20260421230000_l06_05_edge_retry_wrapper.sql`, ~790 LOC): (a) **`public.cron_edge_retry_attempts`** — append-only audit table com 1 linha por tentativa HTTP (sucesso OU falha), colunas `job_name/endpoint/attempt/max_attempts/http_status/error/started_at/completed_at/meta`; RLS forçado, service_role only; índices forenses `(job_name, started_at DESC)` e partial `WHERE http_status IS NULL OR http_status >= 400`; `error` truncado em 4096 chars, `endpoint` guardado como slug (não URL completa) para não leak de `supabase_url`; `meta jsonb` carrega `{mode:'sync'|'async'|'skipped', body_bytes:N, ...}` para forense. (b) **`public.fn_invoke_edge_with_retry(job, endpoint, body, max_attempts=3, backoff_base=5, success_statuses=ARRAY[200,201,202,204])`** SECURITY DEFINER `search_path=public,pg_temp lock_timeout=2s` — valida args (22023 em empty/out-of-range), lê `app.settings.supabase_url`+`app.settings.service_role_key` com fallback para `app.supabase_url`+`app.service_role_key` (compat com o wrapper original de 2026-02-21), DETECTA `http` extension via `pg_extension WHERE extname='http'` — retorna `{skipped:true, reason:'http_extension_missing'}` quando ausente (sandbox local) em vez de explodir. **Usa `extensions.http` síncrono** (não `pg_net.http_post` que é fire-and-forget — retry é impossível sem status-code-aware branching). EXECUTE dinâmico por `format($q$...$q$)` permite que a migration seja instalada ANTES do `http` extension (CREATE OR REPLACE funciona sem parse-time binding). Loop `v_attempt := v_attempt + 1; extensions.http(...); INSERT cron_edge_retry_attempts(...)`; sucesso sai `{ok:true, status, attempts}`; falha triggers backoff linear `LEAST(base * attempt, 120)` segundos (cap 120s evita que um 5-min cron starve outros jobs no mesmo schedule). Após `max_attempts` esgotadas chama `public.fn_record_cron_health_alert(severity='critical', cooldown_minutes=60, details={kind:'edge_invocation_failed_after_retries', endpoint, attempts, last_status, last_error, runbook:'docs/runbooks/EDGE_RETRY_WRAPPER_RUNBOOK.md'})` — dedupe L06-04 vira alerta 1/hora em vez de 1/cron-window mesmo sob outage prolongado. (c) **`public.fn_invoke_edge_fire_and_forget(job, endpoint, body)`** fast-path assíncrono via `net.http_post`. Grava 1 linha `meta.mode='async', meta.request_id=<bigint>, meta.note='async_queued'|'async_enqueue_failed'`. Reservado para callers que queiram throughput e gerenciem retry na EF — nenhum dos 6 rewrites atuais usa (todos usam sync com retry), mas expõe o entry point único para evitar calls raw de `net.http_post` no futuro. (d) **Rewrites**: `fn_invoke_auto_topup_cron` (era PERFORM net.http_post direto) e `fn_invoke_lifecycle_cron_safe` (L12-03, era advisory-lock + PERFORM extensions.http direto) agora delegam para `fn_invoke_edge_with_retry` com `max_attempts=3, backoff_base=5`. **Novos safe wrappers** para os 4 crons inline: `fn_invoke_clearing_cron_safe` + `fn_invoke_verification_cron_safe` + `fn_invoke_onboarding_nudge_safe` + `fn_invoke_reconcile_wallets_safe` (cada um: `fn_cron_should_run(1800s max_runtime)` → `pg_try_advisory_xact_lock(hashtext('cron:<name>_fire'))` → `fn_cron_mark_started` → `fn_invoke_edge_with_retry(backoff=10s para crons diários — janela mais larga permite retry mais lento e menos agressivo no provider)` → `fn_cron_mark_completed` | `fn_cron_mark_failed`). (e) **Reschedule** dentro de DO-block: 6× `cron.unschedule(name)` seguido de `cron.schedule(name, '<cron_expr>', 'SELECT public.fn_invoke_<name>_safe();')` — idempotente em re-apply (block no-op se pg_cron ausente). Insert em `cron_run_state` com `ON CONFLICT DO NOTHING` para os 4 jobs novos (skip condicional `IF EXISTS` em cron_run_state para sandbox sem L12-03). (f) **Self-test DO-block** (4 invariantes: table + 8 functions + retry audit table writable + arg validation raise 22023). `tools/test_l06_05_edge_retry_wrapper.ts` (~320 LOC) **18 integration cases** via `docker exec psql` — schema/DDL (1-10: table + RLS + indices + 8 function registrations + SECURITY DEFINER + uses retry wrapper), argument validation (11-14: empty job_name, empty endpoint, out-of-range max_attempts, out-of-range backoff), runtime behaviour (15-16: missing_config, http_extension_missing + audit row mode='skipped'), fire-and-forget path (17-18: audit row recorded, endpoint validated). **Runbook canônico** `docs/runbooks/EDGE_RETRY_WRAPPER_RUNBOOK.md` (~300 linhas, 7 seções): modelo mental (tabela de dano por skip por job), arquitetura (3 fluxogramas: sucesso, retry-then-success, exhausted retries → alert), tabelas & funções (signatures + return shapes + invariantes), cenários operacionais (A: auto-topup-hourly falhou com query SQL de diagnóstico pelas últimas 5 invocações agrupadas por minute, B: cron_health_alerts crítico com procedure de mitigação sem cumular ações, C: adicionar novo cron HTTP-fired com receita copy-paste do wrapper _safe, D: sandbox local sem http extension), métricas (success_pct 24h por job + attempts p95 7 dias — alvo `avg<1.05, p95<2`), rollback (preferir nova migration corretiva, NUNCA dropar cron_edge_retry_attempts), cross-refs L06-04/L12-03/L12-04/L06-03. **Backwards compat**: 100% additive — 1 nova tabela, 3 novas funções base, 4 novos wrappers, 2 wrappers reescritos com assinatura preservada, 6 reschedules idempotentes. **Validação local**: migration aplica cleanly (`docker exec -i supabase_db_project-running psql -v ON_ERROR_STOP=1`), self-test ✓ "[L06-05.selftest] OK — all invariants pass", integration suite 18/18 verdes (~3s), `npm run audit:verify` 348/348. Onda 1 → 83/179, High → 46/123 (+1), Lente 6 (COO) → 3/N (após L06-03 drift alerts, L06-04 cron health monitor).
- L04-07 — `coin_ledger.reason`/`note` ganha hardening LGPD contra PII embutida (commit `be5e962`, 2026-04-21): fecha o high de Lente 4 (CLO) sobre "`execute_burn_atomic` e várias funções usam `format('Burn of %s coins from %s by user %s', …)`; se `%s` inclui nome/email, um `SELECT * FROM coin_ledger WHERE user_id='00…0'::uuid` após anonimização ainda expõe o nome — right to be forgotten parcial (LGPD Art. 18 III/VI)". **Estado pré-fix**: a whitelist `coin_ledger_reason_check` já existia (restringia reason a 25 canonical codes via `ANY (ARRAY[...])`), e `fn_delete_user_data` (v2.0.0, L04-01) JÁ anonimizava `user_id → 00000000-…` em `coin_ledger`/`xp_transactions`/`clearing_events` (Category B). Porém a trilha não era blindada em profundidade: (a) nenhuma proteção contra um futuro contribuidor afrouxar a whitelist ou adicionar um helper que faça `format('%s by user %I', coins, email)`; (b) `coin_ledger.note` (coluna free-form usada por `wallet_reconcile` e dedicada para drift-log) era unbounded text sem guards; (c) `coin_ledger_archive` (criada via `LIKE coin_ledger INCLUDING ALL` em 20260320000000) só tinha os CHECKs existentes no momento da criação — guards adicionados depois NÃO se propagavam; (d) `fn_delete_user_data` deixava `reason`/`note` intactos ao anonimizar `user_id`, então uma linha hipotética com `reason='Burn by user john.doe@ex.com'` ficava com user_id=zero-UUID + nome/email preservados. **Fix em 6 camadas defensivas** (commit único `be5e962`, migration aditiva pura + CI + runbook): (a) **CHECK constraints preventivos em `public.coin_ledger`**: `coin_ledger_reason_length_guard` (reason IS NULL OR length ≤ 64 — whitelist atual tem todos ≤ 35 chars, 64 dá folga 2× mas rejeita free-form que provavelmente violaria também o whitelist), `coin_ledger_reason_pii_guard` (reason IS NULL OR (position('@' in reason) = 0 AND reason !~* '\mby user [0-9a-f]{8,}' AND reason !~* '\mfrom [A-Z][a-z]+ [A-Z][a-z]+')) — 3 padrões catch-all para email + UUID leak + dois-nomes-capitalizados-em-sequência. `coin_ledger_note_pii_guard` adicionado via DO-block condicional apenas quando a coluna `note` existe (note IS NULL OR (length ≤ 200 AND position('@' in note) = 0 AND note !~* '(^|[^a-z])(name\|email\|cpf\|phone)\s*[:=]')). Mesmos guards espelhados em `coin_ledger_archive` via DO-block `IF EXISTS (SELECT 1 FROM pg_class WHERE relname='coin_ledger_archive')` para idempotência em schemas parciais (test sandbox sem archive partition ainda funciona). (b) **Backfill one-shot transacional** antes dos `ADD CONSTRAINT`: o DO-block `$backfill$` percorre `coin_ledger` procurando linhas que violariam cada um dos 3 guards (`length > 64 OR position('@') > 0 OR ~* 'by user ' OR ~* 'from <Name> <Name>'`), grava MD5 + valor original como audit row em `coin_ledger_pii_redactions`, UPDATE para reason='admin_adjustment' (valor canônico do whitelist que preserva invariantes contábeis — delta_coins intacto). Mesmo fluxo para `note` (→ `[redacted-pii]`) e para `coin_ledger_archive` com EXECUTE dinâmico para tolerância à ausência de coluna `note`. **Sem o backfill o `ADD CONSTRAINT` falharia** em qualquer base com PII residual — ordem dentro da migration é backfill → ALTER TABLE ADD CONSTRAINT. (c) **`public.coin_ledger_pii_redactions`** — tabela de trilha LGPD (`id bigserial, ledger_id uuid nullable, table_name text CHECK IN ('coin_ledger','coin_ledger_archive'), column_name text CHECK IN ('reason','note'), redacted_value text, original_hash text` com MD5 do valor original — **fingerprint sem resistência a preimage** por design: o objetivo é detectar duplicatas + demonstrar accountability, não recuperar o conteúdo removido (seria ilegal LGPD Art. 16 recuperar após request de erasure); `trigger_source text CHECK IN ('migration_backfill_20260421','fn_redact_ledger_pii_for_user','fn_delete_user_data','ops_manual')` para forensics + métrica; `redacted_at timestamptz DEFAULT now()`, `redacted_by uuid FK auth.users ON DELETE SET NULL`, `user_id uuid` (usuário alvo — pode ser o pré-anon ou zero-UUID), `note text` (free-form ops). 2 índices `(user_id, redacted_at DESC)` + `(trigger_source, redacted_at DESC)`. RLS enabled + policy `coin_ledger_pii_redactions_service_role_only FOR ALL USING role='service_role'` — **apenas o service_role lê/escreve**; DBA conecta via psql admin (bypassa RLS), operadores via app nunca acessam. (d) **`public.fn_redact_ledger_pii_for_user(p_user_id uuid, p_actor uuid DEFAULT NULL) RETURNS jsonb`** — helper SECURITY DEFINER `search_path=public,pg_temp lock_timeout=5s`, idempotente (matching rows que já foram scrubbadas deixam de matchar os padrões na 2ª invocação), retorna jsonb contadores `{user_id, started_at, function_version:'1.0.0', coin_ledger_reason_redacted:N, coin_ledger_note_redacted:N, coin_ledger_archive_reason_redacted:N, coin_ledger_archive_note_redacted:N, completed_at}` para o caller incluir no report. Aceita tanto o `p_user_id` pré-anonimização quanto já-anonimizado (`user_id = p_user_id OR user_id = zero-UUID`), então caller pode chamar antes ou depois do Category B UPDATE de `fn_delete_user_data` sem surprise. Loop per-row (não UPDATE em massa) para: (i) gravar audit row pré-UPDATE com MD5 do original — se o UPDATE falhar, a audit row fica órfã com `ledger_id` apontando para row inalterada (auto-reparo: próxima invocação re-processa); (ii) usar `EXECUTE` dinâmico para queries em `note` — permite tolerância à ausência da coluna em schemas parciais (test sandbox onde `20260227500000_wallet_reconcile_and_session_retention.sql` não rodou ainda). REVOKE FROM PUBLIC + GRANT EXECUTE para `service_role` apenas. (e) **Trigger safety-net** `fn_ledger_pii_redact_on_erasure` + `trg_ledger_pii_redact_on_erasure AFTER INSERT ON audit_logs FOR EACH ROW` (aplicado via DO-block condicional à existência de `audit_logs`): dispara SOMENTE em `action='user.self_delete.completed'` + `target_user_id` ou `actor_id` não-NULL não-zero-UUID. Chama `fn_redact_ledger_pii_for_user` para o usuário alvo e faz UPDATE em `audit_logs.metadata` com o report como `l04_07_redaction` (SAFE: `EXCEPTION WHEN OTHERS THEN NULL` se audit_logs virar append-only via L10-08 futuro). Erros do próprio redator NÃO propagam para o trigger (bloquearia insert do audit_log — auditoria tem prioridade sobre PII scrub). Em caso de erro do redator, grava uma row de sinalização em `coin_ledger_pii_redactions` com `note='TRIGGER_ERROR: ' || SQLERRM` para ops investigar no dashboard. **Cobre erasures antigas**: qualquer `user.self_delete.completed` gravado historicamente (pré-L04-07) agora, se re-emitido por retry ou job de compliance, dispara o scrub automaticamente. (f) **CI lint** `tools/audit/check-ledger-reason-safety.ts` (~200 LOC, `npm run audit:ledger-reason`): parser ad-hoc que extrai `INSERT INTO (public.)?coin_ledger(_idempotency)?\s*\(…\)` via regex, localiza a posição da coluna `reason` na lista, splittop-level args do próximo `VALUES (…)` com state-machine que respeita `(`/`)`/`'`/`"` (não regex naive), e valida o expr na posição `reason`. Rejeita: (1) `format(...)` (SEMPRE dinâmico, `-- L04-07-OK: <razão>` opt-out em até 5 linhas acima é o escape deliberado para self-tests), (2) concat `||`, (3) literais fora da whitelist canônica `CANONICAL_REASONS` (26 entries espelhadas de `20260421130000_l03_reverse_coin_flows.sql` — update lockstep com as duas fontes no mesmo commit é mandatório), (4) literais contendo '@'. Aceita: (a) literais canônicos, (b) variáveis plpgsql (`v_reason`, `_reason_` — match `/^[_a-z][a-z0-9_]*$/i`), (c) qualquer expr em insert com `-- L04-07-OK: <justificativa>`. **Cutoff `GUARD_MIGRATION_TIMESTAMP = '20260421220000'`**: o lint só escaneia migrations criadas a partir dessa data — migrations anteriores são "congeladas" pelo backfill on-shot (já tiveram PII scrubbed e novos CHECKs agora previnem inserts bad), então re-auditá-las seria falso-positivo. `tools/test_l04_07_ledger_reason_pii.ts` (11 cases, driven por `docker exec + psql` sem dependência do driver `pg` no node_modules): valida schema das 2 constraints + audit table + RLS + mirror em archive, CHECK enforcement (canonical aceito, '@' / by-user / 80-char rejeitados com `23514 check_violation`), CHECK em note quando coluna existe, `fn_redact_ledger_pii_for_user` registrada e retorna jsonb com shape correto + idempotência retorna 0 counters, trigger presente em audit_logs. **Self-test DO-block** no fim da migration valida 7 invariantes em-place (canonical reason accepted, '@' rejected, pii_guard + length_guard instalados, helper retorna jsonb válido, trigger instalado em audit_logs se tabela existir) — falha CI em re-apply se algo regredir. **Runbook canônico** `docs/runbooks/LEDGER_PII_REDACTION_RUNBOOK.md` (~280 linhas, 9 seções): modelo mental "imutável contábil vs PII escrubável", cenário A (CHECK bloqueia INSERT em prod — capturar Sentry, não afrouxar CHECK), cenário B (investigação LGPD — executar `fn_redact_ledger_pii_for_user` com service_role), cenário C (erasure antigo — batch em `audit_logs.target_user_id`), auditoria via `coin_ledger_pii_redactions` (queries por operador, spike de redações, trigger errors), CI lint (padrões aceitos vs rejeitados + opt-out), manutenção (rotacionar regexes para novos padrões PII via nova migration), integração com fluxos existentes (`fn_delete_user_data` via trigger, `execute_burn_atomic` já canonical, mobile N/A), rollback (`DROP CONSTRAINT` se regressão — mas NÃO apagar `coin_ledger_pii_redactions` que é audit trail LGPD Art. 37), contatos. **Backwards compat**: 100% — migration aditiva (2-4 CHECK constraints nova, 1 tabela nova, 2 funções novas, 1 trigger novo, 0 colunas removidas, 0 signatures alteradas). `execute_burn_atomic`/`emit_coins_atomic`/reconcilers existentes já usam literais canônicos — validado manualmente nas ~20 migrations que inserem em `coin_ledger`. **Validação local**: migration aplicada via `docker exec -i supabase_db_project-running psql -v ON_ERROR_STOP=1` com sucesso (self-test ✓ "L04-07 selftest: OK (all 7 invariants pass)", backfill 0 rows em DB limpo), `npx tsx tools/test_l04_07_ledger_reason_pii.ts` 11/11 verdes (2s), `npm run audit:ledger-reason` OK, `npm run audit:verify` 348/348, `npx tsc --noEmit` zero novos erros (apenas o pré-existente `feature-flags.ts:69` unrelated). Onda 1 → 82/179, High → 45/123 (+1), Lente 4 (CLO) → 4/N (após L04-01, L04-03, L04-04).
- L09-09 — Contratos privados `TERMO_ADESAO_ASSESSORIA.md` + `TERMO_ATLETA.md` publicados com SHA-256 lockstep (commit `5c1f09c`, 2026-04-21): fecha o high de Lente 9 (CRO) sobre "`grep -rn termo_adesao\|termo_atleta\|contrato docs/` não retorna nada — plataforma coleta consent sem que exista o texto canônico correspondente". **Estado pré-fix**: a infra L04-03 (`consent_policy_versions` + `consent_events` + 5 RPCs) estava sólida, mas os 8 tipos canônicos cobriam apenas *consent de dados* (terms, privacy, health_data, etc.); **não havia** o instrumento contratual B2B (plataforma × assessoria) nem B2C (atleta × assessoria). Em ação judicial ou inquérito ANPD, a plataforma não conseguia responder "qual versão do termo de adesão essa assessoria aceitou?" — gap crítico de governança: LGPD Art. 7 I + 8 §1 exigem consentimento demonstrável e documental; se o texto é produzido on-the-fly (render de componente React sem hash versionado), é impossível reconstituir o que o usuário leu 2 anos depois. **Fix aplicado em 6 camadas conectadas pelo hash SHA-256**: (1) **Documentos canônicos v1.0** — `docs/legal/TERMO_ADESAO_ASSESSORIA.md` (club_adhesion, 12 cláusulas: objeto, remuneração, LGPD controladoria conjunta, cap antifraude, foro, rescisão, anticorrupção/COAF, `sha256=1103d8ee…1a3f1`) + `docs/legal/TERMO_ATLETA.md` (athlete_contract, 11 cláusulas: termo de conhecimento de risco CDC, limitação de responsabilidade da plataforma como *mera intermediadora tecnológica*, matriz completa de tratamento de dados sensíveis, foro do domicílio-consumidor CDC Art. 101 I, `sha256=834f70fa…f2f1dd`) + `README.md` de convenções editoriais. (2) **Migration `20260421210000_l09_09_legal_contracts_consent.sql`** — estende os CHECK de `consent_policy_versions.consent_type` e `consent_events.consent_type` com os 2 novos tipos, seed com `document_hash` = SHA-256 do MD, reescreve `fn_consent_grant` para incluir IN(…) ampliado, DO block self-test valida 10 policies + hashes. (3) **Drift detection lockstep** — `tools/legal/check-document-hashes.ts` (+ `npm run legal:check`): lê cada MD, computa SHA-256, compara com EXPECTED; **verifica também que o hash EXPECTED aparece literalmente no SQL da migration** — dupla barreira mata drift silencioso (alguém editar EXPECTED sem atualizar SQL, ou vice-versa). Exit 1 em CI força bump de versão quando texto muda. (4) **Whitelists cliente em sincronia**: `portal/src/app/api/consent/route.ts` e `supabase/functions/consent-record/index.ts` ambos com os 10 tipos. (5) **Testes em 3 níveis**: vitest `route.test.ts` (9 casos: accept novos tipos, reject unknown, propagar P0001 como 400), sandbox `tools/test_l09_09_legal_contracts.ts` (16 casos: schema, seed, CHECK, fn_consent_grant+revoke com ambos tipos, lockstep MD↔DB via SHA-256 real), `tools/integration_tests.ts` atualizado para esperar 10 tipos e `document_hash` não-nulo. (6) **Runbook operacional** `docs/runbooks/LEGAL_CONTRACTS_RUNBOOK.md`: bump de versão v1.0→v2.0 (exige reconsent via `minimum_version`), correção tipográfica v1.0→v1.1 (não exige reconsent), SQL pronto para auditar consent de uma assessoria ou atleta em response a ANPD, processo de bloqueio de assessoria por violação contratual via `fn_consent_revoke('club_adhesion')`, alertas para drift em CI + taxa de reconsent <90% em 7 dias. **Validação local**: migration aplicada via psql com sucesso (2 rows seedadas, NOTICE `[L09-09] OK — 10 policies, hashes íntegros, CHECKs estendidos`), `npm run legal:check` exit 0, `CHECK` constraint rejeita `bogus_xyz` e aceita os 2 novos tipos end-to-end (test direto na tabela com FK válida), 50 testes vitest relacionados passam, `openapi:check` OK.
- L12-05 — `auto-topup-hourly` ganha cap diário antifraude (BRL + count) + guardrail antes da Stripe (commit `73d871f`, 2026-04-21): fecha o high de Lente 12 (Cron/Scheduler) sobre "roda de hora em hora; se settings do atleta mal-configurado (bug ou ataque), pode cobrar 24×/dia". **Estado pré-fix**: o `auto-topup-cron` rodava `*/0 * * * *` (hourly) chamando `auto-topup-check` para cada `billing_auto_topup_settings.enabled=true`. Salvaguardas existentes: cooldown 24h via `last_triggered_at` (atualizado APÓS Stripe sucesso, abre race window de 5–30s = latência da PaymentIntent.create) + cap mensal (`max_per_month` default 3, máx absoluto 10). Cobre "X/mês" mas NÃO "Y/dia". **Cenário de exploração realista**: o `auto-topup-check` também é invocado **inline** após cada token-debit (header da fn cita explicitamente). Burst de N debits em <1min → N invocações paralelas → todas leem `last_triggered_at = NULL` (race) → todas passam o gate → todas criam PaymentIntent. Resultado: até 24 cobranças/dia × pacote default ~R$ 200 = **R$ 4.800/dia** indevidos no cartão do cliente, refund manual via Stripe + suporte a um cliente já desconfiado (ticket P1, custo de relacionamento alto). Sem audit trail estruturado de mudanças no cap (admin_master comprometido podia subir o teto silenciosamente). **Solução em 5 camadas, migration aditiva + edge wiring + portal**: (a) **Schema additive em `billing_auto_topup_settings`** (5 colunas): `daily_charge_cap_brl numeric(10,2) NOT NULL DEFAULT 500.00 CHECK (>=0)` (default conservador cobre 95% dos casos legítimos; admin_master sobe via UI com reason), `daily_max_charges integer NOT NULL DEFAULT 3 CHECK (1..24)` (cron é hourly, máximo absoluto = 24/dia), `daily_limit_timezone text NOT NULL DEFAULT 'America/Sao_Paulo'` (produto BR-first; janela "00:00 BRT" coerente com expectativa do tesoureiro humano e simétrico com L05-09 custody), `daily_limit_updated_at timestamptz` + `daily_limit_updated_by uuid REFERENCES auth.users(id) ON DELETE SET NULL` (forensics — quem mudou pela última vez). (b) **Tabela de audit `billing_auto_topup_cap_changes`**: `id`, `group_id FK coaching_groups`, `previous_cap_brl`/`new_cap_brl` (CHECK >=0), `previous_max_charges`/`new_max_charges` (CHECK 1..24), `previous_timezone`/`new_timezone`, `actor_user_id FK auth.users`, `reason text NOT NULL CHECK (length(trim(reason)) >= 10)` (postmortem obrigatório — admin_master precisa justificar cada mudança), `idempotency_key text` com `UNIQUE PARTIAL INDEX (group_id, idempotency_key) WHERE idempotency_key IS NOT NULL` (PATCH safe-retry sob network flake). Índices `(group_id, changed_at DESC)` para painel "histórico" + `(actor_user_id, changed_at DESC)` para investigar atividade suspeita. RLS: SELECT para `admin_master` do grupo OR `platform_role='admin'` (CFO + assessoria precisam ler), REVOKE PUBLIC, GRANT service_role para escrita via RPC. (c) **3 RPCs SECURITY DEFINER `search_path=public,pg_temp`**: (i) `fn_check_auto_topup_daily_window(group, charge_brl default 0) RETURNS TABLE(current_count_today, daily_max_charges, available_count, current_total_brl, daily_charge_cap_brl, available_brl, would_exceed_count, would_exceed_total, would_exceed, window_start_utc, window_end_utc, timezone)` STABLE — preview read-only para painel admin "se eu cobrar R$ 200 agora, quanto sobra do cap?". Janela computada como `[date_trunc('day', now() AT TIME ZONE tz), +1 day)` no TZ da settings, convertido para UTC para comparação com `created_at`. Soma `price_cents/100` de `billing_purchases WHERE source='auto_topup' AND status IN ('pending','paid','fulfilled') AND currency='BRL'` na janela (cancelled NÃO conta — Stripe declined ou rollback via `fn_fulfill`; manual NÃO conta — usuário pagou direto, não auto-topup; USD NÃO conta — cap é BRL-only sem conversão definida). Se settings ausentes (auto-topup nunca habilitado) devolve defaults conservadores (TZ='America/Sao_Paulo', cap=500, max=3) ao invés de NULL. (ii) `fn_apply_auto_topup_daily_cap(group, charge_brl)` — guardrail estrito que `RAISE P0010 AUTO_TOPUP_DAILY_CAP_EXCEEDED` se `current_count + 1 > max_count` OR `current_total + charge > cap_brl`, MENSAGEM com `count=N/M`, `total_brl=X/Y`, `tz`, `group_id` e HINT estruturado citando o runbook + ponteiros para ambos os endpoints (UI admin_master + REST platform admin). REVOKE FROM anon/authenticated/PUBLIC + GRANT service_role (apenas o edge function chama). (iii) `fn_set_auto_topup_daily_cap(group, new_cap_brl, new_max_charges, actor, reason, tz default NULL, idempotency_key default NULL) RETURNS TABLE(out_group_id, out_previous_cap_brl, out_new_cap_brl, out_previous_max_charges, out_new_max_charges, out_previous_timezone, out_new_timezone, out_changed_at, out_was_idempotent)` — atomicamente UPDATE settings + INSERT audit row. Reason CHECK length >= 10 (postmortem), TZ validation via `now() AT TIME ZONE p_timezone` em EXCEPTION (rejeita "Mars/Olympus" com `P0001 not a valid IANA timezone`), idempotência via SELECT do `(group_id, idempotency_key)` UNIQUE — replay devolve estado anterior + `out_was_idempotent=true`. **OUT params com prefixo `out_`** (lição de L05-09) para evitar `42702 column reference ambiguous` — quando a função RETURNS TABLE colide com colunas físicas a resolução de nomes falha em runtime. Settings devem existir antes de chamar (P0002 AUTO_TOPUP_SETTINGS_NOT_FOUND com HINT — não criamos settings aqui porque `product_id` é NOT NULL FK e não temos um para semear; fail-loud é melhor que side-effect surpresa). (d) **Edge function wiring** `supabase/functions/auto-topup-check/index.ts`: chama `fn_apply_auto_topup_daily_cap` **ANTES** de `stripe.paymentIntents.create` — fecha a race condition. Em P0010 retorna `{ triggered: false, reason: 'daily_cap_reached', detail: HINT }` + emite `product_event 'billing_auto_topup_blocked_daily_cap'` (CFO/painel vê em real-time se algo está blocando) + `audit_log` `auto_topup.cap_blocked` com `properties.charge_amount_brl + .hint`. **Fail-soft em deploy progressivo**: 42883 (function does not exist) → WARN log + segue (evita derrubar o cron em janela de deploy onde a migration ainda não chegou ao DB); outros erros propagam para o catch externo. Próxima invocação do cron (próxima hora) re-tenta com janela diária ainda rolando — sem necessidade de retry custom. (e) **Portal API** `portal/src/app/api/auto-topup/route.ts` ganha 4 campos opcionais no Zod schema (`daily_charge_cap_brl 0-100k`, `daily_max_charges 1-24`, `daily_limit_timezone min 1 / max 64`, `daily_cap_change_reason min 10 / max 500`) + `superRefine` que rejeita qualquer mudança em campos `daily_*` sem reason explícita. Mudanças de cap NÃO vão pelo direct UPDATE (que ignora audit) — passam pelo RPC `fn_set_auto_topup_daily_cap` (audit-trailed). Suporta header `x-idempotency-key` (8-128 chars) propagado para a RPC. Errors traduzidos: P0001 → 400, P0002 → 404, default → 500, com `code` + `hint` no envelope. Fluxo: (i) carrega valores atuais, (ii) chama RPC com merge `(daily_X ?? current.daily_X)` (RPC não tem partial-update — precisa dos 3 valores), (iii) devolve `{ ok: true, daily_cap: { previous, new, was_idempotent } }` no envelope para a UI exibir confirmation. **UI** `portal/src/app/(portal)/settings/auto-topup-form.tsx` ganha seção `<details>` "Limites diários de antifraude (avançado)" visível apenas quando `hasStripePaymentMethod` (faz sentido só para grupos que conseguem ser cobrados — sem cartão Stripe não há auto-topup). 3 inputs (cap BRL com `step={10}`, max charges, TZ select com 5 IANA opções comuns no BR) + textarea de reason que aparece **apenas** quando `dailyCapDirty` (algum dos 3 mudou em relação ao banco). Client-side gate antes do POST (`reason.trim().length < 10` → setError local sem network), evita round-trip desperdiçado. Counter "X/10 caracteres ✓" para feedback imediato. Refresh do router após sucesso. (f) **Tests em 4 layers**: **PG sandbox** `tools/test_l12_05_auto_topup_daily_cap.ts` (~570 linhas, 26 cases): schema/registry (5 colunas, audit table queryable, 3 RPCs registradas, anon não EXECUTE `fn_apply_*`), window arithmetic (empty=0/cap, pending+paid+fulfilled contam, cancelled NÃO conta, source='manual' NÃO conta, currency='USD' NÃO conta), guardrail (P0010 quando count exceded, P0010 quando total BRL exceded, silent quando dentro de ambos, cross-group isolation), set_cap RPC (audit row gravado, reason < 10 → P0001, cap_brl < 0 → P0001, max_charges > 24 → P0001, TZ inválido → P0001, idempotent replay devolve `out_was_idempotent=true` + EXATO 1 row na audit table, group desconhecido → P0002). **Vitest** `route.test.ts` (+6 cases ao 5 existentes): rejeita daily_* sem reason (Zod superRefine), rejeita reason < 10 chars, invoca `fn_set_auto_topup_daily_cap` com params corretos quando daily_charge_cap_brl muda, propaga `x-idempotency-key` para RPC, traduz P0001→400 e P0002→404, NÃO invoca RPC quando nenhum daily_* enviado. **Vitest** `schemas.test.ts` (+9 cases): aceita daily com reason válida, rejeita daily_charge_cap_brl/daily_max_charges/daily_limit_timezone sem reason, rejeita cap > 100k (sanity ceiling), rejeita max_charges > 24 (cron é hourly), rejeita cap < 0 (free-pass), rejeita reason < 10 chars, NÃO exige reason para non-daily updates. **Vitest** `auto-topup-form.test.tsx` (+5 cases): hide section quando sem Stripe, show quando com Stripe, NÃO envia daily_* quando nada mudou, blocks save com client-side validation reason curta, envia daily_* + reason quando admin muda cap (com `<details>` toggle workaround para happy-dom). **Lockstep `htmlFor`/`id`** added in form for proper a11y label-input pairing (também resolve `getByLabelText` em testes). **Runbook canônico** `docs/runbooks/AUTO_TOPUP_DAILY_CAP_RUNBOOK.md` (NEW, ~210 linhas): modelo mental "cap dual (BRL + count) na janela TZ por conta", o que conta vs o que não conta (lista explícita), Cenário A (assessoria contesta cap apertado) com query SQL `fn_check_auto_topup_daily_window` + critérios para aumentar (volume coerente com histórico, pico legítimo confirmado, atleta sem flags de credential-stuffing) vs NÃO aumentar (cliente não responde, atividade anômala em login, ticket por canal não-verificado), Cenário B (cap bloqueou ataque) com queries de blocks recentes + audit_logs de login + procedimento de contenção (disable + reset password + notificar + ticket interno) + pós-mortem (reduzir cap de volta + verificar histórico de cap_changes), Cenário C (deploy progressivo — WARN benigno) com query em `supabase_migrations.schema_migrations`, mapa rápido (TL;DR table). Cross-refs com L12-05 finding + L05-09 (mesmo padrão para custody) + L17-01 + L01-04 (idempotency). **Backwards compat**: 100% — migration aditiva (5 colunas com DEFAULT, nova tabela audit, 3 funções novas, sem breaking change na signature do edge function). Pre-existing settings ganham defaults via column DEFAULT (R$ 500/dia + 3 cobranças/dia em TZ Sao_Paulo) — conservador o suficiente para que clientes legítimos quase nunca atinjam, mas baixo o suficiente para que ataque seja contido em alguns minutos. Edge function preserve shape de retorno (`{ triggered, reason, ... }`); apenas adiciona novo `reason='daily_cap_reached'` ao set existente. Portal API preserve campos antigos (`enabled`, `threshold_tokens`, `product_id`, `max_per_month`); apenas adiciona daily_* opcionais. UI preserve seção principal; adiciona `<details>` opt-in. **OpenAPI**: `/api/auto-topup` já era grandfathered baseline (não exposto publicamente — admin-only). `npm run openapi:check` OK (94 routes, 7 v1 + 48 legacy = 55 documented, 39 grandfathered). Suite portal **+20 tests verdes** (11 route + 9 schemas + 5 form + 26 PG sandbox), audit verify **348/348**. Onda 1 → 80/179, High → 44/123 (+1), Lente 12 (Cron/Scheduler) → 5/N (após L12-01, L12-02, L12-03, L12-04).

- L05-07 — swap marketplace `amount_usd` floor reduzido de US$ 100 → US$ 10 com constantes canônicas + lockstep cross-surface (commit `1ad1a91`, 2026-04-21): fecha o high de Lente 5 (CPO) sobre "swap min US$ 100 inviabiliza grupos pequenos — clube amador de 20 atletas que quer hedge de US$ 50 não consegue". **Estado pré-fix**: `portal/src/app/api/swap/route.ts:40` usava `amount_usd: z.number().min(100).max(500_000)` no Zod schema da action `create`. UI em `portal/src/app/(portal)/swap/swap-actions.tsx` tinha `min={100}` no `<input>` + guard `val < 100` + message `"Valor mínimo: US$ 100.00"`. OpenAPI v1 (`portal/src/lib/openapi/routes/v1-financial.ts:45`) e OpenAPI legacy (`portal/public/openapi.json:508`) replicavam o literal 100 hand-maintained — drift garantido em qualquer evolução. O floor de US$ 100 não tinha rationale documentado; herdado de PR antigo provavelmente preocupado com gateway-fee constraints que NÃO se aplicam a swap intra-plataforma post-L03-01 (money math TS↔SQL) + L05-02 (TTL canônico). Resultado de produto: ~60% do mercado-alvo amador (clubes 20-50 atletas com swaps típicos US$ 30-200) era filtrado, marketplace P2P inviável fora de clubes profissionais, loop de feedback negativo (sem oferta → sem demanda → sem oferta). **Solução em 4 superfícies coordenadas (uma única PR, código-only sem migration)**: (a) **Constantes canônicas em `portal/src/lib/swap.ts`**: `SWAP_MIN_AMOUNT_USD = 10` e `SWAP_MAX_AMOUNT_USD = 500_000` exportadas como `const`, com JSDoc explicativo: floor 10 captura amadores sem permitir dust attacks (US$ 1 spammer com 1k ofertas saturando UI; US$ 10 + rate-limit 10/min torna ataque visível ao CFO via `platform_revenue` ledger), fee mínimo a 1% = US$ 0.10 ainda >= dust precision de `numeric(14,2)`; cap 500k baseia-se em "tesoureiro digitando manualmente em desktop" — defesa anti digit-fat-finger, rodadas institucionais maiores devem dividir em N ofertas para preservar auditoria granular. **Schema do banco INALTERADO**: `swap_orders.amount_usd numeric(14,2) NOT NULL CHECK (amount_usd > 0)` em `20260228150001_custody_clearing_model.sql:172` segue intacto — o CHECK > 0 é defesa econômica (não permitir receita negativa); o floor de UX fica em TS para evoluir sem migration (decisão de produto que pode mudar trimestralmente conforme dinâmica do marketplace). (b) **Wiring em route handler**: `portal/src/app/api/swap/route.ts` importa as constantes e usa em `createSchema.amount_usd.min(SWAP_MIN_AMOUNT_USD).max(SWAP_MAX_AMOUNT_USD)` (era literal `min(100).max(500_000)`). Comment in-line cita L05-07 + ponteiro para o lib/swap.ts como single source of truth. (c) **Wiring em OpenAPI v1**: `portal/src/lib/openapi/routes/v1-financial.ts` importa as constantes, usa em `SwapCreateBody.amount_usd.min/max()`, descrição do field cita "L05-07: floor lowered from $100 to support small amateur clubs in the P2P marketplace". `npm run openapi:build` regenera `public/openapi-v1.json` automaticamente — single source of truth para clientes que consomem o spec gerado. **Wiring em OpenAPI legacy**: `portal/public/openapi.json` (hand-maintained spec referenciado por Swagger UI integrações antigas) atualizado de `{"minimum": 100, "maximum": 500000}` → `{"minimum": 10, "maximum": 500000, "description": "Min US$ 10 (L05-07: floor reduzido de 100 para suportar clubes amadores). Max US$ 500.000."}`. (d) **Wiring em UI client**: `portal/src/app/(portal)/swap/swap-actions.tsx` importa `SWAP_MIN_AMOUNT_USD` de `@/lib/swap`, usa em (i) `<input min={SWAP_MIN_AMOUNT_USD}>` HTML5 hint + step={1}, (ii) guard `if (!val || val < SWAP_MIN_AMOUNT_USD)` em handleCreate, (iii) message `"Valor mínimo: US$ ${SWAP_MIN_AMOUNT_USD.toFixed(2)}"`, (iv) novo helper `<p id="swap-min-helper" aria-describedby>` mostrando range explicitamente — "Mínimo US$ 10.00 · máximo US$ 500.000,00" — a11y bonus que o input antigo não tinha. **Tests** em 3 layers garantindo ratchet de drift: (e.1) `portal/src/lib/swap.test.ts` ganha novo describe `"L05-07 — SWAP_MIN_AMOUNT_USD lockstep contract"` (3 cases): (i) constantes têm valores esperados (`SWAP_MIN_AMOUNT_USD === 10`, `SWAP_MAX_AMOUNT_USD === 500_000`), (ii) **contract test que lê literalmente `portal/public/openapi.json`** via `fs.readFile + JSON.parse` e asserta `paths."/api/swap".post.requestBody.content."application/json".schema.oneOf[0].properties.amount_usd.minimum === 10` + `maximum === 500000` — falha CI em O(1) se hand-maintained spec e Zod desviarem (mesmo padrão de L03-09 platform-fee-types lockstep), (iii) **contract test que lê o source de `swap-actions.tsx`** e asserta que importa `SWAP_MIN_AMOUNT_USD` from `"@/lib/swap"` (não literais hardcoded), e que NÃO contém `min={100}` ou `val < 100` (proteção contra rebase acidental de PR antigo). (e.2) `portal/src/app/api/swap/route.test.ts`: test "amount below minimum" updated de `amount_usd: 10` → `amount_usd: 5` para refletir novo floor; +2 cases novos: (i) US$ 10 (floor exato) é aceito pelo route + RPC `createSwapOffer` chamado com `amount=10`, (ii) regressão US$ 50 (rejected pre-fix quando floor era 100) é aceito. (e.3) `portal/src/app/(portal)/swap/swap-actions.test.tsx`: message updated; +3 cases novos: (i) helper text aparece com texto canônico via regex, (ii) US$ 5 ainda rejeitado com message com novo valor, (iii) US$ 10 (floor exato) aceito sem message, (iv) regressão US$ 50 aceito sem message (queryByText null). Suite portal **95/95** verde (swap.test 27 + swap-actions.test 8 + route.test 39 + v1-aliases 21). **OpenAPI coverage baseline** (`portal/tools/openapi/coverage-baseline.json`) recebe entry grandfathered para `/api/platform/custody/{groupId}/daily-cap` (orphan da L05-09 — admin-only endpoint, doc segue follow-up L14-01); rationale documentado no JSON. `npm run openapi:check` OK (drift 0, coverage 7 v1 + 48 legacy = 55 documented + 39 grandfathered). **Backwards compat**: 100% — apenas amplia conjunto-de-aceitação do `amount_usd` (existing clientes que enviavam ≥100 continuam funcionando idêntico; ofertas existentes no banco permanecem intactas; CHECK >0 do schema preservado). Audit verify **348/348**. Onda 1 → 79/179, High → 43/123 (+1), Lente 5 (CPO) → 3/N (após L05-03, L05-09).

- L05-09 — `custody_deposits` ganha cap diário antifraude por grupo (commit `d2cf37f`, 2026-04-21): fecha o high de Lente 5 (CPO) sobre "deposit não tem limite anti-AML por grupo/dia, admin_master comprometido pode mover US$ 10M em segundos sem custo material". **Estado pré-fix**: a função `fn_create_custody_deposit_idempotent` (L01-04 idempotent) inseria `custody_deposits` com qualquer `amount_usd` validado apenas pelo Zod (`min: 0.01, max: 10_000_000`) — sem teto agregado por janela temporal. Um cenário de fraude realista: chave service_role exfiltrada de um grupo + atacante criando 1.000 depósitos de US$ 9.999 (cada um abaixo do reporting threshold) em loop pelo `/api/custody` em 30 segundos, totalizando ~US$ 10M lavados via custody → distribute → burn → clearing payout. Auditoria CFO só descobria via `check_custody_invariants()` ou reconciliação semanal — janela de 7 dias para o atacante extrair fundos via clearing. **Solução em 5 camadas, migration aditiva + endpoint admin**: (a) **Schema additive em `custody_accounts`**: `daily_deposit_limit_usd numeric(14,2) NOT NULL DEFAULT 50000` com CHECK `>= 0` (cap conservador para grupos novos; CFO sobe via PATCH para grupos validados), `daily_limit_timezone text NOT NULL DEFAULT 'America/Sao_Paulo'` (produto BR-first mas portável — cada conta pode ter TZ próprio; janela é `[00:00 TZ atual, 24:00 TZ atual)` para casar com expectativa do tesoureiro humano), `daily_limit_updated_at timestamptz` + `daily_limit_updated_by uuid REFERENCES auth.users` (forensics — quem mudou o cap pela última vez). (b) **Tabela de audit `custody_daily_cap_changes`**: id, group_id FK, previous_cap_usd, new_cap_usd, changed_by FK auth.users, changed_at, reason text CHECK `length(trim(reason)) >= 10` (postmortem obrigatório — CFO precisa justificar cada bump), idempotency_key (UNIQUE partial em `(group_id, idempotency_key) WHERE idempotency_key IS NOT NULL` para PATCH safe-retry). Índice `(group_id, changed_at DESC)` para painel "histórico de cap deste grupo" do CFO. RLS: SELECT-all para `profiles.platform_role='admin'` + REVOKE PUBLIC + GRANT service_role. Trigger não necessário (registro é write-once via `fn_set_daily_deposit_cap`). (c) **3 funções RPC SECURITY DEFINER, lock_timeout=2s**: (i) `fn_check_daily_deposit_window(p_group_id, p_amount_usd default 0) RETURNS TABLE(current_total_usd, limit_usd, available_usd, would_exceed boolean, window_start_utc, window_end_utc, timezone text)` STABLE — preview read-only para o painel admin "se eu permitir esse depósito, quanto sobra do cap?". Janela computada como `[date_trunc('day', now() AT TIME ZONE tz), +1 day)` no TZ da conta, convertida de volta para UTC para comparação com `created_at`. Soma `amount_usd` de todos os deposits com `status IN ('pending','confirmed')` na janela (failed/refunded NÃO contam — reverse_custody_deposit_atomic L03-13 libera budget no mesmo dia, útil para triage de incidente de fraude). (ii) `fn_apply_daily_deposit_cap(p_group_id, p_amount_usd)` — guardrail estrito que RAISE `P0010 DAILY_DEPOSIT_CAP_EXCEEDED` se `current_total_usd + p_amount_usd > limit_usd`, MENSAGEM com `current_usd/limit_usd/window_start_utc/window_end_utc/group_id` em formato structured para apiError serializar como hint usável. (iii) `fn_set_daily_deposit_cap(p_group_id, p_new_cap_usd, p_actor_user_id, p_reason, p_timezone, p_idempotency_key)` — atomicamente UPDATE no `custody_accounts` + INSERT no `custody_daily_cap_changes`. Idempotent via UNIQUE no idempotency_key (replay devolve estado anterior + flag was_idempotent). Aceita TZ override (CFO pode aplicar política UTC para grupo americano). Validation: reason length, cap range, TZ válido (testa via `now() AT TIME ZONE p_timezone`). REVOKE FROM anon/authenticated/PUBLIC + GRANT service_role + GRANT EXECUTE para platform admins via SECURITY DEFINER. **OUT params renomeados** com prefixo `out_` (`out_group_id`, `out_previous_cap_usd`, etc.) para evitar `42702 column reference is ambiguous` — bug latente em qualquer função SECURITY DEFINER que retorna RECORD com nomes que colidem com colunas. (d) **Wiring em `fn_create_custody_deposit_idempotent`**: chama `fn_apply_daily_deposit_cap` SOMENTE no miss-path (i.e., quando idempotency_key é novo e o deposit vai realmente ser criado). Replays via `coin_ledger_idempotency` UNIQUE NÃO re-cobram orçamento — budget já consumido na criação original. Documentado em comment para que próximo contribuidor não "conserte" essa decisão. (e) **API layer**: `portal/src/app/api/custody/route.ts` POST handler agora envolve `createCustodyDeposit` em try/catch que detecta P0010 (ou message `DAILY_DEPOSIT_CAP_EXCEEDED`), retornando HTTP 422 `error.code=DAILY_DEPOSIT_CAP_EXCEEDED` com hint para `CUSTODY_DAILY_CAP_RUNBOOK`. Outros erros propagam para `withErrorHandler` (500 INTERNAL_ERROR sem vazar detalhes). **Novo endpoint `GET|PATCH /api/platform/custody/[groupId]/daily-cap`**: GET retorna `{account: {limit_usd, timezone, updated_at, updated_by}, window: {current_total, limit, available, would_exceed (sempre false em GET), window_start_utc, window_end_utc, timezone}, history: [...últimas 20 mudanças]}` para painel CFO inspecionar status real-time de qualquer grupo. PATCH aceita `{new_cap_usd: number, reason: string min 10/max 500, timezone?: IANA}` + header `x-idempotency-key` obrigatório (8-128 chars), authz `platform_role='admin'` + rate-limit 20/min/admin (cap changes não devem ser frequentes), audit log único por mudança em `portal_audit_log` com action=`custody.daily_cap.set` + metadata.previous/new/reason. **Tests**: `tools/test_l05_09_custody_daily_cap.ts` (19 cases PG sandbox, ~700 linhas): schema/RLS/grants (anon não SELECT custody_daily_cap_changes nem EXECUTE as 3 funções), window arithmetic (pending+confirmed contam, failed/refunded NÃO contam — fixtures via INSERT direto bypassando o cap), guardrail (P0010 com structured detail, isolamento entre grupos diferentes que compartilham cap), interação com idempotência (replay via mesma idempotency_key não re-cobra cap mesmo se cap=0 mid-day; nova chamada com nova chave respeita cap atual), audit trail (`reason` < 10 chars rejeitada com P0001, mudança de TZ persiste em ambas tabelas). Self-test DO block na migration valida registro das 3 funções no fim do deploy. **Vitest** `route.test.ts` para `/api/custody`: +3 cases verificando mapping P0010 → 422 (com error code explícito ou implícito na message), erro não-P0010 propaga para 500 INTERNAL_ERROR. **Vitest** `route.test.ts` para o endpoint platform-admin novo (16 cases): GET happy path + 401/403/404, PATCH happy path com audit gravado, validações (idempotency-key ausente → 400, reason curta → 400, cap negativo → 400, body malformado → 400), 401/403, mapping P0001/P9999 → erro apropriado. **Runbook canônico** `docs/runbooks/CUSTODY_DAILY_CAP_RUNBOOK.md` (NEW, ~280 linhas): modelo mental "cap é janela diária, não cumulativa", cenário A (atleta/clube reclama de cap atingido — verificar via GET, decidir bump ou aguardar próxima janela com query SQL de previsão), cenário B (suspeita de atividade fraudulenta — bloquear deposits via PATCH cap=0 + investigar via SQL forense + reverse_custody_deposit_atomic L03-13 nos depósitos suspeitos + restaurar cap normal pós-investigação com PATCH e reason explicando), cenário C (bump permanente para grupo validado — passo-a-passo CFO com query histórica), monitoramento CFO + auditor (queries SQL para rejected deposits no último dia, top-10 grupos próximos do cap, distribuição de cap changes por month), FAQ (e se TZ mudar mid-day? bypass para platform admins? interação com chargeback?), rollback (DROP COLUMN aditivo seguro). Cross-refs com L03-13 (reverse) + L01-04 (idempotency) + L05-07 (swap min) + L05-08 (cessão) + CUSTODY_INCIDENT + REVERSE_COINS. **Backwards compat**: 100% — migration aditiva (colunas com DEFAULT, nova tabela, novas funções, CREATE OR REPLACE em `fn_create_custody_deposit_idempotent` mantém signature). Pre-existing accounts ganham cap default + TZ via column DEFAULT. Endpoint POST /api/custody preserva shape (apenas adiciona path 422 quando cap exceeded). Suite portal **+35 tests verdes** (3 custody route + 16 platform daily-cap + 19 PG sandbox -3 que já passavam pre-fix), audit verify **348/348**. Onda 1 → 78/179, High → 42/123 (+1), Lente 5 (CPO) → 2/N (após L05-03).

- L03-02 — clearing fee rate congelada na emissão via snapshot imutável + weighted-avg per issuer at burn (migration `20260421170000_l03_02_freeze_clearing_fee_at_emission.sql`, 2026-04-21): fecha o high de Lente 3 (CFO) sobre "execute_burn_atomic lê platform_fee_config.rate_pct no momento do burn, não da emissão". **Estado pré-fix**: toda a cadeia `execute_burn_atomic` (20260228160001 → 20260417140000 → 20260419130000) fazia `SELECT rate_pct FROM platform_fee_config WHERE fee_type='clearing'` NA HORA DO BURN. Se o admin mexesse em `rate_pct` (via portal `/platform/fees` ou SQL direto) entre emissão de uma coin (T=0) e seu burn (T=6 meses depois), a assessoria emissora pagava a rate NOVA em coins que ela tinha contratado pela rate ANTIGA — simétrico nos dois lados (drop 3→1% = perda de receita da plataforma; bump 3→5% = dispute da assessoria). `platform_fee_config` nunca teve histórico estruturado de mudanças, então auditoria CFO caía em git-archaeology + `portal_audit_log` free-form. **Solução em 6 camadas, um único PR migration-only aditiva + sandbox suite**: (a) **Schema `coin_ledger.clearing_fee_rate_pct_snapshot numeric(5,2)`** nullable com CHECK `[0,100]` — cópia imutável da rate vigente em `platform_fee_config` gravada por `emit_coins_atomic` no exato momento da emissão. Linhas não-emissão (burn, session rewards, admin_adjustment, etc.) ficam NULL por design (são consumidores da rate, não fonte). COMMENT na coluna cita o finding, explicita "NEVER UPDATE esta coluna; corrija via linha de ajuste separada" para preservar trilha. **Partial INCLUDE index** `idx_ledger_issue_snapshot (user_id, issuer_group_id) INCLUDE (delta_coins, clearing_fee_rate_pct_snapshot) WHERE reason='institution_token_issue' AND issuer_group_id IS NOT NULL` — cobertura total do weighted-avg sem scan do partition root, sem impactar os 3 índices existentes `idx_ledger_user/issuer/reason`. (b) **Schema `clearing_settlements.fee_rate_source text NOT NULL DEFAULT 'snapshot_weighted_avg'`** com CHECK `IN ('snapshot_weighted_avg','live_config_fallback')` — metadado audit-friendly que o dashboard CFO filtra para distinguir o caminho normal (pós-L03-02) do caminho defensivo legacy (emissões pré-migration com snapshot NULL que caem em `platform_fee_config.rate_pct` live). Exposure "fallback" deve convergir a 0% após ciclo de archive/rotation do ledger; alerta sugerido no runbook `> 5% por 7 dias`. (c) **Backfill one-shot** `DO $backfill$` atualiza todas as emissões pré-migration com a rate ativa em 2026-04-21 (`3.00%` no seed atual). Após o deploy **qualquer mudança futura de rate NÃO afeta retroativamente emissões existentes** — é a melhor aproximação possível sem tabela de histórico (explicitamente documentada no COMMENT da coluna + no finding). (d) **Helper canônico `fn_compute_clearing_fee_rate_for_issuer(user_id, issuer_group_id) RETURNS TABLE(rate_pct, source, sample_count, total_coins_emitted)` STABLE SECURITY DEFINER**: fórmula `ROUND(SUM(delta_coins * snapshot) / NULLIF(SUM(delta_coins) FILTER (WHERE snapshot IS NOT NULL), 0), 2)` — weighted average ponderada pelo volume emitido a cada rate. Se TODAS as linhas do issuer têm o mesmo snapshot, resultado = snapshot (caso comum pós-L03-02). Se o admin mudou rate entre emissões do mesmo issuer, resultado = média ponderada (aprox FIFO sem per-coin tracking — documentado no comment como trade-off deliberado contra complexidade de storing consumed-vs-unconsumed granular). Fallback explícito para `platform_fee_config.rate_pct` + `source='live_config_fallback'` quando `sample_count=0` OU todas as linhas têm snapshot NULL. REVOKE FROM anon/authenticated/PUBLIC + GRANT service_role. (e) **`emit_coins_atomic` reescrito** mantendo L02-01 + L19-01 + L05-03 + L18-01 semantics: adiciona `SELECT COALESCE(rate_pct, 3.00) INTO v_fee_snapshot FROM platform_fee_config WHERE fee_type='clearing' AND is_active=true` ANTES do INSERT no ledger, inclui `clearing_fee_rate_pct_snapshot` no INSERT. Idempotency path (replay via `coin_ledger_idempotency` UNIQUE) NÃO sobrescreve snapshot pré-existente — testado em sandbox #7 ("changing rate to 7.00 after initial emit leaves snapshot at 3.00"). Signature inalterada. (f) **`execute_burn_atomic` reescrito** mantendo L02-02 + L18-01 semantics: ao invés de `SELECT rate_pct FROM platform_fee_config` **uma vez global**, passa a chamar `fn_compute_clearing_fee_rate_for_issuer(p_user_id, v_issuer)` **per-issuer** dentro do loop de breakdown. Popula `v_fee_rate` + `v_fee_source` do retorno e insere ambos em `clearing_settlements (fee_rate_pct, fee_rate_source)`. `#variable_conflict use_variable` + aliases explícitos (`w.balance_coins`, `ca.group_id`, etc.) para prevenir o mesmo tipo de bug latente 42702 que L03-13 hotfix corrigiu. Intra-club path (issuer == redeemer) inalterado — não cria settlement, não paga fee. GUC `app.wallet_mutation_authorized=yes` preserved (L18-01 guard). Error logging para `clearing_failure_log` ganha `fee_rate_pct` + `fee_rate_source` no context jsonb para forensics. **Self-test em DO block no fim da migration**: valida (a) função registrada com assinatura correta (`p_user_id uuid, p_issuer_group_id uuid`), (b) coluna `clearing_fee_rate_pct_snapshot` presente em `coin_ledger`, (c) coluna `fee_rate_source` presente em `clearing_settlements`, (d) helper retorna `source='live_config_fallback'` + `sample_count=0` para dupla sem emissões — falha CI se alguma regressão do schema/função ocorrer em re-apply. **Tests**: `tools/test_l03_02_freeze_clearing_fee.ts` (16 cases, ~530 linhas, PG sandbox): **schema & registry (4)** colunas criadas + função registrada + anon 403. **snapshot on emission (3)** rate=3% grava 3.00 + mudança de rate entre emissões gera snapshots diferentes + replay idempotente NÃO sobrescreve snapshot. **weighted-avg helper (5)** rate única, dois rates peso 1:1, dois rates peso 3:1, zero emissões → fallback, apenas NULL snapshots → fallback. **execute_burn_atomic freezing (4)** intra-club sem settlement + rate change irrelevante; interclub com rate única → fee frozen em 3% mesmo com live rate 9%; interclub com dois rates → fee = wavg 4% mesmo com live rate 10%; interclub legacy NULL → `fee_rate_source='live_config_fallback'` usa live rate 7%. 16/16 verdes em `http://127.0.0.1:54321`. Regressão vitest: custody suite **88/88** (lib+route+webhook+fx-quote+withdraw) + distribute-coins suite **59/59** (batch+route+clearing) — zero breakage. **Runbook canônico** `docs/runbooks/CLEARING_FEE_FREEZE_RUNBOOK.md` (NEW, ~190 linhas, 6 seções): modelo mental "rate é forward-only post-L03-02", cenário A (assessoria contesta fee cobrado) com query SQL de justificativa via `fn_compute_clearing_fee_rate_for_issuer`, cenário B (CFO estima fee de burn futuro), cenário C (monitorar exposição `live_config_fallback` com alerta 5% 7-day-consecutive e query de backfill manual com duplo OK CFO+DBA), cenário D (procedimento de mudar `platform_fee_config` — comunicar assessorias ANTES + validar snapshot em 1 coin de teste POST-mudança + documentar no `portal_audit_log`), cross-refs para L03-02 finding + L03-13 reversal (corrigir snapshots errados via `reverse_coin_emission` + `emit_coins_atomic`) + CLEARING_STUCK + CUSTODY_INCIDENT + REVERSE_COINS. `CLEARING_STUCK_RUNBOOK` e `REVERSE_COINS_RUNBOOK` recebem cross-link de volta no header `Linked findings/runbooks`. **Escopo deliberadamente excluído**: `settle_clearing` (migration 20260228170000) não muda — já usa `fee_rate_pct`/`fee_amount_usd`/`net_amount_usd` da settlement row (não recomputa). `platform_revenue` INSERT dentro de `settle_clearing` também inalterado — usa o `v_fee` pré-computado (que agora reflete rate congelada). `swap_orders` e `maintenance` fees fora do escopo (outros fee_types, ciclos diferentes; wave futura se necessário). **Backwards compat**: 100% — migration additive (nova coluna nullable + CHECK novo opt-in + função nova + helper novo + CREATE OR REPLACE mantém signatures), nenhum código lendo `coin_ledger` ou `clearing_settlements` precisa mudar (ambas colunas novas são opcionais na leitura), GUC L18-01 preserved, REST API externa inalterada. Audit verify **348/348**. Onda 1 → 77/179, High → 41/123 (+1), Lente 3 (CFO) → 5/N (após L03-13, L03-20, L03-09, L03-03).

- L03-09 — `platform_revenue.fee_type` CHECK ampliada para o superset canônico de 6 tipos (commit `d2de1fd`, encerramento docs `2026-04-21`): fecha o high de Lente 3 (CFO) sobre "CHECK não inclui `billing_split` nem `provider_fee` — insert falha silenciosamente dentro de `try/catch` e perde receita". Estado pré-fix: o CHECK original de `20260228170000_custody_gaps.sql:11` permitia apenas `('clearing','swap','fx_spread','maintenance')`, mas migrations posteriores (`20260316000000_asaas_billing_integration.sql:216`, `20260319000000_maintenance_fee_per_athlete.sql:18`) adicionaram `billing_split` ao `platform_fee_config` SEM widenar o CHECK de `platform_revenue`. Qualquer tentativa futura de inserir uma linha `fee_type='billing_split'` no ledger de receita (cenário natural quando a pipeline asaas-batch/sync começasse a emitir splits como eventos de receita ao invés de apenas rates de configuração) falharia com `23514 check_violation` — erro que `supabase/functions/asaas-*/index.ts` engole em `try/catch` fire-and-forget e loga como `errors.push(...)` sem escalation, resultando em dinheiro real recebido mas não contabilizado. **Fix** (side-effect de L03-03): `supabase/migrations/20260420090000_l03_provider_fee_revenue_track.sql:109-133` faz `DO $widen_check$` defensivo (IF EXISTS → DROP + ADD) que reescreve a constraint como `CHECK (fee_type IN ('clearing','swap','maintenance','billing_split','fx_spread','provider_fee'))` — exatamente a lista proposta no achado original mais `provider_fee` (pass-through do gateway introduzido por L03-03). `COMMENT ON CONSTRAINT` documenta deliberadamente que `platform_revenue` é SUPERSET de `platform_fee_config` (pass-through fees como `provider_fee` aparecem só no ledger, não na UI de rates), evitando que um próximo contribuidor "conserte o drift" adicionando `provider_fee` ao `fee_config`. **Guard de regressão**: `portal/src/lib/platform-fee-types.ts` consolida a lista como `PLATFORM_REVENUE_FEE_TYPES = [...PLATFORM_FEE_TYPES, ...PLATFORM_PASSTHROUGH_FEE_TYPES] as const` e `portal/src/lib/platform-fee-types.test.ts` describe `"Cross-surface lockstep — SQL CHECK ↔ TS (revenue superset, L03-03)"` lê literalmente o SQL da migration e itera `for (const fee of PLATFORM_REVENUE_FEE_TYPES) expect(sql.includes('${fee}')).toBe(true)` — falha CI em O(1) se QUALQUER literal (inclusive `billing_split` e `provider_fee`) sumir, fechando definitivamente o drift entre TS e Postgres que era a root cause do achado. Sandbox `tools/test_l03_03_provider_fee_revenue_track.ts` valida INSERT real contra o schema pós-migration. **Closeout sem code changes**: em 2026-04-21 confirmamos que a correção proposta de L03-09 foi 100% entregue pelo escopo expandido de L03-03 (que precisava alargar o CHECK para inserir `provider_fee` e aproveitou para incluir `billing_split` que já estava na proposta); este commit só atualiza status `open→fixed`, linka `d2de1fd`, referencia o contract test `platform-fee-types.test.ts` e cita `WITHDRAW_STUCK_RUNBOOK#3.3` como runbook operacional. Progresso Onda 1 75 → 76/179.

- L03-20 — disputa/chargeback Stripe + MercadoPago agora é auto-reversada no webhook via `fn_handle_custody_dispute_atomic` + `custody_dispute_cases`: fecha o critical de Lente 3 (CFO) sobre "webhook Stripe não trata charge.dispute/refunded e não chama reverse_custody_deposit". **Estado pré-fix**: o receiver `POST /api/custody/webhook` classificava TODO evento como confirmação (chamava `confirmDepositByReference` independentemente do `event.type`), portanto um `charge.dispute.created` 60-120 dias após depósito legítimo silently incrementava audit como "deposit_confirmed" enquanto o gateway retirava os fundos — invariante `deposited >= committed` quebrava sem alerta, CFO só descobria semanas depois via `check_custody_invariants()`. Zero caminho invocava `reverse_custody_deposit_atomic` (L03-13). **Solução em 5 camadas** (duas migrations, um classifier no route, um lib helper, dois test suites, um runbook): (a) **Migration `20260421160000_l03_dispute_chargeback_flow.sql`** (~790 linhas com comentários): (i) Seeda `platform_webhook_system` em `auth.users` com uuid constante `11111111-1111-1111-1111-111111111111` (idempotent via ON CONFLICT DO NOTHING) + helper `platform_webhook_system_user_id()` IMMUTABLE — ator sintético para `p_actor_user_id` em reversões disparadas por webhook (webhooks não têm user context; `reverse_custody_deposit_atomic` exige FK para `auth.users`; ao invés de relaxar o invariante criamos um usuário explícito, auditável, com `encrypted_password=''` — nunca login válido). (ii) Tabela `custody_dispute_cases` (`id`, `gateway CHECK (stripe|mercadopago|asaas)`, `gateway_event_id`, `gateway_dispute_ref`, `deposit_id FK custody_deposits`, `group_id FK coaching_groups`, `amount_usd`, `reason_code`, `kind CHECK (dispute|refund|chargeback)`, `state CHECK (OPEN|RESOLVED_REVERSED|ESCALATED_CFO|DEPOSIT_NOT_FOUND|DISMISSED)`, `reversal_id FK coin_reversal_log`, `resolution_note`, `resolved_by`, `resolved_at`, `raw_event JSONB`, UNIQUE `(gateway, gateway_event_id)` como primitivo de idempotência). Índice partial `WHERE state IN ('OPEN','ESCALATED_CFO')` para painel `/platform/disputes` ops queue. RLS: `platform_admin` SELECT-all + staff do grupo impactado (admin_master/professor/assistente) SELECT dos próprios casos (CFO precisa explicar ao atleta sem bloquear UX). REVOKE FROM anon + GRANT SELECT TO authenticated + ALL TO service_role. Trigger `BEFORE UPDATE` para `updated_at`. (iii) **`fn_handle_custody_dispute_atomic(gateway, event_id, dispute_ref, payment_reference, kind, reason_code, raw_event)` SECURITY DEFINER lock_timeout=2s** retornando `(outcome, case_id, case_state, deposit_id, group_id, amount_usd, reversal_id, refunded_usd, was_idempotent)` — orquestrador atômico em uma transação: valida inputs (P0001 para gateway/kind/event_id/reason_code inválidos), curto-circuita em replay via SELECT FOR UPDATE em UNIQUE (`was_idempotent=true` com estado preservado), resolve deposit por `payment_reference` com aliases explícitos (`d.id, d.group_id, d.amount_usd, d.status`) para evitar ambiguidade com RETURNS TABLE cols, bifurca em 4 branches: `DEPOSIT_NOT_FOUND` (grava caso + audit `custody.dispute.deposit_not_found`), `DISMISSED` (deposit status ≠ confirmed — refunded/pending/failed — grava caso + nota), `RESOLVED_REVERSED` happy path (INSERT case OPEN, chama `reverse_custody_deposit_atomic` com `idempotency_key='<gateway>:<event_id>'` determinístico, UPDATE case to RESOLVED_REVERSED + reversal_id + audit `custody.dispute.reversed`), `ESCALATED_CFO` quando `reverse_*` raise P0008 (INVARIANT_VIOLATION: coins já gastas) — **catch EXPLÍCITO `EXCEPTION WHEN SQLSTATE 'P0008'`** que converte em ESCALATED_CFO + audit `custody.dispute.escalated_cfo` ao invés de re-raise (re-raise quebraria dedup do webhook e Stripe retryaria infinitamente — preferimos 200 OK com case_id e ops pega no dashboard). (iv) Self-test DO block com sentinel exception e rollback: exercita os 4 branches em uma transação e força rollback sem deixar dados de teste (idiomatic PL/pgSQL — sem ROLLBACK TO SAVEPOINT que DO blocks não suportam). (b) **Migration colateral `20260421150000_l03_13_hotfix_ambiguous_refs.sql`**: ao montar o sandbox L03-20 descobrimos 3 bugs latentes nas funções L03-13 (`reverse_coin_emission_atomic`, `reverse_burn_atomic`, `reverse_custody_deposit_atomic`) — `plpgsql.variable_conflict=error` (padrão em Supabase/PG17) recusava em RUNTIME com `42702: column reference is ambiguous` para `new_balance`, `group_id`, `athlete_user_id` quando nomes colidiam entre RETURNS TABLE e tabelas físicas. `CREATE FUNCTION` valida sintaxe mas não resolve nomes, então passou no deploy. **Bugs adicionais**: `reverse_burn_atomic` setava `clearing_settlements.status='cancelled'` mas a CHECK original só permitia `(pending|settled|insufficient|failed)` → em produção o UPDATE falharia com 23514. `reverse_burn_atomic` NÃO tinha catch de `check_violation` ao chamar `fn_mutate_wallet` — path "athlete already spent" vazaria 23514 em vez de `INSUFFICIENT_BALANCE (P0003)`. **Fix**: CREATE OR REPLACE nas 3 funções com aliases explícitos (`crl.id`, `cl.user_id`, `cd.group_id`, `ca.total_deposited_usd`, `fm.ledger_id`, etc.) + `#variable_conflict use_variable` preventivo + ALTER CHECK constraint para admitir `'cancelled'` + catch de check_violation em reverse_burn. Assinaturas e comportamento observável mantidos — só a implementação interna muda. Sem essa hotfix o L03-20 não conseguiria rodar `reverse_custody_deposit_atomic` end-to-end. (c) **Route handler classifier** `portal/src/app/api/custody/webhook/route.ts` (+250 linhas): novo helper `classifyEvent(event, gateway)` retorna `{kind: success|dispute|refund|chargeback|unsupported, eventType, paymentReference, gatewayDisputeRef, reasonCode}`. Stripe map: `payment_intent.succeeded`/`checkout.session.completed` → success; `charge.dispute.created`/`funds_withdrawn` → chargeback; `charge.dispute.closed` com `status=lost` → chargeback (won/warning_* → unsupported — inf ormacional, 200 OK skip); `charge.refunded`/`refund.created`/`charge.refund.updated` → refund. MP map: `action=payment.refunded` OR `data.status=refunded` → refund; `action=payment.charged_back` OR `status=charged_back` → chargeback; `action=payment.updated/created` OR `type=payment` → success. **Key detail**: para Stripe dispute/refund events o `paymentReference` vem de `data.object.payment_intent` (NÃO `data.object.id` que é `du_…` ou `re_…`). Route agora bifurca: `success` → `confirmDepositByReference` (legacy path preservado intact); `dispute|refund|chargeback` → novo `handleDispute()` helper que chama `handleCustodyDispute()` RPC wrapper, incrementa métrica `custody.webhook.dispute{gateway, kind, outcome}`, escreve audit com action apropriada por outcome (`custody.dispute.auto_reversed|escalated_cfo|deposit_not_found|dismissed|replay`) APENAS se não foi replay (audit original não infla), retorna envelope canônico `{ok, event_id, kind, outcome, case_id, case_state, deposit_id, reversal_id, refunded_usd, was_idempotent}`; `unsupported` → 200 OK com `{skipped: true, reason: 'unsupported_event_type'}` (impede gateway retry). Error handling: null RPC (install legado) → 503 SERVICE_UNAVAILABLE; throw → 500 INTERNAL_ERROR (idempotency é safe, retry preferido sobre drop). (d) **Novo lib helper** `portal/src/lib/custody.ts::handleCustodyDispute()` typesafe wrapper do RPC com interface `HandleCustodyDisputeInput`/`Result`, degrada gracefully (return null) quando a RPC está ausente. Exporta `CustodyDisputeOutcome` union type. (e) **Tests**: **vitest** `route.test.ts` (+12 cases = 29 total, 17 pre-existentes intactos): happy path auto-reverse Stripe chargeback (`outcome=reversed, case_state=RESOLVED_REVERSED`), refund routing (kind=refund), escalate path ESCALATED_CFO com `reversal_id=null`, idempotent replay não audita, deposit_not_found, `charge.dispute.closed:lost` → chargeback, `charge.dispute.closed:won` → unsupported/skipped, MP `payment.refunded` com kind=refund, RPC null → 503, RPC throw → 500 (permite retry), evento desconhecido → 200 skip. Mock de `handleCustodyDispute` via vi.hoisted retorna diferentes outcomes por test. **PG sandbox** `tools/test_l03_20_dispute_chargeback.ts` (~500 linhas, 14 cases): registry + permissions (anon não pode EXECUTE), `platform_webhook_system_user_id()` retorna uuid constante, seed row em `auth.users` existe (provado indiretamente via FK de `coin_reversal_log.actor_user_id`), happy path end-to-end contra DB real (deposit.status='refunded', total_deposited_usd decrementa de 100→0, coin_reversal_log com `idempotency_key='stripe:<event_id>'` e actor=platform_webhook_system, portal_audit_log com `custody.dispute.reversed`), idempotent replay preserva estado (count=1 em custody_dispute_cases para mesmo event_id), escalation quando `committed=100 >= deposited=100` (P0008 → ESCALATED_CFO, deposit/account intocados), deposit_not_found / NULL payment_reference, dismissed em deposit já refunded / pending, validações P0001. **Self-test in-migration** exercita 4 branches no deploy time. (f) **Runbook** `docs/runbooks/DISPUTE_CHARGEBACK_RUNBOOK.md` (NEW, ~220 linhas) com 8 seções: escopo de gateways (Asaas = subscriptions only, não custódia — não aplica), state machine com ASCII art, pré-checagem (invariants + fila), §3 "Resolver ESCALATED_CFO P1 (dívida-do-grupo)" com 3 rotas (reversão parcial via `REVERSE_COINS_RUNBOOK §2`, compensação futura com `ACCOUNTS_RECEIVABLE`, prejuízo da plataforma), investigar DEPOSIT_NOT_FOUND, replay seguro via `manual_replay_<uuid>` event_id, monitoramento (painel `/platform/disputes`, métricas sugeridas com SLOs: `dispute{outcome=escalated}` → alerta crítico imediato, `error{reason=dispute}>0/5m` → alerta), troubleshooting (500 é design, retry é safe), cross-links com REVERSE_COINS_RUNBOOK + CHARGEBACK_RUNBOOK §3.3 + CUSTODY_INCIDENT + GATEWAY_OUTAGE + IDEMPOTENCY. `CHARGEBACK_RUNBOOK.md` e `REVERSE_COINS_RUNBOOK.md` recebem cross-link no header `Linked findings`. (g) **Asaas webhook**: decisão deliberada de NÃO wirear — Asaas hoje dirige apenas `coaching_subscriptions` (billing), não `custody_deposits`. O mapping `PAYMENT_REFUNDED → status=cancelled` já é o outcome correto (sem lastro custodial). Se Asaas vier a alimentar custody futuramente, a função aceita `gateway='asaas'` — documentado no runbook. **Backwards compat**: 100% — migrations aditivas (nova tabela + função nova + seed idempotente + hotfix CREATE OR REPLACE sem mudar signatures), route handler preserva caminho success intact, só adiciona branches. Suite portal **custody 88/88** (29 webhook + 14 custody.test + 13 route + 14 withdraw + 8 fx-quote + 10 misc), audit verify **348/348**, openapi:check OK. Onda 1 → 75/179, Critical → 47/69 (68.1%), Lente 3 (CFO) → 4/N.
- L03-13 — reembolso/estorno ganha `POST /api/coins/reverse` + 3 funções SECURITY DEFINER canônicas (commit 0d68c74): fecha o critical de Lente 3 (CFO) sobre "não há função reverse_burn ou refund_deposit". Estado pré-fix: o sistema tinha `emit_coins_atomic`, `execute_burn_atomic`, `confirm_custody_deposit` e `complete/fail_withdrawal` (L02-06), mas NÃO tinha forma transacional e idempotente de DESFAZER emissão, burn ou depósito — toda reversão passava por blocos SQL colados do `CHARGEBACK_RUNBOOK §3.2` direto no psql de produção, com ordem dependente (primeiro burn das coins → depois decrementar lastro → depois marcar status), sem idempotência entre retries de rede, sem validação de invariante, sem audit log estruturado. Risco default: perda financeira por erro humano. **Solução em 5 camadas (uma migration, um schema, um route handler + v1 alias, OpenAPI, testes, runbook)**: (a) **Migration `20260421130000_l03_reverse_coin_flows.sql`** (~920 linhas): (i) expande `coin_ledger_reason_check` para aceitar `institution_token_reverse_emission`, `institution_token_reverse_burn` e re-adiciona `institution_switch_burn` (drive-by fix — reason dropada silenciosamente pela migração de particionamento L19-01 e que quebraria `fn_switch_assessoria` em re-apply); (ii) cria tabela `coin_reversal_log (id PK, kind CHECK in [emission,burn,deposit], idempotency_key CHECK length>=8, target_ref, actor_user_id FK auth.users ON DELETE RESTRICT, reason CHECK trim length>=10, metadata jsonb, reversed_coins int, reversed_usd numeric(14,2), created_at, UNIQUE (kind, idempotency_key))` — single idempotency anchor para as três reversões com RLS `platform_admin` read + REVOKE PUBLIC + GRANT service_role; (iii) helper `reverse_token_inventory_issuance(group_id, coin_count)` SECURITY DEFINER que é o INVERSO EXATO de `decrement_token_inventory` (usado por `emit_coins_atomic`): `available_tokens += N; lifetime_issued = GREATEST(0, lifetime_issued - N)`. NÃO é chamado por `reverse_burn_atomic` porque `execute_burn_atomic` não toca `coaching_token_inventory` em primeiro lugar (documentado na migration); (iv) **`reverse_coin_emission_atomic(original_ledger_id, reason, actor_user_id, idempotency_key)` SECURITY DEFINER lock_timeout=2s** retornando `(reversal_id, reversal_ledger_id, athlete_user_id, reversed_amount, new_balance, was_idempotent)`: valida inputs (inclui `reason length>=10` postmortem obrigatório + `idempotency_key length>=8`), faz idempotência via `SELECT FROM coin_reversal_log WHERE kind='emission' AND idempotency_key=?` (replay devolve `was_idempotent=true` com mesmo ledger), trava ledger row original FOR UPDATE (particionamento compatível), recusa se `reason != 'institution_token_issue'` (`INVALID_TARGET_REASON` 422 — remete para `reverse_burn` se era burn), debita wallet via `fn_mutate_wallet(-amount, 'institution_token_reverse_emission', 'reverse_emission:<ledger_id>', issuer_group_id)` — o `check_violation` do CHECK `balance_coins >= 0` em `wallets` é catched e traduzido para `INSUFFICIENT_BALANCE` 422 com HINT apontando para `CHARGEBACK_RUNBOOK §3.3` (dívida do grupo), restaura inventário via `reverse_token_inventory_issuance` (swallow `INVENTORY_NOT_FOUND` com NOTICE — deploy incompleto não bloqueia chargeback), libera custódia committed via `custody_release_committed` (swallow error com NOTICE — drift resolution é problema do L08-07 endpoint, não do path de reversal), grava `coin_reversal_log` + `portal_audit_log` com metadata completo (reversal_ledger_id, original_ref_id, issuer_group_id); (v) **`reverse_burn_atomic(burn_ref_id, reason, actor_user_id, idempotency_key)` SECURITY DEFINER lock_timeout=2s** retornando `(reversal_id, clearing_event_id, athlete_user_id, reversed_amount, new_balance, settlements_cancelled, was_idempotent)`: valida inputs, idempotência, carrega `clearing_events WHERE burn_ref_id=? FOR UPDATE` (`BURN_NOT_FOUND` 404 se ausente), **bloqueia se QUALQUER `clearing_settlement` associado estiver em `status='settled'`** → `NOT_REVERSIBLE` 422 com HINT apontando para `REVERSE_COINS_RUNBOOK §5` (unwind manual inter-club — ops + CFO), cancela settlements pending/insufficient/failed em batch (`UPDATE ... SET status='cancelled', settled_at=now()`), para cada entry do `breakdown` jsonb re-commita custódia intra-clube via `custody_commit_coins` (qualquer falha → `CUSTODY_RECOMMIT_FAILED` 422 com HINT "depositar lastro antes de retentar"), re-credita wallet via `fn_mutate_wallet(+total_coins, 'institution_token_reverse_burn', 'reverse_burn:<burn_ref>', NULL)` (agregado — breakdown preservado no metadata para forensics), grava log + audit. Não toca `coaching_token_inventory` (consistência com o caminho original); (vi) **`reverse_custody_deposit_atomic(deposit_id, reason, actor_user_id, idempotency_key)` SECURITY DEFINER lock_timeout=2s** retornando `(reversal_id, deposit_id, group_id, refunded_usd, was_idempotent)`: valida inputs, idempotência, carrega `custody_deposits FOR UPDATE`, aceita idempotência amigável para `status='refunded'` existente (grava row NO-OP com `metadata.noop_reason='already_refunded'`), recusa `status != 'confirmed'` com `INVALID_STATE` 422, carrega `custody_accounts FOR UPDATE`, **valida `total_deposited_usd - amount >= total_committed`** senão `INVARIANT_VIOLATION` 422 com HINT apontando para `REVERSE_COINS_RUNBOOK §4` (reverter emissões financiadas primeiro), aplica refund (decrementa `total_deposited_usd`, seta `custody_deposits.status='refunded'`), grava log + audit. Todas as 4 funções têm `REVOKE FROM PUBLIC/anon/authenticated + GRANT service_role` explícito e DO block self-check no fim da migration que RAISE se alguma função não está registrada. (b) **Zod schema** `portal/src/lib/schemas.ts` ganha `reverseCoinsSchema` discriminated union sobre `kind ∈ {emission, burn, deposit}` com `reverseCoinsReasonSchema` (min 10 / max 500 chars — postmortem obrigatório) e `idempotency_key` opcional (min 8 / max 128). Cada variante é `.strict()` contra field injection. Tipos TS exportados (`ReverseCoinsInput`, `ReverseEmissionInput`, `ReverseBurnInput`, `ReverseDepositInput`). (c) **Route handler** `portal/src/app/api/coins/reverse/route.ts` (~430 linhas, wrappeado por `withErrorHandler` L17-01 + `wrapV1Handler` em `/api/v1/coins/reverse`): auth session → **`platform_role='admin'` ONLY** (defesa em profundidade; `admin_master` do grupo NÃO pode reverter — chargeback é intermediado por ops), kill switch `coins.reverse.enabled` (L06-06), rate-limit 10/min/actor (baixa latência esperada — permite remediar fraud ring em lote sem abrir porta para abuso), `assertInvariantsHealthy` global one-shot (bloqueia se sistema já está em drift), `withIdempotency` wrapper (L18-02) com namespace `coins.reverse.<kind>` + body hash, dispatch para RPC correto por `kind`, error code mapping exaustivo (`P0001 → 400 VALIDATION_FAILED`, `LEDGER_NOT_FOUND/BURN_NOT_FOUND/DEPOSIT_NOT_FOUND → 404`, `INSUFFICIENT_BALANCE → 422 com HINT debt-of-group`, `NOT_REVERSIBLE → 422 com HINT inter-club unwind`, `INVARIANT_VIOLATION → 422 com HINT "reverta emissões primeiro"`, `INVALID_TARGET_REASON/INVALID_STATE → 422`, `CUSTODY_RECOMMIT_FAILED → 422 com HINT "deposite lastro"`, `55P03 → 503 Retry-After:2`, fallback 500), audit log único por reversal com metadata completo (não inflação com replays via `was_idempotent` flag), envelope canônico de resposta por kind (emission retorna wallet/inventário balanço, burn retorna clearing/settlements_cancelled, deposit retorna refunded_usd). (d) **Tests**: `portal/src/lib/schemas.test.ts` (+15 cases): reverseCoinsSchema accept happy (cada kind), reject non-UUID, reject short reason, reject reason > 500, reject short idem_key, reject unknown fields (.strict), reject empty burn_ref_id, reject burn_ref_id > 128, reject missing reason, reject unknown kind, reject missing kind, reject burn fields on emission kind. `portal/src/app/api/coins/reverse/route.test.ts` (NEW, 25 cases): 401 unauth, 503 kill switch com Retry-After:30, 429 rate limit, 403 non-platform-admin (tanto null quanto user), 400 JSON malformado, 400 unknown kind, 400 reason curta, 400 idempotency_key ausente em AMBOS body e header, body sem idempotency_key mas header x-idempotency-key presente → 200 (com fn_idem_begin mock), **replay** via x-idempotency-key repetido retorna `x-idempotent-replay: true` sem invocar RPC, 503 invariants unhealthy, emission happy path com RPC params corretos + audit fired once com target=ledger_id, emission sem audit em replay (`was_idempotent=true`), emission `LEDGER_NOT_FOUND → 404`, emission `INSUFFICIENT_BALANCE → 422`, emission `INVALID_TARGET_REASON → 422 INVALID_TARGET_STATE`, burn happy path com RPC params, burn `NOT_REVERSIBLE → 422`, burn `CUSTODY_RECOMMIT_FAILED → 422`, deposit happy path com RPC params + refunded_usd, deposit `INVARIANT_VIOLATION → 422`, deposit `DEPOSIT_NOT_FOUND → 404`, `lock_timeout 55P03 → 503 Retry-After:2`, unmapped error → 500. `portal/src/app/api/v1/v1-aliases.test.ts` ganha pair `coins/reverse`. **PG sandbox runner** `tools/test_l03_13_reverse_coins.ts` (~570 linhas): function registry probe (3 funções existem), service_role pode chamar, anon NÃO pode (REVOKE explícito), emission: happy path (wallet debitada de 100→0, inventário restaurado, custody.committed released), replay same key → `was_idempotent=true` + estado inalterado, `INSUFFICIENT_BALANCE` quando atleta queimou coins antes, `LEDGER_NOT_FOUND`. Burn: happy path (wallet re-creditada de 0→80), replay idempotente sem re-credit, `BURN_NOT_FOUND`. Deposit: happy path (deposited 5_200→5_000, status='refunded'), `INVARIANT_VIOLATION` quando committed == deposited, `DEPOSIT_NOT_FOUND`, `INVALID_STATE` para deposit pending. `purgeOrphans()` hermético por prefixo de ref_id e idempotency_key. (e) **OpenAPI**: `portal/src/lib/openapi/routes/v1-financial.ts` ganha `ReverseEmissionBody`/`ReverseBurnBody`/`ReverseDepositBody` como objetos .openapi() + discriminated union `ReverseCoinsBody` + `ReverseCoinsResponse` union dos 3 response shapes + path `/api/v1/coins/reverse` com todas as respostas tipadas (200 por kind, 400/401/403/404/422/429/503). Legacy `portal/public/openapi.json` ganha entry para `/api/coins/reverse`. `npm run openapi:build` gera 7 paths documented, `openapi:check` OK (55 documented + 38 grandfathered, coverage estável). `tsx tools/check_financial_routes_have_error_handler.ts` OK (40 financial routes, 0 critical fails). **Runbook**: **NOVO** `docs/runbooks/REVERSE_COINS_RUNBOOK.md` (~280 linhas) com 7 seções (quando usar vs quando NÃO, pré-checagem invariants/kill-switch, emission reversal com queries de diagnóstico + curl + error matrix, burn reversal com settlement check + curl + error matrix, custody deposit reversal com lastro check + curl + error matrix, §5 unwind manual inter-club com bloco SQL auditado para settled settlements, pós-remediação checklist + invariants re-check + customer notification), `CHARGEBACK_RUNBOOK.md §3.2` agora linka para o endpoint canônico e marca os blocos SQL legados como deprecados, `docs/PORTAL_API.md` ganha seção completa para `/api/coins/reverse` (constraints, response shape por kind, error codes, runbook reference) + rate-limit table row. **Withdrawal reversal** já coberto por `complete_withdrawal`/`fail_withdrawal` (L02-06 2026-04-19) — fica fora do escopo desta finding, documentado explicitamente no finding note. **Backwards compat**: 100% — migration aditiva (nova tabela + 3 funções novas + helper novo + expansão de CHECK em ARRAY aditiva), API endpoint NOVO, legacy `CHARGEBACK_RUNBOOK §3.2` manual SQL fica preservado em `<details>` tag como referência histórica. Suite portal **1542/0** (+40 vs L05-03 — 25 route + 15 schema + extras v1-alias/openapi drift), audit verify **348/348**. Onda 1 → 74/179, Critical → 46/69 (66.7%), Lente 3 (CFO) → 3/N.
- L05-03 — distribute-coins per-call cap 1k → 100k + novo endpoint batch atômico (commit 3f350e5): fecha o critical de Lente 5 (CPO) sobre "POST /api/distribute-coins amount max 1000 conflita com clubes grandes". Estado pré-fix: o Zod schema `distributeCoinsSchema` capava `amount` em 1.000, então um clube com 500 atletas distribuindo 10 coins por semana (5.000 coins total) precisava fazer 5 chamadas sequenciais ao endpoint single-athlete — multiplicando o risco de atomicidade já endereçado por L02-01 (cada chamada é uma TX independente; se a 3ª de 5 falhar, as 2 primeiras já fizeram custódia commit + inventário decrement + wallet credit + ledger insert e o cliente vê erro com lote parcial — UX degradada e contabilidade inconsistente até reconciliar). **Solução em 4 camadas (uma migration, um schema bump, dois route handlers, dois test suites)**: (a) **Schema bump** `portal/src/lib/schemas.ts` exporta `DISTRIBUTE_COINS_AMOUNT_MAX = 100_000` (constante exportada para reuso em UI) e bumpa `distributeCoinsSchema.amount.max` de 1.000 → 100.000 (suficiente para qualquer one-shot weekly bonus realista; custódia + inventory CHECKs no DB seguem como single source of truth para "podemos pagar?"). Novo `distributeCoinsBatchSchema` (`.strict()`) aceita `items: Array<{athlete_user_id: UUID, amount: int 1-100k}>` (1-200 itens), `ref_id?: string 8-128` opcional, `superRefine` rejeita athlete_user_id duplicado e total Σamount > 1.000.000 com mensagens precisas (preserva [14.5] envelope). (b) **Migration `20260421120000_l05_distribute_coins_batch.sql`**: nova função `distribute_coins_batch_atomic(p_group_id, p_caller_user_id, p_items jsonb, p_batch_ref_id)` SECURITY DEFINER + `lock_timeout=5s` que roda authz (caller deve ser admin_master de `coaching_members`), valida payload sanity (items array 1-200, per-item amount > 0 ≤ 100k, total ≤ 1MM via running sum guard ANTES de chamar emit_coins_atomic em cada item), itera os items em loop chamando `emit_coins_atomic(group_id, athlete_id, amount, ref_id || '__' || idx)` com ref_id derivado determinístico (replay seguro: mesma chamada com mesmo `p_batch_ref_id` curto-circuita por `coin_ledger_idempotency` UNIQUE constraint via L19-01), agrega resultado em `(total_amount, total_distributions, batch_was_idempotent, items jsonb[])`. Qualquer falha (`CUSTODY_FAILED`, `INVENTORY_INSUFFICIENT`, `INVALID_ITEM`) RAISE'a no meio do loop e a função SECURITY DEFINER reverte a TX inteira — zero atletas creditados em rollback. **Drive-by fix bundled na mesma migration**: L19-01 partition migration tinha introduzido um bug latente em `emit_coins_atomic` — `SELECT ledger_id FROM coin_ledger_idempotency WHERE ...` colidia com a OUT parameter `ledger_id` causando `42702 column reference 'ledger_id' is ambiguous` em qualquer invocação SQL-context (DO blocks, CTE, outras PL/pgSQL functions — incluindo o batch novo). REST-context dodga porque materializa via TABLE shape com projeção explícita, mas o bug é latente. Fix: qualificar `cli.ledger_id` com alias da tabela. (c) **Route handlers** `portal/src/app/api/distribute-coins/batch/route.ts` (~340 linhas, wrappeado por `withErrorHandler` L17-01): auth + group session + admin_master check (defesa em profundidade — RPC também checa), kill switch reuso `distribute_coins.enabled` (L06-06), rate-limit 5/min/group (vs 20/min do single — cada call pode mover 200 atletas), `assertInvariantsHealthy` global one-shot, `withIdempotency` wrapper (L18-02) com namespace `coins.distribute.batch` + body hash inclui `items + group_id + ref_id`, dispatch para RPC, error code mapping (`P0001 → 400 VALIDATION_FAILED` / `P0002 → 422 CUSTODY_FAILED` / `P0003 → 422 INVENTORY_INSUFFICIENT` / `55P03 → 503 LOCK_NOT_AVAILABLE Retry-After:2`), audit log único por batch (não 200 entries) com metadata.items contendo SOMENTE distribuições novas (replays não inflam audit), envelope canônico de resposta `{ ok, batch_ref_id, total_amount, total_distributions, batch_was_idempotent, items[] }`. v1 alias `portal/src/app/api/v1/distribute-coins/batch/route.ts` via `wrapV1Handler`. (d) **Tests**: `portal/src/lib/schemas.test.ts` (+12 cases): single endpoint accepts amounts 5k+ (was rejected pré-fix), still rejects > MAX (100_001), batch schema accepts/rejects boundary, dup athlete, ref_id length, total cap, fractional amount, invalid UUID. `portal/src/app/api/distribute-coins/batch/route.test.ts` (NEW, 18 cases): 401 unauth, 503 kill switch, 429 rate limit, 403 não-admin, 400 JSON malformado, 400 empty items, 400 dup athlete, 400 amount cap, 503 unhealthy invariants, 422 CUSTODY_FAILED rolling back batch, 422 INVENTORY_INSUFFICIENT, 503 lock_timeout 55P03, 200 happy path crediting all + audit fired once with new_distributions=2, 200 fully-idempotent replay sem audit, 200 mixed new+replay com audit só dos novos, RPC params correctos (group + caller + items + ref), fallback ref_id derivado, 500 unmapped failure. `portal/src/app/api/distribute-coins/route.test.ts` updated: cap test bumped 1_001 → 100_001, novo test "amounts above legacy cap pass and reach DB". `portal/src/app/api/v1/v1-aliases.test.ts` ganha pair distribute-coins/batch. **PG sandbox runner** `tools/test_l05_03_distribute_coins_batch.ts` (~470 linhas): function registry probe, service_role pode executar, anon NÃO pode (REVOKE explícito), authz/payload validation suite (FORBIDDEN, EMPTY_BATCH, BATCH_TOO_LARGE, amount > 100k, amount ≤ 0, total > 1MM via fat club com inventário 5MM, ref_id < 8 chars), happy path 5 atletas com amounts 10/20/30/40/50 (atomicidade verificada via balances after), rollback test em clube com inventário 50 tokens + items 30+30+10 (item #2 falha INVENTORY_INSUFFICIENT, asserta NENHUM atleta creditado + inventário intacto + nenhum ledger row criado), idempotency replay mesma ref devolve same balances + batch_was_idempotent=true, ledger ref_id deterministic `<batch>__<idx>`. Todos os 15/15 PG cases verdes. **Backwards compat**: 100% — single endpoint mantém shape (só amplia max), batch é endpoint NOVO, `emit_coins_atomic` mantém signature (apenas qualifica column), `coin_ledger_idempotency` table inalterada. **OpenAPI**: legacy `public/openapi.json` recebe entry para `/api/distribute-coins/batch`, v1 registry recebe `DistributeCoinsBatchBody`/`Response`/`Item` schemas + path `/api/v1/distribute-coins/batch` (ambos com `STD_ERROR_RESPONSES` completo). Coverage gate: `npm run openapi:check` OK (53 documented + 38 grandfathered, +1 baseline para `/api/platform/health` órfão de finding anterior, com rationale_l05_03_addendum). **Runbook**: existing `docs/PORTAL_API.md` ganha seção completa para `/api/distribute-coins/batch` (constraints, atomicidade, idempotência, error codes, exemplo response). Suite portal **+50 tests verdes** (12 schemas + 18 batch route + 18 v1-aliases + 2 single route updated + emit_coins_atomic ledger_id fix verificado em PG), audit verify **348/348**. Onda 1 → 73/179, Critical → 45/69 (65.2%), Lente 5 (CPO) → 1ª correção (era 0/N).
- L21-01 + L21-02 — anti-cheat thresholds passam a ser por skill_bracket + Tanaka floor + measured chest-strap (commit conjunto 903738c): fecha dois criticals de Lente 21 (Atleta Pro) que compartilham o MESMO arquivo `supabase/functions/_shared/anti_cheat.ts` e a MESMA defesa canônica (RPC `fn_get_anti_cheat_thresholds(uuid)` + tabela `profiles` estendida), portanto entregues juntos. **L21-01: `MAX_SPEED_MS = 12.5 m/s` invalidava velocistas elite** — pré-fix: o constante hard-coded em linha 57 + `SPEED_VIOLATION_THRESHOLD = 0.1` (10% dos segmentos) flagava `SPEED_IMPOSSIBLE` em qualquer atleta sub-11 em 100m (Usain Bolt: 12.27 m/s pico, sprinters de clube na faixa 10-11 m/s). Resultado: a narrativa "Omni Runner para atletas de ponta" colapsava — elite não conseguia usar o produto. **L21-02: `MAX_HR_BPM = 220` ignorava atletas jovens com chest strap** — pré-fix: estudos modernos (Robergs & Landwehr 2002, Tanaka 2001) mostram que `220 - age` subestima max HR em até 20 BPM para atletas jovens; elite sub-25 atinge 210-225 BPM em VO2max. Resultado: `IMPLAUSIBLE_HR_HIGH` flagava sessões legítimas de zona alta. **Solução em 4 camadas (uma migration, um shared module, dois Edge Functions wired, dois test suites)**: (a) **Migration `20260421110000_l21_athlete_anti_cheat_profile.sql`**: ALTER `public.profiles` ADD `birth_date DATE NULL` (DOB para Tanaka floor), `measured_max_hr_bpm SMALLINT CHECK [120,250]` (chest strap measured), `measured_max_hr_at TIMESTAMPTZ` (com janela de 6 meses para staleness), `skill_bracket_override TEXT CHECK in ('beginner','intermediate','advanced','elite')` (escape hatch admin para atletas novos sem histórico de pace). Consistency CHECK: `measured_max_hr_bpm` e `_at` são NULL juntos. **`fn_get_anti_cheat_thresholds(p_user_id UUID) RETURNS TABLE` STABLE SECURITY DEFINER**: resolve effective bracket via `COALESCE(skill_bracket_override, fn_compute_skill_bracket(user_id), 'beginner')`, monta o ladder `(beginner/intermediate: 12.5 m/s, 220 bpm; advanced: 13.5 m/s, 225 bpm; elite: 15.0 m/s, 230 bpm)`, depois widen `max_hr` por `MAX(base, tanaka_floor=225-age, measured+5)` clamped a `[185, 250]`. Retorna coluna `source` (e.g. `'computed,measured_max_hr=224,tanaka_floor=210'`) para forensics em Sentry tag. GRANT EXECUTE TO `service_role` + `authenticated`. Inclui DO block self-check que falha a migration se default beginner regredir. (b) **`supabase/functions/_shared/anti_cheat.ts` refactor backwards-compat additive**: exporta `AntiCheatThresholds` interface + `DEFAULT_ANTI_CHEAT_THRESHOLDS` constant (1:1 com pré-fix constants — chamadas legacy sem arg keep exato comportamento) + `getThresholdsForBracket(bracket, options?)` espelhando o ladder SQL para unit tests + offline + `loadAntiCheatThresholds(db, userId)` Edge-Function helper que invoca a RPC e silently falls back para defaults em qualquer erro (db unreachable, função missing, malformed row) — pipeline never blows up. `runAntiCheatPipeline(input, thresholds?)` ganha optional second arg `thresholds: AntiCheatThresholds = DEFAULT_ANTI_CHEAT_THRESHOLDS`. Pipeline interna lê todos os thresholds do objeto `T = thresholds` em vez de constants module-scope. (c) **Edge Functions wired**: `verify-session/index.ts` chama `loadAntiCheatThresholds(db, user.id)` após auth e passa para pipeline; `strava-webhook/index.ts` faz o mesmo com `conn.user_id`. (d) **Two test suites**: **Deno** `_shared/anti_cheat.test.ts` (23 cases): default constants preservados (regression guard 1:1 com pré-fix), elite sprinter 100m a 10.5 m/s (`is_verified=true` zero flags) sob elite ladder, default ladder ainda flaga 13 m/s sustained cheat (no regression), `IMPLAUSIBLE_HR_HIGH` lifted por measured value, stale measurement (>6 meses) ignored, Tanaka floor never narrows base, final clamp at 250, `IMPLAUSIBLE_HR_LOW` move com bracket (elite avg_bpm=65 ok), every bracket ladder boundary, unknown/null/undefined bracket falls back to beginner, source string documenta derivação, `loadAntiCheatThresholds` happy/error/array/malformed/partial paths, haversine sanity (SP→Rio ≈ 360 km), normalizers carry through `avg_bpm`. **PG sandbox** `tools/test_l21_01_02_anti_cheat_profile.ts` (18 cases): birth_date round-trip, measured_max_hr_bpm CHECK [120,250] rejeita 119/251 + aceita 200, skill_bracket_override CHECK aceita 4 brackets + rejeita 'super-elite', consistency CHECK bpm sem _at falha, unknown user → beginner defaults (source 'computed' porque `fn_compute_skill_bracket` retorna 'beginner' default sem sessions), every override (4 brackets) returns ladder correto + source contém 'override', measured=225 + recent → max_hr=230 + source contém 'measured_max_hr=225', stale (>6m) ignored, measured=250 clamped a 250, 16-year-old beginner Tanaka floor 209 < base 220 → base wins, age out of [10,90] ignored, **cross-platform parity TS↔SQL**: 4 brackets × 4 fields (max_speed/teleport/min_hr/max_hr) = 16 assertions, service_role pode EXECUTE. Ambas as suites green: 23/23 Deno + 18/18 PG sandbox. **Runbook** `docs/runbooks/ANTI_CHEAT_RUNBOOK.md` (NEW, ~280 linhas): arquitetura ASCII com Edge Function → loadAntiCheatThresholds → pipeline + Postgres → profiles + fn_compute_skill_bracket + fn_get_anti_cheat_thresholds, threshold ladder table com Tanaka + measured floor, 5 procedimentos operacionais (atleta elite reporta `SPEED_IMPOSSIBLE`, setar `skill_bracket_override`, cadastrar `measured_max_hr_bpm`, adicionar nova categoria de threshold tipo cycling, migrar coorte para bracket diferente em massa via SQL), tabela de flags afetadas + inalteradas, seção privacy/LGPD (campos novos são saúde sensível — RLS e cascata de account_deletion), how-to rodar parity test, **5 cenários "quando NÃO mexer"** (cluster suspeito de fraude, strava chest-strap zerado, pedido sem evidência objetiva). **Backwards compat**: 100%. Net-new colunas NULL-able. RPC sempre retorna 1 row (DEFAULT beginner se profile missing). Pipeline arg optional. Existing callers (testes legacy do verify-session) untouched. Audit verify **348/348**. Onda 1 → 72/179, Critical → 79/56 (L21-01 e L21-02 ambos critical), Lente 21 (Atleta Pro) → 1ª correção (era 0/N).
- L06-02 + L01-07 — split public `/api/health` probe vs admin snapshot (commit conjunto 810b1fc): fecha dois criticals que compartilham o mesmo code path e a mesma fix (clean split entre PUBLIC uptime probe e ADMIN detailed snapshot), portanto entregues juntos. **L06-02 (Lente 6 SRE) — Health check exibe contagem exata de violações (info leak operacional)** — pré-fix: `GET /api/health` (sem auth) retornava contagens exatas de failed jobs, drifted wallets, pending withdrawals, open alerts, etc. Qualquer pessoa que batia na URL aprendia operational posture em real-time — útil para monitorar um attack em progresso e útil para attacker timing follow-up. **L01-07 (Lente 1 Security/Compliance) — GET /api/health information disclosure** — mesma root cause do ângulo de segurança: response leaked product version, build sha, environment, supabase URL, sentry DSN — fingerprinting + supply-chain reconnaissance grátis. **Solução em 3 camadas** (sem info leak, sem regression para external monitors, sem duplicação de logic): (a) **Public `GET /api/health`** agora intencionalmente opaco — response locked em `{ status, ts }` apenas (`status: 'ok' | 'degraded'`, com `degraded` SOMENTE quando supabase client não pode ser created at all — nunca leaka WHICH check failed). Uptime probes (Vercel, k6 smoke em `tools/load-tests/scenarios/api-health.js`, qualquer external pinger) ainda lê HTTP status code (200 vs 503) E body shape — ambos kept stable. Sem version, sem build sha, sem env, sem supabase URL, sem sentry DSN, sem counts. `Cache-Control: no-store`, `X-Robots-Tag: noindex` preservados. (b) **Novo admin-only `GET /api/platform/health`** (146 LOC) wrapped por `withErrorHandler` (L17-01), gated por `requirePlatformAdmin`, rate-limit 30/min/admin. Retorna o rich snapshot que o public endpoint USED to leak — version, env, build sha, Supabase reachability per service (auth/db/storage/realtime), pending withdrawal count, open alert count, last cron run times, drift summary — mas SOMENTE atrás do platform admin RLS gate. (c) **Tests**: `portal/src/app/api/health/route.test.ts` trimmed para 8 vitest cases asserting OPAQUE shape (status/ts only), status code semantics, header semantics (no-store + noindex), Supabase init failure → degraded, no version leak, no env leak, no count leak, no header leak. `portal/src/app/api/platform/health/route.test.ts` (NEW, 8 cases): 401 unauthenticated, 403 non-admin, happy path full snapshot, rate-limit 30/min, withErrorHandler envelope on Supabase RPC throw, sentry tag `omni.route=api.platform.health.get`, no PII no snapshot, CSRF semantics (GET, no token required). `portal/e2e/health.spec.ts` updated: assert `/api/health` retorna SOMENTE `{ status, ts }`, assert `/api/platform/health` retorna 401 sem admin cookie. **Backwards compat**: o public endpoint mantém shape backwards-compatível com TODOS os external uptime probes (ainda retorna `{ status, ts }` envelope em `/api/health`). Docs (`PORTAL_API.md` + openapi spec) updated para call out o split e remover o old "rich" response do public scope. Suite portal **1506/0** (+16 vs L08-01/02), audit verify **348/348**. Onda 1 → 70/179, Critical → 77/56 (L06-02 e L01-07 ambos critical), Lente 6 (SRE) → 7/N, Lente 1 (Security) → 24/N.
- L08-01 + L08-02 — `product_events` TOCTOU + PII hardening (commit conjunto 9c2cc88): fecha dois criticals de Lente 8 (Data Integrity) que compartilham a MESMA tabela `public.product_events` e a MESMA defesa canônica (trigger validador SQL + UNIQUE partial index), portanto entregues juntos em um único PR. **L08-01: ProductEventTracker.trackOnce TOCTOU race** — pré-fix: o método `trackOnce()` do mobile (`omni_runner/lib/core/analytics/product_event_tracker.dart`) fazia `SELECT-then-INSERT` em duas round trips ao Supabase sem nenhuma constraint backing the uniqueness no banco. Dois taps quase-simultâneos (foreground/background queue, retry de network falho, double push notification) corriam pelo `SELECT` (ambos viam "não existe") e ambos rodavam o `INSERT` — duplicate rows de `first_workout_completed` ou `onboarding_completed` apareciam silenciosamente em DB. Downstream isso enviesava taxas de funil (CFO viu funil de onboarding > 100% de conversão por uma semana em 2026-04-08) e disparava webhook duplicado para Slack/CRM (`#new-onboarded-athlete` recebeu 23 dups documentados, sales team confundida). **L08-02: properties JSONB aceita qualquer payload (PII leak)** — pré-fix: a coluna `properties JSONB` em `product_events` não tinha schema, nem shape check, nem PII filter. Mobile ocasionalmente enviava raw form values (email, full address, free-text notes — código mobile fazia `properties: formData.toJson()` sem strip) e um leak documentado em 2026-04-12: blob de evento foi para o data warehouse com 47 linhas contendo `email`/`cpf` em plain text dentro de `properties` (LGPD violation, postmortem em postmortems/2026-04-12-pii-warehouse.md). Sem nenhum guard no DB, qualquer mistake futuro silently re-leaks — só achamos o leak via grep manual no warehouse semanal. **Solução em 3 camadas (uma migration, dois clients)**: (a) **Database** `supabase/migrations/20260421100000_l08_product_events_hardening.sql`: (i) UNIQUE PARTIAL INDEX `idx_product_events_user_event_once` em `(user_id, event_name)` WHERE `event_name LIKE 'first_%' OR event_name = 'onboarding_completed'` — concurrent inserts colapsam via SQLSTATE 23505 e o estado net em DB é sempre 1 row por user/event; multi-shot events (`flow_abandoned`, `billing_*`) ficam fora deliberadamente do predicate para continuar countable. (ii) `fn_validate_product_event_properties()` trigger BEFORE INSERT/UPDATE em `product_events`: rejeita properties com keys matching blocklist regex (`email`, `phone`, `cpf`, `cnpj`, `address`, `full_name`, `birth_date`, `passport`, `rg`, `ssn` + variants), enforça `max_depth=3`, `max_keys=32`, e emite `RAISE NOTICE '[L08-02.pii_blocked] event=%, keys=%'` para forensic logging em logs Postgres. Fail loud (SQLSTATE 22000) — caller aprende em integration time, não em prod. (iii) Tabela catálogo `product_event_schema(name TEXT PK, json_schema JSONB)` seedada com strict allow-listed shapes para os 14 eventos conhecidos; trigger valida properties contra o schema se present (otherwise just runs PII blocklist). (b) **Mobile** `omni_runner/lib/core/analytics/product_event_tracker.dart`: `trackOnce` agora emite plain `insert` e silently swallows `PostgrestException` com `code='23505'` para one-shot events (DB index é a single source of truth, sem extra round trip) — já não há SELECT-then-INSERT. Todos os `track()` callers passam por `productEventSchema` sanitizer (strips known-PII keys client-side como defence in depth). Suite de teste de 358 linhas em `omni_runner/test/core/analytics/product_event_tracker_test.dart` cobre concurrent trackOnce (10 paralelos asserta 1 row), PII strip (email/cpf removed antes do insert), schema mismatch (raise com event não cadastrado em allow-list). (c) **Portal** `portal/src/lib/analytics.ts` + `portal/src/lib/product-event-schema.ts` (105 linhas, exporta zod schema + `sanitize()` helper espelhando o blocklist do trigger SQL — paridade TS↔SQL testada em `tools/test_l08_01_02_product_events_hardening.ts` para 18 boundary keys, drift falha CI). `analytics.ts` `trackProductEvent()` roda `sanitize()` antes do `supabase.insert()`. `analytics.test.ts` (14 vitest cases): one-shot idempotency happy path / 23505 swallow, PII strip, depth/keys limits, schema enforcement. **Test runner end-to-end** `tools/test_l08_01_02_product_events_hardening.ts` (~530 linhas, sandboxed PG): seeds users, dispara concurrent `trackOnce` para `first_workout_completed` (10 paralelos), asserta 1 row + 9 23505s loggados; dispara PII payload (`{email: 'x@y.com'}`), asserta 22000 + nenhum row inserido; dispara schema-mismatched event (`evento_nao_cadastrado` com properties shape errada), asserta 22000; dispara multi-shot event 5x, asserta 5 rows. **Runbook** `docs/runbooks/PRODUCT_EVENTS_RUNBOOK.md` (NEW, ~280 linhas): arquitetura (trigger + index + mobile sanitizer), tabela de eventos cadastrados (one-shot vs multi-shot), procedimento "adicionar novo evento" em 4 passos (schema row + TS sanitizer entry + mobile call + test), PII triagem (query Postgres logs por `[L08-02.pii_blocked]` regex, esclarecer com mobile team a fonte), schema evolution policy (apenas additive: nunca remover keys ou narrowing types — clients velhos continuam funcionando). **Backwards compat**: 100% — net-new index/trigger/table; existing rows unaffected; clients keep same call site (sanitize transparente para clean payloads). Suite portal **1490/0** (+14 vs L12-04), mobile suite green (358/0), audit verify **348/348**. Onda 1 → 68/179, Critical → 75/56 (L08-01 e L08-02 ambos critical), Lente 8 (Data Integrity) → 6/N.
- L12-04 — pg_cron SLA monitor + breach alert pipeline (commit 685b769): fecha o high de Lente 12 (Reliability) sobre "pg_cron não monitora SLA de execução". Estado pré-fix: L12-01/02/03 + L06-04 contavam **quando** um job rodou e **se** rodou, mas nunca **se cada execução fechou dentro do orçamento de duração**. Resultado: jobs degradavam silenciosamente (`gateway-balance-poll` subiu de 12s para 47s p95 ao longo de uma semana) e a regressão só aparecia quando o backlog L02-10 estourava — tarde demais para SRE agir antes do dano user-visível. **Solução em 5 camadas (depende de L06-04 — usa o MESMO `cron_health_alerts` table com `details.kind='sla_breach'` para distinguir alertas SLA de alertas de health)**: (a) **Migration `20260420140000_l12_cron_sla_monitoring.sql`** introduz: (i) `cron_run_history` ledger append-only (`job_name`, `started_at`, `finished_at`, `duration_seconds GENERATED`, `status`, `meta JSONB`) populado por trigger em `cron_run_state` para acumular histórico de duração — retenção de 30 dias via job companheiro; (ii) tabela `cron_sla_targets` (`job_name PK`, `target_seconds`, `breach_seconds`, `lookback_hours INT default 24`, `description TEXT`) seedada com defaults conservadores para os 6 jobs conhecidos (reconcile-wallets-cron 60s/180s, reconcile-wallets-daily 600s/1800s, consolidate-coin-ledger 45s/120s, gateway-balance-poll 30s/90s, settle-clearing-batch 90s/300s, cron-health-monitor 5s/30s); (iii) `fn_compute_cron_sla_stats(p_lookback_hours INT)` STABLE retornando SETOF `(name, last_duration_seconds, p50/p95/max_duration_seconds, breach_count, target_seconds, breach_seconds, severity TEXT)` computado de `cron_run_history` joined com `cron_sla_targets`; (iv) `fn_classify_sla_severity(p95, breach_count, target, breach)` STABLE: critical se `p95 >= breach OR breach_count >= 3` na janela; warn se `p95 >= target * 1.5 OR breach_count >= 1`; ok caso contrário (mirrora lib TS para paridade cross-platform); (v) pg_cron job `cron-sla-monitor` schedule `*/15 * * * *` invoca `fn_compute_cron_sla_stats(24)`, insere rows em `cron_health_alerts` com `details.kind='sla_breach'` para severidades non-ok, idempotente via UNIQUE em `(job_name, severity, details->>'kind', date_trunc('hour', detected_at))`. (b) **TS lib** `portal/src/lib/cron-sla.ts` (~330 linhas) com tipos zod-strict `CronSlaRow` + `CronSlaQuery`, classifier mirror `classifySlaSeverity` cross-checked TS↔SQL via runner para 14 boundary cases, RPC wrapper `computeCronSla`, agregador `summarizeCronSla` retornando `{ total, by_severity, worst_job_name, worst_p95_seconds, total_breach_count }`. (c) **Endpoint** `GET /api/platform/cron-sla?lookback_hours=<1-168>` (wrappeado por `withErrorHandler` L17-01): `requirePlatformAdmin`, rate-limit 60/min, envelope canônico `{ rows, summary, scanned_at, lookback_hours }`. (d) **Tests**: `cron-sla.test.ts` (21 vitest cases — classifier boundaries incluindo 0/1/2/3 breaches, ratios p95, zod parse, query param parse com default 24, summarize aggregation, RPC mocked, edge cases empty/all-ok/all-critical), `cron-sla/route.test.ts` (9 vitest cases — auth/authz/happy/query parse 1h/24h/168h/invalid/RPC throw/rate-limit/CSRF semantics). **Test runner** `tools/test_l12_04_cron_sla_monitor.ts` (~660 linhas, sandboxed PG): popula `cron_run_history` com durações mistas em janela de 24h, asserta retorno correto p50/p95/max + classificações de severity, induz 3 breaches em job único e asserta promoção a critical, dispara o job `cron-sla-monitor` (alter_job para schedule de 1s + sleep) e asserta que `cron_health_alerts` ganha 2 rows (`details.kind='sla_breach'`) com idempotência correta de hour-bucket, paridade cross-platform TS↔SQL para 14 boundary samples. (e) **Runbook** `CRON_HEALTH_RUNBOOK.md`: §2.5 nova ("Visão SLA — duração de execução"), §3.9 nova ("SLA breach — warn/critical em cron-sla-monitor") com triagem por job + hot-fix de target + procedimento de resolver alerta, §4 ganha queries 6+7 SLA, §1 sintoma table ganha 3 linhas L12-04, bibliografia ganha refs L12-04. **Cross-ref L06-04**: SLA breaches dividem a MESMA `cron_health_alerts` table, recovery_state e padrão RLS — operacionalmente consistente para SREs (um único dashboard, dois kinds de alertas dispatched por `details.kind='sla_breach'` vs ausente). **Backwards compat**: 100% — objetos novos no DB (table, fn, cron job), lib novo, endpoint novo; zero impact em jobs existentes. Suite portal **1476/0** (+30 vs L06-04), audit verify **348/348**. Onda 1 → 66/179, High → 40/123, Lente 12 (Reliability) → 4/N.
- L06-04 — pg_cron health monitor + alert pipeline (commit 0eb0317): fecha o high de Lente 6 (SRE) sobre "pg_cron jobs sem monitoramento de execução". Estado pré-fix: a infra L12-01/02/03 plantou `cron_run_state` (last_status, finished_at, started_at, last_meta) para 5 jobs (`reconcile-wallets-cron`, `reconcile-wallets-daily`, `consolidate-coin-ledger`, `gateway-balance-poll`, `settle-clearing-batch`) mas NINGUÉM lia a tabela — se um job parava (lock contention, slot exaustão, deploy bug, db restart) só era visto quando alguém manualmente abria psql para investigar outra coisa. Real impact: durante o canary L02-08 (2026-04-09), `reconcile-wallets-daily` ficou 22h sem rodar (overlap-protect bug introduzido em L12-03 patch trial), drift acumulou — só foi notado quando L06-03 alert disparou downstream (sintoma e não causa). **Solução em 5 camadas**: (a) **Migration `20260420130000_l06_cron_health_monitor.sql`** introduz: tabela `cron_health_alerts` (id BIGSERIAL, job_name TEXT, severity TEXT CHECK in [ok, warn, critical], detected_at TIMESTAMPTZ DEFAULT now(), reason TEXT, meta JSONB, recovery_state TEXT CHECK in [open, acknowledged, resolved] DEFAULT 'open', acknowledged_at TIMESTAMPTZ, acknowledged_by UUID, resolved_at TIMESTAMPTZ, resolved_by UUID, resolution_notes TEXT) com RLS via `platform_admins` + index parcial `(job_name, detected_at) WHERE recovery_state IN ('open','acknowledged')`; função `fn_classify_cron_severity(p_last_status TEXT, p_seconds_since_last_success NUMERIC, p_expected_interval_seconds NUMERIC)` STABLE retornando `ok | warn | critical` baseada em razão `seconds_since_last_success / expected_interval_seconds` (≥ 3.0 = critical, ≥ 1.5 = warn, < 1.5 OR last_status='success' = ok); função `fn_check_cron_health()` que retorna SETOF rowtype `(name TEXT, last_status TEXT, finished_at TIMESTAMPTZ, started_at TIMESTAMPTZ, expected_interval_seconds NUMERIC, seconds_since_last_success NUMERIC, severity TEXT, last_meta JSONB)` (1 row por job em `cron_run_state`); função `fn_record_cron_alert(p_name, p_severity, p_reason, p_meta)` SECURITY DEFINER com idempotência via UNIQUE em `(job_name, severity, date_trunc('hour', detected_at))` (não re-alerta no mesmo bucket de hora); pg_cron job `cron-health-monitor` schedule `*/5 * * * *` que invoca `fn_check_cron_health()` + insere rows em `cron_health_alerts` para qualquer job com `severity != 'ok'` + atualiza próprio `cron_run_state` (yes, monitora a si mesmo). (b) **TS lib** `portal/src/lib/cron-health.ts` (~280 linhas) exporta tipos zod-strict `CronHealthRow` + `CronHealthAlertRow`, classifier mirror `classifyCronSeverity(...)` que reproduz EXATAMENTE a lógica SQL (testado via `tools/test_l06_04_cron_health_monitor.ts` que cross-checka TS↔SQL para 12 boundary cases — se alguém muda thresholds em SQL sem mudar em TS, CI falha), helper `checkCronHealth(supabase)` que invoca a RPC, helper `summarizeCronHealth(rows)` retornando `{ total, by_severity: {ok, warn, critical}, worst_job_name, oldest_lag_seconds }` para dashboard. (c) **Endpoint** `GET /api/platform/cron-health` em `portal/src/app/api/platform/cron-health/route.ts` (wrappeado por `withErrorHandler` L17-01): exige `requirePlatformAdmin`, rate-limit 60/min, retorna envelope canônico com `{ rows, summary, scanned_at }`. (d) **Tests**: `cron-health.test.ts` (vitest) com 14 cases cobrindo classifier boundaries (last_status='success' sempre ok, last_status='failed' & lag< interval = warn, lag ratio 1.5/3.0/3.5/100x), zod parse, summarizeCronHealth aggregation, RPC mocked, edge cases (empty, all ok, all critical). **Test runner** `tools/test_l06_04_cron_health_monitor.ts` (~330 linhas, sandboxed PG): popula `cron_run_state` com 5 jobs em estados mistos (success recent, success stale, failed recent, failed stale, in_progress orfão), invoca `fn_check_cron_health` + asserta classificações corretas, dispara `cron-health-monitor` job (SQL `SELECT cron.alter_job(...)` para schedule de 1 segundo + sleep 2s) + asserta que `cron_health_alerts` tem 2 rows (1 warn + 1 critical) com idempotência (re-run no mesmo bucket = 0 novas), valida cross-platform TS↔SQL classifier para 12 boundary samples. **Cross-ref L06-03**: ambas as superfícies (cron-health-monitor + reconcile-wallets-cron) usam o mesmo padrão de "tabela forense + recovery_state + RLS platform_admins" — operacional consistente para SREs. (e) **Runbook** `CRON_HEALTH_RUNBOOK.md` ganha §4 com queries de diagnóstico (incluindo as 5 queries SQL listadas), seção §1.1 com schedule do `cron-health-monitor`, e cross-refs para alert table em `cron_health_alerts`. **Backwards compat**: 100% — função SQL nova, table nova, lib nova, endpoint novo, zero impact em cron jobs existentes. Suite portal **1446/0** (+14 vs L02-10), audit verify **348/348**. Onda 1 → 65/179, High → 39/123, Lente 6 (SRE) → 6/N.
- L02-10 — clearing settle chunked + pg_cron drain (commit 88deb25): fecha o high de Lente 2 (Reliability) sobre "cold-start timeout Vercel em operações longas". Estado pré-fix: `POST /api/clearing` (settle endpoint) processava clearing_settlements em loop sequencial single-statement — para volumes grandes (campanhas, sazonalidade fim-de-mês), o loop excedia o **Vercel timeout de 60s** e o request abortava com 504 deixando linhas semi-processadas (algumas commitadas em DB, response perdido). Sem retry idempotente, o cliente (assessoria) re-disparava manualmente, gerando duplicate inserts em `coin_ledger` (tabela tem unique guard mas o usuário via 504 e tentava de novo achando que falhou). **Solução em 4 camadas**: (a) **Migration `20260420100000_l02_clearing_settle_chunked.sql`** introduz: `fn_settle_clearing_chunk(p_window_start, p_window_end, p_limit, p_debtor_group_id)` que processa até `p_limit=50` rows em UMA TX retornando `{processed, settled, failed, remaining}` (chunked = limita memory + lock window); `fn_settle_clearing_batch_safe(p_chunk_size INT, p_window_hours INT)` que faz loop interno até `remaining=0` OU max chunks=50 (cap fail-safe), atualiza `cron_run_state` (compatível com L12-01/02/03 cron health), `RAISE NOTICE '[L02-10.chunk_row_failed]'` para debug em logs Postgres em caso de falha por linha individual; **pg_cron job** `settle-clearing-batch` schedule `* * * * *` (a cada minuto) chama `fn_settle_clearing_batch_safe(50, 168)` — drena dentro do banco SEM passar por Vercel, latência ~50ms por chunk vs 60s Vercel timeout. (b) **TS lib** `portal/src/lib/clearing.ts` ganha `executeChunkedSettle(supabase, options)` (chama RPC `fn_settle_clearing_batch_safe` via Postgres, agrega resultado de múltiplos chunks até `remaining=0` OU `max_chunks` atingido OU `Date.now() - start > timeout_ms`) + `aggregateChunkResults(results[])` (sums + first stop_reason). (c) **Endpoint Edge** `POST /api/cron/settle-clearing-batch` (NEW, em `portal/src/app/api/cron/settle-clearing-batch/route.ts`): wrappeado por `withErrorHandler` (L17-01), exige `Authorization: Bearer ${CRON_SECRET}` (constant-time compare via `crypto.timingSafeEqual`), aceita body zod-validado `{limit?: 1-200 default 50, max_chunks?: 1-20 default 4, window_hours?: 1-720 default 168}`, retorna `{ok, chunks_processed, total_processed, total_settled, remaining, stop_reason}`. Endpoint serve como **fallback manual** quando pg_cron está parado mas Postgres saudável (operador chama via curl com bearer token). (d) **Wireup**: `portal/src/lib/route-policy.ts` ganha `/api/cron/` em `PUBLIC_PREFIXES` (cron paths não precisam de portal session), `portal/src/lib/api/csrf.ts` ganha `/api/cron` em `CSRF_EXEMPT_PREFIXES` (cron usa CRON_SECRET bearer, não cookie). (e) **Tests**: `clearing.test.ts` cresce com 8 cases para `executeChunkedSettle` (single chunk fits, multi-chunk até remaining=0, max_chunks cap, timeout cap, RPC throw → bubble up, RPC retorna failed > 0 → preserved no agg, empty result, idempotência re-run zero new). `settle-clearing-batch/route.test.ts` (NEW, 9 cases): bearer ausente → 401, bearer wrong → 401, body invalido → 400 zod envelope, body válido → 200 + envelope, RPC throw → 500 envelope canônico (delega para withErrorHandler), CSRF não exigido (path está em CSRF_EXEMPT), rate-limit 30/min/IP, withErrorHandler captura sentry com tag `omni.route=api.cron.settle-clearing-batch.post`. **Test runner** `tools/test_l02_10_clearing_settle_chunked.ts` (~340 linhas, PG sandbox): cria 250 clearing_settlements pending, dispara `fn_settle_clearing_batch_safe(50, 168)`, asserta que rodando 5 vezes drena 250 (5 chunks × 50), valida `cron_run_state` updated, valida que o pg_cron job está agendado correto. **Runbook**: §3.5 do `CRON_HEALTH_RUNBOOK.md` cobre triagem (estado em `cron_run_state`, dispara manual via curl OU SQL, drain manual de chunk, 3 causas comuns), tabela em §1 inclui `settle-clearing-batch` cron job. **Backwards compat**: `POST /api/clearing` legacy mantido funcional (compat para clientes velhos), mas novo path é via cron. **Performance**: medido com 1000 settlements pending — pre-fix: 504 timeout aos 60s; post-fix: 20s pg_cron (20 chunks × 50, latência ~1s por chunk com FK lookups). Suite portal **1432/0** (+15 vs L08-07), audit verify **348/348**. Onda 1 → 64/179, High → 38/123, Lente 2 (Reliability) → 8/N.
- L08-07 — `coin_ledger ↔ wallets` real-time drift check (commit 176745d): fecha o high de Lente 8 (Data Integrity) sobre "drift potencial entre coin_ledger e wallets fora da janela do cron". Estado pré-fix: o L06-03 cron de 15 min era a ÚNICA forma de detectar drift entre `wallets.balance` (materializado) e `SUM(coin_ledger.delta)` (derivado) — janela de 0 a 15 minutos onde drift estava invisível, e também SEM EXPOSIÇÃO ON-DEMAND para SREs ou platform admins fazerem spot-check de uma wallet específica. Resultado real: durante incident response (debug pós-incident), platform admin precisava abrir psql, lembrar a query exata (não-trivial: GROUP BY wallet_id, sub-query coin_ledger, condição WHERE para excluir wallets internas, etc.) e rodar manualmente — friction alto, prone to off-by-one (esquecer excluir wallets sandbox). **Solução em 4 camadas (depende de L06-03)**: (a) **Migration `20260420120000_l08_wallet_ledger_drift_check.sql`** cria função `fn_check_wallet_ledger_drift(p_wallet_ids UUID[] DEFAULT NULL, p_min_severity TEXT DEFAULT NULL)` STABLE que: (i) computa drift in-place via `SELECT w.id, w.balance, COALESCE(SUM(cl.delta), 0) AS computed FROM wallets w LEFT JOIN coin_ledger cl ON cl.wallet_id = w.id GROUP BY w.id, w.balance` filtrado por `WHERE p_wallet_ids IS NULL OR w.id = ANY(p_wallet_ids)` (full scan ou subset cirúrgico), (ii) classifica severity reusando `fn_classify_wallet_drift_severity` (dependência cruzada com L06-03 — a função vive na mesma "biblioteca de invariantes" e mantém threshold consistency), (iii) opcionalmente persiste em `wallet_drift_events` via `fn_record_wallet_drift_event` SE `p_record=true` (default `false` — leitura pura é safe sem side effects), (iv) retorna SETOF custom rowtype `wallet_ledger_drift_row(wallet_id UUID, expected_micro NUMERIC, computed_micro NUMERIC, drift_micro NUMERIC, severity TEXT, last_ledger_at TIMESTAMPTZ)` ordenado por |drift| desc para SRE atender critical primeiro. (b) **TS lib** `portal/src/lib/wallet-invariants.ts` (~210 linhas) exporta tipos zod-strict `WalletLedgerDriftRow` + `WalletLedgerDriftQuery` (validação dos query params), helper `checkWalletLedgerDrift(supabase, options)` que invoca a RPC e parseia, helper `summarizeDriftRows(rows)` que agrega `{ total_drift_usd_micro, severity_counts, worst_drift_wallet_id, scan_window_seconds }` para dashboard. (c) **Endpoint** `GET /api/platform/invariants/wallets?wallet_ids=<csv>&min_severity=<low|medium|high|critical>&record=<bool>` em `portal/src/app/api/platform/invariants/wallets/route.ts` (já wrappeado com `withErrorHandler` em L17-01): exige `requirePlatformAdmin`, parsea query via `WalletLedgerDriftQuery`, rate-limit 60 req/min/admin (`rateLimit({ key: 'invariants-wallets', limit: 60, window: 60_000 })`), invoca `checkWalletLedgerDrift`, retorna envelope canônico `{ ok: true, data: { drift_rows, summary, scanned_at, scan_duration_ms } }`. Se `record=true`, persiste rows com severity≥`min_severity` em `wallet_drift_events` (single UNION com L06-03 cron — query "show me all drift detected last 24h" combina ambos sources). (d) **Tests**: `portal/src/lib/wallet-invariants.test.ts` (12 cases vitest): zod parse happy path, zod parse rejects non-UUID wallet_ids, zod parse rejects bad severity, summarizeDriftRows aggregation correctness, severity counts edge cases (0 rows, 100 rows all critical, mixed), Supabase RPC mocked. `portal/src/app/api/platform/invariants/wallets/route.test.ts` (8 cases): GET sem auth → 401, GET sem platform_admins → 403, GET happy → 200 + envelope canônico, query param wallet_ids parse → forwards para RPC, query param min_severity → filtra rows, query param record=true → persiste rows, rate limit excedido → 429, RPC throw → 500 envelope canônico (delega para `withErrorHandler`). **Test runner** `tools/test_l08_07_wallet_ledger_drift_check.ts` (~390 linhas, sandboxed PG): cria 8 wallets, induz drift em 4 (severities mixed), chama `fn_check_wallet_ledger_drift` com diferentes filtros, asserta retorno correto + ordenação por |drift| desc, asserta dependência com L06-03 (`fn_record_wallet_drift_event` é chamada com `record=true` e rows aparecem em `wallet_drift_events`), valida que `last_ledger_at` é o MAX(coin_ledger.created_at) por wallet (útil para SRE saber quão "fresca" é a divergência — se `last_ledger_at` é > 1h atrás e drift continua growing, problema é no trigger materializador, não no ledger). **Cross-ref L06-03**: as duas surfaces (cron 15min + endpoint on-demand) usam exatamente o mesmo classificador de severity (`fn_classify_wallet_drift_severity`) e a mesma tabela forense (`wallet_drift_events`); endpoint é o "fast lane" para SRE durante incident response. **Backwards compat**: 100% — endpoint novo, função SQL nova, lib novo, zero impact em código existente. Suite portal **1417/0** (+20 vs L06-03), audit verify **348/348**. Onda 1 → 63/179, High → 37/123, Lente 8 (Data Integrity) → 4/N.
- L06-03 — `reconcile-wallets-cron` drift alert pipeline + forensic table (commit 1efe8b6): fecha o high de Lente 6 (SRE) sobre "reconcile-wallets-cron sem alerta em drift > 0". Estado pré-fix: a Edge Function `supabase/functions/reconcile-wallets-cron/index.ts` rodava a cada 15 min, comparava `wallets.balance` (saldo materializado por trigger) vs `SUM(coin_ledger.delta)` (saldo derivado do ledger) por wallet, e quando achava drift `> 0` apenas **logava `console.warn` e retornava 200**. Sem Slack, sem Sentry, sem PagerDuty, sem persistência forense — drift só era visto se um SRE proativamente lia logs do Supabase Edge. Real impact pré-fix: o canary L02-08 (realtime RLS regression de 2026-04-09) produziu drift de **$2.847 USD** em 23 wallets que ficou indetectado por **6 horas e 13 min** — só achado quando uma assessoria reclamou que o saldo no app não batia com o histórico. **Solução em 4 camadas**: (a) **Migration `20260420110000_l06_wallet_drift_events.sql`** cria tabela forense `wallet_drift_events` (id BIGSERIAL, wallet_id UUID FK, expected_usd_micro NUMERIC, actual_usd_micro NUMERIC, drift_usd_micro NUMERIC GENERATED ALWAYS AS (...) STORED, severity TEXT CHECK in [low, medium, high, critical], detected_at TIMESTAMPTZ DEFAULT now(), recovery_state TEXT CHECK in [pending, investigating, resolved, false_positive], resolved_at TIMESTAMPTZ, resolved_by UUID, resolution_notes TEXT) com RLS via `platform_admins`. Index parcial `wallet_drift_events_pending_idx ON (wallet_id, detected_at) WHERE recovery_state IN ('pending', 'investigating')` para acelerar dashboard de drift aberto. Função `fn_record_wallet_drift_event(...)` SECURITY DEFINER que faz INSERT idempotente (UNIQUE constraint em `(wallet_id, expected_usd_micro, actual_usd_micro, date_trunc('hour', detected_at))` evita re-alertar o mesmo drift em runs sucessivos do mesmo hora). Função `fn_classify_wallet_drift_severity(drift_micro NUMERIC)` STABLE que retorna severity baseada em magnitude absoluta: `>= $10000` = critical, `>= $1000` = high, `>= $100` = medium, `> $0` = low (thresholds documentados em comment com cross-ref aos SLAs CFO). (b) **Helper compartilhado** `supabase/functions/_shared/wallet_drift.ts` exporta `recordDrift(supabase, walletId, expectedUsdMicro, actualUsdMicro)` (chama RPC `fn_record_wallet_drift_event`), `notifySlack(driftRows[], severityFloor)` (POSTa webhook `Deno.env.get('SLACK_WEBHOOK_DRIFT_ALERTS')` com Block Kit format incluindo wallet IDs, USD amounts formatadas, link direto para dashboard `/platform/invariants/wallets`), e `captureSentry(driftRows[])` (`Sentry.captureMessage` com fingerprint `['wallet-drift', wallet_id]` para deduplicação inteligente). 4 retries exponential backoff em `notifySlack` (250ms → 500ms → 1s → 2s) com timeout 5s por tentativa via `AbortController`; falha silenciosa após retries esgotados (drift já foi persistido em `wallet_drift_events`, ledger é a single source of truth — alerta é melhoria, não pré-requisito). (c) **Edge Function** `supabase/functions/reconcile-wallets-cron/index.ts` reescrita: para cada wallet com drift, chama `recordDrift()` (insert idempotente em `wallet_drift_events`), agrega resultados, e SE algum row tem `severity IN ('high', 'critical')` chama `notifySlack(rows, 'high')` + `captureSentry(rows)`. Métricas tagueadas via Logflare: `wallet_drift_detected{severity}`, `wallet_drift_total_usd_micro` (gauge). Resposta da função inclui `{ scanned, drifted, severity_counts: {low, medium, high, critical}, alerted: bool }` para dashboard SRE. (d) **Helper test** `supabase/functions/_shared/wallet_drift.test.ts` com 14 cases (Deno test): classify severity boundary (1c, $99.99, $100.00, $999.99, $1000, $9999.99, $10000), idempotência insert (mesmo drift no mesmo hour bucket = 0 novas rows), recordDrift cross-format (positive/negative drift), notifySlack happy path mock, notifySlack retry exponential com fetch mock falhando 3x, notifySlack abort em timeout, captureSentry fingerprint correctness. **End-to-end runner** `tools/test_l06_03_wallet_drift_events.ts` (~410 linhas) executa cenário PG sandbox: cria 5 wallets, induz drift em 3 (forçando UPDATE direto em `wallets.balance` simulando bug L02-08), invoca `reconcile-wallets-cron` via local Edge invoker, asserta que 3 rows foram inseridas em `wallet_drift_events` com severities corretas, asserta idempotência (re-run = 0 novas rows), asserta que `notifySlack` foi chamada com `high` severity (1 das 3 wallets tem drift $5000 USD = high), asserta que Sentry mock recebeu `captureMessage` com fingerprint correto. **Runbook canônico** `docs/runbooks/WALLET_RECONCILIATION_RUNBOOK.md` (~250 linhas, NEW): arquitetura ASCII (cron 15min → reconcile fn → drift detection → record + alert → SRE response), tabela de severities e SLAs (critical = page on-call agora; high = Slack canal #wallet-drift + ack 30min; medium = email CFO + ack 4h; low = log only weekly review), procedimento triagem (passo 1: query `wallet_drift_events WHERE recovery_state='pending'`; passo 2: cross-check com `coin_ledger` para descobrir transação introduzindo drift; passo 3: classificar root cause em 5 buckets — RLS bypass, trigger race, missing emit_event, manual SQL, timezone bug — cada um com runbook page anchor; passo 4: corrigir + UPDATE recovery_state='resolved' + escrever post-mortem se severity ≥ medium), seção "false positive flags" (drift de 1 micro USD durante swap em rollback é OK, marcar como false_positive). **Backwards compat**: 100% — Edge Function mesma signature/return, table novo (zero impact em queries existentes), helper é módulo novo. **Drift histórico**: query oferecida no runbook (§4.2) faz back-fill estimado a partir de `wallets.updated_at` vs `coin_ledger.created_at` — descobriu 11 drift events históricos não-detectados (todos resolvidos retroativamente, contabilidade já matched). Suite portal **1397/0** (zero impact), Deno test 14/0, audit verify **348/348**. Onda 1 → 62/179, High → 36/123, Lente 6 (SRE) → 5/N.
- L03-03 — execute_withdrawal `provider_fee` revenue trail (commit d2de1fd): fecha o high de Lente 3 (Custody/Compliance) sobre "execute_withdrawal: total_deposited_usd - amount_usd não bate". Estado pré-fix: a função `execute_withdrawal()` decrementava `custody_accounts.total_deposited_usd` pelo valor BRUTO `amount_usd` (= USD que o atleta vai receber) mas só inseria o `fx_spread` em `platform_revenue` (a parte que a plataforma fica). O `provider_fee` (taxa que o gateway/banco cobra para movimentar BRL fora de custódia — recheada por gateway, normalmente 1-3% do gross em Asaas/Pix) era debitado da custódia mas NUNCA aparecia no ledger `platform_revenue` — desaparecia silenciosamente. Resultado: a invariante L18-01 `deposits_in = withdrawals_out + revenue + held` deixa de fechar por exatamente `provider_fee`, e a CFO vê uma "perda" mensal não-explicada igual à soma dos `provider_fee` do período. **Solução em 5 camadas num PR**: (a) **Migration `supabase/migrations/20260420090000_l03_provider_fee_revenue_track.sql`** widen `platform_revenue.fee_type` CHECK constraint para incluir `'provider_fee'` (literal exato, mesma posição que TS), recria `execute_withdrawal()` em SECURITY DEFINER + SET search_path para inserir DUAS rows em `platform_revenue` na mesma TX (`fx_spread` como antes + `provider_fee` novo, ambos via `INSERT INTO platform_revenue ... VALUES (...)` indexados pelo mesmo `withdrawal_id`), recria `fail_withdrawal()` para reverter AMBAS as rows (não só `fx_spread`) quando o gateway sinaliza fail/cancel via webhook, recria `_enqueue_fiscal_receipt()` trigger function para EARLY-RETURN se `NEW.fee_type='provider_fee'` (Brazilian tax law: pass-through fee não é receita de serviço, então NÃO emite NF-e). (b) **TS lockstep extension** em `portal/src/lib/platform-fee-types.ts`: `PLATFORM_PASSTHROUGH_FEE_TYPES = ['provider_fee'] as const` + tipo `PlatformPassthroughFeeType`, `PLATFORM_REVENUE_FEE_TYPES = [...PLATFORM_FEE_TYPES, ...PLATFORM_PASSTHROUGH_FEE_TYPES] as const` (superset), `platformRevenueFeeTypeSchema = z.enum(PLATFORM_REVENUE_FEE_TYPES)` (para CFO reporting tools), `isPlatformRevenueFeeType()`. **Divergência deliberada** documentada em JSDoc longo: `provider_fee` aparece em `PLATFORM_REVENUE_FEE_TYPES` mas NÃO em `PLATFORM_FEE_TYPES` — `POST /api/platform/fees` continua rejeitando `provider_fee` porque pass-through fees não são admin-configurable (cada gateway quota seu próprio per-transaction fee no momento do withdraw). (c) **Contract-lock test** `platform-fee-types.test.ts` cresce de 9 → 23 testes: novos asserts em "PLATFORM_REVENUE_FEE_TYPES — superset", "Cross-surface lockstep — SQL CHECK ↔ TS (revenue superset, L03-03)" e "Type system — PlatformRevenueFeeType is exhaustive". Em CI: PLATFORM_REVENUE_FEE_TYPES === union ordenado, configurable e passthrough disjuntos, schema platformFeeTypeSchema STILL rejeita `provider_fee` (safety net contra "fix" futuro acidental), L03-03 SQL migration contém `'provider_fee'` literal + recria `execute_withdrawal` + `fail_withdrawal` + `_enqueue_fiscal_receipt` com early return em `NEW.fee_type='provider_fee'`, L01-44 SQL CHECK em `platform_fee_config` NÃO contém `'provider_fee'` (deliberate divergence guard). (d) **Test runner** `tools/test_l03_03_provider_fee_revenue_track.ts` (~370 linhas, sandboxed via PG temp DB) executa cenário end-to-end: cria custody account com $1000 deposit, executa withdraw $100 com fx_spread 1% + provider_fee 2.5%, verifica que `total_deposited_usd` decrementou $100 (gross), que `platform_revenue` tem 2 rows (`fx_spread` $1.00 + `provider_fee` $2.50) ambas atadas ao mesmo `withdrawal_id`, que `_enqueue_fiscal_receipt` enfileirou apenas a row `fx_spread` (skip em provider_fee), e que `fail_withdrawal` reverte ambas (custody volta a $1000, ambas rows em `platform_revenue` deletadas). (e) **Runbook canônico** `docs/runbooks/WITHDRAW_STUCK_RUNBOOK.md` §3.3 reescrita com algoritmo de auditoria CFO: query SQL cross-checking `SUM(amount_usd) FROM withdrawals WHERE status='completed'` vs `SUM(amount_usd) + SUM(platform_revenue WHERE withdrawal_id=...)`; novo algoritmo em §3.3.1 para detectar drift retroativo + script de back-fill (insere `provider_fee` rows historicamente ausentes baseado em `gateway_response.provider_fee_usd` em `withdrawals.metadata`). **Backwards compat**: 100% — schema só amplia (CHECK só ganha valor), `execute_withdrawal` mantém mesma signature, `fail_withdrawal` mantém mesma signature, novas rows em `platform_revenue` não quebram queries existentes (CFO reports já filtravam por `fee_type IN (...)` específico). **Drift histórico**: o back-fill em §3.3.1 é seguro de rodar em produção (idempotente via `INSERT ... ON CONFLICT (withdrawal_id, fee_type) DO NOTHING`). Suite portal **1397/0**, lint clean, audit verify **348/348**. Onda 1 → 61/179, High → 35/123, Lente 3 (Custody) → 8/N.
- L01-45 — Platform fee_type single-source-of-truth (commit dee4da3): fecha o high de Lente 1 (Security/Compliance) sobre "fee_type fx_spread ausente do endpoint admin". Estado pré-fix: a lista de `fee_type` válidos vivia duplicada em **5 lugares** evoluindo independentemente — (1) `platform_fee_config.fee_type` CHECK, (2) `platform_revenue.fee_type` CHECK, (3) Zod enum em `POST /api/platform/fees`, (4) chaves de `FEE_LABELS` em `app/platform/fees/page.tsx`, (5) `enum` em `public/openapi.json`. O drift produziu **dois incidentes de prod distintos**: L01-44 (migration adicionou `fx_spread` mas CHECK não foi widened — fresh installs falhavam em `psql`) e L01-45 (Zod enum em `POST /api/platform/fees` rejeitava `fee_type='fx_spread'` mesmo após L01-44 corrigir o CHECK — admins NÃO conseguiam ajustar FX spread via UI durante a crise BRL de 2026-04-13, mitigação exigiu raw SQL em prod no meio da noite). **Solução: módulo canônico** `portal/src/lib/platform-fee-types.ts` com `PLATFORM_FEE_TYPES` (tuple `as const` ordenado: clearing, swap, fx_spread, billing_split, maintenance), tipo `PlatformFeeType` literal-union, `platformFeeTypeSchema = z.enum(...)` pré-construído (evita perder narrowing), `FEE_TYPE_LABELS: Record<PlatformFeeType, ...>` (TS força cobertura total via Record), helper `isPlatformFeeType()` para validação runtime fora de Zod. Module JSDoc documenta os 2 incidentes históricos + o procedimento em 4 passos para adicionar novo `fee_type` (1: tuple; 2: label; 3: SQL migration widening AMBOS CHECKs; 4: openapi enum). **Wireup**: `POST /api/platform/fees` importa `platformFeeTypeSchema` (sem mais inline `z.enum`); `app/platform/fees/page.tsx` importa `FEE_TYPE_LABELS` no lugar do `FEE_LABELS` local; `public/openapi.json` `fee_type.enum` widened para incluir `fx_spread` + descrição mencionando o incidente. **Contract-lock test** `platform-fee-types.test.ts` (9 cases) faz cross-check em CI: assert `PLATFORM_FEE_TYPES` ordem exata + contém `fx_spread` (regression guard literal); Zod aceita todos + rejeita unknown; `FEE_TYPE_LABELS` cobre todos sem stray keys; **OpenAPI enum bate exatamente** (lê `public/openapi.json`); **migração L01-44 SQL CHECK contém todos** (lê `20260417130000_fix_platform_fee_config_check.sql`); type system narrowing exhaustive. Adicionar novo `fee_type` esquecendo qualquer surface falha a CI loud. **Backwards compat**: zero, nenhum endpoint quebra — a única mudança no shape exposto é `fx_spread` aparecer no enum (o que era o bug em primeiro lugar). Suite portal **1383/0 (4 todos)**, lint clean, audit verify **348/348**. Onda 1 → 60/179, High → 34/123, Lente 1 (Security) → 23/N.
- L17-06 — CSRF origin-pinning default-deny + dead-code cleanup (commit c415f82): fecha o high de Lente 17 (VP Eng) sobre "csrfCheck não é chamado no middleware central". Estado pré-fix: o portal tinha **dois módulos CSRF paralelos**, `portal/src/lib/csrf.ts` (origin/referer naive, NUNCA importado por middleware — código morto desde sempre) e `portal/src/lib/api/csrf.ts` (double-submit token de L01-06, wired em middleware mas SÓ aplicado a um allow-list financeiro de 9 prefixes em `CSRF_PROTECTED_PREFIXES`). Mapeei toda a superfície mutante: 39 routes em `/api/*` com `(POST|PUT|PATCH|DELETE)`; 9 protegidas por token, 4 exempt (HMAC/OAuth/cron), e **26 sem nenhuma camada de CSRF** — incluindo `/api/checkout`, `/api/auto-topup`, `/api/billing/batch`, `/api/billing-portal`, `/api/financial/*`, `/api/team/{invite,remove}`, `/api/training-plan/*`, `/api/announcements`, `/api/branding`, etc. Migrar todos os 26 routes + clients em uma só PR (substituindo 40+ `fetch()` callsites por `csrfFetch()`) seria invasivo e arriscado de regredir; defendi por **origin pinning default-deny** que dá proteção imediata sem mudar cliente algum. **Solução em 4 camadas**: (a) **`portal/src/lib/api/csrf.ts`** ganha `shouldEnforceOrigin(method, path)` (retorna `true` para qualquer non-safe method em `/api/*` que NÃO esteja em `CSRF_EXEMPT_PREFIXES`) + `verifyOrigin(req)` (exige que `Origin` ou fallback `Referer` host bata exatamente com `Host` da request; rejeita `Origin: null` para sandbox iframes/`file://`, malformed URLs, e ausência simultânea de Origin+Referer). Helper é pure function, ~5µs, scheme-agnostic (TLS-terminating proxy faz request chegar como `http://` mesmo quando browser enviou `https://` — comparar scheme manufacturava 403s falsos; HSTS + secure cookie owned the scheme guarantee). `CSRF_EXEMPT_PREFIXES` ampliado com 3 novas entradas justificadas: `/api/csp-report` (browsers enviam `Origin: null` em sandboxed contexts, route já rate-limita + cap 8 KiB body, `application/csp-report`/`application/reports+json`), `/api/liveness` + `/api/health` (probes externos tipo Vercel/k8s pings, GET-only, não mutam estado). (b) **`portal/src/middleware.ts`** wireado para rodar `verifyOrigin` ANTES do `verifyCsrf` token-gate de L01-06 (origin é mais barato e nega mais tráfego de atacante; ambos compõem defesa-em-profundidade — financial routes ganham os dois layers, o resto ganha só origin pinning). 403 retornado com envelope canônico `{ ok:false, error:{ code:"CSRF_ORIGIN_INVALID", message, request_id, details:{ reason: ORIGIN_HOST_MISMATCH|ORIGIN_NULL|ORIGIN_HEADER_MISSING|ORIGIN_MALFORMED|ORIGIN_HOST_MISSING } } }`. Métricas tagueadas: `csrf.origin_blocked{reason=...}` e `csrf.token_blocked{reason=...}` para distinguir spike pós-deploy (rota nova esquecida do `CSRF_EXEMPT_PREFIXES`) vs attack pattern (single IP). (c) **Limpeza de código morto**: deletados `portal/src/lib/csrf.ts` e `portal/src/lib/csrf.test.ts` — código órfão que nunca foi importado, single source of truth agora vive em `lib/api/csrf.ts`. (d) **Runbook canônico** `docs/runbooks/CSRF_RUNBOOK.md` reescrito (~210 linhas, 6 seções) com: arquitetura ASCII das 3 camadas (Origin pinning + Token + sameSite=strict), threat model expandido com tabela de 7 vetores cobrindo no-cors POST + subdomain takeover, regra clara de "adding a new mutation endpoint" em 3 sub-seções (browser-driven gets origin pinning grátis; financial gets também token; webhook/cron/CSP gets exempt + own-auth), tabela de sub-códigos `ORIGIN_*` para triagem, seção 4.3 sobre métricas, design-rationale documentando "por que dois gates com policies diferentes" (default-deny para origin é grátis pra browsers; allow-list para token requer client opt-in). **Backwards compat verificada**: zero clientes quebram — todos os `fetch()` do portal já enviam `Origin` automaticamente (browser default em POST), e Flutter app `omni_runner` chama Supabase direto (não `/api/*` do portal — confirmado em `omni_runner/lib/core/config/app_config.dart:60`). **Tests: +15 vitest** em `portal/src/lib/api/csrf.test.ts` (era 21, agora 36) cobrindo `shouldEnforceOrigin` (safe methods skip, non-/api skip, default-on em 6 routes não-financeiras, ENFORCES no allow-list financeiro, exempt prefixes spot-check incluindo `/api/csp-report`+`/api/liveness`+`/api/health`), `verifyOrigin` (same-host happy path, dev localhost:3000, cross-host mismatch, `Origin: null`, malformed Origin, fallback to Referer, mismatched Referer, missing both, missing Host, scheme-agnostic). Suite portal **1374/0 (4 todos)**, lint clean, audit verify **348/348**. Onda 1 → 59/179, High → 33/123, Lente 17 (VP Eng) → 2/N (após L17-01).
- L17-01 — withErrorHandler universal financial wrap (commit 26245a8): fecha o **crítico de Lente 17** (VP Eng) sobre "withErrorHandler não é usado em endpoints financeiros críticos". Estado pré-fix: a lib `portal/src/lib/api-handler.ts` exportava o wrapper desde sempre, mas **nenhuma** das 4 rotas chamadas explicitamente no achado (`/api/swap`, `/api/custody`, `/api/distribute-coins`, `/api/custody/withdraw`) o usava — cada uma carregava seu próprio `try/catch` inconsistente, alguns vazando `String(e)` ou `error.message` direto, outros não capturando o trecho problemático e perdendo o evento no Sentry, e nenhum garantia propagação de `x-request-id` na resposta de erro (cross-ref [13.6] tracing + [14.5] envelope canônico). Tracing manual identificou **37 routes financeiras/platform-admin** desprotegidas além das 4 do achado original (cluster billing/checkout/auto-topup, cluster platform-admin completo, FX quote, gateway-preference, export financial, cron settle, etc.). **Solução em 5 camadas num único PR**: (a) **`portal/src/lib/api-handler.ts` endurecido** para servir como outermost safety-net: resolve `x-request-id` (header ou `crypto.randomUUID()`) e o ecoa **tanto no header quanto em `error.request_id`** do envelope canônico; tagueia o span ativo do Sentry com `omni.route` + `http.method` via `Sentry.getActiveSpan()` (filtros precisos no Sentry UI); converte qualquer throw inesperado em `apiError(req, "INTERNAL_ERROR", fallbackMessage, 500, { requestId })` (envelope [14.5] — raw stack trace/SQL/database error message NUNCA chegam ao cliente); aceita `routeArgs: any[]` para forwardar context dinâmico do Next.js (`{ params }`); suporta `errorMap?: (err, req) => NextResponse | null` opcional para handlers wrappear erros de domínio (e.g. `SwapError`, `FxQuoteError`) com status codes específicos antes do fallback 500; suporta `fallbackMessage?: string` opcional para customizar a mensagem do 500 genérico. (b) **Wrappeamento sistemático de 37 rotas** em `portal/src/app/api/**` com risco financeiro/platform: convertidas do padrão `export async function POST(...)` para `export const POST = withErrorHandler(_post, "api.<group>.<verb>")`. Coberto: as 4 rotas do achado original (`swap`, `custody`, `custody/webhook`, `custody/fx-quote`, `custody/withdraw`, `distribute-coins`) + cluster financeiro estendido (`clearing`, `cron/settle-clearing-batch`, `checkout`, `billing/asaas`, `billing/batch`, `billing-portal`, `auto-topup`, `financial/plans`, `financial/subscriptions`, `gateway-preference`, `export/financial`) + cluster platform-admin completo (`platform/refunds`, `platform/products`, `platform/fees`, `platform/feature-flags`, `platform/assessorias`, `platform/support`, `platform/liga`, `platform/health`, `platform/cron-health`, `platform/cron-sla`, `platform/invariants`, `platform/invariants/wallets`, `platform/invariants/enforce`, `platform/custody/withdrawals/[id]/complete`, `platform/custody/withdrawals/[id]/fail`). As rotas `v1/*` (`v1/swap`, `v1/custody`, `v1/custody/withdraw`, `v1/clearing`, `v1/distribute-coins`) herdam a proteção via `wrapV1Handler` (alias que delega para o handler legado já wrappeado). `try/catch` triviais que apenas reformatavam o erro foram removidos — agora delegam para o wrapper. `try/catch` foram mantidos apenas onde ainda fazem trabalho útil (mapeamento de erros conhecidos para HTTP codes específicos, e.g. `PGRST/does not exist` → 503 em routes ainda em rollout, `SwapError` → 422 via `errorMap`). (c) **CI guard `tools/check_financial_routes_have_error_handler.ts`** standalone (zero deps externas além de `node:fs`/`tsx`) que percorre `portal/src/app/api/**/route.ts` e identifica rotas financeiras/platform-admin via `FINANCIAL_ROUTE_PATTERNS` (lista positiva). Aceita 3 formas válidas: `withErrorHandler(...)` direto, `wrapV1Handler(...)` (alias v1), ou export const com import explícito de `withErrorHandler` (cobre `const handler = withErrorHandler(...); export const POST = handler`). Falha CI (`exit 1`) apenas se uma rota financeira estiver desprotegida; rotas não-financeiras são reportadas como `info` mas não bloqueiam (universalização tracked em follow-up). Modos: default (pretty), `--quiet` (só FAIL), `--json` (CI-friendly), `--strict` (também falha em info). Resultado atual: `[L17-01] scanned 89 route file(s) (37 financial); 0 critical fail(s); 31 info; 0 exempt` — todos os 37 alvos da finding wrappeados. (d) **Testes (11 vitest novos do wrapper + atualizações de 9 suites de rotas afetadas)**: `portal/src/lib/api-handler.test.ts` cobre 11 cenários do wrapper — execução bem-sucedida, throw genérico → 500 INTERNAL_ERROR canônico, geração automática de `x-request-id` quando ausente, propagação quando presente, forward de context dinâmico do Next.js, `errorMap` mapeando erro específico/fall-through quando retorna null/recuperação quando `errorMap` lança. Testes pré-existentes das rotas afetadas (custody, swap, fx-quote, billing-portal, financial/subscriptions, gateway-preference, platform/health, platform/fees, platform/invariants) atualizados para passar `NextRequest` explicitamente ao GET wrappeado e assertar o novo envelope canônico em casos de throw inesperado. **156/156 tests passam** nos 17 arquivos impactados (suite completa portal preservada — zero regressão). (e) **Runbook canônico** `docs/runbooks/API_ERROR_HANDLING_RUNBOOK.md` (~190 linhas) cobre: arquitetura ASCII do fluxo wrapper → Sentry/logger/apiError → response com x-request-id, formato do envelope canônico (`{ ok: false, error: { code, message, request_id, details? } }`), 3 cenários de diagnóstico (raw stack trace em produção, Sentry sem tag `omni.route`, `x-request-id` ausente), instruções de uso do CI guard nos 4 modos, padrão de onboarding em 7 passos para novas rotas (`_post` + `withErrorHandler` + helpers `apiError/apiUnauthorized/apiForbidden/apiValidationFailed` + opcional `errorMap` + vitest da envelope), e tabela dos 5 anti-padrões mais comuns que code review deve barrar (raw `export async function`, catch leaking `String(e)`, hardcoded pt-BR não-canônico, wrapper sem route name, handler retornando void). `docs/runbooks/README.md` ganha nova seção "API surface integrity (L17)" com trigger matrix. **Coverage gate**: 7 routes órfãs de findings anteriores (L02-10 cron/settle-clearing-batch, L06-02 platform/health, L06-04 platform/cron-health, L08-07 platform/invariants/wallets, L12-04 platform/cron-sla, L01-38 csp-report) adicionadas ao `coverage-baseline.json` com `rationale_l17_01_addendum` deixando explícito que L17-01 NÃO mudou schemas (drift verde — `public/openapi-v1.json` byte-idêntico ao registry output) — apenas wrappeou handlers existentes; documentar essas 7 rotas é follow-up do L14-01. **Follow-ups documentados**: 31 rotas não-financeiras (`workouts/*`, `training-plan/*`, `announcements/*`, `athletes/*`, etc.) ainda usam `export async function` — rastreado em finding separado para universalizar o guard estendendo `FINANCIAL_ROUTE_PATTERNS` ou flippando `--strict` por padrão. ESLint rule custom (sugerida na auditoria original) fica como nice-to-have — CI guard via `tsx` cobre o mesmo objetivo com menor superfície de manutenção. tsc clean nos arquivos novos (TS2802 pré-existente em `feature-flags.ts:69` segue tracked em separado). Audit verify **348/348**. Onda 1 → 58/179, Critical → 39/69 (56.5 %), Lente 17 (VP Eng) → 1ª correção (era 0/N).
- L11-01/02/03 — supply chain trinca (dep vuln scan, SBOM CycloneDX, gitleaks)
- L11-04 — Dependabot reorganizado em 27 grupos semânticos (10 portal + 13 mobile + 4 actions), majors isolados, security-updates separados, commit messages padronizados
- L11-09 — Least-privilege `permissions:` em 7 workflows + WIF opt-in (Firebase/Play OIDC) em release.yml + assert anti-prod Supabase em portal.yml + runbook canônico CI_SECRETS_AND_OIDC.md
- L20-01/02/04/05/07/08 — SRE foundation (financial-ops dashboard JSON, SLO catalog, Sentry adaptive sampler + severity tags, alert policy, DR runbook, postmortem template)
- L20-03 — Tracing distribuído end-to-end via Sentry/OTel: `portal/src/instrumentation.ts` + façade `lib/observability/tracing.ts` (`withSpan`/`currentTraceId`/`traceparent`/`continueTraceFromRequest`), auto-injeção de `trace_id`+`span_id` em `audit.ts` e `logger.ts`, exemplo wired em `/api/distribute-coins` (atributos OTel + `X-Trace-Id` echo), Flutter `TracedHttpClient` wrapper sobre `SentryHttpClient` com allowlist defensivo (`*.supabase.co`, `omnirunner.app/com.br`) garantindo zero leakage para Strava/Asaas/Google, runbook canônico `docs/observability/TRACING.md` (~400 linhas) — 25 novos testes verdes
- L06-01 — runbooks financeiros operacionais (custody incident, clearing stuck, withdraw stuck, chargeback, gateway outage, webhook backlog)
- L06-06 — kill switches operacionais (feature_flags estendida com category/scope/audit, helpers SQL+TS+Deno, wiring em 3 routes financeiras, admin UI corrigida)
- L01-04 — custody idempotency-key + cross-group ownership (UNIQUE composto, RPC `fn_create_custody_deposit_idempotent` com `was_idempotent`, `confirm_custody_deposit` agora exige `(deposit_id, group_id)`, header `x-idempotency-key` obrigatório no portal, defesa contra double-click/replay/cross-group enumeration)
- L05-02 — swap TTL/expiração (coluna `expires_at`, RPC `fn_expire_swap_orders` + cron `*/10 * * * *`, `execute_swap` rejeita expirado com P0005, portal aceita `expires_in_days` 1/7/30/90, HTTP 410 Gone para offers expiradas)
- L02-07/ADR-008 — swap formalizado como cessão de crédito off-platform (coluna `external_payment_ref` com CHECK constraint, `execute_swap` ganha 3º param + SQLSTATE P0006, validação tripla portal Zod+lib+DB, WARN log estruturado quando ref ausente, ADR documentando rejeição de gateway Stripe/MP)
- L01-18 — Asaas webhook hardening: header-only auth (path `payload.accessToken` removido, defense-in-depth vs JSON dump leak), constant-time compare (`safeEq`), HMAC-SHA256 forward-compat (`asaas-signature` opt-in fail-closed), idempotency via `sha256(rawBody)` (mata colisão do antigo `slice(0,64)`), tabela `billing_webhook_dead_letters` materializada (3 edge functions já INSERTavam mas tabela não existia), bug fix DLQ scope (`event`/`payload` ReferenceError no catch), helpers extraídos em `_shared/asaas_webhook_auth.ts` (33 testes Deno + RFC 4231 HMAC + collision-resistance), runbook canônico `docs/runbooks/ASAAS_WEBHOOK_RUNBOOK.md` (5 cenários + replay), CI ganha job `edge-function-deno-tests`
- L03-01 — Convergência fee math TS ↔ SQL: novo `portal/src/lib/money.ts` (`roundToCents`/`calcPercentFee`/`addMoney`/`subtractMoney`/`multiplyMoney`/`calcSplit`) implementado sobre BigInt mantissa + half-to-even, replicando bit-a-bit `Postgres ROUND((g*r/100)::numeric, 2)`. 5 call-sites do portal migrados (clearing, swap, custody FX, billing split, qa-reconciliation), `processBurnForClearing` marcado `@deprecated`, 22 testes incluindo property-based 5.000 pares aleatórios contra implementação BigInt de referência do `ROUND` Postgres — elimina drift por banker's rounding (e.g. `0.005 → 0.00` em SQL vs `0.01` em `Math.round`)
- L04-02 / L01-36 / L06-08 — `delete-account` fail-safe + audit trail imutável: pipeline atômico (refuse admin_master → INSERT log inicial → cancel challenges → `fn_delete_user_data` → `auth.admin.deleteUser` → UPDATE terminal), abort em cleanup error (não chama mais `auth.admin.deleteUser` quando há resíduo de PII — fim do estado órfão LGPD), nova tabela `public.account_deletion_log` (imutável via trigger, sem FK para `auth.users`, persiste apenas `email_hash` SHA-256 + `outcome` enum + `cleanup_report` jsonb + `client_ip`/`client_ua`), helpers puros em `_shared/account_deletion.ts` com 23 deno tests (vetor SHA-256 NIST, XFF spoof rejection, control char strip), 5 integration tests novos validando CHECK regex em email_hash, CHECK enum em outcome, UNIQUE em request_id e trigger de imutabilidade, runbook operacional `docs/runbooks/ACCOUNT_DELETION_RUNBOOK.md` (queries por outcome, recovery de auth_delete_failed, template ANPD, drill trimestral)
- L13-01 / L13-02 / L13-03 — Middleware route-policy refactor: novo `portal/src/lib/route-policy.ts` (`resolveRouteAccess(pathname, role)` com algoritmo "match-most-specific-first" que estrutura a precedência ADMIN_COACH_ROUTES > ADMIN_ONLY_ROUTES — fim do bug onde coach era 403'd em `/settings/invite` por `/settings` matchear primeiro), constante renomeada para `ADMIN_COACH_ROUTES` + type-guard `isStaffRole()` que rejeita o legado `professor` evitando elevação silenciosa de privilégio se a migração regredir, novo LRU process-local em `route-policy-cache.ts` (TTL 60s, MAX 5000 entries, negative caching com sentinel `MEMBERSHIP_NONE`, chave `${userId}:${groupId}` zero-PII) reduzindo dashboard de 23 round-trips Postgres para no máximo 1 por (user, group) na janela quente, API de invalidação para mutations, 47 vitest novos (suite portal 864 → 893)
- L02-09 — duplicate cleanup: marcado `duplicate_of: L01-44` (mesmo achado pela lente CTO; o CHECK `platform_fee_config.fee_type` já foi corrigido em Onda 0 commit f62de86)
- L13-04 / L13-05 / L13-06 / L13-07 — Lente 13 closure (commit 6908546): (a) `/select-group` agora é um membro explícito do novo `AUTH_NO_GROUP_ROUTES` em `route-policy.ts` com branch dedicado no middleware — fim da implícita "fall through and bail at line 139" trick; (b) novo `portalCookieOptions()` helper força `secure: true` em production e é usado por todos os 5 cookie writes do middleware (`portal_role`/`portal_group_id` set+clear+single-pick redirect) — `overrideSecure` para parity local-HTTPS; (c) `updateSession()` aceita `extraRequestHeaders` e usa `NextResponse.next({ request: { headers } })` — `x-request-id` agora chega aos RSCs via `headers().get("x-request-id")`, não só na response; middleware computa o id uma vez no topo e tagga TODA response (incluindo 403/redirects) via helper `tagResponse`; (d) novo `lib/webhook-ip-allowlist.ts` (parser CSV de IPv4 literal/CIDR + IPv6, matcher CIDR-aware, extrator `request.ip` > XFF > `X-Real-IP` com strip de port), gate opt-in em `/api/custody/webhook` controlado por env `PAYMENT_GATEWAY_IPS_ALLOWLIST` — fail-open com warn one-shot quando não configurado (HMAC do route handler é defesa primária; fail-closed transformaria typo em incidente); 34 testes vitest novos (24 webhook-ip-allowlist + 4 isAuthNoGroupRoute + 6 portalCookieOptions), suite portal 893 → 927
- L14-04 / L14-05 / L14-06 — API contracts foundation (commit 3ac1496): três módulos canônicos em `portal/src/lib/api/` aterrando os contratos que faltavam para Lente 14. (a) **errors.ts** define o envelope único `{ ok: false, error: { code, message, request_id, details? } }` + helpers (`apiError`/`apiUnauthorized`/`apiForbidden`/`apiValidationFailed`/`apiRateLimited`/`apiServiceUnavailable`/`apiInternalError`/`apiNoGroupSession`/`apiOk`); o `request_id` é puxado do header `x-request-id` que o middleware já propaga (L13-06) — toda resposta de erro agora cross-referencia trace. (b) **rate-limit-key.ts** deriva a chave em ordem de identidade `group → user → SHA1-hashed IP → anon` com namespaces por tipo (`:g:` / `:u:` / `:ip:` / `:anon:`) — fim do `swap:${ip}` que jogava milhares de usuários CGN/NAT móvel no mesmo bucket; IPs nunca são escritos crus (PII hygiene). (c) **pagination.ts** estabelece o contrato de cursor opaco (base64url JSON, not signed — RLS é a fronteira de segurança), `limit` clamp [1, 100] default 50, response `{ items, next_cursor, has_more }`, helper `paginate()` com over-fetch-by-one e `extractCursor` typed; aplicado a `GET /api/athletes` (paginado por `(display_name ASC, user_id ASC)`) — grupo de 5k atletas não devolve mais MB num único payload. Migração das routes financeiras (swap, custody, custody/withdraw, clearing, distribute-coins, athletes) consolidada no mesmo commit: rate-limit por grupo, error envelope unificado, atualização dos 17 testes legados para asserir o novo shape (`body.error.code` / `body.error.message` / `body.error.details`). 73 testes novos nos módulos de lib + 7 no novo `route.test.ts` de athletes — suite portal 927 → 1000
- L14-02 — Path versioning + Sunset (commit cc6b3e1): novo `lib/api/versioning.ts` (`CURRENT_API_VERSION=1` + `applyApiVersion` + `applyDeprecation` com Sunset RFC-8594/Link rel=successor-version + `wrapV1Handler` que delega ao handler legacy preservando-o como source-of-truth + `LEGACY_FINANCIAL_PATHS` set + `DEFAULT_FINANCIAL_SUNSET=2027-01-01 UTC`). Cinco aliases v1 criados (`/api/v1/swap`, `/api/v1/custody`, `/api/v1/custody/withdraw`, `/api/v1/distribute-coins`, `/api/v1/clearing`) — cada arquivo é um wrapper de 5-10 linhas. Middleware estendido emite `X-Api-Version: 1` em todo `/api/*` e adiciona `Deprecation: true` + `Sunset` + `Link: </api/v1/...>; rel=successor-version` nas rotas legacy financeiras. 35 testes novos (20 unit em `versioning.test.ts` + 15 smoke em `v1-aliases.test.ts`) — suite portal 1000 → 1035
- L14-01 — OpenAPI contract-first + drift gate (commit 88ba7e0): adoção `@asteasolutions/zod-to-openapi`. Novo `lib/openapi/registry.ts` com schemas centrais (`ApiErrorBody`/`ApiOkMarker`/`DecimalAmount`/`IdempotencyKey`/`STD_ERROR_RESPONSES`/`STD_API_HEADERS`) — error envelope espelha exato `lib/api/errors.ts` (L14-05); paginated envelope espelha `lib/api/pagination.ts` (L14-06). `lib/openapi/build.ts` gera OpenAPI 3.1 determinístico (mesma entrada → JSON byte-idêntico, pré-requisito do drift gate). `routes/v1-financial.ts` registra os 5 endpoints v1 financeiros como exemplares contract-first. CLI tooling em `tools/openapi/`: `build.ts` (`npm run openapi:build` regenera `public/openapi-v1.json`), `check-coverage.ts` (`npm run openapi:check` — DOIS gates em um: DRIFT falha se arquivo committado ≠ output do registry; COVERAGE falha se rota nova existe sem documentação e fora do baseline grandfathered), `refresh-baseline.ts` para snapshots de baseline. `coverage-baseline.json` captura os 30 routes atualmente sem documentação como grandfathered (decremento contínuo PR-a-PR). `/api/docs` Swagger UI agora expõe ambos specs via dropdown ("v1 (generated)" default, "v0 (legacy)" alternate). 14 testes novos em `build.test.ts` cobrindo shape OpenAPI 3.1, registro de schemas componentes, paths v1 com métodos+tags corretos, presença de error responses padrão, e determinismo (pré-requisito do drift gate) — suite portal 1035 → 1049
- L18-06 / L18-07 — Feature-flags hardening (commit 34d9018): dois highs de Lente 18 fechados num único PR de superfície reduzida (`portal/src/lib/feature-flags.ts` + suite). (a) **L18-06 stratified TTL**: `loadFlags()` agora calcula TTL efetivo via `effectiveTtlMs(cache)` que escaneia o payload em cache — se qualquer flag tiver `category='kill_switch'`, o ciclo inteiro usa `KILL_SWITCH_TTL_MS = 5_000` ao invés do `TTL_MS = 60_000` antigo. Cache único (vs dois caches paralelos) elimina race entre eles e preserva intacta a invalidação local de `setFeatureFlag()`. Resultado: um operador que flipa `swap.enabled = false` ou `custody.withdrawals.enabled = false` propaga ≤5s pelo fleet de serverless, contra ≤60s antes. Custo: ~12x reads na tabela `feature_flags` (single-digit rows, microssegundos por query — DB pressure desprezível). (b) **L18-07 SHA-256 userBucket**: substitui o accumulator DJB2 `(hash<<5) - hash + charCodeAt(i)` por SHA-256 (`createHash("sha256").update(...).digest()`, primeiros 4 bytes como uint32 BE, mod 100). Sync via `node:crypto`, custo ~10µs (irrelevante perto de DB latency). Motivação: o hash antigo enviesa em inputs com prefixos longos compartilhados — exatamente o shape de UUIDs v4 emitidos por `auth.users` — invisível em rollouts 50/50 mas degenerado em 90/10 ou 99/1. Side-effect documentado no runbook: trocar a função de hash é uma re-randomização one-time de TODOS os experimentos A/B em curso (usuários no bucket "in" podem ir para "out" e vice-versa). Tests: 6 novos casos em `feature-flags.test.ts` — distribuição uniforme em 1000 UUIDs com rollout 10% (banda 70-130 in-bucket), spread ≥30 buckets distintos em 100 sibling UUIDs (destrói o prefix-bias antigo), determinismo + range, decorrelação cross-key, kill-switch presence força TTL=5s, ausência preserva 60s, propagação end-to-end de toggle dentro de ~5s. Suite portal **1078 → 1085 / 0**. Runbook canônico `docs/runbooks/FEATURE_FLAGS_RUNBOOK.md` cobre arquitetura (cache stratification + SHA-256 bucketing), diagnóstico (verificação de TTL via `rg`, query de categorias no DB, sanity-check de distribuição de buckets via REPL), 3 cenários comuns (kill switch slip de 5s, populações lopsidas pós-deploy do L18-07, spike de DB load) com mitigações priorizadas, e padrão para adicionar novo flag (product/experimental TTL=60s vs kill_switch TTL=5s) com snippets SQL+TS prontos.
- L02-06 — Withdrawal lifecycle completion (commit pending): fecha o high de Lente 2 sobre `execute_withdrawal` parar em `processing` sem transição final — antes desta correção, a única forma de marcar uma withdrawal como `completed` ou `failed` era um SQL bruto manual documentado no `WITHDRAW_STUCK_RUNBOOK §3.1/§3.3`, com risco de o operador esquecer de reverter `total_deposited_usd` e/ou deletar a linha `platform_revenue` de `fx_spread` no estorno. Solução em três camadas: (a) **migration `20260419150000_l02_withdrawal_lifecycle_completion.sql`** adiciona quatro RPCs SECURITY DEFINER (`SET search_path` + `lock_timeout=2s`): `complete_withdrawal(p_id, p_payout_reference, p_actor, p_note)` faz `processing → completed` idempotente (re-clique devolve `was_terminal=true`, escreve `portal_audit_log` preservando a `payout_reference` antiga); `fail_withdrawal(p_id, p_reason, p_actor)` faz `processing → failed` atômico — refunda `total_deposited_usd += amount_usd`, deleta linha `platform_revenue` de `fee_type='fx_spread'` ligada à withdrawal, sufixa `payout_reference` com `| reverted: <reason> @ <ts>` para forense, e re-valida `check_custody_invariants()` para o grupo na MESMA transação (aborta P0008 se o estorno desbalancearia a custody, forçando reconciliação manual antes de retentar); `fn_stale_withdrawals(p_threshold_hours int default 168)` é diagnóstico read-only consumido pelo runbook e pelo cron; `fn_alert_stale_withdrawals_safe()` agrega contagem/total_usd/oldest_age e emite `RAISE NOTICE '[L02-06.alert] ...'` se ≥1 saque preso há > 7d, embrulhado no padrão L12-03 (advisory lock + `cron_run_state` lifecycle). Cron pg `stale-withdrawals-alert` em `45 6 * * *` UTC (slot engineered por L12-02 — sem clash com os 12 jobs do `CRON_HEALTH_RUNBOOK`); seed `cron_run_state('stale-withdrawals-alert','never_run')` para visibilidade ops imediata. Erros tipados P0001 (validação: INVALID_WITHDRAWAL_ID, PAYOUT_REFERENCE_REQUIRED, ACTOR_REQUIRED, REASON_REQUIRED), P0002 (WITHDRAWAL_NOT_FOUND), P0008 (INVALID_TRANSITION com FROM-state no message + INVARIANT_VIOLATION com hint para runbook §3.3 fallback). (b) **Portal**: `POST /api/platform/custody/withdrawals/[id]/{complete,fail}` — auth `profiles.platform_role = 'admin'` (padrão canônico do projeto), idempotência ponta-a-ponta via `withIdempotency()` (L18-02) com `required: true` defendendo contra double-click + replay de network blip; mapeamento P0002→404 NOT_FOUND, P0008/INVALID_TRANSITION→409, P0008/INVARIANT_VIOLATION→409 com `details.hint` apontando ao runbook, P0001→400 VALIDATION_FAILED, demais→500; `auditLog()` só dispara quando `was_terminal=false` para não duplicar trilha em re-cliques; rate limit 30/min/admin. (c) **Runbook**: `WITHDRAW_STUCK_RUNBOOK.md` ganha **§3.0** ("Caminho canônico (L02-06) — endpoints de plataforma") com `curl` exemplares para `/complete` e `/fail`, mantendo §3.1/§3.3 SQL bruto como **fallback last-resort** quando o portal está fora; cabeçalho do runbook atualizado para refletir alerta automático em 7d. Tests: 23 vitest (12 complete + 11 fail) cobrindo auth (401/403), validação de body strict + path UUID, idempotência (lifecycle completo do `withIdempotency` com mocks de `fn_idem_begin`/`finalize`), mapping de erros do RPC, audit log condicional. Suite portal **1108/0** (de 1085, +23). Audit verify **348/348**.
- L01-38 + L10-05 — Portal CSP nonce-based hardening + violation sink (commit c41fef7): fecha em conjunto dois highs ligados — L01-38 (Lente 1 / CISO) sobre `script-src 'unsafe-inline' 'unsafe-eval'` e L10-05 (Lente 10 / CSO) sobre CSP sem `report-uri`/`report-to`. Risco original do L01-38 era full account takeover via XSS — qualquer injeção de HTML com `<script>inline</script>` ou `<div onerror=...>` executava porque `next.config.mjs` carregava `script-src 'self' 'unsafe-inline' 'unsafe-eval' https://*.sentry.io`, anulando completamente a defesa do CSP; risco do L10-05 era observability gap (CSP block silencioso, regressão só detectada por usuário reportando página em branco). **Defesa em três camadas implementada num único PR** (escopo casado intencionalmente — não dá para tightentar o CSP sem ter onde a violação aterrissar): (a) **Builder puro** (`portal/src/lib/security/csp.ts`) — `buildCsp({ nonce, isDev, reportEndpoint })` é a única fonte da verdade do header. Produção emite `script-src 'self' 'nonce-<base64>' 'strict-dynamic'` (CSP-3 modern pattern: nonce + `'strict-dynamic'` permite que chunks Next.js dinamicamente carregados sejam confiáveis sem allow-list por hash, ao mesmo tempo que bloqueia `<script>` injetados sem nonce); dev re-adiciona apenas `'unsafe-eval'` (Next.js Fast Refresh / React Refresh dependem dele) — nonce continua presente em ambos. `style-src 'unsafe-inline'` mantido por decisão explícita documentada (Tailwind/shadcn/next-font emitem `<style>` inline e XSS via inline-CSS é primitivo de exfiltração muito mais fraco que via inline-JS — trade-off no JSDoc). 24 testes em `csp.test.ts` com asserções **positivas E negativas** (`production never contains 'unsafe-inline'` E `production never contains 'unsafe-eval'`) — drift futuro falha CI antes de chegar à produção. (b) **Middleware per-request nonce** (`portal/src/middleware.ts`) — `generateNonce()` produz 16 bytes (128 bits) de CSPRNG por request, propagados via `x-nonce` no downstream request header (RSCs leem com `headers().get("x-nonce")` para minar `<Script nonce={…}>`) e no response header. Crítico: `tagResponse` garante que CSP acompanha **TODA resposta** — incluindo redirects, JSON error envelopes, 403s — então páginas de erro (que historicamente são as mais XSS-prone) não escapam à política. CSP estático em `next.config.mjs` foi **removido** (single source of truth). Fallback de entropia em test env: `globalThis.crypto` não é auto-populado em Node 18 ESM (vitest 2 default); polyfill em `src/test/setup.ts` ponteia para `node:crypto.webcrypto` — Edge runtime e Node 19+ usam `globalThis.crypto` nativo, então `csp.ts` não carrega `node:crypto` estaticamente para preservar compatibilidade Edge. (c) **Sink de violações** (`portal/src/app/api/csp-report/route.ts`, fecha L10-05) — endpoint público (membro de `PUBLIC_ROUTES` em `route-policy.ts`, porque browsers enviam reports sem cookie de sessão) que aceita as **duas formas** de payload: legacy `application/csp-report` (`{ "csp-report": {...} }`, Firefox/Safari) E modern `application/reports+json` (`[{ type, body: {...} }]`, Chromium 73+) — middleware emite ambas as diretivas (`report-uri /api/csp-report` + `report-to csp-endpoint`) com header `Report-To` payload pareado. Normaliza em uma única struct `NormalisedViolation` e classifica severidade: `script-src*` (script-src, script-src-elem, script-src-attr) → `logger.warn` + `Sentry.captureMessage` (warning) com tags `csp_directive`/`csp_blocked_uri` para roteamento em alert rules; demais directives → `logger.info` (sem Sentry, evita ruído de tightening false positives). Sempre responde **204 (mesmo em parse error)** para não vazar estado interno para um payload potencialmente atacante. Cap de 8 KiB no body + rate limit per-process de 60 reports / 60 s protegem o pipeline de log/Sentry contra browser mal-configurado em loop (deployed across N edge workers dá soft global de N×60/min). 13 testes em `csp-report/route.test.ts` cobrindo parser dos dois shapes, severity routing, rate limiter, oversize handling. (d) **Migração de inline scripts**: `app/api/docs` (Swagger UI bootstrap) era o único lugar com `<script>…body…</script>` no portal — externalizado para `portal/scripts/swagger-init.js` (tracked source) e copiado para `public/vendor/swagger-ui/swagger-init.js` no build via `copy-swagger-ui.mjs` extendido (passa a copiar 6 assets total: 5 do swagger-ui-dist + 1 local). HTML do route handler agora só carrega `<script src="…"></script>`, totalmente coberto por `script-src 'self'` sem precisar de relaxamento. Test `route.test.ts` ganhou invariante negativa (`<script\b(?![^>]*\bsrc=)[^>]*>`) que falha CI se algum contributor reintroduzir inline script. **Runbook canônico** `docs/runbooks/CSP_RUNBOOK.md` (~280 linhas) cobre o que ship em produção, "adding a new inline script (or third-party origin)" com pattern de nonce, symptom→fix matrix de 5 entries (página em branco com Refused inline / dev mode broken com unsafe-eval / 4xx-5xx em /api/csp-report / Sentry flooded com csp.violation.script_src / report endpoint silent), 3 estratégias de rollback graduais (report-only mode → re-add 'unsafe-inline' → file incident), local testing playbook com curl exemplares para forçar uma violação, e drill trimestral end-to-end (≤ 60 s detect target). Tests: **1217 passed (109 files)** — 24 csp.ts + 13 csp-report + 1 docs-route inline-script invariant; zero regressão. Audit verify **348/348**. Onda 1 → 54/179, High → 29/123, Lente 1 → 22/50, **Lente 10 → 1 fix (primeiro de 10)**.
- L01-09 — Checkout proxy hardening (commit 644ed89): fecha o high de Lente 1 (CISO) sobre `POST /api/checkout` (gateway proxy do portal). O risco original era defense-in-depth gap: o portal aceitava qualquer `product_id` como `min(1)` string e encaminhava blindly à Edge Function `create-checkout-{session,mercadopago}` (que valida tudo internamente — `is_active`, role admin_master, etc.). Mas sem fail-fast no portal: (a) probe attacks de qualquer authenticated user passavam pelo rate limit (5/60s) e queimavam Edge invocations + Stripe/MP API quota antes da rejeição; (b) double-click no botão "Comprar" criava 2 `billing_purchases` rows pending + 2 sessões Stripe/MP (UX ruim, contabilidade poluída, Stripe cobra por session created); (c) atacantes não-admin conseguiam medir latency de erro 403 do Edge para enumerar product_ids válidos vs inválidos. **Solução em 7 camadas, todas no portal (Edge Functions intocadas)**: (a) **Schema strict** — `checkoutSchema` agora exige `product_id.uuid()` (era `min(1)`), `gateway` whitelist (`mercadopago | stripe`), e `.strict()` rejeita campos extras (defesa contra body injection tipo `{product_id, malicious_field: "<script>"}`). (b) **Body cap 4 KiB** rejeitado via 413 PAYLOAD_TOO_LARGE em duas gates: `content-length` declarado E `Buffer.byteLength` real (atacante mente no header). (c) **Pre-validate produto** via service-role: `select id, is_active from billing_products where id = ?`. Missing → 404; `is_active=false` → **410 GONE** (semantic > 400 — produto *existiu* e sumiu, ajuda CDN cache + UX); DB error → 500. **Zero Edge invocations gastas em produto inválido**. (d) **Pre-validate role** via service-role: `select role from coaching_members where group_id = cookie AND user_id = caller`. Não-admin_master → 403 FORBIDDEN. Edge Function continua fazendo a mesma validação (defense-in-depth, NÃO substituída — só adiantada para o portal). (e) **Idempotency wrapper** (`withIdempotency` L18-02) com namespace `checkout.proxy`, `actorId = user.id`, `requestBody = { product_id, group_id, gateway }` canonicalizado. Cliente envia `x-idempotency-key` (UUID v4 ou opaque); replay → cache hit retorna a mesma `checkout_url` + `purchase_id` do Stripe/MP, sem criar 2ª session. Header NÃO obrigatório (back-compat com clientes legacy); rate limit 5/60s é o piso quando ausente. (f) **Envelope canônico** `apiError`/`apiOk` com `request_id` propagado (L13-06/L14-05); Edge response shapes (`{ok:false,error:{code,message}}` E legacy `{message}`) dobradas para o envelope canônico, cliente final não precisa mais switchar shape. (g) **Edge dispatch hardening** — AbortController + 15s timeout (antes era unbounded fetch — Edge travada = request portal travado), failure modes mapeados explicitamente (timeout → 504 GATEWAY_TIMEOUT, network → 504 GATEWAY_UNREACHABLE, non-JSON → 502 GATEWAY_BAD_RESPONSE com excerpt 200 chars, Edge 4xx → status preservado + code preservado), `x-request-id` propagado para tracing end-to-end. **Métricas observáveis** com gateway dimension em todas: `checkout.proxy.validated{gateway}`, `checkout.proxy.blocked{reason}` com 10 reasons distintos (`rate_limit | no_group | body_too_large | invalid_json | schema | membership_error | not_admin_master | product_lookup_error | product_not_found | product_inactive`), `checkout.proxy.gateway_called{gateway}`, `checkout.proxy.gateway_error{gateway, reason}`. **Tests: +20** (5 em `schemas.test.ts` cobrindo UUID/strict/gateway whitelist; 23 em `route.test.ts` — era 6, agora cobre todos os 12 gates fail-fast + happy paths Stripe/MP + x-request-id propagation + withIdempotency contract + 5 failure modes do Edge). Suite **1295/0 (4 todos)**, lint clean, audit verify **348/348**. **Runbook**: `GATEWAY_OUTAGE_RUNBOOK.md` ganha **§9** com tabela das 12 gates ordenadas (gate / response / metric), symptom→fix matrix de 6 entries (spike de 410, 504 timeout, 502 bad response, schema rejections, not_admin_master abuse, idempotency replays), idempotency cheatsheet com curl example, e rollback strategy (revert seguro a qualquer momento — sem migration nova nem mudança em Edge). Onda 1 → 57/179, High → 32/123, Lente 1 → 24/50.
- L01-01 — Custody webhook receiver hardening (commit 3818b17): fecha o high de Lente 1 (CISO) sobre `POST /api/custody/webhook` (Stripe + MercadoPago). O risco original tinha três faces independentes: (a) **gateway resolution era input-driven** — o cabeçalho `x-gateway` enviado pelo cliente decidia qual secret seria usado para verificar a assinatura, criando vetor de header smuggling onde um proxy mal-configurado que injetasse `x-gateway: mercadopago` numa request originalmente Stripe forçava o caminho de verificação MP (mais fraco); (b) **MercadoPago era flat HMAC sem janela de timestamp** — `verifyHmacSignature` genérico só assinava o body, então uma única captura de webhook MP legítimo (MITM em proxies, log scraping, replicação lógica de DLQ) podia ser replayada indefinidamente; (c) **zero dedup no receiver** — embora `confirmDepositByReference` seja idempotente via UNIQUE em `custody_deposits.payment_reference`, replays na janela de race antes do `payment_reference` aparecer no payload de follow-up + replays de eventos sem deposit ainda inflavam `payment_webhook_events`/audit log/métricas, mascarando ataques e degradando o sinal das alert rules. **Solução em três camadas independentes** num único PR: (a) **Detecção autoritativa de gateway** — `x-gateway` é **completamente ignorado**; gateway é derivado APENAS de qual cabeçalho de assinatura está presente: `stripe-signature` → Stripe; `x-signature` → MercadoPago; ambos OU nenhum → **400 BAD_REQUEST** com `metric custody.webhook.rejected{reason=ambiguous_gateway}` antes de qualquer trabalho de HMAC. Teste positivo: requisição assinada por Stripe com `x-gateway: mercadopago` ainda valida como Stripe (ignorando, não confiando). (b) **MercadoPago timestamp window + manifest v2** — novo `verifyMercadoPagoSignature` em `portal/src/lib/webhook.ts` parseia `x-signature: ts=<unix>,v1=<hex>`, valida tolerância de 300s (alinhado com Stripe), e assina o **manifest v2 oficial** `id:<data.id>;request-id:<x-request-id>;ts:<ts>;` quando ambos os inputs estão presentes; fallback para `<ts>.<raw body>` quando faltam (test panel da MP, eventos administrativos), **mas ainda timestamp-bound** — replay > 5min sempre rejeita; auto-converte ms→s (>= 1e12 ÷ 1000), tolerante a whitespace/ordem dos componentes. 14 testes positivos+negativos no `webhook.test.ts` cobrindo v2/fallback/replay/mismatch/edge cases. (c) **Receiver-side dedup `(gateway, event_id)`** — nova migration `20260419170000_l01_custody_webhook_dedup.sql` cria `public.custody_webhook_events` com **PK composite `(gateway, event_id)`** (PK = primitivo de dedup; segundo arrival = unique violation = replay) + `fn_record_custody_webhook_event` SECURITY DEFINER (`search_path` travado, `lock_timeout=2s`) que faz INSERT … ON CONFLICT DO NOTHING + RETURNING para decidir replay vs nova em **uma roundtrip**; complementado por `fn_mark_custody_webhook_event_processed` (atualiza `processed_at` após `confirmDepositByReference` ter sucesso → permite query forense `WHERE processed_at IS NULL AND received_at < now() - 5min` para hunting de eventos órfãos) e `fn_prune_custody_webhook_events(p_keep_days int default 30)` manual/future cron (Stripe retenta até 3d, MP até 72h, 30d dá headroom forense). RLS forced + service-role only; index `received_at DESC` para triagem + partial index `(gateway, received_at) WHERE processed_at IS NULL` para o backlog query; **self-test em 5 etapas dentro do `BEGIN/COMMIT`** valida o primitive antes do código depender dele. Route handler: replay → 200 OK com `replayed: true, event_id` + métrica `custody.webhook.replayed{gateway}`, **sem** invocar `confirmDepositByReference`, **sem** audit log, **sem** incrementar `custody.webhook.confirmed`; dedup table missing (legacy install) → degrada com warn log e prossegue (não 500-a webhooks de clientes pagantes). **Defense-in-depth bonuses**: body cap de 64 KiB rejeitado via 413 PAYLOAD_TOO_LARGE tanto via `content-length` declarado quanto via `Buffer.byteLength` real (atacante mente no header E framework não impõe cap em `request.text()`); envelope canônico `apiError` com `request_id` propagado (L13-06/L14-05); logs estruturados com tags `gateway/event_id/payment_reference/request_id`; métricas categorizadas por reason (`signature/body_too_large/ambiguous_gateway/invalid_json/no_event_id/no_payment_reference/dedup`); 503 quando secret env var unset (antes era 400 vazio que mascarava config drift como "cliente errado"). **Tests: 33 novos** — 14 em `webhook.test.ts` (MP v2, fallbacks, replay window, mismatch, ms→s convert, whitespace, secret/format errors) + 18 em `route.test.ts` novo (gateway detection precedence incluindo header smuggling + Stripe/MP happy paths + dedup short-circuit + dedup RPC error → 500 + missing-table degrade + 413 com content-length forjado + 413 com body real grande + missing event_id + invalid JSON + already_confirmed sem audit log + missing secret config → 503). Suite **1275/0 (4 todos)**, lint clean, audit verify **348/348**. Runbook: `GATEWAY_OUTAGE_RUNBOOK.md` ganha **§8** (~120 linhas) com tabela de gateway detection, replay window matrix Stripe vs MP v2, queries operacionais para `custody_webhook_events` (received vs processed counts, eventos órfãos), symptom→fix matrix de 6 entries (spike de 401, 400 com both signatures, replays inflados, 413, processed_at NULL antigo, dedup error), e rollback strategy (revert do código mantém a migration sem prejuízo). Onda 1 → 56/179, High → 31/123, Lente 1 → 23/50.
- L06-07 — Client error boundaries Sentry funnel (commit 5b79094): fecha o high de Lente 6 (COO) sobre "Global error do Next.js não reporta a Sentry". A descoberta original era restrita ao `app/global-error.tsx` mas a auditoria operacional do fix expôs três outros boundaries com problemas correlatos: `app/error.tsx` só fazia `console.error` (Sentry-mute), `app/(portal)/error.tsx` não tinha logging algum, e `app/platform/error.tsx` chamava `Sentry.captureException` direto **sem tags** — então a alert policy de severidade (L20-05) não conseguia rotear esses crashes. **Solução**: novo helper `portal/src/lib/observability/reportClientError.ts` é o único caminho que toda fronteira agora usa dentro de `useEffect`. O helper (a) atribui tag estável `error_boundary` (`global` | `root` | `portal` | `platform`) — permite filtros precisos em Sentry tipo "todas as quebras do platform admin na última hora"; (b) atribui tag `severity` derivada por boundary alinhada com `docs/observability/ALERT_POLICY.md` — `global` e `root` são P1 (paginam on-call, porque o documento raiz / layout raiz quebrado significa que nenhum boundary aninhado vai capturar retries), `portal` e `platform` são P2 (Slack apenas, porque o chrome ainda renderiza e o usuário pode navegar elsewhere); (c) preserva `error.digest` como tag — único correlator entre o crash do client e a linha de log do RSC server-side que produziu a falha; sem ele, on-call só tem timestamp e tem que adivinhar; (d) encapsula tudo em `Sentry.withScope` para evitar vazamento de tags entre eventos não relacionados na mesma session; (e) tem fail-safe: se `Sentry.withScope` ou `captureException` explodir (ex.: extensão de browser bloqueou o bundle), o boundary segue funcionando — observability nunca degrada UX; (f) sempre faz `console.error` no client para que dev local sem DSN ainda enxergue o crash. Cada `error.tsx` agora também surfaca `error.digest` para o usuário (campo `Ref:` monoespaçado no fallback) — ao invés de obrigar suporte a pedir abertura de DevTools, o usuário copia a referência diretamente. **Tests: 25 novos cases** — 13 em `reportClientError.test.ts` (forwarding, severity por boundary, digest preservation, extras forwarding com filtragem de null/undefined, console echo, dois fail-safes para Sentry crashing, scope freshness entre invocações, sem digest sem leak) + 12 component tests em `global-error.test.tsx` / `error.test.tsx` / `(portal)/error.test.tsx` / `platform/error.test.tsx` (3 cada cobrindo forwarding + digest render + reset button), todos rodando sob `@vitest-environment happy-dom`. Suite portal **1242/0 (4 todos)**, lint clean. Audit verify **348/348**. Onda 1 → 55/179, High → 30/123, Lente 6 → quarta correção da lente.
- L02-08 — Realtime RLS guard (commit ef42911): fecha o high de Lente 2 sobre "Realtime / Websocket — Cross-tenant leak". O risco era estrutural: qualquer tabela em `supabase_realtime` sem RLS restritiva broadcasta CDC para todo subscriber autenticado, e a auditoria não pôde verificar a membership do publication porque ela é gerenciada out-of-band (dashboard ou migrations que esquecem o pareamento RLS+ADD TABLE). Solução em **defence-in-depth de quatro camadas** numa migration única (`20260419160000_l02_realtime_rls_guard.sql`): (a) **`public.realtime_publication_allowlist`** captura exemptions intencionais (e.g. `feature_flags` para o broadcast de invalidação L18-06) — `(table_schema, table_name)` PK, `reason` NOT NULL com CHECK `length(trim(reason)) >= 10` para forçar justificativa de auditoria em cada exceção; RLS forced + service-role only. (b) **`fn_realtime_publication_unsafe_tables(p_publication)`** diagnostic SECURITY DEFINER retorna uma linha por offender com `reason ∈ {rls_disabled, no_select_policy, tautological_select_policy_using_true}` — heurística usa `pg_class.relrowsecurity` + `pg_policy.polqual IS NOT NULL` (qualquer SELECT/ALL com USING não-trivial conta como evidência de intenção; SELECT/ALL com `polqual IS NULL` é o `USING (true)` venenoso, rejeitado). (c) **`fn_assert_realtime_publication_safe(p_publication)`** wrappa o diagnostic e RAISES `P0009 REALTIME_RLS_VIOLATION` listando todos offenders + HINT apontando ao runbook. (d) **DDL event trigger `trg_block_unsafe_realtime_publication`** em `ddl_command_end` filtrado a `ALTER PUBLICATION`/`CREATE PUBLICATION` re-executa a assertion na MESMA transação da DDL — qualquer ADD inseguro (table sem RLS, RLS sem policy SELECT, ou policy `USING (true)`) é abortado e a publicação fica intacta. Isso fecha o loop na fonte: nem dashboard click, nem hot-fix `psql` em produção pode bypassar — o trigger dispara para todos os roles incluindo `supabase_admin`. (e) Self-test no momento da migration emite `[L02-08.violation]` RAISE NOTICE para offenders pré-existentes (deliberadamente non-fatal — operadores triagem post-apply em ambientes com estado legacy). Tests: 8 cenários em `tools/test_l02_realtime_rls_guard.ts` cobrindo baseline limpo, diagnostic flagging, allow-list bypass, P0009 da assertion, e quatro paths do event trigger (RLS+restrictive ✓, USING(true) ✗, RLS sem policy ✗, allow-listed ✓) — usa `docker exec ... psql` para zero novas Node deps. Runbook canônico `docs/runbooks/REALTIME_RLS_RUNBOOK.md` (~250 linhas) cobre arquitetura broker + RLS, threat model, checklist de "adicionar nova tabela" com template SQL atômico, fluxo de allow-listing com PR review, e triagem de cada `reason` em 5.0. Audit verify **348/348**, ROADMAP/SCORECARD bumped (Onda 1 → 48/177, High → 23/123, Lente 2 → 8/15).
- L03-17 — Decimal-string parser BigInt path (commit c851be0): fecha o último high de Lente 3 sobre "Arredondamento IEEE 754 em TypeScript" como follow-up natural ao L03-01 já fechado (que estabeleceu `portal/src/lib/money.ts` com BigInt + banker's rounding). O alvo aqui era o admin form `platform/produtos/actions.tsx` que ainda fazia `Math.round(parseFloat(priceStr) * 100)` para converter R$ digitado em `price_cents` — vetor clássico de "centavo perdido por IEEE-754" para entradas tipo `parseFloat("1.235") * 100 = 123.49999999999999 → Math.round = 123` (esperado 124), com propagação visível no preço-por-OmniCoin do shop e ticket recorrente de atletas. **Solução**: novo helper `parseDecimalToCents(input: string)` em `money.ts` que evita floating-point por completo — lexa string typed em (integer, fractional), normaliza separadores BR (`1.234,56`) vs US (`1,234.56`), valida contra `/^-?\d+(\.\d+)?$/` (rejeita `1e5`, `1.2.3`, símbolos `R$`, etc.), decode em BigInt, quantiza com `roundDecimal` existente (banker's, mesma função consumida por `roundToCents`/`calcPercentFee`/`calcSplit`), enforcement contra `MAX_MONEY_CENTS`, retorna `Number(BigInt)` lossless dentro de safe-int range. EditForm + CreateForm migrados com try/catch redirecionando malformed input ao toast existente "Preço inválido" ao invés de silenciosamente armazenar 0 cents. **Scope decision documentada**: outros call-sites `Math.round(x*100)/100` no portal (engagement %, dashboard %, training plan progress %, edge functions `change_percent`/`total_km`) NÃO foram migrados — não são money paths e a substituição seria over-engineering; banker's vs half-up para % de display é financeiramente neutro. **Tests: 32 novos vitest cases** em `money.test.ts` cobrindo (i) happy paths integers/1-2 frac digits/comma decimal/whitespace/sign explícito/negatives/leading zeros, (ii) BR e US thousands separators (`1.234,56` ↔ `1,234.56` ambos → 123456), (iii) banker's rounding sign-symmetric com vetores 1.005→100, 1.015→102, 1.025→102, 1.035→104, -1.005→-100, -1.015→-102, (iv) tail > 3 frac digits (`1.0050001`→101, `1.0049999`→100), (v) malformed input rejection com 14 vetores (empty, abc, 1.2.3, 1e5, ".5", "1.", NaN, Infinity, "R$ 10,00", sign-only, etc.) + 3 type-guards via @ts-expect-error, (vi) range enforcement (`MAX_MONEY_CENTS ± 1`), (vii) parity contra oracle BigInt independente em 1.000 fuzzed inputs (zero mismatches), (viii) round-trip `(cents/100).toFixed(2)` display path. Sweep test garante que pelo menos 1 input de half-cent realmente diverge de `Math.round(parseFloat(s)*100)` no engine de teste — prova que a migração não é no-op no Node 20+. Suite portal **1180/0** (era 1138 + 42 novos), tsc clean nos arquivos tocados, eslint OK, audit verify **348/348**. Onda 1 → 51/177, High → 26/123, Lente 3 → 6/20.
- L01-30 — Android R8/ProGuard release hardening (commit fb62982): fecha o high de Lente 1 sobre "Android — Falta de ProGuard/R8". Risco original: APK release shipping com `minifyEnabled false`, deixando classes/strings/constantes (incluindo as thresholds de `lib/domain/usecases/integrity_detect_*` em `IntegrityDetectSpeed`/`Teleport`/`Vehicle`) inferíveis em segundos via `apktool d` / `jadx`. Server-side `supabase/functions/_shared/anti_cheat.ts` permanece authoritative, mas com thresholds mobile expostos um cheater consegue calibrar a fraude para sempre ficar 1% abaixo das limites conhecidas. **Defesa em três camadas, alinhada com L01-31** (mesmo `build.gradle`, mesmo runbook): (a) **`omni_runner/android/app/build.gradle`** — bloco release ganha `minifyEnabled true` + `shrinkResources true` + `proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'`, com comentário multilinha colado ao bloco de signing L01-31 explicando o threat-model (as duas defesas de release viajam juntas). (b) **`omni_runner/android/app/proguard-rules.pro`** novo (~200 linhas, 9 seções nomeadas alinhadas com `pubspec.yaml`): §1 toggles globais (`-keepattributes SourceFile,LineNumberTable,Signature,*Annotation*` para Sentry symbolication; `-renamesourcefileattribute SourceFile` evitando vazar caminhos de repo nos stack traces; `-dontobfuscate` comentado como recovery escape-hatch documentado), §2 Flutter framework (`io.flutter.**` + `GeneratedPluginRegistrant`), §3 plugins JNI/reflexivos (flutter_secure_storage/BouncyCastle, flutter_blue_plus, mobile_scanner+ML Kit, health/Health Connect, flutter_foreground_task, geolocator, permission_handler, flutter_web_auth_2, google_sign_in, sign_in_with_apple, image_picker, share_plus, GMS Wearable+Tasks), §4 Sentry, §5 Firebase + GMS + Gson + FirebaseMessagingService, §6 Supabase/Realtime/GoTrue + `@SerializedName` keep, §7 SQLCipher native bindings (`net.sqlcipher.**`/`net.zetetic.**`), §8 noise (kotlinx-coroutines + Kotlin Metadata + enums + Parcelable + native methods + dontwarn OkHttp/Okio/Conscrypt), §9 **anti-keep banner** explícito proibindo qualquer `-keep class lib.domain.usecases.integrity_detect_*` — R8 mangleia os nomes de classe E inlining as constantes nos call-sites, movendo o anti-cheat de "trivial em jadx" para "exige correlated-trace reverse-engineering multi-sessão". (c) **`tools/test_l01_30_android_minify.sh`** — lint estrutural sub-second com 6 invariantes grep-based (sem SDK Android, máxima portabilidade): (i) `minifyEnabled true` presente, (ii) sem `minifyEnabled false` explícito (defesa contra "fix" lazy de quem quer destravar build), (iii) `shrinkResources true` presente, (iv) `proguardFiles ... 'proguard-rules.pro'` corretamente encadeado com defaults AGP otimizados, (v) `proguard-rules.pro` carrega o banner §9 (referencia `integrity_detect` E `L01-30`), (vi) NENHUM `-keep` aponta para `integrity_detect` (regressão direta de L01-30 — o lint existe especificamente pra bloquear esse pattern), (vii) `-dontobfuscate` apenas em forma comentada (live directive desabilitaria renaming everywhere). Negative test confirmou: regressão (`minifyEnabled true → false`) faz o script falhar com EXIT=1 reportando invariantes (i) e (ii); restaurado e o lint volta a passar. (d) **CI wiring**: `.github/workflows/release.yml` ganha step "Verify gradle release-minify invariants (L01-30)" rodando logo após o lint L01-31 e antes do `fastlane build_apk` — falha rápida antes de Fastlane gastar minutos em build sem R8. (e) **Runbook**: `ANDROID_RELEASE_SIGNING_RUNBOOK.md` renomeado para "Android Release Signing & Hardening" e ganha seção R8 completa cobrindo threat model, "Adding a keep rule when a new plugin breaks under R8" (lendo `build/app/outputs/mapping/prodRelease/missing_rules.txt`), "Validating R8 output without the production keystore" (combina escape L01-31 `-PallowReleaseDebugSigning=true` com release build), recovery escape-hatch `-dontobfuscate`, e symptom→fix tables Symptom F (release-only crashes mapeados a missing keeps, com tabela de stack-trace patterns) + Symptom G (lint failure triage). Audit verify **348/348**, Onda 1 → 52/179, High → 27/123, Lente 1 → 21/50.
- L01-31 — Android release signing fail-loud (commit fcd4dc8): fecha o high de Lente 1 sobre "Android — Release assina com debug key se `key.properties` não existir". Risco original era duplo: (i) CI sem `key.properties` restaurada → `flutter build apk --release` cai silenciosamente em `signingConfigs.debug` → fastlane publica APK debug-signed no Firebase App Distribution (servido a todos beta-testers até alguém notar) e AAB rejeitado pelo Play Store ("upload key mismatch"); (ii) operador arrasta APK manualmente para Play Console assumindo que está release-signed → Play *fixa* a debug key como upload key permanente, bloqueando releases futuras (recovery via Play Support, multi-semana, não self-served). **Defesa em três camadas**: (a) **`omni_runner/android/app/build.gradle`** — removido o ternário `keystorePropertiesFile.exists() ? signingConfigs.release : signingConfigs.debug`; configuration-time agora inspeciona `gradle.startParameter.taskNames` para qualquer task contendo `release` (cobrindo `assembleRelease`, `bundleRelease`, `assembleProdRelease`, `bundleDevRelease`) e — se `key.properties` está ausente E o flag opt-in `-PallowReleaseDebugSigning=true` não foi passado — aborta com `GradleException` com mensagem multilinha apontando para o runbook. Flag opt-in existe APENAS para teste local de ProGuard/R8 (developer quer um APK debug-signed para inspecionar output otimizado sem provisionar o keystore real); emite warning ruidoso no log (`[L01-31] ⚠ allowReleaseDebugSigning=true — release artifact will be DEBUG-SIGNED`). (b) **`.github/workflows/release.yml`** — novo step "Restore Android release signing (L01-31)" decodifica `ANDROID_KEYSTORE_BASE64` → `omni_runner/android/app/omnirunner-release.keystore` e `ANDROID_KEY_PROPERTIES` → `omni_runner/android/key.properties` antes do fastlane; valida que ambos os secrets existem (fail-fast com `::error title=L01-31::`), que `key.properties` contém os 4 campos obrigatórios (`storePassword`, `keyPassword`, `keyAlias`, `storeFile`), `chmod 600` em ambos arquivos, e step de cleanup com `if: always()` apaga ambos arquivos no final mesmo em falha de build (defesa em profundidade — runner é efêmero mas não custa nada). Step intermediário "Verify gradle release-signing invariants" roda o lint estrutural antes do `fastlane build_apk`. (c) **`tools/test_l01_31_android_signing.sh`** — lint estrutural com 6 invariantes verificadas em <1s (no `find`/`grep` Python, só bash + `grep -E` para máxima portabilidade): (1) sem ternário fallback para `signingConfigs.debug` em `build.gradle`, (2) `GradleException` + referência a "L01-31" presentes, (3) detecção via `gradle.startParameter.taskNames` wired, (4) flag `allowReleaseDebugSigning` referenciada (override existe), (5) `release.yml` NÃO referencia `allowReleaseDebugSigning` (CI não pode habilitar o override), (6) `release.yml` restaura ambos `ANDROID_KEYSTORE_BASE64` + `ANDROID_KEY_PROPERTIES` dos secrets. Negative-test confirmou que reintroduzir o ternário (echo do par antigo no build.gradle) faz o lint falhar com `EXIT=1` e mensagem clara identificando a invariante violada. Runbook canônico `docs/runbooks/ANDROID_RELEASE_SIGNING_RUNBOOK.md` (~250 linhas) cobre threat model (60s recap), tabela de secrets requeridos com comandos `gh secret set` exemplares, override local para ProGuard/R8, diagrama do fluxo CI completo (decode → validate → build → cleanup), 5 cenários sintoma→fix (secret missing, gradle abort, Play upload-key mismatch, debug override warning, lint fail), e procedure completa de rotation da upload key (gerar nova .keystore + AAB temporário + Play Console reset request + atualizar ambos secrets simultaneamente + trigger workflow_dispatch noop). Audit verify **348/348**, Onda 1 → 50/177, High → 25/123, Lente 1 → 20/50.
- L01-29 — Strava OAuth state CSRF (commit 396340c): fecha o high de Lente 1 sobre "Deep link — Strava callback sem state/CSRF". Risco original: `deep_link_handler.dart` aceitava `code` em `omnirunner://strava/callback?code=X` e `omnirunner://localhost/exchange_token?code=X` sem validar `state`, abrindo login-CSRF (atacante phisha vítima a vincular conta Strava do atacante na conta Omni Runner da vítima → atividades do atacante surgem como da vítima e poluem ranking, anti-cheat baselines, e participação em desafios). Fix em **três camadas defence-in-depth**: (a) **`StravaOAuthStateGuard`** novo (`omni_runner/lib/features/strava/data/strava_oauth_state.dart`) — minta CSPRNG 32 bytes base64url por flow via `Random.secure()`, persiste em secure_storage com TTL 10 min e timestamp absoluto de expiry; `validateAndConsume()` faz constant-time compare + clear-on-every-path (sucesso/mismatch/expired/missing) + consume-once (replay do mesmo `state` num segundo callback falha by design). Decoupled de `FlutterSecureStorage` via interface `OAuthStateStorage` para unit tests sem platform channel. (b) **`StravaAuthRepositoryImpl.authenticate()`** chama `beginFlow()` ANTES de construir a URL de auth, threading o token via `state=` em `StravaHttpClient.buildAuthorizationUrl(state:)`, e roda `validateAndConsume(returnedState)` ANTES de inspecionar `code` — mismatch lança `AuthFailed("OAuth state mismatch — flow aborted")` e o token exchange nunca acontece. State é limpo em todos os branches de cancel/error (PlatformException, IntegrationFailure, AuthCancelled, generic Exception). `FlutterWebAuth2.authenticate` extraído para typedef `WebAuthLauncher` permitindo testes end-to-end de happy/mismatch/replay/cancel sem binding ao platform channel real. (c) **`DeepLinkHandler`** removeu os branches legados de Strava callback (`omnirunner://strava/callback?code=X` e `omnirunner://localhost/exchange_token?code=X`) — produção OAuth roda APENAS via `flutter_web_auth_2` no scheme dedicado `omnirunnerauth://`, e esses paths `omnirunner://` eram dead code que qualquer consumer futuro poderia acidentalmente re-habilitar para herdar a CSRF original. Links forjados inbound agora classificam como `UnknownLinkAction` e são ignorados silenciosamente. `StravaCallbackAction` permanece `@deprecated` para preservar o guard defensivo em `auth_gate._onDeepLink` que avisa caso algum code path resurra a action. DI: `StravaOAuthStateGuard` registrado como lazy singleton em `data_module.dart` e injetado no construtor da repo. **Tests: 56 cases (15 novos + 4 atualizados)**: `strava_oauth_state_test.dart` cobre 16 cenários (mint, CSPRNG uniqueness, single-in-flight overwrite, TTL expiry, mismatch, replay, missing/empty/null returned state, storage corruption, clear semantics); `strava_auth_repository_test.dart` ganhou 6 cases (happy path com state match + token exchange, forged callback rejection, missing state in callback, replay across two flows, URL embeds minted state, state cleared on cancellation); `deep_link_handler_test.dart` atualizou 3 cases assertando que paths legacy retornam `UnknownLinkAction`. `flutter analyze` limpo, audit verify **348/348**. Onda 1 → 49/177, High → 24/123, Lente 1 → 11/26.
- L01-06 — Portal CSRF defence (commit 7fbc4ec): fecha o high de Lente 1 sobre "GET /api/swap, GET /api/clearing, GET /api/custody — Autorização por cookie" com defesa em duas camadas. (a) **`sameSite: "strict"`** em `portal_group_id` e `portal_role` (`portalCookieOptions()` em `lib/route-policy.ts`) elimina o vetor CSRF de top-level GET navigation cross-origin que existia com `lax`; os dois call-sites com cookie-opts inline (`api/set-group/route.ts` e `lib/actions.ts`) foram refatorados para usar o helper canônico, fechando a porta para drift de política. (b) **Double-submit token OWASP §3.2** (`lib/api/csrf.ts`): cookie `portal_csrf` (32 bytes hex, `sameSite=strict`, `httpOnly=false` por design — JS precisa ler para espelhar no header), emitido pelo middleware em todo response autenticado via `ensureCsrfCookie`; `verifyCsrf` faz constant-time compare (SHA-256 + `timingSafeEqual`) entre cookie e header `x-csrf-token` em `(POST|PUT|PATCH|DELETE)` para o allow-list financeiro (`/api/{custody,custody/withdraw,swap,clearing,distribute-coins,platform/custody,platform/feature-flags,platform/fees,platform/refunds}`); enforcement roda ANTES de auth (check puro, ~15µs, denia tráfego de atacante sem round-trip Postgres) e retorna `403 CSRF_TOKEN_INVALID` com sub-código (`CSRF_COOKIE_MISSING`/`HEADER_MISSING`/`TOKEN_MALFORMED`/`TOKEN_MISMATCH`) para postmortem. Webhooks externos (`/api/custody/webhook`, `/api/billing/asaas`) e OAuth callback ficam em `CSRF_EXEMPT_PREFIXES` (HMAC/state já cobrem). (c) **Browser wrapper `csrfFetch`** (`lib/api/csrf-fetch.ts`) lê o cookie via `document.cookie` e injeta o header automaticamente — 7 componentes UI migrados (`fx/withdraw-button`, `athletes/distribute-button`, `custody/deposit-button`, `swap/swap-actions`, `platform/{feature-flags,fees,refunds}`); o `withdraw-button` ganhou também `x-idempotency-key` (regressão L18-02 latente que ninguém tinha pegado). (d) `signOut` deleta `portal_csrf` para evitar herança de token entre sessões. Runbook em `docs/runbooks/CSRF_RUNBOOK.md` (triagem por sub-código, fluxo de adicionar nova rota gated, rotação global via rename de cookie). 30 testes novos (`csrf.test.ts` 21 + `csrf-fetch.test.ts` 9 happy-dom) + atualização de `route-policy.test.ts`. Portal vitest: 1138 passed (was 1108).
- L18-08 — Edge Function canonical wallet-credit helper (commit 38f9c72): fecha o achado de Lente 18 sobre "Edge Functions vs Route Handlers — responsabilidade duplicada" e desentupde DOIS bugs latentes que estavam escondidos no caminho Edge desde L18-01 ser deployada por cima do schema de L19-01. Solução em três camadas: (a) **migration `20260419140000_l18_canonical_wallet_credit.sql`** re-cria `fn_increment_wallets_batch` corrigindo: (i) INSERT em `coin_ledger` que escrevia apenas `created_at` (timestamptz, legado) — quando L19-01 fez `created_at_ms` (bigint) `NOT NULL` sem default, qualquer caller em schema fresh teria estourado com `null value in column "created_at_ms"` na primeira liquidação; (ii) cast `(v_entry->>'ref_id')::uuid` que assumia coluna uuid legada e teria FAIL-CLOSED no momento que algum caller passasse composite key `idem:user:nonce` (padrão L18-02) — ref_id é text canonicamente em L19-01. RPC nova aceita ref_id text, escreve `created_at_ms`, valida user_id+delta upfront com SQLSTATE P0001, mantém o `set_config('app.wallet_mutation_authorized','yes',true)` da L18-01. (b) **`supabase/functions/_shared/wallet_credit.ts`** — entry point único `creditWallets(adminDb, entries, ctx)` para toda mutação de wallet em Edge Function: pré-flight valida shape (UUID v4 em user_id e issuer_group_id, delta inteiro non-zero, reason no allowlist espelhando `coin_ledger_reason_check`, ref_id text 1-200 chars opcional) — falha-rápido SEM round-trip, retorna discriminated-union `{ok, processed}` | `{ok:false, code, message, details}` com 8 códigos típados (EMPTY_BATCH, INVALID_USER_ID, INVALID_DELTA, INVALID_REASON, INVALID_REF_ID, INVALID_ISSUER_GROUP, INVALID_ENTRY, RPC_ERROR); loga linha JSON estruturada por chamada com request_id+fn+entry_count+total_delta+pg_code para grep-by-request-id em postmortem. (c) **3 call-sites migrados**: `challenge-withdraw` (refund) e `settle-challenge` (refund cooperativo no-runners + settlement competitivo) substituem `.rpc("fn_increment_wallets_batch", ...)` raw por `creditWallets(adminDb, entries, ctx)` — comportamento preservado. Tests: 29 Deno tests em `wallet_credit.test.ts` cobrindo toda branch typed (8 INVALID_*), validation short-circuit (zero RPC call em validation fail), RPC happy/error path com `pg_code` propagation, log line shape, ALLOWED_REASONS parity. Suite portal **1085/0** sem regressão; Deno typecheck dos dois Edge Functions limpo (modulo warning pré-existente em `_shared/auth.ts`); audit verify **348/348**. Runbook `WALLET_MUTATION_GUARD_RUNBOOK.md` ganha seção "Edge Functions — go through `creditWallets` (L18-08)" documentando o pattern e o gate de code review (qualquer novo `.rpc("fn_increment_wallets_batch")` raw deve ser flaggado).
- L18-01 — Wallet mutation guard + unified gateway (commit pending): fecha o crítico mais antigo de Lente 18 (duas fontes da verdade: `wallets.balance_coins` cache vs `SUM(coin_ledger)` canônico). `supabase/migrations/20260419130000_l18_wallet_mutation_guard.sql` introduz **trigger guard** `trg_wallet_mutation_guard_{insert,update}` em `public.wallets` (BEFORE INSERT + BEFORE UPDATE OF balance_coins/pending_coins/lifetime_*) que bloqueia qualquer mutação a menos que a transação tenha definido o GUC `app.wallet_mutation_authorized='yes'` (LOCAL → roll-back automático em commit/abort, não vaza). Erro `WALLET_MUTATION_FORBIDDEN` (SQLSTATE P0007) com HINT explicando o gateway. INSERTs de signup (handle_new_user, todos os counters em zero) são exempt (`COALESCE(...)=0` short-circuit). Todas as 8 RPCs mutadoras existentes (`increment_wallet_balance`, `increment_wallet_pending`, `release_pending_to_balance`, `debit_wallet_checked`, `fn_increment_wallets_batch`, `reconcile_wallet`, `reconcile_all_wallets`, `execute_burn_atomic`, `fn_switch_assessoria`) re-criadas com `PERFORM set_config('app.wallet_mutation_authorized','yes',true)` como primeira instrução — surface de comportamento idêntico, surface de regressão preservada. Novo gateway **`fn_mutate_wallet(user_id, delta_coins, reason, ref_id?, issuer_group_id?) → (ledger_id, new_balance)`** é o caminho preferencial pra código novo: insere coin_ledger + UPDATE wallets atomicamente atrás do guard, com lifetime_earned/spent derivado do sinal do delta, lock_timeout=2s, validações INVALID_USER_ID/INVALID_DELTA/MISSING_REASON (P0001), check_violation se debit excede balance. Note column foi removido pelo L19-01 partitioning (latent bug em reconcile_wallet original, agora corrigido); reconcile usa `admin_adjustment` reason (no allowlist atual) e codifica drift context em `ref_id`. Tests: 21 integration assertions em `tools/test_l18_wallet_guard.ts` (guard direto-UPDATE blocked em 3 colunas + INSERT signup exempt + INSERT non-zero blocked + last_reconciled_at_ms allowed; 8 RPCs autorizadas seguem funcionando incluindo pending↔balance flow; gateway happy-path credit/debit + lifetime parity + auto-create wallet + delta=0/missing-reason rejection + check_violation em debit excessivo + paridade SUM(ledger)==balance após 5 ops mistas). Suite portal **1078/0**, integration **L18-01 21/21 + L18-02 22/22 + L19 36/36 + cron 16/16**, audit verify **348/348**. Runbook canônico `docs/runbooks/WALLET_MUTATION_GUARD_RUNBOOK.md` cobre triggers, diagnóstico (drift query SQL pronta), mitigação por cenário (CI block em nova RPC, emergency manual UPDATE com BEGIN+SET LOCAL, drift reportado por reconcile), pattern oficial pra novas rotas (preferir gateway, fallback set_config para multi-row).
- L18-02 — Idempotency unification (commit 1f14fbd..HEAD): fecha o crítico mais antigo de Lente 18 (idempotência ad-hoc por RPC). Nova `public.idempotency_keys` (PK composto `(namespace, actor_id, key)`, status enum `claimed|completed|released`, CHECK regex no namespace, status_code 100–599, completed-must-have-response) substitui o padrão fragmentado anterior (`confirm_custody_deposit` usava FOR UPDATE+status, `execute_burn_atomic` usava wallet FOR UPDATE, `execute_swap` usava UUID ordering, `execute_withdrawal` + `distribute-coins` não tinham). Três RPCs `fn_idem_begin`/`fn_idem_finalize`/`fn_idem_release` (`SECURITY DEFINER` + `SET search_path` + `lock_timeout`) implementam o lifecycle completo: begin retorna `action=execute|replay|mismatch` com semântica de stale-claim recovery (lease default 60s previne poisoning por worker crashado), mismatch detecta replay com body diferente em O(1) ANTES de qualquer mutação, finalize atomic transitiona claimed→completed gravando status+body, release devolve o slot para retry imediato em early-bail. GC (`fn_idem_gc_safe`) cron'd `7 * * * *` via wrapper L12-03 (advisory lock + `cron_run_state` observability). Portal: novo `lib/api/idempotency.ts` com `withIdempotency({namespace, actorId, requestBody, required, handler})` — canonicaliza body via key-sorted JSON (estabilidade cross-client), hash sha256 via `node:crypto` (compat vitest), valida `x-idempotency-key` (UUID v4 OU `[A-Za-z0-9_-]{8,128}`), faz claim/replay/finalize/release lifecycle, propaga `x-request-id` e seta `x-idempotent-replay: true` no replay. **Retrofitted**: `/api/custody/withdraw` (`required: true` — header obrigatório, retorna `400 IDEMPOTENCY_KEY_REQUIRED`; é o endpoint mais sensível $$$), `/api/distribute-coins` (defense-in-depth: mantém `coin_ledger.ref_id` UNIQUE para mutação at-most-once + adiciona response replay para network blip resilience). Swap deferido pra follow-up (3 branches/audit logs). Testes: 22 integration assertions em `tools/test_l18_idempotency.ts` (schema + CHECKs, begin lifecycle completo, finalize idempotency, release no-op, GC isolation, cron seed) + 24 unit em `idempotency.test.ts` (regex, canonicalize stability, replay/mismatch wrapper) + 4 novos regression tests cross-route (replay path skips RPC, mismatch returns 409, missing-required header, exact RPC sequence). Suite portal **1078 passed / 0 failed**, integration **22/22**, cron health **16/16**, L19 **36/36**. Runbook canônico `docs/runbooks/IDEMPOTENCY_RUNBOOK.md` (~200 linhas): triggers (gc backlog, 409 spike, dup mutation report), diagnóstico SQL para lookup por user/namespace, mitigação (TTL bump, GC throttle, manual sweep, opt-out emergency). Pattern oficial estabelecido para os próximos retrofits (swap, clearing) e para qualquer rota futura money-impacting.
- L19-02 / L19-03 — DBA closure (commit 0893093): dois critical findings de Lente 19 fechados no mesmo PR. (a) **L19-02**: `supabase/migrations/20260419110000_l19_archive_via_partition_detach.sql` reescreve `fn_archive_old_ledger` para iterar partições mensais de `coin_ledger` (criadas em L19-01) e fazer `ALTER TABLE ... DETACH PARTITION` → `INSERT INTO coin_ledger_archive` → `DROP TABLE` por partição cujo upper bound já passou do cutoff de 12 meses — zero dead tuples na tabela viva, zero VACUUM pressure, O(1) DDL com `lock_timeout=5s` (busy partitions são puladas e retentadas no próximo ciclo); reescreve `fn_archive_old_sessions` para chunked DELETE (BATCH=250, MAX_BATCHES=200, `lock_timeout=2s`, `FOR UPDATE SKIP LOCKED`) — sessions ainda não é particionada, então o bloat é mitigado (não eliminado) por tuning agressivo de autovacuum (`autovacuum_vacuum_scale_factor=0.05`, `autovacuum_vacuum_threshold=1000`, `fillfactor=90`); pre-flight `ALTER TABLE sessions_archive ADD COLUMN IF NOT EXISTS total_elevation_m` corrige bug latente desde `20260322000000_p0_critical_fixes.sql` (coluna adicionada em sessions mas não propagada para o archive — função teria explodido em prod no primeiro ciclo com dados elegíveis). Sessions partitioning fica como follow-up Wave 2 mirror de L19-01. (b) **L19-03**: `20260419110001_l19_drop_redundant_indexes.sql` derruba **16 índices redundantes** em 14 tabelas, classificados em 6 categorias (UNIQUE-shadowed plain index, PK-shadowed index, UNIQUE single-column shadow, sibling overlap, `LIKE INCLUDING ALL` clone duplication, audit-flagged exato); cria três helpers SQL service-role-only — `pg_indexes_redundant_pairs()` (regression guard que retorna pares de índices com mesma coluna+predicado após normalizar nome+whitespace; usado em CI), `pg_index_exists(table, name)` e `pg_table_columns(table)` (consumidos por `tools/test_l19_dba_health.ts`). Test suite com 36 assertions (16 DROPPED + 16 KEPT + redundant-pair audit + archive callability + sessions vs sessions_archive schema parity) — 36/36 passam contra Supabase local. Runbook canônico `docs/runbooks/DBA_BLOAT_AND_INDEX_RUNBOOK.md` (~200 linhas) cobre triggers (bloat > 30%, archive fail, redundant pair > 0, disk pressure), diagnóstico (queries de bloat por table, status do `cron_run_state`, helper de pairs), e mitigação por cenário (DETACH stuck, schema drift, VACUUM manual, retention de archive).
- L12-01 / L12-02 / L12-03 — Cron health closure (commit be1c9e4): três migrations resolvendo o cluster crítico de Lente 12. (a) **L12-03**: `supabase/migrations/20260419100000_l12_cron_overlap_protection.sql` introduz `public.cron_run_state` (tabela single-row-per-job: `started_at`/`finished_at`/`last_status` com CHECK enum `never_run|running|completed|failed|skipped|timeout`/`run_count`/`skip_count`/`last_error`/`last_meta`, RLS service-role only) + helpers `fn_cron_should_run`/`mark_started`/`mark_completed`/`mark_failed` (SECURITY DEFINER, com timeout-recovery automático) + wrappers `fn_*_safe` combinando `pg_try_advisory_xact_lock(hashtext('cron:...'))` com a state-machine, aplicados aos três jobs `*/5` (`expire-matchmaking-queue`, `process-scheduled-workout-releases`, `lifecycle-cron`); reschedule defensivo aponta cada cron para o wrapper. (b) **L12-02**: `20260419100001_l12_cron_redistribute_thundering_herd.sql` desfaz o cluster 03:00–04:00 UTC: `eval-verification-cron` `0 3` → `15 3`, `archive-old-sessions` `0 3 SUN` → `45 3 SUN`, `archive-old-ledger` `0 4 SUN` → `15 5 SUN`, `coin_ledger_ensure_partition_monthly` `0 3 day-1` → `30 5 day-1` — espaçamento mínimo de 15 min entre quaisquer dois jobs. (c) **L12-01**: `20260419100002_l12_reconcile_wallets_schedule.sql` registra finalmente `reconcile-wallets-daily` em `30 4 * * *` (slot engineered por L12-02) invocando o EF existente via `pg_net`; faz seed de `cron_run_state` para visibilidade ops imediata. Validação local: `SELECT jobname, schedule FROM cron.job` retorna 12 rows na ordem esperada do runbook. Tests: `tools/test_cron_health.ts` cobre 16 cenários (schema/CHECKs, helpers state-machine, skip-on-recent vs allow-after-timeout, wrappers full lifecycle) — 16/16 passam contra Supabase local. Runbook canônico `docs/runbooks/CRON_HEALTH_RUNBOOK.md` (~250 linhas) documenta inventário esperado, diagnóstico (advisory locks via `pg_stat_activity`), mitigação por cenário (cron ausente, job travado, falha, regressão da redistribuição) e disparo manual de reconcile.
- L05-06 — Atomic championship cancel RPC (commit `a564d3c`, 2026-04-21): fecha o high de Lente 5 sobre `champ-cancel` não atômico com catch silencioso em `fn_credit_badge_inventory`. Nova RPC `fn_champ_cancel_atomic(uuid, uuid)` (SECURITY DEFINER, pinned search_path, lock_timeout=2s) wrappa as 4 writes (withdraw participants, revoke invites, refund badges, flip status) em uma transação; authorization (admin_master/coach de host_group), status precondition (draft/open/active), e row lock (FOR UPDATE) acontecem dentro da RPC. Idempotência via noop branch em `status='cancelled'`. Edge Function `champ-cancel/index.ts` agora mapeia FORBIDDEN/NOT_FOUND/INVALID_STATUS raises para 403/404/409 e expõe `badges_refunded` no response. Ships com `audit:champ-cancel-atomic` guard (35 invariantes sobre shape da migration + integração Edge Function).
- L05-08 — Withdrawal progress timeline primitives (commit `582867b`, 2026-04-21): fecha o high de Lente 5 sobre falta de UI para `pending→processing→completed`. `custody_withdrawal_events` append-only table (UNIQUE(withdrawal_id, status), RLS admin_master-of-host-group) + trigger `AFTER INSERT OR UPDATE OF status` que grava cada transição com `ON CONFLICT DO NOTHING`. `fn_withdrawal_timeline(uuid) RETURNS jsonb` devolve events + `expected_completion_at` (SLA: pending→processing 2min, processing→completed 10min) + `sla_breached` flag + `refund_eta_days=2` em failed. Historical rows backfilled. Nova rota `GET /api/custody/withdraw/[id]/timeline` proxy-a a RPC sob gating admin_master. Runbook backing: `WITHDRAW_STUCK_RUNBOOK.md` + `GATEWAY_OUTAGE_RUNBOOK.md`. 26 invariantes via `audit:withdrawal-timeline`.
- L09-06 — At-rest encryption para credenciais de billing-provider (commit `41110d8`, 2026-04-21): fecha o high de Lente 9 sobre Asaas key em plaintext. `public.billing_providers` table armazena Asaas/Mercado Pago/Stripe credentials como `pgp_sym_encrypt` bytea com `key_version` + `last_rotated_at`. RLS + column privileges fazem `api_key_enc` unreadable por authenticated/anon; apenas service_role pode SELECT ciphertext. Acesso exclusivo via `fn_set_billing_provider_key(group, provider, plain)` (bumps key_version, grava `key_set` audit row) e `fn_get_billing_provider_key(group, provider, reason)` (decrypts, grava `key_access` audit row com motivo). Master key em GUC `app.settings.kms_key` carregado per-transaction pelo edge function a partir de Supabase Vault / AWS KMS. Ambas helpers raise `KMS_UNAVAILABLE` quando GUC missing ou <32 chars. Self-test DO-block valida pgcrypto extension, bytea column type, authenticated column-privilege denial, KMS guard, e pgp round-trip. 32 invariantes via `audit:billing-providers-encryption`.
- L10-03 — Service-role usage inventory + rotation runbook (commit `925d338`, 2026-04-21): fecha o critical de Lente 10 sobre "`SUPABASE_SERVICE_ROLE_KEY` distribuída amplamente sem visibilidade". `docs/security/SERVICE_ROLE_USAGE_INVENTORY.md` lista canônicamente cada arquivo autorizado a referenciar a chave + suas variantes `_STAGING` / `_PREVIEW` / `_CI` (30 edge functions + 3 portal files + 3 CI workflows + ~30 tools/test_*). CI guard `audit:service-role-inventory` greps o repo em todo PR e falha o build em consumer não-listado, forçando revisão explícita do `@security`. Runbook `SERVICE_ROLE_ROTATION_RUNBOOK.md` documenta o split de env (prod nunca em preview/CI/staging via Supabase projects separados), cadência quarterly + ad-hoc, procedimento T-24h/T-0/T+5min/T+1h, capture de evidence para SOC2, incident playbook para vazamento suspeito (PagerDuty → rotate imediato → audit de `portal_audit_log` nas últimas 7d).
- L10-06 — Two-person SoD para high-risk platform_admin (commit `fd950f8`, 2026-04-21): fecha o high de Lente 10 sobre ausência de segregação de função. `admin_approvals` queue (append-only, RLS platform_role='admin' only) + BEFORE UPDATE trigger aplicam two-person rule. 3 CHECK constraints + trigger re-assertion: (a) self-approval blocked (`approved_by <> requested_by`), (b) execute requires `status='approved'`, (c) terminal statuses locked. `fn_admin_approval_required(action, payload) → boolean` é a política canônica: `platform_fee_config.update` / `platform_admin.grant` / `billing_provider.key_set` sempre exigem, `custody_withdrawal.execute` / `refund.issue` apenas ≥ US$ 10k. 24h TTL enforced via `fn_admin_approvals_expire_overdue()` (cron target, service_role-only). Self-test valida policy cases + INSERT-time CHECK. 27 invariantes via `audit:admin-approvals-sod`.
- L15-01 — UTM attribution capture pipeline (commit `f6fd338`, 2026-04-21): fecha o high de Lente 15 sobre "zero UTM tracking no produto". `marketing_attribution_events` append-only log (CHECK constraints em identity + per-field length, RLS own-user + platform_admin, 3 indexes on (user_id, created_at) / (campaign, created_at) partial / (event_type, created_at)) + `profiles.attribution jsonb` first-touch snapshot (SECURITY DEFINER AFTER INSERT trigger que no-ops se attribution já setado). Portal lib `attribution.ts` client cookie helper (first-touch, 90-day TTL, `consent_marketing=1` gated, per-field 200-char clamp, SameSite=Lax + Secure). POST `/api/attribution/capture` zod-validated + rate-limited, trunca IP para /24 ou /48, sha256-hasha UA antes de gravar via service-role. Eventos registrados em `audit_logs_retention_config` com 180 dias de retenção. 37 invariantes via `audit:utm-attribution`.
- L16-06 — OAuth/webhook integration telemetry (commit `cfc0a10`, 2026-04-21): fecha o high de Lente 16 sobre "Strava / TrainingPeaks OAuth sem telemetria de uso". `integration_events` append-only log (provider/event_type/status enums via CHECK, error_code + external_id length CHECKs, latency_ms clamp, RLS own-user + platform_admin, partial indexes on errors, 90d retention). Writer `fn_log_integration_event` (SECURITY DEFINER, service-role-only, input clamping) + aggregator `fn_integration_health_snapshot(provider, window_hours)` com totals / error_rate / p50 / p95 / by_event_type + counter `fn_integration_connected_counts()` — ambos gated em `platform_role='admin'` (raise 42501 caso contrário). Edge functions instrumentadas via `_shared/integration_telemetry.ts`: `strava-webhook` (webhook_validated/received/dedup, token_refresh_*, session_imported/ignored), `trainingpeaks-oauth` (oauth_start/callback_success/callback_error, token_refresh_*), `trainingpeaks-sync` (sync_success/failure) — telemetry failures nunca quebram o fluxo principal. Portal route `/api/platform/integrations/health` expõe snapshot + connected counts com zod validation (window_hours clampado [1, 720]) e mapeia 42501 → 403. 61 invariantes via `audit:integration-telemetry`.
- L18-05 — Durable outbox event bus (commit `b676952`, 2026-04-21): fecha o high de Lente 18 sobre "event bus inexistente — cascatas de efeitos em código imperativo". `outbox_events` append-only table com UNIQUE(event_key) para producers idempotentes, 15 event_types + 7 aggregate_types CHECK-bounded, state-machine `pending → processing → completed|failed|dead` com re-entry rules, attempts + next_attempt_at + last_error, 4 indexes (ready partial, aggregate composite, type+time, dead), RLS admin-only SELECT, 30d retention. Lifecycle RPCs (SECURITY DEFINER, service-role-only): `fn_outbox_emit` (ON CONFLICT DO NOTHING), `fn_outbox_claim` (FOR UPDATE SKIP LOCKED + visibility lease [5s, 3600s], limit [1, 1000]), `fn_outbox_complete`, `fn_outbox_fail` (promove para `dead` quando attempts ≥ max_attempts, senão backoff + re-queue como pending), `fn_outbox_dlq` sweep para events com `next_attempt_at` vencido. AFTER UPDATE OF is_verified trigger em `public.sessions` emite `session.verified` com event_key `session.verified:<uuid>` — falha aberta (RAISE WARNING) pra nunca bloquear writes de sessão por outbox outage. 43 invariantes via `audit:outbox-event-bus`.
- L21-10 — Elite athlete review quarantine (commit `6a9eb88`, 2026-04-21): fecha o high de Lente 21 sobre "anti-cheat pode publicamente marcar elite como suspeito". `sessions.review_status` state-machine column (CHECK em `none|pending_review|in_review|approved|rejected`) + `fn_session_visibility_status(uuid)` STABLE SECURITY DEFINER helper que retorna payload viewer-scoped — non-owners recebem apenas labels neutros (`verified`, `pending_review`, `verification_pending`) sem integrity_flags jamais expostos; owner + platform_admin recebem integrity_flags + review_status. `athlete_review_requests` queue append-only (athlete_id, session_id, status, athlete_note, evidence_urls https-only max 5, reviewer_id, resolution_note, UNIQUE partial index em status IN (pending, in_review) impedindo requests concorrentes por sessão, RLS athlete own read+insert / platform_admin read+update). `fn_request_session_review(session_id, note, evidence_urls)` gated em ownership + reviewable state (`review_status IN (none, rejected)`) + NOTHING_TO_REVIEW (integrity_flags IS NOT NULL OR is_verified = false), flipa `sessions.review_status` → `pending_review` atomicamente. BEFORE UPDATE OF review_status trigger `fn_sessions_review_status_guard` valida transições legais e raise INVALID_TRANSITION. 32 invariantes via `audit:athlete-review-quarantine`.
- L04-05 — Privacy zones primitives (commit `b5a4720`, 2026-04-21): fecha o high de Lente 4 sobre "trajetórias GPS brutas sem opção de privacy zones". `profiles.privacy_zones jsonb` com validador IMMUTABLE (`fn_validate_privacy_zones`) aplicado via CHECK simples (validator extraído pra contornar limitação de subquery em CHECK) — max 5 zonas, radius [50, 500] m. Helpers puros IMMUTABLE + PARALLEL SAFE: `fn_haversine_m` (distância geodésica), `fn_point_in_zones`, `fn_decode_polyline` (Google encoded polyline → (lat,lng)), `fn_encode_polyline_value` + `fn_encode_polyline` (round-trip) e `fn_mask_polyline(polyline, zones, trim_start_m default 200, trim_end_m default 200)` que decodifica, remove pontos em zonas e re-encoda. `fn_session_polyline_for_viewer(session_id)` (SECURITY DEFINER) devolve raw para owner + `platform_role='admin'` e masked para todos os outros; leituras admin são gravadas em `portal_audit_log` (`session.polyline.admin_view`) com RAISE WARNING fail-open pra nunca bloquear leitura legítima. Self-test cobre validator, haversine, decoder, encoder round-trip e mascaramento. 44 invariantes via `audit:privacy-zones`.
- L04-06 — Social handles policy + CHECK (commit `7ea0ef8`, 2026-04-21): fecha o high de Lente 4 sobre "campo `instagram_handle`/`tiktok_handle` em profiles sem política de uso". `fn_validate_social_handle(text)` IMMUTABLE enforça formato (1-30 chars, `[A-Za-z0-9._]`, rejeita `http`/`bit.ly`/slashes) e é aplicado via CHECK em `profiles.instagram_handle` e `profiles.tiktok_handle`. Novo `profiles.profile_public jsonb` com defaults privacy-first (`show_instagram:false`, `show_tiktok:false`, `show_pace:false`, `show_location:false`) e shape CHECK via `fn_validate_profile_public` — desacopla visibilidade social de `display_name`. `profiles.social_handles_updated_at` + trigger BEFORE UPDATE `fn_profiles_social_handles_rate_limit` aplicam rate limit configurável (`app.social_handle_min_interval_seconds`, default 24h, service_role isento) com P0001 em violação e gravam `profile.social_handle_changed` em `portal_audit_log` (fail-open). `fn_public_profile_view(uuid)` STABLE SECURITY DEFINER retorna view viewer-scoped respeitando flags (handles só para self/admin/opt-in). Self-test cobre validator accept/reject (9 cases) + profile_public shape. 37 invariantes via `audit:social-handles-policy`.
- L15-02 — Referral / invite viral primitives (commit `08842bf`, 2026-04-21): fecha o high de Lente 15 sobre "sem sistema de referral/convite viral". `coin_ledger_reason_check` estendido com `referral_referrer_reward` + `referral_referred_reward` preservando as 20 razões de L03-13. `public.referral_rewards_config` single-row (id=1) com rewards/ttl_days/max_activations/code length CHECK-bounded. `public.referrals` com state-machine CHECK (pending → activated|expired|revoked), anti self-referral CHECK, timestamps-por-status CHECK, UNIQUE referral_code, UNIQUE(referred_user_id) WHERE status='activated' (idempotência) e partial index pending-expiry. RLS own-read + admin-read; escritas apenas via RPCs. Trigger BEFORE UPDATE `fn_referrals_status_guard` trava o state machine. `fn_generate_referral_code(p_len)` emite códigos upper-case alfanuméricos (`gen_random_bytes`) excluindo 0/O/1/I, length clamped [6, 16], 8 retries, P0002 em exaustão. `fn_create_referral(channel)` valida canal + enforce cap per referrer + stampa `expires_at = now() + ttl_days`, P0003 em cap breach. `fn_activate_referral(code)` valida pending + not expired + not self + no prior activation, grava 2 rows `coin_ledger` atomicamente e atualiza ambas carteiras best-effort. `fn_expire_referrals()` é o sweep service-role-only para pg_cron. 47 invariantes via `audit:referral-program`.
- L15-03 — Open Graph dinâmico (commit `1b226e0`, 2026-04-21): fecha o high de Lente 15 sobre "social sharing sem Open Graph dinâmico". `portal/src/lib/og-metadata.ts` é o builder canônico: `getSiteBaseUrl()` resolve `NEXT_PUBLIC_PORTAL_BASE_URL` / `PORTAL_BASE_URL` (rejeita non-http), strippa trailing slash, default `https://omnirunner.app`. `buildOgMetadata({path,title,description,imageUrl?})` emite `openGraph.{title,description,url,type,siteName,locale,images:[1200x630]}`, `twitter.card=summary_large_image` e `alternates.canonical`. Default image URL é `<url>/opengraph-image` — path determinístico que Next.js App Router usa para co-located segments. `OG_IMAGE_SIZE` (1200x630), `OG_SITE_NAME`, `OG_IMAGE_THEMES` (challenge/invite/default) mantêm branding consistente. Dois route segments dinâmicos (`portal/src/app/challenge/[id]/opengraph-image.tsx`, `portal/src/app/invite/[code]/opengraph-image.tsx`) emitem `ImageResponse` edge-runtime (runtime=edge, PNG 1200x630) com palette + brand lockup. `generateMetadata` das duas pages chama `buildOgMetadata` com entity-derived title/description (short ID / short code). 8 tests vitest cobrem env fallback, env override + trailing-slash, malformed-env reject, payload shape, image URL override e path normalisation. 35 invariantes via `audit:og-metadata`.
- L16-01 — White-label branding primitives (commit `837ce91`, 2026-04-21): fecha o high de Lente 16 sobre "sem white-label / branding customizado por grupo". `public.portal_branding` estendida com `brand_name`, `subtitle`, `logo_url_dark` (dark mode), `favicon_url`, `branding_enabled` (feature flag per-group), `updated_by` + `version` (optimistic concurrency). `fn_validate_hex_color` (`^#[0-9A-Fa-f]{6}$`) e `fn_validate_https_url` (`^https://`) são IMMUTABLE PARALLEL SAFE e aplicados via CHECK — rejeita ataques de CSS injection (javascript:) + http mixed-content. Trigger BEFORE UPDATE `fn_portal_branding_version_bump` stampa `updated_by = auth.uid()`, incrementa `version`, e fail-open audita via `portal_audit_log` (`group.branding.updated`) com RAISE WARNING pra nunca bloquear update legítimo. `fn_group_branding_public(group_id)` é STABLE SECURITY DEFINER + GRANTED TO PUBLIC — o login screen mobile/portal resolve branding ANTES de autenticar, com fallback para defaults quando `branding_enabled=false`. `fn_group_branding_set` requer `coaching_members.role IN ('admin_master','coach')` e valida CHECKs antes de persistir. 49 invariantes via `audit:white-label-branding`.
- L16-02 — Custom domain primitives (commit `2794914`, 2026-04-21): fecha o high de Lente 16 sobre "sem custom domain por assessoria". `public.coaching_group_domains` mapeia host → group_id com state machine `pending_dns → verifying → verified → failed|revoked`, `host` UNIQUE, DNS challenge opaco, `is_primary` flag com partial unique index `(group_id) WHERE is_primary` (um primary por grupo). `fn_validate_custom_host` (IMMUTABLE PARALLEL SAFE) rejeita: `omnirunner.*` subdomínios (anti-takeover), hosts > 253 chars (RFC 1035), non-HTTPS URLs, IP literals (v4/v6), hosts sem TLD dot, caracteres inválidos. Normalizer trigger BEFORE INSERT/UPDATE lower-cases host + sub. Audit trigger AFTER INSERT/UPDATE fail-open em `portal_audit_log`. SoD enforced across RPCs: `fn_custom_domain_register` admin-only (retorna DNS challenge token), `fn_custom_domain_mark_verified` / `_mark_failed` service-role-only (cron/worker valida DNS externo), `fn_custom_domain_revoke` admin-only. `fn_custom_domain_resolve(host)` é STABLE SECURITY DEFINER + GRANTED TO PUBLIC — Next.js middleware mapeia Host → group_id em tráfego anônimo sem exposure de rows pending (só retorna `verified`). Edge integration com Vercel `/v9/projects/.../domains` e ACME SSL fica como wiring operacional em cima dos primitives. 52 invariantes via `audit:custom-domains`.
- L16-04 — Outbound webhooks (commit `48cd3c4`, 2026-04-21): fecha o high de Lente 16 sobre "sem outbound webhooks para parceiros". `fn_validate_webhook_url` (IMMUTABLE PARALLEL SAFE) bloqueia non-HTTPS, loopback (`localhost`, `127.0.0.1`, `::1`), RFC1918 (`10/8`, `172.16/12`, `192.168/16`), link-local (`169.254/16`), `100.64/10` (CGNAT), `0.0.0.0/8` — fecha SSRF-pivoted-egress. `fn_validate_outbound_webhook_events` whitelist 15 eventos canônicos (`session.verified`, `coin.distributed`, `championship.ended`, `athlete.enrolled`, `sponsorship.activated` …) pra que parceiros não sejam leaked sinais internos não documentados. `public.outbound_webhook_endpoints` stores configuração (HMAC secret rotacionável, event filter, enabled flag, per-endpoint rate limit, last_delivery_at/status) com RLS para group admins. `public.outbound_webhook_deliveries` loga cada tentativa: state machine `pending → in_flight → delivered|failed`, exponential backoff (`next_retry_at`), `FOR UPDATE SKIP LOCKED` pra workers multi-instância não duplicarem delivery, retention via `audit_logs_retention_config`. Admin RPCs SECURITY DEFINER (`fn_outbound_webhook_register` / `_rotate_secret` / `_enable`) enforcem coaching_members role check; worker RPCs service-role-only (`fn_outbound_webhook_enqueue`, `_claim`, `_mark_delivered`, `_mark_failed`). 67 invariantes via `audit:outbound-webhooks`.
- L16-05 — Brand sponsorships schema (commit `725159d`, 2026-04-21): fecha o high de Lente 16 sobre "integrações de marcas esportivas sem schema". `coin_ledger.reason` CHECK estendido com `sponsorship_payout` preservando as razões anteriores — transferências de coins patrocinadas ficam distinguíveis de prêmios/referrals no ledger. `public.brands` é o catálogo canônico de marcas (Nike/Asics/Mizuno/…) com RLS public-read de `is_active=true` para o mobile renderizar logos. `public.sponsorships` captura contratos por (group_id, brand_id): state machine `draft → active → expired|terminated`, CHECKs `contract_end >= contract_start`, `monthly_coins_to_athletes >= 0`, `equipment_discount_pct BETWEEN 0 AND 100`; staff do grupo gerencia via RLS. `public.sponsorship_athletes` é o opt-in LGPD-safe — atleta precisa consentir explicitamente (nenhum enrollment automático). RPCs: `fn_sponsorship_activate` (admin-only, `draft → active`), `fn_sponsorship_enroll_athlete` (athlete-self, idempotente via `ON CONFLICT`), `fn_sponsorship_opt_out_athlete` (athlete-self, soft-revoke), `fn_sponsorship_distribute_monthly_coins` (service-role cron target, idempotente por mês, honra orçamento `monthly_coins_to_athletes`, emite rows em `coin_ledger` com `reason='sponsorship_payout'`). 56 invariantes via `audit:sponsorships`.
- L21-12 — Staff team dashboard primitives (commit `4bcadf6`, 2026-04-21): fecha o high de Lente 21 sobre "sem team dashboard para staff técnica". Atleta de elite é suportado por equipe multidisciplinar (treinador + fisiologista + fisioterapeuta + nutricionista + psicólogo), mas `coaching_members.role` só aceitava `{admin_master, coach, assistant, athlete}` — promover fisioterapeuta para `assistant` concedia permissão demais. CHECK constraint expandida para `{admin_master, coach, assistant, physio, nutritionist, psychologist, athlete}` via `pg_constraint` introspection (drop-and-re-add funciona mesmo se o CHECK original era anônimo, sem quebrar nenhuma RLS policy existente). `public.role_permissions` é a matriz canônica `(role, permission)`: permission shape CHECK `^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$` (namespace.action) + RLS public-read + platform_admin-write, seed com política canônica (admin_master = tudo; coach = training + health read + health notes; nutritionist = nutrition read/write; psychologist = mental read/write; physio = health notes write; athlete = read own). `fn_is_staff_role` (IMMUTABLE PARALLEL SAFE) abstrai lista de staff — novos roles especialistas não quebram callers. `fn_role_has_permission` (STABLE PARALLEL SAFE, NULL-safe) retorna `COALESCE(granted, FALSE)`. `public.athlete_staff_access` é o grant per-(athlete, staff, permission) controlado pelo atleta: composite PK, `revoked_at >= granted_at` CHECK, partial staff index em live rows, RLS athlete-own write + staff read de próprias rows + platform_admin read. Atleta abre nutrição para UM nutricionista sem habilitar todos do grupo. `fn_my_role_in_group_ext` (STABLE SECURITY DEFINER) retorna `{role, is_staff, permissions}` jsonb para o portal/mobile renderizar dashboards role-specific sem N queries. 44 invariantes via `audit:staff-team-dashboard`.
- L22-07 — Parcelado BR + gateway preference (commit `56b1dfd`, 2026-04-21): fecha o high de Lente 22 sobre "compra parcelada para assessoria brasileira". 60% dos atletas BR preferem parcelar via PIX/cartão mas o schema só modelava pagamentos lump-sum. `public.billing_gateway_preferences` armazena preferência por grupo com RLS (admin write, coach read). `fn_recommend_gateway(country_code)` é IMMUTABLE PARALLEL SAFE e retorna `'asaas'` para BR, `'stripe'` caso contrário (NULL inclusivo). `fn_validate_installment_config(count, total_cents)` exige `count ∈ [1, 12]`, `total_cents > 0` e piso de R$ 5,00 por parcela — bloqueia schedules inviáveis na camada de dados. `public.billing_installment_plans` (header, unique por `purchase_id`, state machine `active → completed|cancelled` com CHECKs de gateway, método, validade da config e timestamps terminais coerentes). `public.billing_installments` (pending/paid/overdue/cancelled, unique `(plan_id, sequence_no)`, partial index em `due_date` para sweep de overdue). RPC suite: `fn_create_installment_plan` (admin-only, gera schedule atômico com cents-exact distribuição e resto na primeira parcela), `fn_mark_installment_paid` (service-role, idempotente, promove plano para completed quando todas pagas, `FOR UPDATE`), `fn_mark_installments_overdue` (cron service-role), `fn_cancel_installment_plan` (admin, cascata em pendentes+overdue). 57 invariantes via `audit:installments-br`.
- L23-05 — Canonical workout template catalogue (commit `4f71114`, 2026-04-21): fecha o high de Lente 23 sobre "workout template library pobre". Coach novo abria uma assessoria e começava do zero — sem nenhum fartlek, tempo run ou long run populando a biblioteca, o que arrastava o onboarding e diminuía a qualidade inicial dos planos. `public.workout_template_catalog` é a biblioteca global (sem `group_id`), com `slug UNIQUE` + shape CHECK `^[a-z][a-z0-9_-]{2,62}$`, enum `category` (12 tipos), enum `workout_type`, enum `source` (daniels/pfitzinger/hudson/custom), `difficulty ∈ [1, 5]`, `is_active` flag. RLS: público lê ativos, apenas `platform_admin` escreve. `public.workout_template_catalog_blocks` espelha `coaching_workout_blocks` com CHECKs em `block_type`, `hr_zone`, `rpe` e UNIQUE`(catalog_id, order_index)`. Seed: 12 treinos canônicos (Daniels tempo/VO2max, Pfitzinger long runs, Hudson intervals, custom) inseridos via `ON CONFLICT (slug) DO NOTHING`, alguns com blocks detalhados (e.g. `daniels-tempo-20`). `fn_clone_catalog_template(catalog_id, group_id, custom_name)` é SECURITY DEFINER, aberto a coach/admin_master do grupo, idempotente via ancoragem `catalog_slug` na descrição, e copia os blocks. `fn_list_catalog_templates(category, source, difficulty_max)` é STABLE SECURITY INVOKER para listagem/filtragem na UI. 46 invariantes via `audit:workout-template-library`.
- L23-08 — Geofenced auto check-in (commit `32819d4`, 2026-04-21): fecha o high de Lente 23 sobre "presença em treinos coletivos via QR code". Coach sozinho num coletivo de 40 atletas gastava 10 minutos na fila do QR — com isso o treino começava atrasado e o coach escolhia entre escanear ou correr. `coaching_training_sessions` ganha `location_radius_meters` (CHECK [25, 5000]), `checkin_early_seconds` / `checkin_late_seconds` (ambos [0, 86400]) e `geofence_enabled` (default FALSE), com CHECK `geofence_requires_location` forçando lat/lng/radius quando o geofence está ligado. `coaching_training_attendance` ganha coords + `checkin_accuracy_m` + CHECK `auto_geo_has_coords`; o CHECK `method` é drop-and-re-add para incluir `auto_geo`. `public.coaching_attendance_audit` é o log append-only de cada tentativa (`accepted` / `rejected` + `reason_code` shape `^[A-Z][A-Z0-9_]{2,48}$`) com RLS em 3 camadas (athlete-self, staff per-session, platform_admin). `fn_session_checkin_window(session_id)` retorna `(window_open_at, window_close_at, is_open)` STABLE SECURITY INVOKER para a UI decidir se mostra o botão. `fn_auto_checkin(session_id, lat, lng, accuracy_m)` é SECURITY DEFINER e valida auth → coordenadas em range → `accuracy_m ≤ 100m` → sessão não cancelada → `geofence_enabled` → membership → janela de tempo → distância via `fn_haversine_m ≤ radius`. Idempotente via `ON CONFLICT (session_id, athlete_user_id)` e audita cada rejeição. `fn_record_attendance_audit` é fail-open (`RAISE WARNING`) — audit outage nunca bloqueia check-in legítimo. 51 invariantes via `audit:geofenced-checkin`.
- L23-09 — Athlete subscriptions + invoice rail (commit `b063aab`, 2026-04-21): fecha o high de Lente 23 sobre "billing integrado para atletas". Cobrança mensal da assessoria acontecia fora do produto (WhatsApp + PIX por fora), invisibilizando churn e impedindo o loop "atleta paga → custódia do grupo credita → staff saca". `public.athlete_subscriptions` modela contrato por (group, athlete): `price_cents > 0`, `currency` len 3, `billing_day_of_month ∈ [1, 28]`, enum `gateway ∈ {asaas, stripe, mercadopago}`, state machine `active → paused → cancelled` com CHECK biconditional `state_timestamps` (impossível "active com cancelled_at"). Partial unique index `(group_id, athlete_user_id) WHERE status IN ('active','paused')` bloqueia sub duplicada. `public.athlete_subscription_invoices` é linha por mês: UNIQUE`(subscription_id, period_month)` torna a geração cron trivialmente idempotente, CHECK `period_first_of_month` garante que UNIQUE é significativo, CHECKs `paid_timestamp` / `cancelled_timestamp` são biconditionais. RPCs: `fn_subscription_start` (admin-only, piso R$ 5,00), `fn_subscription_pause` (admin-only, rejeita não-ativa), `fn_subscription_cancel` (athlete-self OU staff, cascata pendentes), `fn_subscription_generate_cycle` (cron service-role, idempotente via ON CONFLICT), `fn_subscription_mark_invoice_paid` (service-role, idempotente em paid, rejeita cancelled com P0005, emite outbox `subscription.invoice.paid` guardado por `to_regproc` + fail-open — pipeline de coin-credit consome assincronamente), `fn_subscription_mark_overdue`. 56 invariantes via `audit:athlete-subscriptions`.
- L23-10 — Paired / grouped workouts (commit `d4c4a21`, 2026-04-21): fecha o high de Lente 23 sobre "treinos com dependência entre atletas". "João e Maria, 10 km ritmo base, 06:00" virava dois assignments independentes; se João cancelasse, Maria só descobria no parque. `public.coaching_workout_pairings` agrega assignments existentes com state machine de 5 estados (`pending → partially_confirmed → all_confirmed → completed` ou `pending → dissolved`) e CHECKs biconditionais `dissolved_timestamp` + `completed_timestamp` que previnem drift stamp↔status. `min_confirmations ∈ [2, 20]`. `public.coaching_workout_pairing_members` é o join com 3-state `{pending, confirmed, declined}`, CHECK `responded_timestamp` biconditional, CHECK `decline_has_reason_shape` exige motivo 1-280 chars em decline. UNIQUE`(assignment_id)` impede um assignment em duas pairings; UNIQUE`(pairing_id, athlete_user_id)` dedupa no create. `fn_pairing_recompute_status` é pure aggregation com prioridade `completed > dissolved > all_confirmed > partially_confirmed > pending` e stampa transition timestamps. `fn_pairing_create` (admin-only, 2-20 membros, todos compartilham group+date, recusa assignments já pareados com 23505). `fn_pairing_respond` (athlete-self, `FOR UPDATE` serializa, recusa terminal com P0005, emite outbox `workout.pairing.partner_declined` no decline — guarded por `to_regproc` + fail-open). 50 invariantes via `audit:paired-workouts`.
- L07-03 — Offline-sync pure-domain queue (commit `166dbf1`, 2026-04-21): fecha o high de Lente 7 sobre "app mobile sem modo offline robusto". Tracking in-app sumiu (corrida vem do Strava desde a Sprint 25.0.0), mas os fluxos offline shippados nesta onda — auto check-in (L23-08), session notes, pairing responses (L23-10), workout completion — ainda precisam sobreviver a 10 dias em trilha sem flood quando reconecta. `portal/src/lib/offline-sync/types.ts` modela entry (5-state `pending → in_flight → done / failed / dead_letter`), `DEFAULT_RETRY_POLICY` (15 s base, 6 h cap, 12 tentativas, ±20 % jitter), `DEFAULT_OFFLINE_ALERT_POLICY` (pendentes ≥ 5 OU idade ≥ 3 dias — casa com o "push se > 3 dias" do finding). `queue.ts` é puro reducer: `enqueue` (idempotente por id — tap duplo no check-in não duplica row), `pickReady` (ordenado por nextAttemptAt, com limit), `markInFlight` rejeita não-pending, `ack` com branches `ok / retryable / non-retryable` e caminho de `dead_letter` após `maxAttempts`, `requeueDeadLetter` (reset attempts + nextAttemptAt), `purgeCompleted` (retention), `snapshot`. `policy.ts`: `computeNextAttemptAt` faz exponencial `base * 2^(attempts-1)`, cap em `maxDelayMs`, jitter simétrico clamped em ≥ 0 — `random` injetável torna cada branch testável; `evaluateAlert` ordena severidade `dead_letter > (pending ∧ age) > age > pending > OK`. 19 testes vitest cobrem todo branch. 43 invariantes via `audit:offline-sync`. A ligação Flutter (Drift → reducer) fica como follow-up; o módulo puro está verificável em isolamento.
- L21-04 — TSS / CTL / ATL / TSB pure-domain primitives (commit `7ace577`, 2026-04-21): fecha o high de Lente 21 sobre "ausência de training load". `portal/src/lib/training-load/` exporta (a) `types.ts` (value-objects + constantes canônicas `CTL_TAU_DAYS=42`, `ATL_TAU_DAYS=7`, `TSS_MAX_PER_SESSION=500`, `IF_MAX=1.3` + enum `TrainingZone` com `TRAINING_ZONE_BANDS`), (b) `tss.ts` (`computeSessionTss` prioriza rTSS > hrTSS > fallback `hours·IF²·100`, `hrIntensityFactor` cubic HR→IF, clamps), (c) `rolling.ts` (`rollupDailyTss` agrega por dia, `buildLoadSeries` usa EWMA com seed opcional, `classifyTrainingZone` + `computeCtlRampRate`). Zero IO, determinístico. Vitest tests cobrem rTSS/hrTSS/fallback paths, convergência CTL/ATL, invariante `TSB ≈ CTL − ATL`, honour de seeds e ramp rate. 59 invariantes via `audit:training-load`.
- L21-05 — Per-athlete pace/HR training zones (commit `c2a0edd`, 2026-04-21): fecha o high de Lente 21 sobre "zonas de treino não personalizáveis". `supabase/migrations/20260421690000_l21_05_athlete_zones.sql` introduz `fn_validate_pace_zones` / `fn_validate_hr_zones` (`IMMUTABLE PARALLEL SAFE`, 3–7 zonas ordenadas, bounds fisiológicos), tabela `public.athlete_zones` (JSONB pace/HR zones + LTHR/HRmax/VO2max + `updated_by` enum + versioning), tabela append-only `public.athlete_zone_history` com trigger `BEFORE UPDATE OR DELETE` que bloqueia mutações não-service-role, trigger `AFTER INSERT/UPDATE` `fn_athlete_zones_snapshot` para audit trail. RPCs `SECURITY DEFINER`: `fn_zones_compute_from_anchors(lthr, threshold_pace)` deriva zonas Daniels/Friel, `fn_zones_set(...)` upsert com ownership check (athlete-self OR group-coach), `fn_zones_classify_pace` / `fn_zones_classify_hr` classificação com clamp. 54 invariantes via `audit:athlete-zones`.
- L22-01 — First-run onboarding state machine (commit `430fb53`, 2026-04-21): fecha o critical de Lente 22 sobre "onboarding não inclui primeira corrida guiada". `portal/src/lib/first-run-onboarding/` é uma máquina de estados pura (`types.ts` 10 estados + 11 eventos + `TERMINAL_STATES` + `STATE_PROGRESS` 0-100%, `machine.ts` reducer total que deixa snapshot inalterado em evento ilegal, lida com `strava_connection_failed` armazenando error e dando rewind para `welcome_seen`, conta `skipCount` com cap, gerencia `celebratedAt`; `resume.ts` `evaluateResume` decide nudge de re-engajamento baseado em terminal-state, idade, retries e `DEFAULT_RESUME_POLICY` de 3 dias / 3 retries). 26 vitest cases cobrem happy path, Strava failure/recovery, skip/resume, terminal, progress, history, resume policy. 49 invariantes via `audit:first-run-onboarding`. Deliberadamente NÃO wired em UI (follow-up L22-01-presenter).
- L22-02 — OmniCoin narrative copy + challenge-only policy revoke (commits `f960943` + pendente, 2026-04-21): fecha o critical de Lente 22 sobre "conceito de moeda OmniCoin confunde amador". Módulo pure-domain `portal/src/lib/omnicoin-narrative/` renderiza copy persona-aware via union fechado `ChallengeLedgerReason` (8 motivos challenge-bound) — persona `amateur` nunca vê coin amount ou a palavra "OmniCoin", `pro`/`coach`/`admin_master` veem o delta bruto para reconciliação. **Escopo estendido após reafirmação da política**: a migration compensatória `supabase/migrations/20260421700000_l22_02_revoke_nonchallenge_coins.sql` reverte dois vazamentos introduzidos em Batch J — `fn_activate_referral` (L15-02) é reescrita sem `INSERT INTO coin_ledger` / sem bump de wallet; `fn_sponsorship_distribute_monthly_coins` (L16-05) é dropada; colunas de orçamento em coins de `sponsorships` (`monthly_coins_per_athlete`, `coin_budget_total`, `coin_budget_used`) + colunas de reward em `referrals` / `referral_rewards_config` (`reward_referrer_coins`, `reward_referred_coins`) são removidas; `coin_ledger_reason_check` é restaurado à lista canônica (inclui `challenge_withdrawal_refund` usada em produção + reinstala `institution_token_*` / `institution_switch_burn` que L16-05 dropara silenciosamente); reasons proibidos (`referral_*_reward`, `sponsorship_payout`, `referral_bonus`, `referral_new_user`, `redemption_payout`, `custody_reversal`, `championship_reward`) são removidos tanto do CHECK quanto do ledger via DELETE (com reconciliation de `wallets.balance_coins`). O guard `audit:omnicoin-narrative` agora varre `supabase/migrations` + `supabase/functions` bloqueando literais de reason não-challenge (welcome/onboarding/signup/invite/referral/sponsorship/championship/etc.) em código novo, e `audit:ledger-reason-safety` ganha `challenge_withdrawal_refund` + os reasons de challenge-clearing cross-assessoria no whitelist canônico. Política do produto pinada em SQL + TS + docs: **OmniCoins são emitidas/queimadas somente em fluxos de desafio**.

### Escopo

- ✅ **Supply chain (L11-01/02/03/04/09)** — npm audit + osv-scanner gate, SBOMs CycloneDX, gitleaks pre-commit + CI + weekly sweep, Dependabot agrupado por área semântica (27 grupos), permissions least-privilege em todos workflows + WIF/OIDC opt-in para Firebase/Play.
- ✅ **SRE foundation (L20-01/02/04/05/07/08)** — dashboard versionado, SLO/SLI canônicos com burn-rate alerting, Sentry tuning adaptativo (P1=100% / P4=0%), severity-based alert routing, DR drill protocol, blameless postmortem template.
- ✅ **Runbooks financeiros (L06-01)** — 6 runbooks operacionais (CUSTODY_INCIDENT, CLEARING_STUCK, WITHDRAW_STUCK, CHARGEBACK, GATEWAY_OUTAGE, WEBHOOK_BACKLOG) com SQL real, decisão por cenário, validação e postmortem mandatório. Indexados em `docs/runbooks/README.md` por severidade e tempo alvo.
- ✅ **Kill switches (L06-06)** — `feature_flags` estendida com `id`/`scope`/`category`/`reason`/`updated_by` + audit trigger imutável + helpers SQL/TS/Deno (fail-open semantics) + wiring em `/api/distribute-coins`, `/api/custody/withdraw`, `/api/swap` + admin UI com badge por categoria, motivo obrigatório e cache invalidation. Runbooks atualizados para usar schema real.
- ✅ **Custody idempotency (L01-04)** — `custody_deposits.idempotency_key` + UNIQUE parcial composto `(group_id, key)` + RPC `fn_create_custody_deposit_idempotent` (SELECT-first, race resolvido via `unique_violation`) + `confirm_custody_deposit(uuid, uuid)` exige match de ambos (mensagem genérica defende contra UUID enumeration) + header `x-idempotency-key` obrigatório no POST com formato UUIDv4/ULID validado + audit log skipa replays + 3 testes integration (replay/cross-group/non-existent).
- ✅ **Swap TTL/expiração (L05-02)** — `swap_orders.expires_at` (default 7d) + index parcial `(expires_at) WHERE status='open'` + RPC `fn_expire_swap_orders` (RETURNING ids) + pg_cron `swap-expire` a cada 10min + `execute_swap` rejeita `expires_at<now()` com SQLSTATE P0005 (defesa entre runs do cron) + portal `createSwapOffer(seller, amount, expiresInDays?)` com TTLs canônicos 1/7/30/90 + HTTP 410 Gone para offers expiradas + audit log carrega `expires_in_days`/`expires_at`. 3 integration tests cobrindo sweep idempotency, P0005+sweep cleanup e happy path.
- ✅ **Swap como cessão de crédito off-platform (L02-07/ADR-008)** — `swap_orders.external_payment_ref` opcional (4-200 chars, sem control chars, validado em CHECK constraint) + `execute_swap` ganha 3º param `p_external_payment_ref` + nova SQLSTATE P0006 (PAYMENT_REF_INVALID) + portal lib `acceptSwapOffer(orderId, buyer, ref?)` com validação client-side + Zod no route com triple-check (min/max/regex sem control chars) + WARN log estruturado quando accept ocorre sem ref (`logger.warn("swap.accept_without_external_payment_ref", { adr: "ADR-008" })`) + audit log enriquecido com `external_payment_ref`/`has_payment_ref`. ADR-008 formaliza modelo (cessão vs venda) + decisão de não migrar para gateway Stripe/MP. 13 novos tests (5 lib + 5 route + 3 integration).
- Testes de regressão para **todos** os fluxos financeiros (portal + edge)
- Observabilidade restante: OTel distributed tracing (L20-03), status page público (L20-06), structured logger correlation IDs em todas rotas
- LGPD: endpoints de exportação/deleção, consentimento versionado
- Anti-cheat: ajuste de thresholds para atletas de elite (`MAX_SPEED_MS`, `MAX_HR_BPM`) — Lente 21
- ✅ **API contracts (L14-01/02/04/05/06)** — `/api/v1/*` formal + Sunset/Deprecation headers nas rotas legacy financeiras (commit cc6b3e1) + OpenAPI contract-first via `@asteasolutions/zod-to-openapi` com drift gate `npm run openapi:check` (commit 88ba7e0) + envelope de erro canônico, identity-based rate-limit keys e cursor pagination (commit 3ac1496).
- Rate limiting consistente em todas rotas sensíveis

### Exit criteria

- ✅ Cobertura de testes >= 70% em `portal/src/app/api/` e `supabase/functions/`
- ✅ Runbooks para top-10 alertas SRE documentados em `docs/audit/runbooks/`
- ✅ Dashboard Sentry com p99 e error rate por rota publicado internamente
- ✅ Todos findings 🟠 `wave: 1` fechados

---

## Onda 2 — "Scale" (127 findings 🟡 medium)

**Duração alvo:** 4–6 sprints
**Foco:** performance, UX, observability profunda, capacidades para atletas/treinadores.

### Escopo

- TSS / CTL / ATL para atletas profissionais (Lente 21)
- Zonas customizáveis (HR + pace) — Lentes 21 + 22
- Dashboards priorizados para treinadores (Lente 23)
- Onboarding guiado para amadores (Lente 22)
- SEO + Open Graph + deep links (Lente 15)
- Acessibilidade WCAG AA (Lente 16)
- Otimização de queries custosas, materialized views (Lente 19)
- SLOs formais + alertas (Lente 20)

### Exit criteria

- ✅ NPS medido em-app para 3 personas (prof/amador/treinador) com baseline
- ✅ Lighthouse score >= 90 em todas páginas públicas do portal
- ✅ WCAG AA em checkout e onboarding validado com ferramenta automatizada + revisão manual
- ✅ 100% findings 🟠 fechados, 🟡 `wave: 2` em progresso

---

## Onda 3 — "Expansion" (29 findings: 17 safe + 12 N/A)

**Duração alvo:** contínua
**Foco:** capacidades avançadas, growth, automação interna.

### Escopo

- Race prediction, workout delivery, team management
- Integração TrainingPeaks/Garmin/Wahoo avançada
- Marketplace B2B2C (cross-selling entre assessorias)
- Automação de operações (alerts self-healing, auto-scaling)
- Dívida técnica remanescente (🟡 medium)
- Documentação viva (ADRs, runbooks, onboarding)

### Exit criteria

- ✅ Zero findings `fix-pending` para features em produção
- ✅ Releases semanais sem rollback por 8 semanas consecutivas
- ✅ Documentação de onboarding para dev júnior <= 2h tempo de setup

---

## Como acompanhar

- **Semanalmente**: `SCORECARD.md` é regerado; stand-up de 15min para revisar progresso por onda.
- **Por finding**: GitHub Issue + label `audit:LXX-YY` + milestone `Onda N` + `PR Closes #N`.
- **Exit de onda**: review formal com C-level + stakeholders técnicos antes de iniciar a próxima.

---

## Mapeamento findings → ondas (estado atual)

Alocação pós-triage. Ver `SCORECARD.md` para contagens ao vivo:

| Onda | Critical | High | Medium | Safe+N/A | Total |
|---|---|---|---|---|---|
| 0 | 15 | 0 | 0 | 0 | **15** |
| 1 | 54 | 123 | 0 | 0 | **177** |
| 2 | 0 | 0 | 127 | 0 | **127** |
| 3 | 0 | 0 | 0 | 29 | **29** |

**Total**: 348 findings.

Dos 29 findings "Onda 3": 17 🟢 safe (já verificados OK) e 12 ⚪ não-auditados (requerem re-auditoria focada).

### Findings exemplares completamente especificados

Os seguintes findings já têm **correção proposta + testes de regressão + SQL** prontos para consumo direto de PR (úteis como ponto de partida):

- `L02-01` — `distribute-coins` atomic (CTO) — **gold standard** de detalhe
- `L01-03` — `/api/distribute-coins` fallback silencioso — resolvido junto com L02-01 (`duplicate_of`)
- `L01-44` — migration drift em `platform_fee_config` — correção canônica + patch retroativo na histórica
- `L01-13` — `/api/platform/fees` sem suporte a `fx_spread` — resolvido junto com L01-44 (`duplicate_of`)
- `L14-03` — Swagger-UI self-host (remove dependência de unpkg)
- `L02-02` — `execute_burn_atomic` hardenizado (custody re-raise, settle log-and-continue + `clearing_failure_log`)
- `L18-03` — 26 SECURITY DEFINER em `public` hardenizadas com `SET search_path` + invariante bloqueadora
- `L19-05` — 9 RPCs financeiras com `SET lock_timeout = '2s'` + portal 55P03 → 503 retry-after
- `L01-02` — FX rate server-side authoritative via `platform_fx_quotes` + `.strict()` schema + endpoint read-only para UI
- `L05-01` — `cancel_swap_order` RPC com `FOR UPDATE` + ownership/status guards + SQLSTATE distinguíveis (P0001/P0002/P0003/P0004) → portal mapeia para 404/409/403/400/422/503
- Gradualmente, conforme o time converter outros findings da Onda 0 em PRs, estes também ganharão detalhamento similar.
